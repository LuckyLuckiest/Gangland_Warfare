package me.luckyraven.inventory.handler;

/**
 * Handles {@code OnSwapHand} (PlayerSwapHandItemsEvent) slot events.
 *
 * <p>Triggers the configured command / inventory-open when the player presses F on this slot
 * inside the custom GUI.
 */
public class SwapHandSlotHandler extends AbstractCommandSlotHandler { }
