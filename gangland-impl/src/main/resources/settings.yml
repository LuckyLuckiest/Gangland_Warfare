#   +------------------------------------------------------+   #
#  /     __________      _______________       ____         \  #
# |     / ________/      \_____   _____/      / __ \         | #
# |    | /                     | |           / /  \ \        | #
# |    | |  _______            | |          / /____\ \       | #
# |    | |  \_____ \           | |         / ________ \      | #
# |    | \_______/ ||          | |        / /        \ \     | #
# |     \__________/           |_|       /_/          \_\    | #
# |                                                          | #
# |                    File: settings.yml                    | #
# |                  Author: [LuckyRaven10]                  | #
#  \                                                        /  #
#   +------------------------------------------------------+   #

# +----------------------------------+ #
# |      Settings Configuration      | #
# +----------------------------------+ #

# If you have faced any problem with the file, you can create a new one by changing [Config_Version] to a different
# number.
# However, when a new update comes out by default, the config version would have a different version resulting in a new
# file.
# The old version file would have a suffix '-old' to indicate they are from the old version.
# Every YAML file from this plugin supports this feature, even if the configuration section of [Config_Version]
# wasn't explicitly mentioned.
# Adding this section and changing the value would result in the same functionality, otherwise it won't recreate the
# file.
Config_Version: '${project.version}'

# +---------------+ #
# | Update Config | #
# +---------------+ #

# Get notified and updated for a new release of the plugin.
Update_Checker:
   # It is recommended to be left enabled since it doesn't take any space in the RAM.
   Enable: true
   # Only privileged players will be notified when they join if there is a new update.
   Notify_Privileged_Players: false
   # This feature will auto-install the update into the plugin's own folder, inside a folder called 'release'.
   Auto_Download: true

# +-----------------+ #
# | Language Config | #
# +-----------------+ #

# Messages language type. You can create a custom language by simply creating a folder called 'message' and inside it
# the language.
# You should respect the prefix of the file name, it should be message_xx.yml, and xx can be any letter you prefer. To
# switch between the languages, you enter the xx values in the 'Language' configuration, and it would load that
# configuration.
# Any faulty written language names entered to this attribute 'Language' then the plugin would disable. To make the
# plugin work, use any valid language. To get the list of the available languages, enter an invalid name, and a message
# of the available list suffix would be displayed.
Language: en

# +----------------------+ #
# | Resource Pack Config | #
# +----------------------+ #

# This plugin contains a custom resource pack that can be used in the server.
# A manual download is applicable if there was any problem faced.
Resource_Pack:
   Enable: true
   # This is the direct download url link.
   URL: "https://drive.google.com/uc?export=download&id=1uu61eecMNUVPU4sdwqNBx2AolyaUwqSX"
   # Kicks the player if they didn't accept the resource pack
   Kick: false

# +-----------------+ #
# | Database Config | #
# +-----------------+ #
Database:
   # Specify which type of database to be used, [mysql, sqlite]
   Type: sqlite
   MySQL:
      # Necessary credentials to enter your database
      Host: localhost
      Port: 3306
      Username: root
      Password: ""
   SQLite:
      # Will create a backup if the type is mysql or sqlite
      Backup: true
      # Will use SQLite if unable to connect to MySQL
      # It is recommended to set this true
      Failed_MySQL: true
   Auto_Save:
      # Enabling auto save would clear update the databases.
      # If disabled, it would write to the database and clear the cache only when the player leaves or when the server
      # closes.
      # Disabling this option is not recommended since it would create a lot of overhead to the CPU every time a user
      # leaves.
      # This effect takes place when the server contains a large number of player bases or players constantly joining
      # and leaving the server.
      Enable: true
      # Time to auto save in minutes.
      Time: 10
      # Enable if you want to print periodical updates in the console.
      Debug: true
   Clean_Up:
      # Time to clean up the database from old data in days.
      Time: 30

# +-------------------+ #
# | Scoreboard Config | #
# +-------------------+ #
Scoreboard:
   Enable: true
   # The plugin supports multiple drivers, and each driver has its own algorithm and way of updating scoreboards.
   # Drivers' list:
   #  1) Driver_V1: Caches the lines into memory and uses a clustering algorithm that updates similar line intervals
   #                each tick.
   #  2) Driver_V2: Caches the lines into memory similar to Driver_V1 algorithm. The difference is it uses a built-in
   #                library to manage the clusters.
   #  3) Driver_V3: Uses interactive scoreboard to temporarily disable the main one and use the alternative scoreboard.
   Driver: "Driver_V3"

# +------------------+ #
# | Inventory Config | #
# +------------------+ #
Inventory:
   # Inventory fill item and its name
   Fill:
      Item: BLACK_STAINED_GLASS_PANE
      Name: " "
   # Inventory line item and its name
   Line:
      Item: WHITE_STAINED_GLASS_PANE
      Name: " "
   # MultiInventory heads data tags
   Multi_Inventory:
      Next_Page: "eyJ0ZXh0dXJlcyI6eyJTS0lOIjp7InVybCI6Imh0dHA6Ly90ZXh0dXJlcy5taW5lY3JhZnQubmV0L3RleHR1cmUvOTYzMzlmZjJlNTM0MmJhMThiZGM0OGE5OWNjYTY1ZDEyM2NlNzgxZDg3ODI3MmY5ZDk2NGVhZDNiOGFkMzcwIn19fQ=="
      Previous_Page: "eyJ0ZXh0dXJlcyI6eyJTS0lOIjp7InVybCI6Imh0dHA6Ly90ZXh0dXJlcy5taW5lY3JhZnQubmV0L3RleHR1cmUvZjg0ZjU5NzEzMWJiZTI1ZGMwNThhZjg4OGNiMjk4MzFmNzk1OTliYzY3Yzk1YzgwMjkyNWNlNGFmYmEzMzJmYyJ9fX0="
      Home_Page: "eyJ0ZXh0dXJlcyI6eyJTS0lOIjp7InVybCI6Imh0dHA6Ly90ZXh0dXJlcy5taW5lY3JhZnQubmV0L3RleHR1cmUvZjg0ZjU5NzEzMWJiZTI1ZGMwNThhZjg4OGNiMjk4MzFmNzk1OTliYzY3Yzk1YzgwMjkyNWNlNGFmYmEzMzJmYyJ9fX0="

# +-------------+ #
# | User Config | #
# +-------------+ #
User:
   # The user balance account config
   Account:
      # On Player Join his default account will have the amount of money that you set.
      Initial_Balance: 0
      # The maximum amount of money the player can have.
      Maximum_Balance: 10_000_000
   # The user bank account config
   Bank:
      # When a player creates a bank account, his account will have the amount of money that you set.
      Initial_Balance: 0
      # When a player wants to create a bank account, they're required to pay a fee.
      # Set it to zero, and the requirement is lifted.
      Create_Cost: 5_000
      # Charged (from cash) every time the player renames their account at a Banker NPC. 0 disables.
      Rename_Fee: 1_000
      # Length of the rolling window (seconds) after which the deposit counter resets. Defaults to 24h.
      # Measured from the player's first interaction within a window — not midnight — so splitting a transaction
      # across midnight never counts against two separate windows.
      # Maximum balance, daily deposit limit, interest rate, death-loss discount, and weekly / monthly loan
      # amounts are all per-tier now — see plugin/npc/bank_tiers.yml.
      Reset_Period_Seconds: 86_400
   # The user level config
   Level:
      # The maximum level the player can have.
      Maximum_Level: 100
      # The base amount to be used in calculation.
      Base_Amount: 1_000

      # This formula is used to calculate the levels linearly.
      # It is possible to use mathematical functions; such as
      # [neg: negation of the expression, logb: log base of x for y; logb(x, y), sin: sine function, etc...]
      # Treat this string as a scientific calculator

      # There are only four variables that can be used for the formula
      # [base: base amount, max: maximum level, level: current level, experience: current experience]
      Formula: "base * level ^ 1.5"
      Skill:
         Upgrade: 1
         Cost: 500
         Formula: "base * level ^ 1.8"
   # When the user dies
   Death:
      Enable: true
      # Money-related configuration upon death
      Money:
         # Command used when the user dies
         Command:
            # If the command was enabled, it would ignore the [Lose_Money] option.
            Enable: false
            # The server executes all the executables.
            Executable:
               - "/glw eco withdraw %player% 20"
         # When the user dies, they will either lose or gain money according to the value set
         Lose_Money: true
         # The formula used when calculating the value to lose/gain.

         # There are only five variables that can be used for the formula
         # [balance: current balance, level: current level, experience: current experience, bounty: current
         #  withholding bounty, wanted: current wanted level]
         # Be careful that some values might be equal to zero
         Formula: "balance * 0.15"
         # The amount of money the user should at most have to not lose/gain money
         Threshold: 1_000
      Respawn:
         Enable: false
         # Seconds to wait before the player is respawned. Set to 0 for instant respawn.
         Delay: 10
         Screen:
            Enable: true
            Title: "&cWASTED"
            Subtitle: "&7Respawning after &a%time%"
         GameMode:
            # GameMode applied while the player waits to respawn.
            Change_To: "spectator"
            # Allow the player to fly while waiting to respawn so they can observe the scene.
            Allow_Fly: true
         Teleport:
            Enable: true
            Waypoint: "spawn"
         Health: 20
         Hunger: 20

# +---------------+ #
# | Bounty Config | #
# +---------------+ #
Bounty:
   Kill:
      # Each kill committed will be added on the total bounty amount.
      Each: 5
      # The maximum total amount after each kill committed.
      Maximum: 50_000
   # This option is only for users
   Repeating_Timer:
      Enable: true
      # Each time the timer gets to the [Time] value, it would multiply the current amount to the multiple.
      Multiple: 2
      Time: 300
      # The maximum total amount after each timer multiple.
      Maximum: 20_000

# +---------------+ #
# | Wanted Config | #
# +---------------+ #
Wanted:
   Enable: true
   Take_Money:
      # The amount of money taken from the player when they are wanted.
      Amount: 50
      # The multiplier used to calculate the amount of money taken from the player.
      # (amount * multiplier ^ stars)
      Multiplier: 5
   # A timer that reduces the wanted level after the specified time.
   Repeating_Timer:
      Enable: true
      # The default time to wait before reducing the wanted level.
      Time: 120
      # (time * amount ^ stars)
      Multiplier:
         Enable: true
         Amount: 1.1
   # Wanted level scaling.
   Level:
      Increment: 1
      Maximum: 5
   # Number of kills to get a wanted level.
   Kill_Combo:
      Enable: true
      # The time till the kill combo resets.
      Reset_After: 10
      # The kill counter to increment the wanted level.
      Kill_Counter:
         - 2
         - 5
         - 10
         - 15
         - 20

# +-------------+ #
# | Cops Config | #
# +-------------+ #
Cops:
   Count:
      # Set to true to evaluate the Formula string below instead of the linear Base/Per_Level/Max calculation.
      Formula_Enabled: false
      # Formula evaluated when Formula_Enabled is true.
      # Available variables: level, base, perLevel, max
      # Example: "base + (level - 1) * perLevel" (equivalent to the built-in linear formula)
      Formula: "base + (level - 1) * perLevel"
      # Cops at wanted level 1 (used for both the linear formula and as the 'base' variable)
      Base: 2
      # Additional cops per wanted level above 1 (used for both modes, exposed as 'perLevel')
      Per_Level: 1
      # Hard cap - result is always clamped to this value
      Max: 8

   # Core behavior values shared across all cop tiers.
   Behaviour:
      # Hard cap on the number of cop NPCs active per wanted player at any time.
      Max_Per_Player: 8
      # Game ticks between each AI evaluation cycle. Lower = faster reactions, higher CPU cost.
      AI_Tick_Rate: 10
      # Game ticks between each check that decides whether to spawn more cops.
      Spawn_Check_Rate: 40
      # Default radius within which a cop can attempt to cuff the target (blocks). Tiers may override this.
      Cuff_Radius: 3.0
      # How many cuff attempts a cop makes before giving up and switching to combat.
      Max_Cuff_Attempts: 3
      # Game ticks the cop waits between consecutive cuff attempts.
      Cuff_Cooldown_Ticks: 100
      # Distance at which an idle cop detects a wanted player and begins pursuit (blocks).
      Alert_Range: 40.0
      # Base melee engagement range (blocks). Ranged attack range is derived from this value.
      Combat_Range: 4.0
      # Game ticks between melee attacks.
      Attack_Cooldown_Ticks: 20

   # Controls where cop NPCs spawn relative to the target player.
   Spawn:
      # Minimum distance from the player to spawn a cop (blocks).
      Min_Distance: 10.0
      # Maximum distance from the player to spawn a cop (blocks).
      Max_Distance: 50.0
      # Minimum ring distance used during phase-1 (preferred-ring, behind-player) spawn attempts (blocks).
      Phase1_Min_Distance: 30.0
      # Amount the ring radius shrinks per phase-2 iteration (blocks).
      Radius_Shrink_Step: 5.0
      # Vertical range searched above/below the player's Y level to find valid ground (blocks).
      Vertical_Search_Range: 10
      # Y-offset added to the player's Y when searching for spawn ground. 0 = same level as player.
      Y_Offset: 0
      # Minimum number of open horizontal sides required at a spawn position.
      Min_Open_Sides: 2
      # Radius within which a registered cop spawner is preferred over a random position (blocks).
      Spawner_Preference_Radius: 80.0
      # Distance within which another player triggers the visibility check during despawn (blocks).
      Visibility_Check_Distance: 48.0
      # Number of attempts in phase-1 (preferred ring, behind-player) of spawn location selection.
      Phase1_Attempts: 20
      # Number of attempts per shrink step in phase-2 of spawn location selection.
      Phase2_Attempts: 15

   # Controls when a pursuing cop gives up chasing its target and transitions to RETURNING.
   # Acts as a final-fallback leash so stuck/unreachable cops don't occupy the per-player cap forever.
   Pursuit:
      # Horizontal distance from the target (blocks) beyond which the pursuing cop gives up.
      Max_Distance: 80.0
      # Maximum AI ticks a cop spends in PURSUING before giving up.
      # Default 120 = ~60 s at AI_Tick_Rate: 10 (10 game ticks per AI tick, 20 TPS).
      Max_Ticks: 120

   # Controls how a cop returns to its station and despawns after the target is detained.
   Return:
      # Maximum AI ticks the cop waits before being force-despawned. (600 = ~30 s at default AI tick rate)
      Max_Ticks: 600
      # Distance to the station at which the cop considers itself arrived and attempts to despawn (blocks).
      Station_Arrival_Distance: 3.0

   # Number of full magazine reloads worth of ammo given to a cop NPC on spawn.
   Starting_Ammo_Magazines: 3

# +-------------------+ #
# | Detainment Config | #
# +-------------------+ #
Detainment:
   Jail:
      # The maximum capacity of jail inmates a jail can hold
      Max_Capacity: 10
   # After a cop successfully handcuffs a player, the cop stays in GUARDING state
   # next to the player. When the delay expires (or the player dies), the player
   # is teleported to the nearest non-full jail, their inventory is seized, and
   # they are given a Jail Paperwork item that opens the bail/bribe/sentence GUI.
   Transit:
      # Ticks between the cuff landing and the auto-teleport to jail. 400 = 20 seconds.
      Delay_Ticks: 400
      # Radius (blocks) the guarding cop tries to stay within the cuffed player.
      Guard_Radius: 5.0
   # While handcuffed, the player can spam the Sneak key to fill a break-free meter. If they reach the required
   # number of taps before the reset window runs out the cuffs come off and they run free — wanted level stays, so
   # cops will re-engage on the next AI tick.
   Break_Free:
      # Total sneak taps needed within the window to break out.
      Taps_Required: 25
      # Ticks of inactivity that reset the counter back to zero (20 ticks = 1 second).
      Reset_Window_Ticks: 40
   # While handcuffed, right-clicking the guarding cop opens a small GUI with a
   # "Bribe" button. Cost is debited from the player's balance; no money item
   # needs to be in hand. The money.yml variation closest to the cost is shown
   # as the button icon.
   Handcuff_Bribe:
      Base_Cost: 500.0
      Per_Wanted_Level: 250.0
   # Once jailed, "Pay Bail" on the Jail Paperwork item releases the player
   # immediately for this cost. Balance-based.
   Bail:
      Base_Cost: 2_500.0
      Per_Wanted_Level: 1_000.0
   # "Bribe Officer" on the Jail Paperwork item is cheaper than bail but rolls
   # a chance. On failure the money is still consumed and the sentence timer is
   # extended by Fail_Penalty_Seconds.
   Jail_Bribe:
      Base_Cost: 1_000.0
      Per_Wanted_Level: 500.0
      Success_Chance: 0.35
      Fail_Penalty_Seconds: 60
   # "Serve Sentence" closes the GUI and counts down. Sentence length is based
   # on the player's wanted level at arrest time. Expires -> automatic release.
   Sentence:
      Base_Seconds: 180
      Per_Wanted_Level_Seconds: 60
   # Waypoint used when a jail has no explicit exit location configured via
   # /glw jail setexit. Resolved through the existing Waypoint
   Fallback_Exit_Waypoint: spawn
   # Sound names played at key detainment moments.
   Sounds:
      Bail_Success: BLOCK_NOTE_BLOCK_PLING
      Bribe_Success: ENTITY_VILLAGER_YES
      Bribe_Fail: ENTITY_VILLAGER_NO
      Transit_Commit: BLOCK_IRON_DOOR_CLOSE
      Sentence_Complete: BLOCK_BELL_USE

# +-------------+#
# | Gang Config |#
# +-------------+#
Gang:
   Enable: true
   # Allow duplicating gang names.
   Name_Duplicates: false
   Display_Name_Char: '*'
   # Hierarchy of ranks
   Rank:
      # The initial rank
      Head: "member"
      # The final rank
      Tail: "owner"
   Account:
      # When a player creates a gang account, his account will have the amount of money that you set.
      Initial_Balance: 0
      # When a player wants to create a gang account, they are required to pay a fee.
      # Set it to zero, and the requirement is lifted.
      Create_Cost: 100_000
      # Sets the maximum gang money.
      Maximum_Balance: 100_000_000_000
      # The total amount of money deposit for 1 contribution.
      # ex: deposit $1 -> 0.001
      # ex: deposit $1000 -> 1
      Contribution_Rate: 1_000

# +----------------+ #
# | Economy Config | #
# +----------------+ #

# [Money_Symbol] used as %money_symbol% prefix.
Money_Symbol: '$'
Balance_Format:
   Enable: true
   Format: "%,.2f"

# +----------------+ #
# | NPC Navigation | #
# +----------------+ #

# Shared pathfinding and stuck-detection settings used by all Citizens NPC types (cops, civilians, etc.).
NPC_Navigation:
   # Ticks between navigation path recalculations. Lower = more responsive but higher CPU cost.
   Recalculation_Ticks: 10
   # AI ticks between movement-progress samples for stuck detection.
   Stuck_Check_Interval: 5
   # Consecutive stuck samples required before navigation is considered stuck.
   Max_Stuck_Checks: 3
   # Consecutive stuck samples required before navigation is considered permanently hopeless.
   Max_Hopeless_Stuck_Checks: 6
   # Distance (blocks) below which a hopeless NPC can still navigate to the target directly.
   Hopeless_Close_Threshold: 8.0
   # Minimum distance the NPC must travel between samples to count as progress (blocks).
   Min_Progress_Distance: 0.75
   # Ranged NPCs hold their firing position when the target is within this distance range (blocks).
   Ranged_Min_Distance: 7.0
   Ranged_Max_Distance: 12.0
   # Minimum AI ticks an NPC waits before re-pathing after losing sight of target.
   Min_Repath_After_Loss_Ticks: 2

# +----------+ #
# | Civilian | #
# +----------+ #

# Configuration for civilian NPC AI behavior. Per-type behavior (wander range, flee range, etc.)
# is configured in civilians.yml.
Civilians:
   Behaviour:
      # Set to false to disable all civilian NPC AI ticking.
      Enabled: true
      # Game ticks between each civilian AI evaluation cycle.
      AI_Tick_Rate: 20
   # Controls where civilian NPCs spawn relative to the target player.
   Spawn:
      # Minimum distance from the player to spawn a civilian (blocks).
      Min_Distance: 10.0
      # Maximum distance from the player to spawn a civilian (blocks).
      Max_Distance: 50.0
      # Minimum ring distance used during phase-1 (preferred-ring, behind-player) spawn attempts (blocks).
      Phase1_Min_Distance: 30.0
      # Amount the ring radius shrinks per phase-2 iteration (blocks).
      Radius_Shrink_Step: 5.0
      # Vertical range searched above/below the player's Y level to find valid ground (blocks).
      Vertical_Search_Range: 10
      # Y-offset added to the player's Y when searching for spawn ground. 0 = same level as player.
      Y_Offset: 0
      # Minimum number of open horizontal sides required at a spawn position.
      Min_Open_Sides: 2
      # Radius within which a registered cop spawner is preferred over a random position (blocks).
      Spawner_Preference_Radius: 80.0
      # Distance within which another player triggers the visibility check during despawn (blocks).
      Visibility_Check_Distance: 48.0
      # Number of attempts in phase-1 (preferred ring, behind-player) of spawn location selection.
      Phase1_Attempts: 20
      # Number of attempts per shrink step in phase-2 of spawn location selection.
      Phase2_Attempts: 15
   # Controls automatic civilian spawning from registered spawner points based on player proximity.
   # Civilians spawn at spawner locations when a player enters the activation radius, similar to
   # Minecraft village spawning. They despawn when all players move beyond the despawn radius.
   Spawner_Proximity:
      # Radius within which a player activates a civilian spawner (blocks). Large values mean
      # civilians are already present when the player arrives at the location.
      Activation_Radius: 60.0
      # Radius beyond which all civilians from a spawner are despawned when no player is within it (blocks).
      # Should be larger than Activation_Radius to avoid immediate despawn on the boundary.
      Despawn_Radius: 80.0
      # Maximum number of civilians that can be alive at once per spawner.
      Max_Npcs_Per_Spawner: 5
      # Distance (blocks) beyond which a wandering civilian is biased back toward its spawner.
      # Keeps NPCs from drifting off the map without forcing an abrupt despawn.
      Npc_Soft_Leash_Radius: 30.0
      # Distance (blocks) beyond which a wandering civilian is despawned individually, freeing the
      # Max_Npcs_Per_Spawner slot so a replacement can spawn. Should be < Despawn_Radius.
      Npc_Hard_Leash_Radius: 50.0
      # Ticks between each proximity check cycle. 100 ticks = 5 seconds at 20 TPS.
      Check_Interval: 100
      # Default civilian type ID to use when a spawner has no pinned type.
      # Must match a type key defined in civilians.yml. Leave empty to skip untyped spawners.
      Default_Type_Id: ""

# +------------+ #
# | Loot Chest | #
# +------------+ #
# The loot chest is a block that players can open to receive random rewards. These rewards can be configured from types
# of items in loot-chest.yml.
Loot_Chest:
   # The amount of time to wait before opening the loot chest in seconds.
   Countdown_Timer: 300
   # The sounds when interacting with a loot chest.
   Sound:
      Opening: BLOCK_CHEST_OPEN
      Locked: BLOCK_CHEST_LOCKED
      Closing: BLOCK_CHEST_CLOSE
   # The blocks that can be as loot chests.
   Allowed_Blocks:
      - CHEST
      - TRAPPED_CHEST
      - BARREL
      - SHULKER_BOX
      - ENDER_CHEST
   Rewards:
      # The amount of money to reward players for opening a loot chest.
      Money:
         Minimum: 10
         Maximum: 1_000
      # The amount of experience to reward players for opening a loot chest.
      Experience:
         Minimum: 5
         Maximum: 100
      # The commands to execute when a player opens a loot chest.
      Commands:
         - ""

# +--------------------+ #
# | Money Drop Config  | #
# +--------------------+ #
# Master switch for the cash item drop system. Variations, weights, and the
# per-source rules live in money.yml. When this flag is false, no entity drops
# money and the right-click / pickup handlers become inert.
Money_Drop:
   Enabled: true

# +-----------------+ #
# | Gadgets Config  | #
# +-----------------+ #
Gadgets:
   Jetpack:
      # Ticks of continuous thrust to ramp from 10% to 100% ascend power.
      Thrust_Ramp_Ticks: 20
      # Downward acceleration added per tick while descending (m/tick²).
      Descent_Accel: 0.022
      # Terminal descent speed — descent will not exceed this (negative = downward).
      Max_Descent_Speed: -0.5
      # Horizontal speed delta added per tick when a directional key is held (m/tick).
      Horiz_Influence: 0.03
      # Maximum horizontal speed while airborne (m/tick).
      Max_Horiz_Speed: 0.25
   Car:
      # Fraction of max speed used when reversing (0.5 = half max speed).
      Reverse_Speed_Ratio: 0.5
      # Multiplier applied to deceleration when the driver hard-brakes (sneak key).
      Hard_Brake_Multiplier: 3.0
      # Fuel units consumed per tick while the car is moving.
      Fuel_Consume_Per_Tick: 1

# +--------------------+ #
# | Block Regeneration | #
# +--------------------+ #

# Tuning for the weapon Break_Blocks modifier. Controls how blocks broken/cracked by gunfire
# return to their original state. Per-weapon mode (RESTORE / CRACK_ONLY / DESTROY) is configured
# on each weapon's Modifiers.Break_Blocks entry.
Block_Regeneration:
   # Ticks between a block fully breaking and reappearing in RESTORE mode (20 ticks = 1 second).
   Restore_Delay_Ticks: 100
   # Ticks between the last hit on a cracked block and the start of crack reverse-decay.
   Regeneration_Delay_Ticks: 100
   # Ticks between each crack-stage decrement during reverse-decay.
   Regeneration_Step_Ticks: 4

# +--------+ #
# | Trader | #
# +--------+ #

# General trader NPC knobs. Trait-specific numbers live in plugin/trader_traits.yml.
Trader:
   # Seconds between a trader being killed and respawning at the same location.
   Respawn_Cooldown: 60
   # Blocks within which a trader will head-track the nearest player.
   Head_Track_Radius: 8
   # Trait id substituted at load time if a stored trait is missing from trader_traits.yml.
   Fallback_Trait_Id: "easygoing"
   # Upper cap on the multiplier used by the shop price-editor's +/- buttons (via the custom-mode anvil).
   Max_Mode_Multiplier: 1000000
   # Player sell flow. Traders evaluate items dropped into the SellView against admin-defined
   # Sell_Categories in each shop file; a friendly trader pays closer to the listed base price.
   Sell:
      Max_Offer_Slots: 20
      Mood_Per_Sale: 0.02
   # Flat tip amount paid (in currency) when the player clicks the Tip button on the NegotiationView.
   Tip_Amount: 100.0

# +--------+ #
# | Banker | #
# +--------+ #

# General Banker NPC knobs. Every per-tier knob (max balance, daily deposit limit, interest rate, death-loss
# discount, weekly / monthly loans) lives in plugin/npc/bank_tiers.yml.
Banker:
   # Blocks within which a banker will head-track the nearest player.
   Head_Track_Radius: 8
   # Max health in half-hearts (20 = 10 hearts). Only applied when Invulnerable is false.
   Max_Health: 20.0
   # If true the banker cannot be damaged. Leave enabled in production — damaged bankers have no respawn loop.
   Invulnerable: true
   # Tier id substituted when a player's bank has no stored tier id or references a missing tier.
   Fallback_Tier_Id: "Basic"