1~~P2~~WeaponAddon.registerWeapon silently skips the whole file when Config_Version is present~~Log a warn (or actually implement versioned-config handling) instead of a bare `return;` so a Config_Version key doesn't make the weapon vanish with zero log output~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/WeaponAddon.java:48-51~~`String configVersion = root.get("Config_Version").asString().orNull(); if (configVersion != null) { return; }` — any weapon yml that carries a `Config_Version:` key (a normal versioning convention admins reach for) aborts registerWeapon() before any section is parsed, before the report is logged, and before `weapons.put(fileName, weapon)` runs. The weapon simply never appears in the catalogue, with no warning anywhere. No shipped weapon/*.yml currently sets this key, so it is a live trap for the next config author, not yet triggered. Was Gangland WP-39.~~High
2~~P1~~WeaponLoader swallows a weapon parse failure at info level, hiding it from admins~~Log the caught InvalidConfigurationException at log.warn/log.error, not log.info, so a broken weapon config isn't buried in routine startup chatter~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/file/WeaponLoader.java:29-37~~`this.load(true, fileHandler -> { try { weaponAddon.registerWeapon(ammunitionManager, fileHandler); } catch (InvalidConfigurationException exception) { log.info("There was a problem loading the weapon: {}", exception.getMessage()); } }, fileManager);` — any of the many InvalidConfigurationException throws inside GunWeaponParser/BiologicalWeaponParser/ThrowableWeaponParser/IncendiaryWeaponParser/MeleeWeaponParser (missing Selective_Fire, missing Projectile, missing Damage, bad Display_Item.Material, etc.) lands here and is logged at info, indistinguishable from normal startup noise on a server with Debug disabled. The weapon does correctly stay out of WeaponAddon's map (so tab-completion does not offer it, unlike the original Gangland report), but the admin gets no visible signal that a weapon failed to load at all. Partially reproduces Gangland WP-31 (log level only).~~High
3~~P2~~GunWeaponParser defaults Shoot.Projectile.Consumed_Amount to 0, giving an infinite magazine~~Default Consumed_Amount to 1 (or make it required) so an omitted key can't silently disable ammo consumption~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/GunWeaponParser.java:78~~`int projectileConsumed = projectile.get("Consumed_Amount").asInt().min(0).orDefault(0);` flows into `GunWeapon.consumeShot()`'s `setCurrentMagCapacity(Math.max(0, getCurrentMagCapacity() - projectileData.getConsumed()));` (GunWeapon.java:49) — with Consumed_Amount omitted or set to 0, the magazine capacity never decreases on shot, i.e. an infinite magazine. Every shipped weapon/*.yml explicitly sets `Consumed_Amount: 1`, so this is a latent trap for the next hand-authored gun config, not a live bug today. Was Gangland WP-13.~~High
4~~P2~~AmmunitionSectionParser allows Capacity/Restore/numbered-reload-amount of 0, feeding a division by zero in NumberedReload~~Enforce min(1) on Ammunition.Capacity/Restore and on the `-N` amount parsed out of Reload.Type (e.g. "numbered-0") before handing them to NumberedReload~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/AmmunitionSectionParser.java:38-59~~`int capacity = ammo.get("Capacity").asInt().min(0).orDefault(0);` and `int restore = ammo.get("Restore").asInt().min(0).orDefault(capacity);` both accept 0, and `typeAmount = Integer.parseInt(parts[1])` (line 57) for a `Type: numbered-0` string is likewise unvalidated. Both feed NumberedReload.java: `int numberOfInsertions = leftToInsert / ammunitionData.getRestore();` (line 65, divides by Restore) and `int maxPossibleInsertions = numberOfAmmunition / amount;` (line 78, divides by the numbered-reload amount) — either a Capacity/Restore of 0 or a `numbered-0` Type throws ArithmeticException the moment a player with an inventory reloads that weapon. No shipped weapon uses the "numbered" reload type (all use "instant"), so this is currently unreachable but crashes immediately once someone authors one. Was Gangland WP-11.~~High
5~~P2~~BiologicalWeaponParser lets Effects_Per_Level ship empty, unlike ThrowableWeaponParser's equivalent STUN check~~Validate Effects_Per_Level is non-empty in BiologicalWeaponParser the same way ThrowableWeaponParser validates STUN's Effects list, throwing InvalidConfigurationException instead of letting an empty list through~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/BiologicalWeaponParser.java:38~~`List<String> effectsPerLevel = shoot.get("Effects_Per_Level").asList().ofStrings().orEmpty();` has no emptiness check, whereas the sibling ThrowableWeaponParser explicitly guards its analogous list: `if (type == ThrowableType.STUN && effects.isEmpty()) { throw new InvalidConfigurationException(...); }` (ThrowableWeaponParser.java:66-68) — a real divergence between two structurally similar parsers. Downstream, BiologicalAction.java:116 does `data.getEffectsPerLevel().get(Math.min(level, data.getEffectsPerLevel().size()) - 1)`; with an empty list this is `Math.min(level, 0) - 1 = -1`, an IndexOutOfBoundsException thrown the first time the syringe gun's charge attack releases. The shipped syringe_gun.yml supplies 3 entries so this is currently latent, but any custom BIOLOGICAL weapon that omits the key crashes on use. Was Gangland WP-15.~~High
6~~P2~~Unknown Category and unmatched Material silently fall back to OTHER/FEATHER/IRON_PICKAXE with no warning~~Log a warn (Severity.WARNING on the ConfigReport, or log.warn) whenever WeaponType.getType/XMaterial.matchXMaterial falls through to its default, in both WeaponAddon and AmmunitionAddon~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/WeaponAddon.java:63-70~~`WeaponType category = WeaponType.getType(Objects.requireNonNull(categoryString));` resolves via `WeaponType.getType`'s `default -> OTHER;` (bartizan-api/.../WeaponType.java:19) for any typo'd Category, silently routing the weapon through GunWeaponParser (`case GUN, OTHER ->`) with zero warning. The same block falls back `material = XMaterial.FEATHER.get();` when the Material string doesn't resolve (lines 66-70), and AmmunitionAddon.java:77-79 does the analogous `var xMaterial = xMaterialOptional.orElse(XMaterial.IRON_PICKAXE);` for ammo. A pinning unit test (`TypeEnumParsingTest.weaponType_unknownCategory_becomesOther`) already documents the Category half of this as Observation #30. Was Gangland WP-30.~~High
7~~P3~~Durability.Base of 0 silently yields a durability-0 item instead of an error~~Require Durability.Base >= 1 (min(1)) in WeaponAddon so buildItem's division can't degrade to NaN~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/WeaponAddon.java:74-79~~`durability = (short) dur.get("Base").asInt().min(0).required().orDefault(0);` accepts 0. Weapon.java:207-208's `buildItem` then computes `(durability - currentDurability) * (builder.getItemMaxDurability() / (double) durability)` — with durability=0 and currentDurability defaulting to the same 0, this is `0 * (max/0.0)` = `0 * Infinity` = NaN, and `(short) Math.floor(NaN)` silently truncates to 0, so the item just renders at full/near-full durability instead of erroring. No shipped weapon uses Durability.Base: 0 (all are >= 1), so this is a dormant trap. Was Gangland WP-33.~~High
8~~P3~~The "flare" ammunition entry in ammunition.yml is dead — no weapon references it~~Either add a flare-firing weapon with Ammo_Type: flare, or delete the unused entry~~-~~bartizan-plugin/src/main/resources/items/ammunition.yml:45-50~~`flare:\n   Material: "SUGAR"\n   Name: "&cFlare&r"` is defined alongside 9mm/7,62/5,56/rocket/50_bmg/slugs/fuel_canister/pathogen_vial, but grepping every weapon/*.yml's `Ammo_Type:` key across all 22 shipped weapons shows only 9mm, 7,62, 5,56, pathogen_vial, fuel_canister, rocket and slugs are ever referenced — "flare" is never consumed, so it can never be given to a player through any weapon's reload path. Was Gangland WP-41.~~High
9~~P2~~Weapon.modifiersData stays null for any weapon whose yml has no Modifiers: section, NPE-ing every unguarded getModifiersData() caller~~Have WeaponAddon/ModifiersSectionParser always set an empty ModifiersData when the Modifiers: section is absent, instead of leaving the field null~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/ModifiersSectionParser.java:41-43~~`public static void apply(NodeReader root, Weapon weapon, ConfigReport report) { MappingNode modifiersSection = root.get("Modifiers").asMapping().orNull(); if (modifiersSection == null) return; ... weapon.setModifiersData(new ModifiersData()); }` only calls `setModifiersData` when a Modifiers: section exists; `Weapon.modifiersData` (bartizan-api Weapon.java:59) is a plain `@Getter` field with no other initializer, so it is `null` otherwise. Multiple call sites across the plugin call `weapon.getModifiersData()` unguarded, e.g. MeleeAction.java:78-82 (`weapon.getModifiersData().hasFlatDamage()`), BiologicalAction.java:118-119, IncendiaryAction.java:117-118, ThrowableAction.java:192-193, and WeaponRaytracerImpl.java:501/515/555-556 — every one NPEs the instant such a weapon fires or is hit. All 22 shipped weapon/*.yml files currently include a Modifiers: section, so this is dormant, but a config author who omits it (a perfectly valid-looking yml) breaks that weapon's combat path entirely. Was Gangland WP-7.~~High
10~~P1~~BartizanSettings crashes plugin startup if Money_Symbol is configured as an empty string~~Fall back to the default "$" when the read Money_Symbol is blank, before calling substring(0,1), e.g. via a helper that checks isBlank() first~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/file/BartizanSettings.java:96-99~~`moneySymbol    = str(root, "Money_Symbol", "$").substring(0, 1);` — `str()` only substitutes the "$" default when the key is *absent*; if an admin explicitly sets `Money_Symbol: ""` (a plausible way to try to disable the symbol), `str()` returns the empty string as-is and `"".substring(0, 1)` throws StringIndexOutOfBoundsException. This runs inside `BartizanSettings.initialize()`, a FILE-phase bean initializer, so the exception aborts the whole bean-instantiation pipeline and the plugin fails to boot — not a graceful per-feature degradation.~~High
11~~P1~~MeleeWeaponParser never calls setAllowedSelectiveFires, so every melee weapon can be cycled to AUTO/BURST via sneak+swap-hand despite Selective_Fire: single~~Have MeleeWeaponParser set weapon.setAllowedSelectiveFires(EnumSet.of(current)) (mirroring what GunWeaponParser/IncendiaryWeaponParser/BiologicalWeaponParser do via SelectiveFireSectionParser) instead of leaving the field unset~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/MeleeWeaponParser.java:24-43~~MeleeWeaponParser.parse() builds and returns a MeleeWeapon (lines 40-42) without ever calling `setAllowedSelectiveFires(...)`, unlike BiologicalWeaponParser.java:51-59 and IncendiaryWeaponParser.java:49-57, which both call `SelectiveFireSectionParser.parse(...)` and fall back to `weapon.setAllowedSelectiveFires(EnumSet.of(SelectiveFire.SINGLE))` when Allowed_Modes is absent. `Weapon.allowedSelectiveFires` (Weapon.java:71) has no default and stays null for melee weapons. crowbar.yml/knife.yml/machete.yml all configure `Selective_Fire: single` under their Attack: section, which WeaponAddon.applyOptionalShootConfig does read generically and apply via `setCurrentSelectiveFire` (WeaponAddon.java:180-183) — but with allowedSelectiveFires left null, `WeaponSelectiveFireChangeListener`'s sneak+swap-hand handler calls `weapon.getCurrentSelectiveFire().getNextState(weapon.getAllowedSelectiveFires())`, and `SelectiveFire.getNextState(Set)` treats a null/empty set as "no restriction" (SelectiveFire.java:41-42), so every shipped melee weapon silently cycles through AUTO/BURST/SINGLE via a completely normal player action, contradicting its configured single-mode-only intent. This is a divergence between the five weapon-type parsers, not covered by any existing test.~~High
