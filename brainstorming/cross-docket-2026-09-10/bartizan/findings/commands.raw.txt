# CM system (commands) - Bartizan bug scan - 2026-09-10
1~~P1~~WeaponGiveCommand/AmmunitionGiveCommand/WearableGiveCommand.giveWeapon(...) never bounds the parsed amount, so a negative amount throws NegativeArraySizeException and a zero/near-zero amount silently claims success with no item given~~Clamp the parsed amount to `[1, some sane max]` (or at least `>= 1`) right after Integer.parseInt succeeds, before computing `slots`, in all three give commands~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WeaponGiveCommand.java:104-117~~`weaponAmount = Integer.parseInt(args[3])` is never range-checked before `int slots = (int) Math.ceil(amount / (double) maxStackSize);` and `ItemStack[] items = new ItemStack[slots];`. For `amount=-100, maxStackSize=64`, `slots` evaluates to `-1`, so `new ItemStack[-1]` throws `NegativeArraySizeException`; the framework's `Argument.execute` catch-block forwards `throwable.getMessage()` (a bare number) to the admin as if it were a normal error, and `giveWeapon` never reaches its `return true`. For `amount=0` or a small negative that ceils to 0, `slots=0` so the loop never runs, `inventory.addItem(new ItemStack[0])` is a no-op, yet `giveWeapon` still returns `true` and the caller sends the "received" message — the admin is told the give succeeded while nothing was placed in the inventory. The identical unguarded pattern is duplicated in `AmmunitionGiveCommand.java:91-104` and `WearableGiveCommand.java:96-108`. A huge amount (e.g. 2_000_000_000) likewise drives an unbounded `new ItemStack[~31_000_000]` allocation with no cap.~~High
2~~P2~~plugin.yml's "weapon" alias on /bartizan collides with WeaponCommand's own subcommand label, breaking `/weapon give|info|list`~~Drop "weapon" from plugin.yml's `commands.bartizan.aliases` (keep only `btz`), or rename it to something that doesn't shadow a registered subcommand label~~-~~bartizan-plugin/src/main/resources/plugin.yml:17-23~~`plugin.yml` declares one Bukkit command, `bartizan`, with `aliases: [btz, weapon]`. When a sender types `/weapon give ak47 5`, Bukkit strips `weapon` off as the alias used to invoke the command and calls `CommandManager.onCommand(sender, command, "weapon", ["give","ak47","5"])`; `onCommand` then tries to match `args[0]="give"` against the registered subcommand labels (`weapon`, `ammo`, `wearable`, `debug`, `reload` from `registry`), finds none, and reports "unknown subcommand" with a did-you-mean suggestion. Only `/bartizan weapon give ...` or `/btz weapon give ...` actually reaches `WeaponGiveCommand`; the `weapon` alias is silently useless for every subcommand invocation and only works for the bare `/weapon` (shows the plugin info card via `show(sender)`).~~High
3~~P2~~WeaponGiveCommand's tab-completion source diverges from WeaponAddon's registered-weapon keys, offering names of weapon files that failed to parse (was Gangland WP-31)~~Have WeaponGiveCommand's name completion pull from `weaponAddon.getWeaponKeys()` (or `weaponManager`'s registered set), matching WeaponInfoCommand/WeaponListCommand, instead of `weaponLoader.getFiles()`~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WeaponGiveCommand.java:69-72~~WeaponGiveCommand's `name` OptionalArgument supplies completions via `return weaponLoader.getFiles().stream().map(FileHandler::getName).toList();` — every YAML file physically present in the weapon folder, regardless of whether it registered a `Weapon` — whereas WeaponInfoCommand.java:71-72 and WeaponListCommand.java use `weaponAddon.getWeaponKeys()`, which only contains successfully-parsed weapons. A weapon file that fails to load (bad material, missing key, etc.) still appears in `/bartizan weapon give <tab>`, but selecting it makes `weaponManager.getWeapon(player, null, name, true)` return null and the player gets `INVALID_WEAPON` instead of an item — the exact "parse failures still get tab-completion offered" pattern filed as WP-31 against the old Gangland location, now reproduced at Bartizan's new command site (AmmunitionGiveCommand/WearableGiveCommand do not have this divergence — they source completions from their own manager/addon, matching their info/list siblings).~~High
4~~P3~~BartizanMessages.WEARABLE_INVALID/WEARABLE_NOT_REGISTERED/WEARABLE_NOT_WEARABLE render with the plain prefix instead of the error prefix, unlike INVALID_WEAPON/INVALID_AMMO~~Change these three entries' `Type` from `PREFIX` to `ERROR` so they render through `Errors.Prefix` like INVALID_WEAPON/INVALID_AMMO~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/file/BartizanMessages.java:40-42~~`WEARABLE_INVALID("Errors.Wearable.Invalid", Type.PREFIX)`, `WEARABLE_NOT_REGISTERED("Errors.Wearable.Not_Registered", Type.PREFIX)` and `WEARABLE_NOT_WEARABLE("Errors.Wearable.Not_Wearable", Type.PREFIX)` all use `Type.PREFIX`, which `getValue()` renders via `BartizanChatUtil.prefixMessage(data)` — i.e. the generic `Normal.Prefix` ("&8&l[&7B&fT&7Z&8&l]&7 "). Every sibling failure message for weapons/ammo (`INVALID_WEAPON`, `INVALID_AMMO`) is `Type.ERROR`, rendered with `Errors.Prefix` ("&4Error&7: "). So `WearableGiveCommand`/`WearableInfoCommand` failures (wrong name, unregistered key, item not a wearable) read like plain informational chat instead of an error, inconsistent with the identical weapon/ammo error paths in the same command package.~~Medium
