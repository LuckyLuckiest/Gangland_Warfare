# BZ-CF config-parsing — verified findings (Bartizan @ 0e18356)
1~~P2~~WeaponLoader logs a weapon parse failure at log.info, burying a broken config in startup chatter~~Bump `log.info` to `log.error` (or at least `log.warn`) in WeaponLoader.initialize()'s catch block so a weapon that silently drops out of the catalogue is visible above routine startup chatter, independent of whether the failure came from a ConfigReport-tracked issue or an early structural throw.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/file/WeaponLoaderTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/file/WeaponLoader.java:73-75~~The load callback is `try { weaponAddon.registerWeapon(ammunitionManager, fileHandler); } catch (InvalidConfigurationException exception) { log.info("There was a problem loading the weapon: {}", exception.getMessage()); }`. Every validation throw in the five weapon parsers — missing Selective_Fire, missing Projectile, missing Damage, an unresolvable Display_Item.Material — lands here and is reported at info level, indistinguishable from routine startup output. The weapon does correctly stay out of WeaponAddon's map so tab-completion no longer offers it, which is an improvement on the original Gangland report, but the admin gets no visible signal that a weapon silently vanished from the catalogue. Partially reproduces Gangland WP-31, log level only.~~High
2~~P2~~WeaponAddon.registerWeapon returns early and skips the entire file when a Config_Version key is present~~Either implement real per-version handling, or at minimum log a ConfigReport WARNING and fall through to parse the rest of the file instead of returning immediately.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/configuration/WeaponAddonTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/WeaponAddon.java:70-73~~registerWeapon opens with `String configVersion = root.get("Config_Version").asString().orNull(); if (configVersion != null) { return; }`, aborting before any section is parsed, before the ConfigReport is logged, and before the weapon is put into the catalogue map. Adding a `Config_Version:` key — an ordinary versioning habit for a config author — therefore makes the weapon disappear with zero output anywhere. No shipped weapon YAML sets the key, so this is a live trap rather than a current break. Was Gangland WP-39.~~High
3~~P2~~GunWeaponParser defaults Shoot.Projectile.Consumed_Amount to 0, giving an omitted key an infinite magazine~~Change `.min(0).orDefault(0)` to `.min(1).orDefault(1)` at GunWeaponParser.java:147.~~GunWeaponParserTest, GunWeaponParserTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/GunWeaponParser.java:147~~The parser reads `int projectileConsumed = projectile.get("Consumed_Amount").asInt().min(0).orDefault(0);`, which flows into GunWeapon.consumeShot()'s `setCurrentMagCapacity(Math.max(0, getCurrentMagCapacity() - projectileData.getConsumed()));`. With the key omitted the subtraction is a no-op while consumeShot() still returns true, so the gun fires forever and never needs a reload. All 26 shipped weapon YAMLs set Consumed_Amount: 1, making this a latent trap for the next hand-authored gun rather than a default-config break. Was Gangland WP-13.~~High
4~~P2~~AmmunitionSectionParser accepts Capacity, Restore and a numbered-reload amount of 0, feeding division by zero in NumberedReload~~Enforce `.min(1)` on Ammunition.Capacity and Restore, and clamp a parsed `numbered-N` amount to at least 1 (falling back to 1 with a ConfigReport WARNING on 0 or a parse failure) in AmmunitionSectionParser.~~AmmunitionSectionParserTest, AmmunitionSectionParserTest, AmmunitionSectionParserTest, AmmunitionSectionParserTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/AmmunitionSectionParser.java:53,55,70-77 and bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/reload/NumberedReload.java:67,81~~`int capacity = ammo.get("Capacity").asInt().min(0).orDefault(0);` and `int restore = ammo.get("Restore").asInt().min(0).orDefault(capacity);` both permit 0, and the `typeAmount = Integer.parseInt(parts[1])` for a `Type: numbered-0` string is unvalidated. NumberedReload then divides by both: `int numberOfInsertions = leftToInsert / ammunitionData.getRestore();` and `int maxPossibleInsertions = numberOfAmmunition / amount;`. Either a zero Restore or a numbered-0 type throws ArithmeticException the moment a player reloads that weapon. Every shipped weapon uses instant reload, so this is currently unreachable but crashes immediately once someone authors a numbered one. Was Gangland WP-11.~~High
5~~P2~~BiologicalWeaponParser never validates that Effects_Per_Level is non-empty, unlike ThrowableWeaponParser's STUN check~~The crash is fixed; the remaining gap is purely the missing parser-level validation. Add the same guard ThrowableWeaponParser uses - a ConfigReport WARNING (or InvalidConfigurationException, to match the sibling parser's severity) when Effects_Per_Level is empty for a BIOLOGICAL weapon, so an admin who omits the key finds out instead of shipping a silently inert weapon.~~BiologicalWeaponParserTest, BiologicalWeaponParserTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/BiologicalWeaponParser.java:39 (parser gap still open); bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/BiologicalAction.java:100-108,116-129 (crash now guarded)~~The parser reads `List<String> effectsPerLevel = shoot.get("Effects_Per_Level").asList().ofStrings().orEmpty();` with no emptiness check, while ThrowableWeaponParser explicitly guards its analogous list with `if (type == ThrowableType.STUN && effects.isEmpty()) { throw new InvalidConfigurationException(...); }` — a direct divergence between two structurally similar parsers. Downstream, BiologicalAction.java:115-116 indexes `data.getEffectsPerLevel().get(Math.min(level, data.getEffectsPerLevel().size()) - 1)`, which is get(-1) for an empty list and throws IndexOutOfBoundsException after ammo, sound and recoil have already been applied. The shipped syringe_gun.yml supplies three entries, so this is latent until someone authors a biological weapon without the key. Was Gangland WP-15.~~High
6~~P2~~An unknown Category and an unresolvable Material silently fall back to OTHER, FEATHER and IRON_PICKAXE with no warning~~In WeaponAddon, add a ConfigReport WARNING when WeaponType.getType or the XMaterial match falls through to its default. In AmmunitionAddon, add a log.warn (it has no ConfigReport wiring) when the ammo Material fails to resolve and falls back to IRON_PICKAXE.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/configuration/WeaponAddonTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/configuration/AmmunitionAddonMaterialWarningTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/WeaponAddon.java:85-92 and bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/AmmunitionAddon.java:77-78~~`WeaponType category = WeaponType.getType(Objects.requireNonNull(categoryString));` resolves any typo'd Category through WeaponType.getType's `default -> OTHER;`, which silently routes the weapon into GunWeaponParser because that parser handles `case GUN, OTHER ->`. The same block falls back to `material = XMaterial.FEATHER.get();` when the Material string does not resolve, and AmmunitionAddon.java:77-79 does the analogous `xMaterialOptional.orElse(XMaterial.IRON_PICKAXE)`. A weapon with a misspelled category or material therefore loads as a subtly wrong item with no diagnostic at all. Was Gangland WP-30.~~High
7~~P2~~MeleeWeaponParser never calls setAllowedSelectiveFires, so melee weapons cycle to AUTO and BURST despite Selective_Fire single~~Call `SelectiveFireSectionParser.parse(shoot, report, base.fileName())` in MeleeWeaponParser and apply its result the same way IncendiaryWeaponParser does (default to SelectiveFire.SINGLE / EnumSet.of(SINGLE) when Selective_Fire is absent from a melee YAML).~~MeleeWeaponParserTest, MeleeWeaponParserTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/MeleeWeaponParser.java:24-43~~parse() builds and returns a MeleeWeapon without ever calling setAllowedSelectiveFires, while GunWeaponParser.java:117, BiologicalWeaponParser.java:55-58 and IncendiaryWeaponParser.java:53-56 all set it and fall back to an explicit single-mode EnumSet. Weapon.allowedSelectiveFires has no default and stays null, and SelectiveFire.getNextState(Set) opens with `if (allowed == null || allowed.isEmpty()) return getNextState();` — documented as no restriction. crowbar.yml, knife.yml and machete.yml all configure Selective_Fire: single, yet a normal sneak plus swap-hand cycles them through AUTO and BURST. Melee firing behaviour is unaffected because WeaponInteract only reads the mode off gun and incendiary typed variables, so the damage is a wrong mode shown in the action bar, in /bartizan weapon info and in the item's SELECTIVE_FIRE NBT tag.~~High
8~~P2~~BartizanSettings.init throws StringIndexOutOfBoundsException and aborts plugin boot when Money_Symbol is set to an empty string~~Guard the substring: `String raw = str(root, "Money_Symbol", "$"); moneySymbol = raw.isEmpty() ? "$" : raw.substring(0, 1);`~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/file/BartizanSettingsTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/file/BartizanSettings.java:131~~init() runs `moneySymbol = str(root, "Money_Symbol", "$").substring(0, 1);` and the str() helper is `if (parent == null) return def; return parent.get(key).asString().orDefault(def);` — the default only substitutes for an absent key, so an explicitly configured `Money_Symbol: ""` returns the empty string and substring(0, 1) throws. This runs inside a FILE-phase bean initializer, so the exception aborts the whole bean-instantiation pipeline and the plugin fails to enable rather than degrading one feature. Confidence is Medium only because it depends on Keystone's orDefault treating an explicit empty value as present rather than absent.~~Medium
9~~P3~~WeaponAddon accepts Durability.Base of 0, producing a NaN durability scale that silently truncates to 0~~Change `.min(0)` to `.min(1)` on Durability.Base in WeaponAddon so the downstream divisions can never see a zero denominator.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/configuration/WeaponAddonTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/WeaponAddon.java:101; bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/Weapon.java:460-461; bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/durability/DurabilityCalculator.java:28,53~~`durability = (short) dur.get("Base").asInt().min(0).required().orDefault(0);` permits 0. Two independent consumers then divide by it with no guard: Weapon.buildItem() at Weapon.java:207-208 computes `(durability - currentDurability) * (builder.getItemMaxDurability() / (double) durability)`, and DurabilityCalculator.getWeaponDurability computes `double scale = itemMaxDurability / weaponMaxDurability;`. Both yield NaN or Infinity, and the narrowing cast to short silently gives 0, so the item renders at full durability instead of surfacing the misconfiguration. Validating once at parse time fixes both call sites. Was Gangland WP-33.~~High
10~~P3~~The flare entry in ammunition.yml is dead — no shipped weapon references that ammo type~~Delete the unused flare entry, or add the flare-firing weapon it was written for~~-~~bartizan-plugin/src/main/resources/items/ammunition.yml:45-50~~The catalogue defines `flare:` with `Material: "SUGAR"` and `Name: "&cFlare&r"` alongside 9mm, 7,62, 5,56, rocket, 50_bmg, slugs, fuel_canister and pathogen_vial. Grepping the Ammo_Type key across every shipped weapon YAML shows only 9mm, 7,62, 5,56, pathogen_vial, fuel_canister, rocket and slugs are ever referenced, so flare can never be consumed or handed to a player through any weapon's reload path. Was Gangland WP-41.~~High
11~~P2~~A missing Information.Category or Information.Material throws an uncaught NullPointerException instead of the clean per-file skip the rest of the loader is built for~~Drop the Objects.requireNonNull wrapping on categoryString/materialString and throw InvalidConfigurationException with an explicit message when either is null, same as the existing `if (informationSection == null) throw new InvalidConfigurationException(...)` pattern two lines above — matching displayName's non-throwing `.required().orNull()` read but adding the explicit null check + clean exception before WeaponType.getType()/XMaterial.matchXMaterial() are called.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/configuration/WeaponAddonTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/configuration/WeaponAddonTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/WeaponAddon.java:85-89 (registerWeapon)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] `String categoryString = information.get("Category").asString().required().orNull(); WeaponType category = WeaponType.getType(Objects.requireNonNull(categoryString));` and the identical pattern two lines later for `materialString`. Every other `.required().orNull()` read in this same method (e.g. `displayName` on line 83) is deliberately non-throwing by design — `.required()` just records a ConfigReport ERROR and still returns null/default so the caller can degrade gracefully, and `WeaponLoader.initialize()`'s per-file catch is written specifically to expect a clean `InvalidConfigurationException` with a readable message. These two lines defeat that contract: if a weapon YAML is missing `Information.Category` (e.g. a copy-pasted custom weapon with a typo'd `Catagory:` key), `categoryString` is null and `Objects.requireNonNull(null)` throws a bare, message-less `NullPointerException`. `WeaponLoader`'s lambda only catches `InvalidConfigurationException`, so the NPE escapes it and is instead caught by `FolderLoader.loadData()`'s generic `catch (Exception exception)`, which logs `log.error("...There was a problem registering the {} {}", ..., exception)` with a raw NPE stack trace and no mention of which key is actually missing — a materially worse diagnostic than the InvalidConfigurationException path every other required-field violation in this file produces (e.g. the explicit `throw new InvalidConfigurationException("Information section not found...")` two lines earlier for a missing `Information:` block).~~High
12~~P1~~WeaponAddon.weapons and AmmunitionManager.ammunition are never pruned on reload, so a weapon/ammo entry that stops parsing keeps serving its stale pre-edit definition forever~~Clear both catalogues from hooks the reload pass already calls. In WeaponLoader, add `@Override public void onClear() { clear(); weaponAddon.clear(); }`. This is BeanLifecycle.onClear. FolderLoader.clear() is only reached through FileManager.onClear() for registered initializers, and WeaponLoader is never registered, so overriding clear() alone would never run. Calling super's clear() also drops folderFiles, so a deleted custom weapon file is not re-registered from its stale in-memory config. In AmmunitionAddon, add `@Override public void clear() { ammunitionManager.clear(); }`. AmmunitionAddon is a registered initializer (FilesConfig:72), so FileManager.onClear() reaches it. All onClear calls finish before any onInitialize(false), so both maps are empty before they are re-parsed.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/file/WeaponLoaderOnClearTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/configuration/AmmunitionAddonClearTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/file/WeaponLoader.java:68-77 (no onClear override) and configuration/AmmunitionAddon.java:27-62 (no clear() override); stale maps are WeaponAddon.java:49/165/195 and ammo/AmmunitionManager.java:13/29~~[found 2026-09-24, 0.5.0 @ 9fa25c3] `weapons` is a plain HashMap populated only by `weapons.put(fileName, weapon)` at the end of a successful `registerWeapon` call; `WeaponAddon.clear()` exists (line 195) but is called from nowhere in the codebase (verified by grepping every `.clear()` call site against `weaponAddon`/`ammunitionManager`). `WeaponLoader` and `AmmunitionAddon` are both bean-lifecycle-driven: on `/bartizan reload`, `BeanFactory.reloadLifecycleBeans()` calls each bean's `onClear()` then `onInitialize(false)`, and `WeaponLoader`/`FolderLoader.clear()` only empties its own `folderFiles` file-discovery list, never `WeaponAddon.weapons`; `AmmunitionAddon` doesn't override `FileInitializer.clear()` at all (it's the interface's no-op default), and `AmmunitionManager.clear()` (AmmunitionManager.java:29-31) is likewise never invoked by anyone. Concretely: an admin edits `weapon/pistol.yml` on a live server to fix a balance issue but introduces a typo that trips a real parse error (e.g. sets both `Ammo_Type` and `Types`, hitting the fatal `ammo.both_ammo_type_and_types` code, or breaks the `Information:` block). They run `/bartizan reload`. `registerWeapon` throws before reaching `weapons.put("pistol", ...)`, so the OLD, pre-edit `Weapon` object for `pistol` — loaded at boot — is left completely untouched in the map. The admin sees only a buried `log.info` line (already BZ-CF-01) or an `log.error` and reasonably assumes their change either applied or the weapon is now broken/unavailable; in fact the previous (possibly exploitable/overpowered) weapon definition keeps being handed out via `/bartizan weapon give pistol` and functioning exactly as before, with no way to discover this short of a full server restart. The identical gap exists for `AmmunitionManager.ammunition` via `AmmunitionAddon.registerAmmunition`'s `manager.register(key, ammo)` (AmmunitionAddon.java:86) — a removed ammo id in `items/ammunition.yml` stays fully registered and givable after a reload.~~High
13~~P2~~ModifiersSectionParser silently drops every malformed Modifiers.* DSL entry with zero ConfigReport warning and zero log line~~In each of the five methods, replace the bare `continue`/`return`/`ignored` catch with a `report.add(Severity.WARNING, modifiers.mapping().location(), modifiers.mapping().path() + ".<Key>", "malformed <Key> value '" + raw + "'", "modifiers.<key>.malformed")` call, mirroring the pattern already used in applyBreakBlocks' bad-mode-token branch (line 75) and in EffectsSectionParser.parseEntry (missing/unknown Type). Threading `report` into applyRicochet/applyTracer/applyArmorPiercing/applyFlatDamage requires adding the `ConfigReport report` parameter to their signatures (only applyPenetration currently receives it) and updating the four call sites in `apply()` (lines 54-57).~~ModifiersSectionParserTest, ModifiersSectionParserTest, ModifiersSectionParserTest, ModifiersSectionParserTest, ModifiersSectionParserTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/ModifiersSectionParser.java — applyPenetration (lines 96, 102-103: `if (parts.length == 3) { try {...} catch (NumberFormatException ignored) {} }` with no else-branch warning), applyRicochet (144-145 `if (parts.length != 3) continue;` and 154 `catch (NumberFormatException ignored) {}`), applyTracer (163 `if (parts.length != 3) return;` and 173 `catch (NumberFormatException | IndexOutOfBoundsException ignored) {}`), applyArmorPiercing (183 `catch (NumberFormatException ignored) {}`), applyFlatDamage (193 `catch (NumberFormatException ignored) {}`)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] applyPenetration/applyRicochet/applyTracer/applyArmorPiercing/applyFlatDamage all parse a custom hyphen-delimited DSL (`Penetration: 2-3-0.25`, `Tracer: FF5500-true-0.8`, etc.) with a bare `if (parts.length != N) return/continue;` or a `catch (NumberFormatException ignored) { }` — neither path ever calls `report.add(...)` or `log.warn(...)`. Every other section parser invoked from the same `WeaponAddon.registerWeapon` call chain (EffectsSectionParser, ExplosionSectionParser, HudSectionParser, SkinSectionParser, StatusSectionParser, even ModifiersSectionParser's own `applyBreakBlocks` for a bad Break_Blocks *mode* token) consistently routes a malformed value through `ConfigReport` WARNING so an admin can find it in the reload/startup log. Concrete trigger: an admin editing `rifle.yml`'s `Tracer: FF5500-true-0.8` accidentally drops the `-0.8` (`Tracer: FF5500-true`) or writes a 4-part `Penetration: 2-3-0.25-extra`. `parts.length` no longer equals 3, the method returns immediately, the weapon loads with zero indication anywhere (console, ConfigReport, in-game) that its tracer/penetration modifier silently vanished — the admin has to notice the missing in-game effect and reverse-engineer the DSL by reading source, since even `report.log(log)` in WeaponAddon.registerWeapon (line 153) has nothing to print for this failure.~~High
14~~P2~~An unrecognised Shoot.Selective_Fire (or Reload.Type) value silently becomes a different, live fire mode instead of erroring or warning~~Add SelectiveFire.fromKey(String): Optional<SelectiveFire> and ReloadType.fromKey(String): Optional<ReloadType> (mirroring HandlingData.Trigger.fromKey/HandlingData.Rule.fromKey in bartizan-api/.../dto/HandlingData.java:55-107), keep getType(String) as a thin default-on-empty wrapper for callers without a ConfigReport, and at each of the three call sites above use fromKey and report.add(Severity.WARNING, ..., "selectiveFire.unknown_mode"/"reload.unknown_type") when it's empty, falling back to the current default — the same pattern WeaponAddon.applyHandling already uses via warnBadHandlingValue (WeaponAddon.java:343-357,413).~~SelectiveFireSectionParserTest, SelectiveFireSectionParserTest, SelectiveFireSectionParserTest, AmmunitionSectionParserTest, WeaponAddonTest (pins the fix)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/SelectiveFire.java:13-19 (getType) and bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/reload/ReloadType.java:20-26 (getType), consumed silently at bartizan-plugin/.../configuration/parser/SelectiveFireSectionParser.java:44-57 (Shoot.Selective_Fire + Allowed_Modes loop), bartizan-plugin/.../configuration/WeaponAddon.java:231-234 (applyOptionalShootConfig's independent Selective_Fire read for beam/incendiary/biological), and bartizan-plugin/.../configuration/parser/AmmunitionSectionParser.java:70,79 (Reload.Type)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] `SelectiveFire current = SelectiveFire.getType(currentString);` — `SelectiveFire.getType` (bartizan-api/.../SelectiveFire.java:13-19) is `switch (type.toLowerCase()) { case "single" -> SINGLE; case "burst" -> BURST; default -> AUTO; }`, i.e. ANY unrecognised string (a typo, wrong case handled fine, but e.g. `sinlge`, `Single_Fire`, a stray leading/trailing character) silently resolves to AUTO — a materially different, live gameplay behaviour (full-auto vs single-fire), not a cosmetic default. The same `getType(raw)` call is reused unconditionally inside the `Allowed_Modes` loop (line 56), so a typo'd entry there is also silently coerced into AUTO rather than rejected, and can even paper over the mismatch check on lines 63-67 (a typo that happens to normalise to the same fallback as `current` passes validation cleanly). No ConfigReport entry, no log line, no error is ever produced — contrast with `HandlingData.Trigger`/`HandlingData.Rule` two files over in WeaponAddon.applyHandling, which use `Optional<...>.fromKey(...)` and explicitly warn via `warnBadHandlingValue` on an unrecognised value. The same silent-default idiom is reachable a second way in the same package: `AmmunitionSectionParser.java:70-80` reads `Reload.Type`, and `ReloadType.getType` (bartizan-api/.../reload/ReloadType.java:20-25) likewise defaults any unrecognised string to INSTANT with no diagnostic (e.g. `Type: 2-num`, a reversed numbered-reload spec, silently becomes a plain instant reload). Concrete trigger: admin editing a single-fire pistol accidentally writes `Selective_Fire: singel` — the weapon loads clean, no warning anywhere, and ships as a full-auto weapon instead of single-fire until someone notices in play.~~High
15~~P1~~AmmunitionConverter overwrites a freshly-attributed item with a stale pre-attribute ItemMeta snapshot, silently discarding name/lore/color overrides~~Delete the dead stale-meta block entirely (line 30's `meta` capture and lines 34-38's fallback) — applyAttributes(itemStack, attributes) is already sufficient because Ammunition.buildItem() always sets lore via ItemBuilder.setLore() before applyAttributes ever runs, so the fallback is redundant even when it isn't actively harmful. convert() becomes just buildItem() + applyAttributes() + return, matching WeaponConverter's pattern. If the fallback must be kept, re-fetch itemStack.getItemMeta() after applyAttributes() runs, not before.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/item/AmmunitionConverterTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/item/AmmunitionConverter.java:29-38 (convert()); reached via BartizanItemVocabulary.java:52 (registrar.converter({"ammunition","ammo"}, ammunitionConverter)) from Keystone's item-string parser used by loot chests, shops and signs (documentation/bartizan-api.md:431-435)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] convert() does `ItemStack itemStack = ammunition.buildItem(); ItemMeta meta = itemStack.getItemMeta();` BEFORE calling `applyAttributes(itemStack, attributes)`. Bukkit's getItemMeta() returns a detached copy, not a live view, so `meta` is a snapshot of the item as it existed pre-attributes. applyAttributes() then obtains its OWN meta internally, sets display name/lore/leather color from the `attributes` map, and calls itemStack.setItemMeta(...) with that new meta — this correctly lands on the itemStack. But immediately after, the converter runs `if (meta != null && !meta.hasLore()) { meta.setLore(ammunition.getLore()); itemStack.setItemMeta(meta); }`. `meta` is still the STALE, pre-attribute snapshot; calling itemStack.setItemMeta(meta) on it overwrites the whole meta object — including the display name and any lore/color applyAttributes just set — reverting the item to its un-attributed defaults. The guard `!meta.hasLore()` is true precisely for ammo types with no configured `Lore:` (Ammunition.buildItem() always feeds ItemBuilder.setLore(), which sets a non-null Bukkit lore list only when the underlying list is non-empty, so hasLore() tracks 'does this ammo have any lore text' faithfully). Concrete trigger: an admin defines a lootchest/shop/kit entry as `ammo:<type>{name=&6Rare Round}` (or `{lore=...}`) for any ammo whose ammunition.yml entry has no Lore: block — a completely ordinary, supported use of the per-instance attribute feature (ItemAttributes/WeaponConverter/WearableConverter apply it correctly; only AmmunitionConverter has this stale-snapshot bug). Wrong outcome: the item the player receives shows the ammo's plain default name, not 'Rare Round' — the attribute override is silently swallowed, with no warning anywhere.~~High
16~~P2~~AmmunitionItemRefresher falls back to a display-name/material match when an ammo item's registered type has been removed from config, risking a wrong-ammo match instead of failing safely~~In resolveAmmunition(), when Ammunition.isAmmunition(source) is true and the tag resolves to a non-empty name, resolve the outcome fully there: if ammunitionManager.getAmmunition(name) is null, log.warn the missing ammo id (source, item type) and `return null` immediately instead of falling into the Material+display-name loop below. Only reach the heuristic loop when the stack carries no ammo tag at all (untagged legacy/vanilla-material ammo), which is the loop's only legitimate use case.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/item/AmmunitionItemRefresherTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/item/AmmunitionItemRefresher.java:41-64 (resolveAmmunition)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] resolveAmmunition() first tries the item's own `ammo` NBT tag against ammunitionManager.getAmmunition(name); if that returns null (the ammo type that originally built this stack was renamed/removed from ammunition.yml since), it falls through to a loop matching ANY currently-configured ammo type by (Material == source.getType()) AND (colored display name == item's current display name) instead of returning null. If a shop/lootchest still holds a stale entry for a removed ammo id, and a currently-configured ammo type happens to share both the same vanilla Material and an identical display-name string (plausible when configs reuse generic materials like IRON_NUGGET and boilerplate names), refresh() silently substitutes that unrelated ammo type instead of leaving the stale entry unrefreshed/flagged. No warning is logged either way.~~High
17~~P2~~Explosion.Cluster/Airstrike Count has a floor but no ceiling, so one weapon config can fan out into an unbounded number of live sub-munition entities/tasks per detonation~~Add `.max(N)` to both reads, e.g. `cluster.get("Count").asInt().min(1).max(32).orDefault(3)` and the Airstrike equivalent — `NodeReader.IntAccess.max()` already exists (Keystone repo: keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/config/NodeReader.java:454), reports Severity.ERROR and falls back to orDefault(3) when exceeded, exactly like the existing GunWeaponParser Crit.Chance/Projectile.Drag and StatusSectionParser Contagion.Chance bounds.~~ExplosionSectionParserTest, ExplosionSectionParserTest, ExplosionSectionParserTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/ExplosionSectionParser.java:98 (parseCluster Count) and :110 (parseAirstrike Count)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] parseCluster and parseAirstrike both read Count as `cluster.get("Count").asInt().min(1).orDefault(3)` / `airstrike.get("Count").asInt().min(1).orDefault(3)` — a floor of 1 but no `.max(...)` ceiling, even though this same NodeReader/ConfigReport framework is used elsewhere in the parser package specifically to bound risky values (GunWeaponParser bounds Crit.Chance to [0,100] and Projectile.Drag to [0,1] with `.max(...)`; StatusSectionParser bounds Contagion.Chance to [0,1]), so the absence here is an oversight, not a framework limitation. ExplosionHandler's depth-0-only recursion guard correctly stops a cluster bomblet from spawning its own cluster/airstrike (no infinite recursion), but that guard does nothing about width: every count entry is a real spawnVisual() entity plus a full SteppedProjectileTask (its own RepeatingTimer, its own eventual ExplosionHandler.explode call with its own getNearbyEntities scan, its own up-to-512-block BlockDamageManager pass if Block_Damage is set) scheduled via Bukkit.getScheduler().runTaskLater. Trigger: a weapon YAML (hand-authored, imported, or a future WM import) sets `Explosion.Cluster.Count: 5000` (or Airstrike) with no other malformed key to catch it in review — every detonation of that weapon spawns 5000 simultaneous cosmetic entities and 5000 concurrent scheduler tasks, each potentially triggering its own nested explosion and block-damage scan, which is enough to stall or crash a live server on a single misfire.~~High
18~~P3~~Legacy multi-variant block names resolve silently to one arbitrary variant~~Product decision between two options. (a) Register the legacy multi-variant names as aliases of the existing groups in the static block: STAINED_CLAY and HARD_CLAY to the TERRACOTTA group, STAINED_GLASS and STAINED_GLASS_PANE to GLASS, and so on. (b) When `Material.getMaterial(upperName) == null` but XMaterial matched, log.warn('<name> is a legacy name resolved to the single material <X>; use a block group such as TERRACOTTA for all variants'). BlockGroupResolver needs @CustomLog for (b). Option (a) is the less surprising one.~~BlockGroupResolverTest: STAINED_CLAY resolves to the TERRACOTTA group under (a), or still resolves to one material and logs a warning under (b). A modern single name such as RED_TERRACOTTA resolves to exactly itself with no warning.~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/util/BlockGroupResolver.java:85-86~~Deferred from the 0.5.1 fix wave (descends from BZ-RT-09): Since BZ-RT-09, a single-material name goes through XMaterial.matchXMaterial, so the pre-1.13 name STAINED_CLAY resolves to {BLACK_TERRACOTTA} only, and the test pins exactly that. An admin who writes STAINED_CLAY, STAINED_GLASS, STAINED_GLASS_PANE, CARPET and so on almost certainly means every colour. They get one colour and no warning. This is better than the old empty set, but still a silent misread of Break_Blocks/Ricochet/Penetration config.~~high
