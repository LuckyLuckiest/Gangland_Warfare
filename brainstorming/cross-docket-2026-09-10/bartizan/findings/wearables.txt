# BZ-WE wearables — verified findings (Bartizan @ 0e18356)
1~~P0~~WearableEquipListener only sees InventoryClickEvent, so right-click and hotbar-swap equip bypass the wearable permission~~Same fix still applies to today's code: add a `PlayerInteractEvent` handler in `WearableEquipListener` that resolves the item in the player's main hand when it is about to be equipped via right-click (Bukkit fires the interact event before the item moves into the armor slot, so check `Wearable.isArmorItem(item)` and re-run the same permission check on a short delay or via `PlayerArmorStandManipulateEvent`/`EntityEquipmentChangedEvent`-equivalent — simplest is a 1-tick-later re-check of `player.getInventory().getHelmet()/getChestplate()/...` against the item that was in hand), add `HOTBAR_SWAP` to the recognised armor-slot actions in `resolveArmorBeingEquipped`, and add a `BlockDispenseArmorEvent` handler for dispenser-equip.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListener.java:39-92~~The class declares exactly one handler, `onArmorEquip(InventoryClickEvent event)`, and resolveArmorBeingEquipped only returns an item for an ARMOR-slot click whose action is `PLACE_ALL || PLACE_ONE || PLACE_SOME || SWAP_WITH_CURSOR`, or for a shift-click; every other case hits `return null;`. Right-clicking an armor piece held in hand — the ordinary vanilla way to equip armor — fires PlayerInteractEvent and never an InventoryClickEvent, and pressing a hotbar number key over an armor slot produces action HOTBAR_SWAP, which the whitelist omits. Neither path ever reaches `if (permission != null && !permission.isEmpty() && !player.hasPermission(permission))`, so any player can wear a permission-gated wearable by right-clicking it on, defeating bartizan.wearables.<key> entirely.~~High
2~~P2~~WearableAddon accepts any Traits key without validating it against TRAIT_TABLE, so a typo silently grants nothing~~In `WearableAddon.readTraits`, after lower-casing `traitKey`, check `Wearable.traitMaxLevel(traitKey) > 0` (already public, returns 0 uniquely for an unmapped key since every real entry's max level is >= 1) and if false call `report.add(Severity.WARNING, traitsSection.location(), joinPath(traitsSection.path(), traitKey), ownerDescription + " has an unrecognised Traits key '" + traitKey + "' - contributes nothing", "wearable.unknown_trait")`, mirroring `readAttributes`'s existing pattern in the same class.~~WearableAddonTest#unrecognisedTraitsKey_warnsAndContributesNothing (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/wearable/WearableAddon.java:296-307 (readTraits)~~The loader walks the Traits section and stores every key unconditionally with `traits.put(traitKey.toLowerCase(Locale.ROOT), level);`, with no check against the TRAIT_TABLE declared at Wearable.java:55. Wearable.traitBonus then does `double[] definition = TRAIT_TABLE.get(key); if (definition == null) return 0;`, so a misspelling such as REINFORCE instead of REINFORCED loads cleanly and contributes exactly zero protection. The same method logs warnings for a bad Material or Name a few lines earlier, so the silence here is a divergence rather than a house style, and the admin gets a wearable that looks configured but does nothing.~~High
3~~P3~~WearableEquipListener's javadoc documents drop-cancellation for non-droppable wearables that is not implemented~~Delete the stale javadoc paragraph (simplest, matches actual behaviour), or implement a `PlayerDropItemEvent` handler behind a new `droppable` flag on `Wearable`/`wearables.yml` if the feature is actually wanted.~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListener.java:17-28 (class javadoc)~~The class javadoc states `<p>On drop: if the wearable is marked as non-droppable the drop event is cancelled.` but the class declares only the single InventoryClickEvent handler. A case-insensitive grep for droppable and PlayerDropItemEvent across the wearable package, the api wearable package and wearables.yml returns nothing at all, so neither the flag nor the handler exists. The documented drop protection has never run in Bartizan and every wearable can be dropped freely.~~High
4~~P3~~The wearable permission-denied message is a hardcoded English string sent through Keystone ChatUtil, not BartizanMessages~~Add the string as a new `BartizanMessages` entry backed by a key in `message_en.yml`, and send it through `BartizanChatUtil` (with the existing money-symbol substitution pipeline) instead of raw Keystone `ChatUtil`.~~WearableEquipListenerTest#onArmorEquip_permissionDenied_sendsThroughBartizanMessages (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListener.java:54-59~~The denial path sends `player.sendMessage(ChatUtil.color("&cYou are not authorized to equip this armor."));` using the raw Keystone `org.luckyraven.keystone.util.ChatUtil` import. Every other user-facing string in the plugin resolves through the BartizanMessages enum backed by message/message_en.yml and is sent via BartizanChatUtil, which also applies the money-symbol substitution. This string is therefore untranslatable on a non-English server and is the only wearable message not present in message_en.yml.~~High
5~~P3~~Wearable.buildItem stamps wt_ trait and wr_base NBT tags that nothing ever reads back~~Drop the `NBT_BASE_REDUCE` and per-trait `NBT_TRAIT_PREFIX` stamps from `buildItem` (and the two now-unused constants), since every consumer already resolves live values from the registry by `NBT_KEY`.~~bartizan-api/src/test/java/org/luckyraven/bartizan/api/wearable/WearableBuildItemNbtTest (pins the fix)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/wearable/Wearable.java:366-374 (buildItem)~~buildItem writes `builder.addTag(NBT_BASE_REDUCE, baseDamageReduction);` and, per trait, `builder.addTag(NBT_TRAIT_PREFIX + entry.getKey(), entry.getValue());`. A repo-wide grep for NBT_TRAIT_PREFIX and NBT_BASE_REDUCE finds only their declarations and these two write sites — no reader anywhere. WearableItemSerializer, WearableRefresher and BartizanItemPredicates all key exclusively off NBT_KEY and look the rest up live in the registry. The stamped values are write-only data that silently diverges from wearables.yml the moment an admin edits a trait, with no consumer on either side.~~Medium
6~~P2~~WearableRefresher.canRefresh claims any wearable-tagged ItemStack, but WearableService.getWearable can miss for a foreign or stale tag - the refresh silently falls through instead of refreshing or reporting an error~~Still a design question rather than a one-line fix, as the original entry says: distinguish 'no wearable tag at all' (correctly not `WearableRefresher`'s concern) from 'has a wearable tag `WearableService` doesn't recognise' (worth at least a `log.warn`/diagnostic instead of a silent pass-through) in `canRefresh`/`refresh`. Needs a Bartizan-side decision on whether an unrecognised tag should log, or a `WEARABLE_REFRESH_MISS`-style event.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/item/WearableRefresherTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/item/WearableRefresher.java:21-28 (canRefresh)~~canRefresh only rejects a source with no wearable NBT tag at all (!hasNBTTag(Wearable.NBT_KEY) -> false); once the tag is present it returns true even when wearableService.getWearable(key) misses (unregistered/foreign/stale tag), relying on refresh() below to no-op safely (if (wearable == null) return null;, and ItemRefresherRegistry.java:62-70 falls through cleanly rather than throwing) - currently safe, not a crash, but a silent no-op with no diagnostic. As of the WS7 gadget wave (0.9.2/0.4.0) the method also carries a WS7-D4 comment and an !wearable.isExternal() exclusion for the jetpack's external-wearable registration, added for an unrelated reason but touching the same lines. Found WS7 G0-G1 review (this wave's setup gate), recorded as docket candidate (a); pre-existing, not introduced by and not itself touched by any WS7 gate.~~Medium
7~~X~~An externally-registered Wearable (WearableCatalog.register) has getPermission() return null by design, meaning none of Bartizan's own wearable-permission-check machinery ever applies to it~~None needed for the jetpack specifically (already covered by Gangland's own Jetpack.getPermission() check in JetpackService.activate); worth a documentation note in bartizan-api.md's "External wearable registration" section (already present, confirmed during this gate) - this row exists mainly to make the relationship to Gangland docket GD-07 explicit and searchable, not to request a code change~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/wearable/WearableCatalog.java (external flag handling); Wearable#getPermission()~~An externally-registered wearable (the mechanism Gangland's jetpack uses to keep its Bartizan-hosted armour traits since 0.9.2/0.4.0, see documentation/migration-0.9.2.md 3) is damage-only: never built, converted, given, listed or serialised by Bartizan, and getPermission() returns null unconditionally, so an external registrant gets no permission-node enforcement from Bartizan's side at all - by design, since Bartizan never builds/gives it and never runs its own equip-permission path for it. Cross-referenced against Gangland's open GD-07 ("the wearable permission is only checked on inventory clicks"), which is about Bartizan-native wearables' permission check being incomplete - a different case entirely from an external registrant having no Bartizan-side check at all. Found WS7 G0-G1 review, recorded as docket candidate (b).~~Medium
8~~P2~~A Set tier's REACTIVE trait level is parsed but never rolled or applied to damage~~In the set-tier loop (WearableService.java:253-260), roll the tier's own reactive level the same way the per-piece loop does at line 233 via Wearable.rollReactive(): `if (tier.traits().getOrDefault("reactive", 0) > 0 && ThreadLocalRandom.current().nextDouble() < Wearable.traitBonusForLevel("reactive", tier.traits().getOrDefault("reactive", 0))) return 0;` — add the import for java.util.concurrent.ThreadLocalRandom, place the check before (or alongside) the reinforced/bulletproof setReduction computation to mirror the per-piece ordering (reactive checked first, line 232-233), and update the method's javadoc (lines 199-203) plus the "review, §2" note to say the set fold-in now covers reactive too — currently the javadoc explicitly says only reinforced/bulletproof fold in, which is the other half of this same inconsistency.~~WearableServiceTest#applyWearableReduction_setReactiveTrait_canNullifyHit (Monte-Carlo, 5000 trials at 6% proc chance) (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/wearable/WearableService.java:220-263 (applyWearableReduction), specifically the set-tier fold-in loop at 253-260 — exactly as reported, and the line numbers are current (not stale; verified against HEAD f16cfbd).~~[found 2026-09-24, 0.5.0 @ 9fa25c3] wearables.yml's own header comment (lines 28-33) documents that REINFORCED/BULLETPROOF/REACTIVE are all resolved per worn piece, and that 'an active worn Set's own bonus level ... folds in as one further, SEPARATE discount for these three'. applyWearableReduction's per-piece loop does roll reactive from each worn item's own traits (`if (wearable.rollReactive()) return 0;`, line 233), and the set-tier loop right after it does fold in a Set tier's reinforced/bulletproof levels (`Wearable.traitBonusForLevel("reinforced", tier.traits().getOrDefault("reinforced", 0))` and the matching bulletproof line, 254-257) — but that loop never reads `tier.traits().getOrDefault("reactive", 0)` at all. `Wearable` is a plain POJO with no reference back to WearableService/SetTier, so a Set-tier REACTIVE level has no other code path to reach a roll. resolveTraitLevels() does sum 'reactive' into its body-wide totals map (WearableService.java:140-149), but nothing ever reads that map's 'reactive' key either (the only readers of resolveTraitLevels are toughened/insulated/fire_resistant and, via traitLevel, StatusEffectService's 'sealed' lookup). Concretely: an admin adds `Sets.<name>.Pieces_N.Traits: { REACTIVE: 2 }` to wearables.yml expecting an extra ~4% per-hit dodge chance once N pieces of that set are worn (exactly the treatment REINFORCED/BULLETPROOF get in the same tier), tests it, and the set-granted reactive level silently never rolls — only a piece's own individually-configured REACTIVE level (as currently shipped only on gang_jacket/gang_beanie, neither of which is in a Set) ever procs.~~High
9~~P2~~A Set tier's SWIFT trait level is summed body-wide but has no consumer, so it's a complete no-op~~Reject/warn on 'swift' (and any other build-time-only trait, i.e. any TRAIT_TABLE key not read by resolveTraitLevels' consumers) inside a Sets.*.Traits block: add the check once in WearableAddon.readTraits (or right after it in loadSets, since readTraits is the single shared parser for both per-wearable and per-set-tier Traits:) emitting a Severity.WARNING via the existing ConfigReport (same pattern as the unrecognised-tier-key warning at loadSets:189-193) and dropping just that one key, plus fix the wearables.yml Sets: header comment (line 81) to carve out the SWIFT exception explicitly. This is the lazy root-cause fix (one guard in the shared loader path) versus building a whole new runtime MOVEMENT_SPEED attribute-apply/remove lifecycle in WearableEffectsService just to make a corner case that's never even used in shipped config (grep confirms SWIFT appears nowhere under Sets: in wearables.yml today) actually work.~~WearableAddonTest#swiftUnderSetTier_warnsAndDropped (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/wearable/WearableAddon.java:173-208 (loadSets, never calls appendSwiftAttribute for a SetTier's traits) and :296-307 (readTraits, shared by both per-wearable and per-set-tier Traits: blocks, accepts 'swift' unconditionally in both); bartizan-plugin/src/main/java/org/luckyraven/bartizan/wearable/WearableService.java:127-158 (resolveTraitLevels computes a real non-zero 'swift' total including Set contributions) with zero callers reading key 'swift' (only 'toughened' at :293, 'insulated' at :320, 'fire_resistant' at :342, and StatusEffectService's config-driven Cure.Wearable_Trait at StatusEffectService.java:93 which is for bio-status cure, not movement); bartizan-plugin/src/main/resources/items/wearables.yml:49-50 vs :81 (SWIFT's own doc line says build-time-stamped/not a runtime reduction 'like the others', but the Sets: header two lines later claims 'every other trait still merges into the body-wide sum' with no SWIFT carve-out)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] SWIFT's actual movement-speed effect is implemented entirely differently from every other trait: WearableAddon.loadWearables calls `appendSwiftAttribute(attributes, traits)` (line 252) using the WEARABLE ENTRY'S OWN parsed `traits` map, which bakes a MOVEMENT_SPEED ADD_SCALAR AttributeModifier directly onto that one item's NBT at build time (via AttributeModifiers.apply inside Wearable.buildItem). loadSets() (173-208), which parses `Sets.<name>.Pieces_N.Traits` into a SetTier record, never calls appendSwiftAttribute or stamps any attribute anywhere — a SetTier's traits map is only ever consumed by resolveTraitLevels' generic body-wide summation (WearableService.java:145-149), which computes a real non-zero total for 'swift' (TRAIT_TABLE's swift perLevel is 0.05, not 0) but that total is never read by any caller (grep confirms resolveTraitLevels' only readers key off 'toughened', 'insulated', 'fire_resistant', and 'sealed' via traitLevel). wearables.yml's own Sets: header comment (lines 78-83) claims 'every other trait still merges into the body-wide sum' with no exception carved out for SWIFT, contradicting the SWIFT trait's own description two lines earlier ('a MOVEMENT_SPEED attribute bonus stamped on the item at build time ... not a runtime-calculated reduction like the others'). Concretely: an admin adds `Sets.<name>.Pieces_N.Traits: { SWIFT: 2 }` expecting a set-bonus speed boost once N pieces are worn (as the doc implies); the player can wear the full set and gets zero speed change, with no warning anywhere that this trait key is silently inert when placed under a Set tier.~~High
10~~P1~~Worn-effect removal strips ANY active effect of that PotionEffectType, including one from an unrelated source~~Store what was granted, not just the type: change lastWornEffectTypes to Map<UUID, Map<PotionEffectType, Integer>> (type -> the highest granted Bukkit amplifier). When a type drops out, look up the player's current effect with player.getPotionEffect(type) and remove it only if current != null && current.getAmplifier() == grantedAmp && !current.isInfinite() && current.getDuration() <= WORN_EFFECT_DURATION_TICKS. Otherwise the active instance came from another source (a longer or stronger effect), so leave it alone. Known limit: if another source's stronger effect is sitting on top of the worn grant, vanilla keeps the worn grant as a hidden effect, and it can come back for up to 220 ticks after the other effect expires. That linger is bounded and much less harmful than the current wipe. Add one unit test with a mocked Player covering both cases: the service's own 220-tick grant is removed, and a longer same-type effect is kept.~~WearableEffectsServiceTest#isStillTheWornGrant_matchesAmplifierAndDurationWindow (extracted pure-arithmetic method; real PotionEffectType cannot be constructed/mocked in this test env) (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/wearable/WearableEffectsService.java:163-175 (removeDroppedWornEffects, the unconditional player.removePotionEffect(type) at line 172). The lastWornEffectTypes field at line 81 only stores types, not what was granted.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] removeDroppedWornEffects diffs this tick's granted Effects_While_Worn potion TYPES against last tick's, and for any type no longer granted calls `player.removePotionEffect(type)` (line 172) unconditionally. Bukkit's removePotionEffect(PotionEffectType) clears the player's current effect of that type regardless of what applied it — it has no concept of 'the wearable's own instance' versus an effect from any other source. The class's own javadoc on WORN_EFFECT_DURATION_TICKS (lines 53-60) confirms the intent is only to stop the wearable's OWN long-duration grant from lingering after unequip, but the implementation removes the type wholesale. Concretely, using the shipped hazmat_chest (Effects_While_Worn: 'SLOWNESS-60-1', wearables.yml:250-251): a player wears it (gets a refreshed 220-tick Slowness), is then separately hit by an unrelated Slowness source while still wearing it — a thrown splash potion of Slowness, or (in this same codebase) a biological/status weapon's on-hit potion effect (BiologicalAction applies PotionEffects directly to hit targets, per BZ-FA-01) — then removes the chestplate. The very next tick, removeDroppedWornEffects sees SLOWNESS is no longer granted by worn gear and calls player.removePotionEffect(SLOWNESS), instantly wiping out the player's still-active, independently-acquired Slowness dose from the other source, well before its real remaining duration would have expired.~~High
11~~P2~~Dispenser equip bypasses the bartizan.wearables.<key> permission gate~~Add to WearableEquipListener: `@EventHandler(priority = EventPriority.LOW, ignoreCancelled = true) public void onDispenseArmor(BlockDispenseArmorEvent event) { if (event.getTargetEntity() instanceof Player player && isPermissionDenied(player, event.getItem())) event.setCancelled(true); }` with braces on their own lines. BlockDispenseArmorEvent#getTargetEntity is available on the 1.16.5 compile floor. Remove the §4 row from docket-fix-wave-0.5.1.md.~~WearableEquipListenerTest: a denied Player target is cancelled; a permitted Player target is not; an ArmorStand target is never cancelled; a non-wearable item is not cancelled.~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListener.java:43-90~~Deferred from the 0.5.1 fix wave (descends from BZ-WE-01): BZ-WE-01 now gates hotbar-swap, right-click and drag/shift-click equips, but has no BlockDispenseArmorEvent handler. A player who holds a gated wearable without the permission can put it in a dispenser, stand in front of it and trigger it, and the dispenser equips the piece directly. docket-fix-wave-0.5.1.md §4 lists this as out of scope.~~high
12~~P1~~API decision: bartizan-api removed two public NBT constants from Wearable (NBT_TRAIT_PREFIX, NBT_BASE_REDUCE)~~DECISION for the user: bartizan-api is meant to be additive-only. Either restore the old signature as a deprecated bridge (delegating to the new one) or accept the break for 0.5.1 (no known consumer uses it; only Gangland's stale legacy copies). No code change requested - this is flagged for the user to decide whether to accept the removal as-is (recommended, since the tags were write-only and never read anywhere) or keep the two constants declared (unused) for one more release as a deprecation buffer before deleting them in a future major version.~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/wearable/Wearable.java:55~~Final review of the 0.5.1 fix wave (2026-09-25), deferred as a product decision: HEAD deletes `public static final String NBT_TRAIT_PREFIX = "wt_";` and `public static final String NBT_BASE_REDUCE = "wr_base";` from bartizan-api's public `Wearable` class (they existed at f16cfbd, gone at HEAD), and stops `buildItem()` from writing those tags at all. This is a removed public signature on the one artifact CLAUDE.md says a consumer plugin depends on (`provided` scope) - per the review task's own rule, any bartizan-api public signature removed is a required P1 api-compat finding regardless of whether it is judged safe, so it is flagged here for the user's decision rather than silently accepted. I independently verified the implementer's own ruling: I greped the sibling Gangland Warfare repo (`E:/Programming/java/Gangland Warfare [Cubed-GTA recoded]`) for `NBT_TRAIT_PREFIX`/`NBT_BASE_REDUCE`/`wr_base`/`"wt_"` and for any import of `org.luckyraven.bartizan.api.wearable.Wearable` - the only in-repo occurrences of those constant names are in Gangland's own pre-split legacy copy at `org.luckyraven.gangland.item.wearable.Wearable` (a different, unrelated class in `gangland-item`, not bartizan-api), and the one Gangland file that does import bartizan-api's `Wearable` (`JetpackBartizanTraitBridge.java`) only calls `Wearable.builder()`, never the two removed constants. So no in-repo or Gangland consumer breaks today, matching the implementer's report ruling in wearables-report.md (BZ-WE-05 section: "a repo-wide grep confirms no in-repo or Gangland consumer ever referenced them"). This is a real, if low-risk, API removal that a third-party consumer plugin (outside this monorepo, which the review's visibility does not cover) could still reference; it is being surfaced per policy rather than as a confirmed break.~~high
