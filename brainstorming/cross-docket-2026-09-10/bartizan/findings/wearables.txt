# BZ-WE wearables — verified findings (Bartizan @ 0e18356)
1~~P0~~WearableEquipListener only sees InventoryClickEvent, so right-click and hotbar-swap equip bypass the wearable permission~~Add a PlayerInteractEvent handler for right-click equip, add HOTBAR_SWAP to the recognised armor-slot actions, and cover BlockDispenseArmorEvent~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListener.java:39-92~~The class declares exactly one handler, `onArmorEquip(InventoryClickEvent event)`, and resolveArmorBeingEquipped only returns an item for an ARMOR-slot click whose action is `PLACE_ALL || PLACE_ONE || PLACE_SOME || SWAP_WITH_CURSOR`, or for a shift-click; every other case hits `return null;`. Right-clicking an armor piece held in hand — the ordinary vanilla way to equip armor — fires PlayerInteractEvent and never an InventoryClickEvent, and pressing a hotbar number key over an armor slot produces action HOTBAR_SWAP, which the whitelist omits. Neither path ever reaches `if (permission != null && !permission.isEmpty() && !player.hasPermission(permission))`, so any player can wear a permission-gated wearable by right-clicking it on, defeating bartizan.wearables.<key> entirely.~~High
2~~P2~~WearableAddon accepts any Traits key without validating it against TRAIT_TABLE, so a typo silently grants nothing~~Validate each Traits key against Wearable's TRAIT_TABLE during load and log.warn on an unmapped key~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/wearable/WearableAddon.java:130-138~~The loader walks the Traits section and stores every key unconditionally with `traits.put(traitKey.toLowerCase(Locale.ROOT), level);`, with no check against the TRAIT_TABLE declared at Wearable.java:55. Wearable.traitBonus then does `double[] definition = TRAIT_TABLE.get(key); if (definition == null) return 0;`, so a misspelling such as REINFORCE instead of REINFORCED loads cleanly and contributes exactly zero protection. The same method logs warnings for a bad Material or Name a few lines earlier, so the silence here is a divergence rather than a house style, and the admin gets a wearable that looks configured but does nothing.~~High
3~~P3~~WearableEquipListener's javadoc documents drop-cancellation for non-droppable wearables that is not implemented~~Delete the stale javadoc paragraph, or implement a PlayerDropItemEvent handler behind a real droppable flag~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListener.java:17-31~~The class javadoc states `<p>On drop: if the wearable is marked as non-droppable the drop event is cancelled.` but the class declares only the single InventoryClickEvent handler. A case-insensitive grep for droppable and PlayerDropItemEvent across the wearable package, the api wearable package and wearables.yml returns nothing at all, so neither the flag nor the handler exists. The documented drop protection has never run in Bartizan and every wearable can be dropped freely.~~High
4~~P3~~The wearable permission-denied message is a hardcoded English string sent through Keystone ChatUtil, not BartizanMessages~~Add the string to BartizanMessages and message_en.yml and send it through BartizanChatUtil like the rest of the plugin~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListener.java:56-59~~The denial path sends `player.sendMessage(ChatUtil.color("&cYou are not authorized to equip this armor."));` using the raw Keystone `org.luckyraven.keystone.util.ChatUtil` import. Every other user-facing string in the plugin resolves through the BartizanMessages enum backed by message/message_en.yml and is sent via BartizanChatUtil, which also applies the money-symbol substitution. This string is therefore untranslatable on a non-English server and is the only wearable message not present in message_en.yml.~~High
5~~P3~~Wearable.buildItem stamps wt_ trait and wr_base NBT tags that nothing ever reads back~~Drop the per-trait and base-reduction stamps from buildItem, since every consumer resolves live values from the registry by NBT_KEY~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/wearable/Wearable.java:276-284~~buildItem writes `builder.addTag(NBT_BASE_REDUCE, baseDamageReduction);` and, per trait, `builder.addTag(NBT_TRAIT_PREFIX + entry.getKey(), entry.getValue());`. A repo-wide grep for NBT_TRAIT_PREFIX and NBT_BASE_REDUCE finds only their declarations and these two write sites — no reader anywhere. WearableItemSerializer, WearableRefresher and BartizanItemPredicates all key exclusively off NBT_KEY and look the rest up live in the registry. The stamped values are write-only data that silently diverges from wearables.yml the moment an admin edits a trait, with no consumer on either side.~~Medium
6~~P3~~WearableRefresher.canRefresh claims any wearable-tagged ItemStack, but WearableService.getWearable can miss for a foreign or stale tag - the refresh silently falls through instead of refreshing or reporting an error~~canRefresh should distinguish "no wearable tag at all" (correctly not its concern) from "has a wearable tag WearableService doesn't recognise" (arguably worth a diagnostic/fault report instead of silent pass-through) - needs a Bartizan-side design decision, not a one-line fix; filed as triage, not a ready-to-fix row~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/item/WearableRefresher.java:22-27 (canRefresh)~~canRefresh only rejects a source with no wearable NBT tag at all (!hasNBTTag(Wearable.NBT_KEY) -> false); once the tag is present it returns true even when wearableService.getWearable(key) misses (unregistered/foreign/stale tag), relying on refresh() below to no-op safely (if (wearable == null) return null;, and ItemRefresherRegistry.java:62-70 falls through cleanly rather than throwing) - currently safe, not a crash, but a silent no-op with no diagnostic. As of the WS7 gadget wave (0.9.2/0.4.0) the method also carries a WS7-D4 comment and an !wearable.isExternal() exclusion for the jetpack's external-wearable registration, added for an unrelated reason but touching the same lines. Found WS7 G0-G1 review (this wave's setup gate), recorded as docket candidate (a); pre-existing, not introduced by and not itself touched by any WS7 gate.~~Medium
7~~P3~~An externally-registered Wearable (WearableCatalog.register) has getPermission() return null by design, meaning none of Bartizan's own wearable-permission-check machinery ever applies to it~~None needed for the jetpack specifically (already covered by Gangland's own Jetpack.getPermission() check in JetpackService.activate); worth a documentation note in bartizan-api.md's "External wearable registration" section (already present, confirmed during this gate) - this row exists mainly to make the relationship to Gangland docket GD-07 explicit and searchable, not to request a code change~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/wearable/WearableCatalog.java (external flag handling); Wearable#getPermission()~~An externally-registered wearable (the mechanism Gangland's jetpack uses to keep its Bartizan-hosted armour traits since 0.9.2/0.4.0, see documentation/migration-0.9.2.md 3) is damage-only: never built, converted, given, listed or serialised by Bartizan, and getPermission() returns null unconditionally, so an external registrant gets no permission-node enforcement from Bartizan's side at all - by design, since Bartizan never builds/gives it and never runs its own equip-permission path for it. Cross-referenced against Gangland's open GD-07 ("the wearable permission is only checked on inventory clicks"), which is about Bartizan-native wearables' permission check being incomplete - a different case entirely from an external registrant having no Bartizan-side check at all. Found WS7 G0-G1 review, recorded as docket candidate (b).~~Medium
