# BZ-FA firing-actions — verified findings (Bartizan @ 0e18356)
1~~X~~BiologicalAction.fireRay impact handler applies potion effects but never damages, so biological shots deal 0 HP~~In the impactHandler call target.damage(damage, player) for LivingEntity hits as IncendiaryAction does, or drop the custom handler so the raytracer's default damage path runs~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/BiologicalAction.java:133-153~~fire() computes `double damage = data.getBaseDamage() * level + flatBonus;` and passes it to fireRay, which sets `.baseDamage(damage)` but also supplies a custom `.impactHandler(event -> { if (event.getHitEntity() instanceof LivingEntity target) { for (PotionEffect effect : effects) target.addPotionEffect(effect); } })`. Verified in WeaponRaytracerImpl.java:426-437: a non-null impact handler runs and then `return;`s before the default `living.damage(event.getDamage(), shooter)` block at line 448, so the handler short-circuits damage entirely. Every charged biological shot applies only potion effects and zero health damage no matter what Base_Damage or charge level is configured. Was Gangland WP-3.~~High
2~~P1~~ThrowableAction EXPLOSIVE detonation damages twice and passes Explosion_Radius as vanilla explosion power~~Stop using createExplosion for damage (power 0 or a cosmetic-only burst) and let the manual WeaponRaytraceImpactEvent loop own the configured damage and radius~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/ThrowableAction.java:213-247~~detonate() calls `world.createExplosion(center.getX(), center.getY(), center.getZ(), (float) data.getExplosionRadius(), false, false, player);` — the trailing booleans suppress fire and block-breaking but NOT entity damage — then loops getNearbyEntities in the same radius and calls `target.damage(impactEvent.getDamage(), player)`. Anyone inside the blast takes vanilla explosion damage plus the full configured Explosion_Damage a second time. The fourth argument is explosion POWER, not radius: grenade.yml ships `Explosion_Radius: 4.0`, which is TNT-strength and reaches roughly 8 blocks, so entities 4-8 blocks out take vanilla damage the manual loop never intended to touch. Was Gangland WP-4.~~High
3~~P2~~MeleeAction and ThrowableAction never call weapon.consumeShot(), so configured magazines never deplete~~Call weapon.consumeShot() in MeleeAction.activate() (gate it on weapon.getReloadData() != null, same guard already used for isMagazineEmpty()) and in ThrowableAction.activate() before/alongside decrementHeldStack, mirroring the BiologicalAction.fire() pattern (weapon.consumeShot() + weaponService.persistHeldWeapon(weapon, player)) added by the gate-HB review fix.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/weapon/action/MeleeActionAmmoTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/weapon/action/ThrowableActionAmmoTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/MeleeAction.java:65-159 (activate); bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/ThrowableAction.java:81-229 (activate)~~MeleeAction.activate() guards with `if (weapon.getReloadData() != null && weapon.isMagazineEmpty())` but never calls consumeShot() anywhere in the class; ThrowableAction.activate() never references ammunitionData, reloadData or consumeShot at all and only decrements the held ItemStack. A repo-wide grep for `consumeShot()` returns only NpcWeaponControllerImpl, BiologicalAction, GunAction and IncendiaryAction. Any melee or throwable weapon authored with Ammunition:/Reload: sections has unlimited uses, because the isMagazineEmpty() guard reads a currentMagCapacity nothing ever decrements. Was Gangland WP-14 (melee/throwable half).~~High
4~~P1~~PluginFireRegistry.onShutdown() clears the tracked set without reverting the FIRE blocks it placed~~In onShutdown(), before tracked.clear(), iterate each world's Set<BlockVector>, resolve each still-FIRE block via Bukkit.getWorld(worldUuid) and set it to AIR (skip worlds no longer loaded / blocks no longer FIRE), then clear.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/fire/PluginFireRegistryTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/fire/PluginFireRegistry.java:72-75~~The whole shutdown hook is `@Override public void onShutdown() { tracked.clear(); }`. Fire blocks placed by IncendiaryAction/ThrowableAction are reverted by per-block scheduled tasks, and those tasks are cancelled when the plugin disables, so any fire still burning at server stop or /reload is left in the world as a real block. Because tracked.clear() also erases the bookkeeping, PluginFireProtectionListener can no longer recognise those blocks as plugin-placed on the next start, so leftover fire behaves as vanilla fire and can spread and burn player structures. Was Gangland WP-38.~~High
5~~P2~~GunAction refunds a hardcoded 1 round on a cancelled WeaponShootEvent regardless of Consumed_Amount~~Refund weapon.getProjectileData().getConsumed() instead of the literal 1 (or, more robustly, capture the pre-consume magazine count and restore it exactly, in case a future subclass computes consumeShot() differently again).~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/weapon/action/GunActionCancelRefundTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/GunAction.java:75-78~~`boolean consumed = weapon.consumeShot();` subtracts projectileData.getConsumed() rounds via GunWeapon.consumeShot(), but the cancellation branch is `if (shootEvent.isCancelled()) { weapon.addAmmunition(1); return; }`. For any weapon configured with Consumed_Amount greater than 1, a plugin that cancels WeaponShootEvent leaves the shooter with fewer rounds than before the shot that never happened, destroying ammo on every cancelled press. Was Gangland WP-12.~~High
6~~P2~~Durability_On_Shot is silently ignored for biological, melee and throwable weapons~~Call weapon.decreaseDurability(heldWeapon, onShot) in BiologicalAction (after persistHeldWeapon, using the already-resolved heldWeapon), MeleeAction and ThrowableAction, gated on getDurabilityData().getOnShot() > 0, exactly as GunAction/IncendiaryAction do — or, if Durability_On_Shot is meant to be gun/incendiary-only by design, document that restriction and drop the key's applicability for the other three types' YAML schema.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/weapon/action/BiologicalActionDurabilityTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/weapon/action/ThrowableActionDurabilityTest~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/BiologicalAction.java, MeleeAction.java, ThrowableAction.java (no decreaseDurability call in any of the three)~~GunAction.java:71-74 and IncendiaryAction.java:99-101 both read `weapon.getDurabilityData().getOnShot()` and call decreaseDurability; a repo-wide grep shows those are the only two call sites. BiologicalAction.fire() and MeleeAction.activate() never call it, so a Durability_On_Shot configured on a biological or melee weapon never wears the item and those weapons never break. Separately only GunAction fires a cancellable WeaponShootEvent before the shot, so a protection plugin can veto gun fire but not incendiary, biological, melee or throwable fire. Was Gangland WP-42.~~Medium
7~~P3~~BiologicalAction action bar emits literal section-sign colour codes instead of the &-code convention~~Replace the literal section signs with &6/&e and route through BartizanChatUtil.color()~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/BiologicalAction.java:72-73~~The charge tick sends `ActionBarManager.send(player, "§6Charging... §e[" + "■".repeat(charge[0]) + ...)` with literal section-sign bytes in the source, while the release message two dozen lines later in the same class correctly uses &-codes (`"&aReleased at charge level " + level`). CLAUDE.md states colored chat must use &-codes through the ChatUtil path and never a literal section sign in source files. Was Gangland WP-27.~~High
8~~P3~~ThrowableAction.detonateStun plays a raw Sound enum constant instead of resolving through SoundEffect~~Replace the raw Sound.ENTITY_GENERIC_EXPLODE with a SoundEffect/XSound-resolved call~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/ThrowableAction.java:309-313~~detonateStun() calls `world.playSound(center, Sound.ENTITY_GENERIC_EXPLODE, 2.0f, 1.8f);` against the raw Bukkit Sound enum, while activate() and detonate() in the same file use SoundEffect.playSounds(...). CLAUDE.md requires version-drifting enums (Sound, Material, Particle, PotionEffectType) to resolve through XSeries so they survive a Minecraft version rename. Was Gangland WP-28.~~High
9~~P1~~BiologicalAction.fire never persists the magazine after consumeShot, so biological ammo is restored on the next interaction~~Call weapon.updateWeaponData(heldWeapon) and weapon.updateWeapon(player, heldWeapon, slot) after consumeShot, as GunAction and IncendiaryAction do~~WeaponConsumeShotTest (pins the in-memory decrement only, not the NBT write-back)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/BiologicalAction.java:102-126~~fire() decrements the magazine with `if (!weapon.consumeShot()) return;` and then does nothing to write that state onto the item — a grep of the whole class for updateWeapon, heldWeapon, AMMO_LEFT and updateTag returns no hits, while GunAction.java:68,76 and IncendiaryAction.java:97,104 both call updateWeaponData followed by updateWeapon. Weapon.updateWeaponData is what stamps the tag, via `if (!updatedCurrentAmmo) updateTag(itemBuilder, WeaponTag.AMMO_LEFT, getAmmoLeftForTag());` at Weapon.java:249. On the next interaction WeaponService.validateAndGetWeapon resyncs from the held item and setWeaponData runs `weapon.setCurrentMagCapacity(amountLeft)` off the stale NBT, discarding the decrement and refilling the magazine. A biological weapon therefore never runs out of ammo across interactions no matter how many charged shots are fired. Was Gangland WP-14 (biological ammo NBT half).~~High
10~~P0~~BURST follow-up rounds keep firing after a hotbar swap, drawing on the old weapon's in-memory magazine and writing it onto the newly held weapon~~In GunAction.weaponShoot, right after getHeldWeaponItem, return when !weapon.getUuid().equals(WeaponService.getWeaponUUID(heldWeapon.build())). The check has to come before consumeShot(). Put the same guard in IncendiaryAction's held-item write path. That one check also closes the off-hand fallback dupe described below. Optionally, keep each burst SequenceTimer in a map keyed by weapon uuid and stop it in onWeaponHeld next to autoTask.stop(), so the leftover rounds of a holstered weapon don't fire at all.~~GunActionHeldWeaponTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/GunFireDispatcher.java:78-98 (untracked burst SequenceTimer); bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/GunAction.java:43-96 (no held-uuid check before consumeShot/updateWeaponData/updateWeapon); bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:365-409 (onWeaponHeld never stops the sequence)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] GunFireDispatcher.shoot schedules the remaining Per_Shot-1 BURST rounds on an untracked SequenceTimer that is bound to the original GunWeapon instance. onWeaponHeld unlocks the press gate and stops FullAutoTask and activeTasks, but never cancels this sequence. Each later round runs GunAction(A).weaponShoot: 1. heldWeapon = getHeldWeaponItem(player), which is now weapon B. 2. A.consumeShot() runs on the in-memory instance only. 3. A.updateWeaponData(B's builder) stamps A's AMMO_LEFT, AMMO_TYPE and SELECTIVE_FIRE onto B, and updateWeapon writes B's slot. A's own item is never written for these rounds, so the next validateAndGetWeapon re-syncs A from its unchanged NBT and returns them. B's magazine is replaced with A's count, unclamped and possibly above B's Capacity, and B's mode becomes BURST even if B's Allowed_Modes exclude it. Shipped steyr_aug (BURST, Per_Shot 3, Cooldown 1): click, then scroll or press a number key within 1-2 ticks. The window grows with the burst Cooldown. Pressing F during the burst does the same. Every such burst creates ammo on both guns.~~High
11~~P1~~MeleeAction's armor-piercing follow-up hit leaves the target in the never-pruned static pendingDamage set, so the next player attack on that entity skips WeaponInteract's cancel guard~~As proposed: wrap each pendingDamage.add/target.damage(...) pair in a try/finally that removes the UUID immediately after the synchronous damage() call returns, instead of relying on WeaponInteract's event-based drain. For the no-source pierce call at MeleeAction.java:183 specifically, either pass the player as damager (so it produces a draining EntityDamageByEntityEvent like the armored hit does) or drop the pendingDamage.add for that call entirely and drain synchronously in a finally block — do not rely on WeaponInteract to ever see it. Apply the same try/finally pattern in IncendiaryAction.java (its sourced call at line 47 still leaks when the target is fully invulnerable/creative, since no event fires at all in that case either).~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/weapon/action/MeleeActionPendingDamageTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/weapon/action/IncendiaryActionPendingDamageTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/MeleeAction.java:181-184 (applyMeleeImpact's no-source pierce call); root cause is the drain contract in bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:322-340 (onEntityDamage only ever fires on EntityDamageByEntityEvent, so it can never observe a target.damage(amount) call that carries no damager)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] applyMeleeImpact adds the target UUID to MeleeAction.pendingDamage and then calls target.damage(pierceDmg) with no damager (lines 182-183). A damage() call with no source can only raise a plain EntityDamageEvent, never EntityDamageByEntityEvent. Here it usually raises nothing: the armored hit one line earlier set invulnerableTime=20 and lastHurt=armoredDmg, and pierceDmg < armoredDmg whenever bypass < 0.5, so the call is swallowed by invulnerability frames (the pierce damage also never lands). The only drain is WeaponInteract.onEntityDamage(EntityDamageByEntityEvent), and its comment assumes every add is matched by an inner event. Nothing removes entries on death, quit or despawn. Trigger: the shipped machete (Armor_Piercing: 0.1) hits a zombie that survives the 8.1 armored hit, and the zombie's UUID stays in the static set. The next EntityDamageByEntityEvent from any player on that zombie (the machete's next swing, or a player hitting it with a gun in hand) drains the stale UUID, sees wasMeleeRefire=true and returns before event.setCancelled(true). The vanilla item attack goes through (IRON_HOE damage, vanilla knockback, vanilla durability loss; for a gun holder, the gun material's melee damage) and the configured melee path is skipped for that hit. So every second consecutive machete hit on a surviving target is resolved as vanilla. An entity hit once and never again by a player (a mob that despawns, a player who logs off) keeps its UUID in the set for the whole uptime. Same leak, narrower triggers: MeleeAction:186-187 when the target is inside another hit's invulnerability frames, and IncendiaryAction:225-226 against creative or invulnerable targets, where damage() raises no event.~~High
12~~P1~~GunFireDispatcher's fire-rate lock map is static and JVM-wide, so it outlives plugin reload and is only ever pruned on weapon swap~~Add a shared "forget this weapon" removal (GunFireDispatcher.unlock plus the sibling per-weapon maps) called from (a) Weapon#removeWeapon / wherever durability zeroes the item, and (b) WeaponQuitCleanupListener#onPlayerQuit — not just from the swap path in WeaponInteract#onWeaponHeld. Note: making the map instance-scoped is not itself curative — WeaponInteract doesn't implement BeanLifecycle, so `/bartizan reload` (BeanFactory#reloadLifecycleBeans) doesn't touch WeaponInteract at all today, and its own instance maps (equipDelayUntil, pressHoldState, releaseCallbacks, continuousFire, lastMeleeSwingMs, autoTasks, activeTasks — WeaponInteract.java:99-137) leak identically for the same reason (only cleared on swap). The real fix is adding destroy/quit removal call sites, not the static/instance distinction.~~GunFireDispatcherTest (pins the fix)~~bartizan-plugin/.../weapon/action/GunFireDispatcher.java:40 (static `pressLockUntilTick` map), :57-59 (`lock`, called on every SINGLE/BURST shot from WeaponInteract.java:729/783 and WeaponSelectiveFireChangeListener.java:144), :65-67 (`unlock`, whose only caller is WeaponInteract.java:382 inside `onWeaponHeld`). Orphaning path confirmed at bartizan-api/.../weapon/Weapon.java:637 `removeWeapon()` (just sets the slot to AIR, no map cleanup) reached from GunFireDispatcher.shootInterval (bartizan-plugin GunFireDispatcher.java:106-109) whenever a consumable gun (`Weapon_Consumed_On_Shot`) fires its last round — this is ordinary play, not an edge case, and it orphans the entry without any later swap ever being able to clean it (the swapped-from slot no longer holds that weapon).~~[found 2026-09-24, 0.5.0 @ 9fa25c3] `private static final Map<UUID, Long> pressLockUntilTick = new ConcurrentHashMap<>();` is a static field keyed by weapon uuid, written by `lock()` on every SINGLE/BURST shot and only ever removed by `unlock()`, whose sole caller is WeaponInteract.onWeaponHeld on a weapon swap (WeaponInteract.java:382). There is no removal on player quit, weapon destruction/removal, or plugin shutdown/reload — and because the map is `static` rather than an instance field owned by a bean, it also survives a `/bartizan reload` (a fresh WeaponInteract/GunFireDispatcher bean instance still shares the same static map), unlike a bean-scoped map that a reload hook could clear. Every gun weapon ever given and fired at least once in SINGLE/BURST mode leaves a permanent entry for the remainder of the server process's uptime unless the player happens to swap away from it before the server stops.~~High
13~~P0~~Hoppers can collect a thrown grenade's display Item entity, turning each throw into a real vanilla item (TNT for the shipped smoke grenade) while it still goes off~~In ThrowableAction.activate, after world.dropItem, mark the grenade. A PDC flag is the simplest choice (for example NamespacedKey(plugin, "cosmetic")) because it also covers grenades left behind after a disable or reload. Registering the entity id with WeaponVisualSpawner would also work, but that means passing the spawner in through WeaponInteract. Then add one handler to ProjectileDamageListener: @EventHandler(priority = LOWEST) onHopperPickup(InventoryPickupItemEvent e), which cancels when e.getItem() has the flag or visualSpawner.isCosmetic(e.getItem().getEntityId()) is true. The isCosmetic check also covers the DROPPED_ITEM visuals from WeaponVisualSpawner.spawnItem. The event fires for both hoppers and hopper minecarts.~~ProjectileDamageListenerTest, ProjectileDamageListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/ThrowableAction.java:106-110 (the grenade Item spawn), :133-139 (the fuse still detonates after the Item is gone); the handler belongs in bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/projectile/ProjectileDamageListener.java. Secondary: bartizan-api/.../raytrace/WeaponVisualSpawner.java:176-187 (spawnItem, the DROPPED_ITEM visual).~~[found 2026-09-24, 0.5.0 @ 9fa25c3] activate() decrements the held throwable, then spawns world.dropItem(eyeLoc, visual) with only setPickupDelay(Integer.MAX_VALUE). The visual is Throw.Display_Item, or new ItemStack(weapon.getMaterial()) when no Display_Item is set. Pickup delay stops players and mobs but not hoppers: vanilla HopperBlockEntity sucks in any live ItemEntity. Nothing in bartizan-plugin handles InventoryPickupItemEvent, and the grenade entity is not registered with WeaponVisualSpawner's cosmetic set. Repro: throw the shipped smoke_grenade (Display_Item TNT, 'Smoke Canister') onto a hopper or a hopper minecart path. The hopper stores a placeable TNT item. The physics timer stops on isDead(), but the fuse timer still calls detonate() at the entity's last location, so the thrower keeps the effect and also gets the item. The same happens with the tomahawk (STONE_AXE), molotov (HONEY_BOTTLE), flashbang (FIREWORK_STAR) and frag grenade (LIME_DYE). Any throwable configured with a valuable Material or Display_Item becomes an item generator. This also puts TNT in players' hands on servers that disable TNT crafting. WeaponVisualSpawner.spawnItem (DROPPED_ITEM projectile visuals) has the same exposure when configured.~~High
14~~P2~~Throwable magazines now deplete, so a throwable reload runs on the one Weapon instance every holder of that type shares~~This needs a product decision. The smallest safe option: refuse throwable reloads in WeaponService.tryReload (return false for WeaponType.THROWABLE), and have WeaponAddon log a ConfigReport warning when a Category: throwable file declares Reload:/Ammunition:. With that, the shared instance never carries reload state across players. The alternative is to drop consumeAmmoIfTracked for throwables and treat the stack count as the ammo.~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/ThrowableAction.java:257~~Final review of the 0.5.1 fix wave (2026-09-25), deferred as a product decision: Every throwable of a type carries one deterministic uuid (WeaponService.mintUuid:421-424), so every player holding that type resolves to the same registered Weapon, and so to the same Reload object and reloading flag (getWeapon:296-301 returns the existing entry). Before this wave a throwable magazine never dropped below full, so tryReload (isMagazineFull) never started a throwable reload. consumeAmmoIfTracked now depletes it, so sneak+Q (WeaponDroppedListener -> tryReload) starts a reload on that shared instance. Scenario, with a custom throwable that has Ammunition:/Reload: (the config this fix targets): (1) Player A reloads grenade type X. W.isReloading() is now true for everyone, so Player B's right click with X returns at WeaponInteract.handleNonGunInteract:535 ('block all actions while reloading'), and B's sneak+Q is cancelled at WeaponDroppedListener:47. (2) Any validateAndGetWeapon on B's X stack during A's reload re-syncs W.currentMagCapacity from B's NBT. A's next NumberedReload insertion then does addAmmunition on B's count and writes it onto A's stack (NumberedReload:169-178), so A gains rounds without consuming the matching ammo items. A single player can do the same with two X stacks whose AMMO_LEFT differs, because the OFF_HAND interact still calls validateAndGetWeapon (WeaponInteract:187) before the OFF_HAND return. Also, ThrowableAction.activate's new EmptyMagSoundGate.play keys the gate by the shared weapon uuid, while isPressGated refreshes it by pressKey. One player's empty click can therefore mute another player's empty click, and the refresh never reaches the gate.~~high
