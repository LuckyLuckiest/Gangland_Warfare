# BZ-IM importer — verified findings (Bartizan 0.5.0 @ 9fa25c3, hunt 2026-09-24)
1~~P2~~WmItemConverterListener patches the imported ammo-left NBT tag after buildItem(), so the converted item's displayed ammo count (baked into the display name at build time) is wrong until the weapon is next fired/reloaded~~After computing ammoLeft, call fresh.setCurrentMagCapacity(ammoLeft) (and mirror the other fields setWeaponData syncs -- ammo type / skin -- for full parity) BEFORE calling fresh.buildItem(), then drop the separate modifyTag(AMMO_LEFT, ...) call since buildItem()'s initializeTags() will already write the correct AMMO_LEFT from the now-updated currentMagCapacity. This matches the report's proposed fix.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/importer/wm/WmItemConverterListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/importer/wm/WmItemConverterListener.java:105-113 (root cause traces into Weapon#buildItem/buildDisplayName at bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/Weapon.java:455-476,746-751, and the player==null path in bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/WeaponService.java:263-296)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] convert() does `Weapon fresh = weaponManager.getWeapon(null, null, fileKey, true); ItemStack rebuilt = fresh.buildItem();` — buildItem() bakes the display name via Weapon#buildDisplayName(), which embeds `currentMagCapacity` (a freshly-minted weapon's full magazine) into the '«ammo/max»' suffix, and also picks the render SkinState via currentSkinState() (isMagazineEmpty() reads currentMagCapacity). Only AFTER building the item does the code do `new ItemBuilder(rebuilt).modifyTag(Weapon.getTagProperName(WeaponTag.AMMO_LEFT), ammoLeft)` — a raw NBT tag write that does not touch the ItemMeta display name/lore already baked into `rebuilt`, and does not update the registered `fresh` Weapon instance's own in-memory currentMagCapacity field either (that stays at max). Trigger: with Import.Convert_WeaponMechanics_Items enabled (or the runtime override set via /bartizan import weaponmechanics), any WeaponMechanics item with partial ammo (e.g. 12/30) gets converted on the player's next inventory click / hotbar swap / join. Wrong outcome: the resulting Bartizan item's tooltip shows the full magazine (e.g. '30/30') even though its actual ammo-left NBT tag (and the value a subsequent shot/reload will read) is 12 — a visible display/NBT mismatch that only self-corrects once the player actually interacts with the weapon (which re-syncs via WeaponService#setWeaponData).~~High
2~~P1~~appendAmmunition hand-builds YAML text into the shared ammunition.yml with no escaping for Material and no newline escaping for Name, risking corruption of every weapon's ammo, not just the imported one~~Escape backslashes before quotes in both fields: `s.replace("\\", "\\\\").replace("\"", "\\\"")`. Put that in one static helper and call it from appendAmmunition (for both Material and Name) and from WmYamlEmitter.quoteIfNeeded, which has the same hole. The report's suggestion to "reuse WmYamlEmitter" would not fix anything on its own. A simpler option: build the appended block with a scratch YamlConfiguration (set id.Material, id.Name and id.Lore) and append `saveToString()`, so SnakeYAML's dumper does the escaping. Newlines do not need handling.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/importer/wm/WmYamlEmitterTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/command/WmImportCommandAmmoBlockTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WmImportCommand.java:259-263 (appendAmmunition). The same missing backslash escape is in importer/wm/WmYamlEmitter.java:135 (quoteIfNeeded).~~[found 2026-09-24, 0.5.0 @ 9fa25c3] appendAmmunition() appends raw text directly onto plugins/Bartizan/items/ammunition.yml - the ONE shared ammo file every weapon's Ammunition.Ammo_Type resolves against. `Material: \"` + ammo.material() + `\"` embeds WmWeaponImporter's ammoSection.getString("Item_Ammo.Bullet_Item.Type", ...) value with zero escaping at all; `Name: \"` + ammo.name().replace("\"","\\\"") + `\"` escapes only literal quote characters, never a literal newline. ammo.name() is WmColorTranslator.translate(ammoSection.getString("Item_Ammo.Bullet_Item.Name", ref)) - the same kind of free-text field that can carry a raw '\n' if the source ammo yml used a YAML block-literal style. Unlike WmYamlEmitter (which at least tries to detect-and-quote), this code path has no detection at all. If either field ever contains an unescaped `"` or a literal newline, the malformed text breaks parsing of the WHOLE ammunition.yml (one shared SnakeYAML document, per Keystone's ConfigParser), so every other weapon on the server - not just the newly imported one - loses its ammo resolution until an admin manually edits the file.~~Medium
3~~P1~~buildSelectiveFire always starts an imported full-auto-capable gun in Auto mode, regardless of the weapon's intended default~~Keep the `Selective_Fire.Default` lookup, because it is live. Change only the fallback that applies when Default is absent: if `shoot.isConfigurationSection("Selective_Fire")` is true, start in "single", which is WM's implicit default (a missing selective-fire NBT tag reads as 0 = SINGLE). Keep `hasAuto ? "auto" : (hasBurst ? "burst" : "single")` only for weapons with no Selective_Fire section, where WM fires in its one configured mode. Do NOT take the proposed fix of removing the Default lookup: that would break FR_5.56 (`Default: "BURST"`), which imports correctly as burst today.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/importer/wm/WmSelectiveFireDefaultTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/importer/wm/WmWeaponImporter.java:512-515 (buildSelectiveFire start-mode fallback)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] startMode is read from `shoot.getString("Selective_Fire.Default", "")`, but real WM configs never have such a key - WM's `Shoot.Selective_Fire` is a section describing the *trigger* to switch modes (see test fixture FN_FAL.yml:45-50: `Selective_Fire: { Trigger: {...}, Mechanics: [...] }`), not a starting-mode string. So startMode is empty for every real weapon and the fallback `hasAuto ? "auto" : (hasBurst ? "burst" : "single")` always fires. Any imported gun with `Fully_Automatic_Shots_Per_Second` set (most modern rifles/SMGs, e.g. AK_47, M4A1, Uzi, FN_FAL, FR_5.56 in the test fixtures) is therefore imported with `Selective_Fire: auto` as its STARTING fire mode, even for a weapon whose own WM lore says "Semi-automatic" (FN_FAL.yml line 7) or that also ships an explicit Burst block (FR_5.56.yml) which would be the more controllable default. Bartizan's own shipped weapons are mixed on this (mp5/rifle/minigun default to auto, but pistol/awp/steyr_aug default to single/BURST), so there's no house rule forcing 'auto' whenever available - this priority order just happens to always pick the most aggressive mode. Trigger: import any WM assault rifle/SMG default -> every one spawns firing full-auto instead of a controlled single/burst default, silently changing weapon balance for the whole imported arsenal.~~Medium
4~~P1~~sanitizeKey collapses distinct WM ammo refs that differ only by punctuation (comma vs. period, e.g. 5.56mm/5,56mm) into the same Bartizan ammo id, silently discarding the second ammo's data~~Carry the original (pre-sanitize) ref through AmmoAppend (add a `String sourceRef` field). In appendAmmunition, track a Map<id, sourceRef> of ids added this run; when a later AmmoAppend has an id already present but a different sourceRef (or a different material/name), don't silently `continue` — call the weapon's report.dropped(...)/approximated(...) noting "Reload.Ammo '<ref2>' collided with '<ref1>' after id-sanitizing to '<id>' — keeping '<ref1>'s item, '<ref2>' discarded". Optionally disambiguate by suffixing a short counter to ammoId when sanitize output already exists under a different sourceRef, so the second weapon's Ammo_Type doesn't silently point at the wrong item. Root-cause fix belongs in the shared dedup point (appendAmmunition) since that's the only place both colliding AmmoAppends are seen together; WmWeaponImporter alone can't detect the collision since it only sees one weapon at a time.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/command/WmImportResolveAmmoAppendsTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/importer/wm/WmWeaponImporter.java:88-92 (sanitizeKey collapses '.' and ',' identically) and :716-728 (buildReloadAndAmmo derives ammoId = "wm_"+sanitizeKey(ref) with no check against ids already minted this run, then builds AmmoAppend from ammos.get(ref) keyed by the un-sanitized original ref); bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WmImportCommand.java:250-269 (appendAmmunition), specifically the dedup at line 257 `if (existing.contains(ammo.id())) continue;` which silently keeps only the first colliding AmmoAppend's Material/Name and drops the second with no report line.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] sanitizeKey() replaces every run of non-`[a-z0-9]` characters with a single underscore, so both '.' and ',' collapse to '_'. buildReloadAndAmmo() builds the Bartizan ammo id as `"wm_" + sanitizeKey(ref)` where ref is the raw WM `Reload.Ammo` reference string. Two different WM ammo refs that differ only in the decimal/thousands separator - e.g. "5.56mm" and "5,56mm", or "7.62mm" and "7,62mm" (this project's own memory notes explicitly record comma-form ammo ids like 7,62/5,56 as intentionally distinct from the period form elsewhere in Bartizan/Gangland) - both sanitize to the identical id "wm_5_56mm"/"wm_7_62mm". When two weapons reference these as their Reload.Ammo, appendAmmunition()'s dedup (`if (existing.contains(ammo.id())) continue;`) silently keeps only the FIRST one's Material/Name and drops the second's entirely, with no report.dropped()/approximated() line anywhere recording that a collision happened - unlike almost every other lossy decision in this importer, which is meticulously logged. The second weapon's Ammo_Type then quietly resolves to the wrong ammo item (wrong Material/display name) and, worse, two conceptually different ammo types now interchange as one Bartizan ammo id.~~Medium
5~~P2~~--dry-run only checks disk state for existing-file conflicts, so it misses intra-run weapon fileKey collisions a real run would flag or overwrite~~In runImport, create `Set<String> claimedKeys = new HashSet<>()` before the weaponFiles loop and pass it into importOne alongside ammoAppends. In importOne, change the guard from `if (outFile.exists() && !force)` to `if ((claimedKeys.contains(imported.fileKey()) || outFile.exists()) && !force) { entry.error(...); return; }`, then after the guard passes, unconditionally `claimedKeys.add(imported.fileKey());` (both dry-run and real) before the `if (!dryRun)` write block. The in-memory set does double duty as the dry-run collision detector, and force keeps bypassing the whole check in both modes, matching current real-run semantics.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/command/WmImportCollidesWithClaimedFileTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WmImportCommand.java:194-221 (importOne), called in a loop from runImport (135-192) over all weapon files gathered by collectYamlFiles~~[found 2026-09-24, 0.5.0 @ 9fa25c3] importOne()'s only overwrite guard is `if (outFile.exists() && !force)`, checked against the real filesystem. In a real (non-dry) run this doubles as intra-run collision detection, because the first weapon's Files.writeString() actually creates the file, so a second source weapon sanitizing to the same fileKey (WmWeaponImporter.sanitizeKey strips all punctuation/case, so e.g. two differently-titled WM entries can collide) correctly hits the 'already exists' error on its turn. In `--dry-run` mode, no file is ever written, so outFile.exists() only reflects state from BEFORE this run - two colliding source weapons in the same dry run both report success with no error, giving the admin a false 'clean' dry-run report; running the same import for real then behaves differently (the second one is silently skipped without --force, or silently overwrites the first with --force), contradicting the dry run's preview.~~High
6~~P1~~WmMechanicsTranslator maps both Push/Leap's speed and height onto the same Strength key, so one silently overwrites the other~~Report the collision instead of redesigning the data model. In WmWeaponImporter.addHook(), right next to the existing `if ("sound".equals(result.type()) || "custom_sound".equals(result.type()))` lossy-conversion check (~line 847), add a parallel check: `if ("push".equals(result.type()) && value(parsed, "speed") != null && value(parsed, "height") != null) report.approximated(wmPath + ": '" + entry + "' - Push/Leap has both speed and height; only one is kept as Strength");`. This reuses the exact pattern/helpers already in that method (no new abstraction) and makes the import report honest, matching every other lossy-mapping case in this file. Rejected the "two keys + combine in PushHookEffect" alternative: PushHookEffect's model (one scalar Strength along a look/away unit vector) has no natural place for an independent vertical component without a much larger redesign, and there's no evidence (docs, tests, or vendored WM configs) that this combination is common enough to justify it.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/importer/wm/WmPushLeapCollisionReportTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/importer/wm/WmMechanicsTranslator.java:266-271 (the `case "push", "leap"` arm of `translate()`); the missing report call belongs in the caller, `WmWeaponImporter.addHook()` in bartizan-plugin/src/main/java/org/luckyraven/bartizan/importer/wm/WmWeaponImporter.java (around line 847, alongside the existing sound noise/delayBeforePlay check that already has `report` and the `value(parsed, key)` helper in scope).~~[found 2026-09-24, 0.5.0 @ 9fa25c3] case "push", "leap" -> { copy(parsed, "speed", out, "Strength"); copy(parsed, "height", out, "Strength"); ... } copies two distinct WM args into the same Bartizan key. copy() only writes when its source key is present in the entry, but when a WM Leap{speed=..., height=...} mechanic (a leap plausibly needs both a horizontal launch speed and a vertical height) carries both, the second call's write to 'Strength' silently clobbers the first — PushHookEffect only ever reads one scalar Strength, so the horizontal component is discarded with no trace. WmWeaponImporter.addHook() reports every other lossy conversion it performs (e.g. Sound's dropped noise/delayBeforePlay) via report.approximated/dropped, but has no such check here, so the import report shows this mechanic as a clean 'mapped' even though half its data vanished.~~Medium
7~~P1~~WmMechanicsTranslator defaults an unset Command console flag to full console execution, unreported~~Match WeaponMechanics' real default. At line 263, replace `console == null || Boolean.parseBoolean(console)` with `Boolean.parseBoolean(console)`. That way an absent console key gives `As: player`, the same as WM's `getBool(false)`. Add an absent-key case to WmMechanicsTranslatorTest.command() that asserts "player". The report.approximated line is optional once the default matches WM, since nothing is being assumed any more. The one remaining difference: when a WM player-mode command hits a non-player target, WM does nothing and Bartizan's As: player does nothing too. So no extra report line is needed.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/importer/wm/WmMechanicsTranslatorTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/importer/wm/WmCommandDefaultReportTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/importer/wm/WmMechanicsTranslator.java:263 (case "command"); consumed by WmWeaponImporter.addHook (WmWeaponImporter.java:828-867) and executed by CommandHookEffect.java:30-37~~[found 2026-09-24, 0.5.0 @ 9fa25c3] case "command" -> { ...; out.put("As", console == null || Boolean.parseBoolean(console) ? "console" : "player"); ... } treats a WM Command{command=...} entry with no explicit console key as As: console (full, unrestricted permissions) in the translated Bartizan effect. Every other ambiguous/absent WM value the translator or WmWeaponImporter.addHook() defaults gets an explicit report.approximated/dropped line so the person running the import sees what was assumed — this one does not. If WeaponMechanics' own default for its console flag is player-scoped execution (the conventional least-privilege default for a plugin that runs a configurable command string), every migrated weapon whose author relied on that implicit default silently escalates, post-import, from running with the shooter's own permissions to running as console — and, combined with the CommandHookEffect finding above, becomes exploitable by anyone who can name a mob, with no server-owner config change needed.~~High
8~~P3~~WmImportCommand's --dry-run/ammo-collision fixes (BZ-IM-02, BZ-IM-04, BZ-IM-05) are wired only through extracted static helpers that no test drives end-to-end~~Not urgent given WmImportCommand's existing test-infrastructure gap (no CommandSender/Tree<Argument> scaffolding exists for this package, same limitation the WS7-D4 report already flagged for command/ classes). If/when that scaffolding exists, add one test that calls runImport with two weapon YAMLs sanitizing to the same fileKey under --dry-run and asserts the second gets an 'already exists' entry.error(...), and one that calls appendAmmunition with two colliding AmmoAppends and asserts the written ammunition.yml file only has one entry plus a runNote.~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WmImportCommand.java:202~~Final review of the 0.5.1 fix wave (2026-09-25), deferred as a product decision: BZ-IM-02 (ammoBlock/escapeDoubleQuoted), BZ-IM-04 (resolveAmmoAppends) and BZ-IM-05 (collidesWithClaimedFile) were each fixed by extracting a small static, directly-testable helper out of importOne/appendAmmunition, and each helper is well unit-tested in isolation (WmImportCommandAmmoBlockTest, WmImportResolveAmmoAppendsTest, WmImportCollidesWithClaimedFileTest). But no test in the repo calls WmImportCommand#runImport, #importOne or #appendAmmunition themselves (grep for those three names across bartizan-plugin/src/test/java turns up nothing beyond the three new helper tests, and there was no pre-existing WmImportCommand integration test either). Concretely: if importOne's guard were reverted from `collidesWithClaimedFile(...)` back to bare `outFile.exists()` (BZ-IM-05's actual regression), or appendAmmunition stopped calling `resolveAmmoAppends`/`ammoBlock` and went back to the old inline `existing.contains(ammo.id())` + unescaped string concatenation (BZ-IM-02/04's actual regressions), every existing test would still pass, because the extracted helpers being tested in isolation would still be correct - just unused. The wiring itself (which parameter is threaded into which call, which branch calls which helper) is the part these three fixes actually changed, and it is the part with no regression coverage.~~high
