# BZ-CM commands — verified findings (Bartizan @ 0e18356)
1~~P2~~The three give commands never bound the parsed amount, so a negative throws and a zero reports a false success~~Clamp the parsed amount to at least 1 and to a sane maximum right after Integer.parseInt, in all three give commands~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WeaponGiveCommand.java:104-117~~giveWeapon takes the amount straight from `weaponAmount = Integer.parseInt(args[3])` with no range check and computes `int slots = (int) Math.ceil(amount / (double) maxStackSize);` then `ItemStack[] items = new ItemStack[slots];`. For an amount of -100 against a stack size of 64, slots is -1 and `new ItemStack[-1]` throws NegativeArraySizeException, which the command framework surfaces to the admin as a bare number. For an amount of 0 the array is empty, the loop never runs, and giveWeapon still returns true, so the admin is told the give succeeded while nothing entered the inventory. A very large amount drives an unbounded allocation instead. The identical pattern is duplicated in AmmunitionGiveCommand.java:91-104 and WearableGiveCommand.java:96-108.~~High
2~~P2~~The weapon alias on /bartizan shadows WeaponCommand's own subcommand label, so /weapon give never dispatches~~Remove weapon from plugin.yml's aliases list for the bartizan command, keeping btz~~-~~bartizan-plugin/src/main/resources/plugin.yml:17-23~~plugin.yml registers a single Bukkit command, bartizan, with `aliases: [btz, weapon]`, while WeaponCommand registers weapon as one of its own subcommand labels alongside ammo, wearable, debug and reload. Typing `/weapon give ak47 5` makes Bukkit consume weapon as the invoking alias and hand the dispatcher args[0]="give", which matches no registered subcommand, so the sender gets an unknown-subcommand error with a did-you-mean suggestion. Only `/bartizan weapon give ...` and `/btz weapon give ...` reach WeaponGiveCommand, leaving the weapon alias useful only for the bare `/weapon` info card.~~High
3~~P2~~WeaponGiveCommand tab-completes from weaponLoader.getFiles(), offering weapon files that failed to parse~~Source the name completion from weaponAddon.getWeaponKeys() as WeaponInfoCommand and WeaponListCommand already do~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WeaponGiveCommand.java:69-72~~The name argument's completion supplier is `return weaponLoader.getFiles().stream().map(FileHandler::getName).toList();`, which lists every YAML physically present in the weapon folder whether or not it registered a Weapon. WeaponInfoCommand and WeaponListCommand instead use weaponAddon.getWeaponKeys(), which holds only successfully parsed weapons. A weapon whose config failed to load is therefore still offered in `/bartizan weapon give <tab>`, and picking it makes getWeapon return null so the admin gets INVALID_WEAPON instead of an item. AmmunitionGiveCommand and WearableGiveCommand do not share the divergence. Was Gangland WP-31.~~High
4~~P3~~The three WEARABLE error messages use Type.PREFIX while the weapon and ammo equivalents use Type.ERROR~~Change WEARABLE_INVALID, WEARABLE_NOT_REGISTERED and WEARABLE_NOT_WEARABLE to Type.ERROR~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/file/BartizanMessages.java:40-42~~The enum declares `WEARABLE_INVALID("Errors.Wearable.Invalid", Type.PREFIX)` plus the same for WEARABLE_NOT_REGISTERED and WEARABLE_NOT_WEARABLE, while the sibling failures are `INVALID_WEAPON("Errors.Not_Valid_Weapon", Type.ERROR)` and `INVALID_AMMO("Errors.Not_Valid_Ammo", Type.ERROR)`. Type.PREFIX renders through the generic plugin prefix rather than the red Errors.Prefix, so a wrong wearable name, an unregistered key or a non-wearable item reads as ordinary informational chat instead of an error, inconsistent with the identical weapon and ammo paths in the same command package.~~Medium
