# BZ-CM commands — verified findings (Bartizan @ 0e18356)
1~~P2~~The three give commands never bound the parsed amount, so a negative throws and a zero reports a false success~~Apply the same one-line clamp WeaponGiveHelper.give already uses — `amount = Math.max(1, Math.min(amount, MAX_AMOUNT))` — inside AmmunitionGiveCommand.giveAmmunition and WearableGiveCommand.giveWearable, right before the slots computation. WeaponGiveHelper is package-private to org.luckyraven.bartizan.command and WearableGiveCommand lives in the .wearable subpackage, so either duplicate the one-liner (cheapest, matches how the give-building logic is already duplicated three times) or, if a fourth caller ever shows up, promote a small shared public helper instead of a third copy-paste.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/command/AmmunitionGiveCommandTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/command/wearable/WearableGiveCommandTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/AmmunitionGiveCommand.java:94-128 (giveAmmunition); bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/wearable/WearableGiveCommand.java:100-134 (giveWearable) — the WeaponGiveCommand copy of this bug is gone: it now delegates to WeaponGiveHelper.give(), WeaponGiveHelper.java:20,37-41~~giveWeapon takes the amount straight from `weaponAmount = Integer.parseInt(args[3])` with no range check and computes `int slots = (int) Math.ceil(amount / (double) maxStackSize);` then `ItemStack[] items = new ItemStack[slots];`. For an amount of -100 against a stack size of 64, slots is -1 and `new ItemStack[-1]` throws NegativeArraySizeException, which the command framework surfaces to the admin as a bare number. For an amount of 0 the array is empty, the loop never runs, and giveWeapon still returns true, so the admin is told the give succeeded while nothing entered the inventory. A very large amount drives an unbounded allocation instead. The identical pattern is duplicated in AmmunitionGiveCommand.java:91-104 and WearableGiveCommand.java:96-108.~~High
2~~P2~~The weapon alias on /bartizan shadows WeaponCommand's own subcommand label, so /weapon give never dispatches~~Drop 'weapon' from plugin.yml's aliases list for the bartizan command, keeping only btz, exactly as originally proposed.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/PluginYamlCommandAliasTest (pins the fix)~~bartizan-plugin/src/main/resources/plugin.yml:17-23~~plugin.yml registers a single Bukkit command, bartizan, with `aliases: [btz, weapon]`, while WeaponCommand registers weapon as one of its own subcommand labels alongside ammo, wearable, debug and reload. Typing `/weapon give ak47 5` makes Bukkit consume weapon as the invoking alias and hand the dispatcher args[0]="give", which matches no registered subcommand, so the sender gets an unknown-subcommand error with a did-you-mean suggestion. Only `/bartizan weapon give ...` and `/btz weapon give ...` reach WeaponGiveCommand, leaving the weapon alias useful only for the bare `/weapon` info card.~~High
3~~P2~~WeaponGiveCommand tab-completes from weaponLoader.getFiles(), offering weapon files that failed to parse~~Change the completion supplier in WeaponGiveCommand.weaponGive() to `weaponAddon.getWeaponKeys().stream().toList()`, matching WeaponInfoCommand/WeaponListCommand. This also frees WeaponGiveCommand's weaponLoader field/constructor parameter (and WeaponCommand's corresponding constructor argument, WeaponCommand.java:70) if nothing else in the class still needs it.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/command/WeaponCommandTabCompletionTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WeaponGiveCommand.java:58-60~~The name argument's completion supplier is `return weaponLoader.getFiles().stream().map(FileHandler::getName).toList();`, which lists every YAML physically present in the weapon folder whether or not it registered a Weapon. WeaponInfoCommand and WeaponListCommand instead use weaponAddon.getWeaponKeys(), which holds only successfully parsed weapons. A weapon whose config failed to load is therefore still offered in `/bartizan weapon give <tab>`, and picking it makes getWeapon return null so the admin gets INVALID_WEAPON instead of an item. AmmunitionGiveCommand and WearableGiveCommand do not share the divergence. Was Gangland WP-31.~~High
4~~P3~~The three WEARABLE error messages use Type.PREFIX while the weapon and ammo equivalents use Type.ERROR~~Change WEARABLE_INVALID, WEARABLE_NOT_REGISTERED and WEARABLE_NOT_WEARABLE from Type.PREFIX to Type.ERROR, exactly as originally proposed.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/file/BartizanMessagesTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/file/BartizanMessages.java:46-48~~The enum declares `WEARABLE_INVALID("Errors.Wearable.Invalid", Type.PREFIX)` plus the same for WEARABLE_NOT_REGISTERED and WEARABLE_NOT_WEARABLE, while the sibling failures are `INVALID_WEAPON("Errors.Not_Valid_Weapon", Type.ERROR)` and `INVALID_AMMO("Errors.Not_Valid_Ammo", Type.ERROR)`. Type.PREFIX renders through the generic plugin prefix rather than the red Errors.Prefix, so a wrong wearable name, an unregistered key or a non-wearable item reads as ordinary informational chat instead of an error, inconsistent with the identical weapon and ammo paths in the same command package.~~Medium
5~~P1~~WeaponGiveHelper mints one Weapon (one UUID) and stamps it onto every physical stack when amount exceeds max stack size~~In WeaponGiveHelper.give(), give each non-throwable item its own UUID. Inside the loop, build each item from weaponManager.createTransientWeapon(name).buildItem(receiver). That call mints a UUID and does not register it. validateAndGetWeapon registers it when the item is first used. Don't call getWeapon(...,true) N times, because that adds N registry entries. For non-THROWABLE weapons, also treat maxStackSize as 1, so a stackable material such as the flamethrower's BLAZE_ROD can't fold N items into one stack with one UUID. Throwables share one UUID per type on purpose (WeaponService.mintUuid), so they keep the current stacking. Keep the null check on the template before the loop.~~WeaponGiveHelperTest, WeaponGiveHelperTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WeaponGiveHelper.java:34,51. The harm happens at bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/reload/Reload.java:448-468 (findWeaponSlot returns the first UUID match) and InstantReload.java:157-166 / NumberedReload.java:164-170, with the WeaponReloadCompleteEvent write-back in WeaponService.java:169-183.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] WeaponGiveHelper.give() calls `weaponManager.getWeapon(receiver, null, name, true)` exactly once (line 34), obtaining a single Weapon instance that WeaponService.getWeapon (WeaponService.java:263-296) mints with one fresh UUID and registers once in the live `weapons` map (`weapons.put(finalUuid, finalWeapon)`). The method then loops `for (int i = 0; i < slots; i++) { ItemStack item = weapon.buildItem(receiver); ... }` (lines 46-58), calling buildItem() on that same Weapon object once per 64-item slot. Weapon.buildItem() -> initializeTags() (Weapon.java:787-795) always stamps `this.uuid` (the one instance field) into the item's WeaponTag.UUID NBT, with no re-mint per call. Most shipped weapons use non-stackable materials (e.g. rifle.yml: `Material: IRON_PICKAXE`, vanilla max stack size 1), so an ordinary `/bartizan weapon get rifle 3` or `/bartizan weapon give <player> rifle 3` (both plain, documented, amount>1 usages with no special permission) produces N physical ItemStacks that all carry the identical UUID and are all backed by the SAME live Weapon object in WeaponService.weapons. Runtime-only state that is not persisted to item NBT until an action completes - reload progress/isReloading (Weapon.reload field, Weapon.java:250-251), scope state, press-lock/recoil state - therefore lives on that one shared object regardless of which of the N physical duplicates is currently held. Trigger -> wrong outcome: give a player 2 rifles this way; they start a staged reload on rifle #1 (shared Weapon's `reload` object enters isReloading=true, not yet written to any NBT); they hotbar-swap to rifle #2 before the reload finishes. validateAndGetWeapon(player, rifle#2) resolves the same UUID -> same shared Weapon object, so the HUD/firearm_state placeholder reports rifle #2 as "reloading" even though it was never touched, and whichever reload-completion task fires next writes the completed magazine onto whatever item WeaponService.getHeldWeaponItem(player) currently reports (rifle #2), leaving rifle #1 stuck with a reload that was silently redirected to a different physical item. The same shared-identity problem crosses players if any of the duplicate items are dropped/traded, since WeaponQuitCleanupListener and every firing/reload action all key off this shared registry entry rather than per-item state.~~High
6~~P2~~WeaponSkinCommand treats the literal skin name "default" as the reserved reset keyword, making a configured skin actually named default unreachable~~Fix at the root: in SkinSectionParser.parse(), when iterating Named keys (line 62), reject/warn (Severity.WARNING, issue id e.g. "skins.reserved_name") on a key that equalsIgnoreCase("default") and skip adding it to the `named` map, since "default" is the reserved clear-keyword baked into the command syntax `<name|default>` (documented in documentation/bartizan-api.md:153) and into WeaponSkinCommand/BartizanApiImpl's shared contract. This is a smaller, single-point fix than patching WeaponSkinCommand alone: it also fixes the tab-completer's silent key/keyword duplicate (lines 63-73) for free, and it keeps behavior consistent with BartizanApi.setSkin(Player, String) (BartizanApiImpl.java:101-108), which currently has NO "default"-interception at all and would already correctly select a Named "default" skin if one existed — i.e. the command and the cross-plugin API disagree today, which is itself evidence this is a bug rather than intended behavior. (Fixing only WeaponSkinCommand's string comparison, e.g. `skinName.equalsIgnoreCase("default") && (skins == null || skins.named(skinName) == null)`, would also work but leaves the parser free to accept a name that can never be reached by BartizanApi callers who don't know to avoid "default" either — less root-cause.)~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/configuration/parser/SkinSectionParserTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/command/WeaponSkinCommand.java:92 (the equalsIgnoreCase("default") -> null interception, before weapon.setSelectedSkin is ever consulted); secondary: lines 63-73 tabComplete() unconditionally appends "default" without checking skins.namedKeys() for a collision; root cause is upstream at bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/SkinSectionParser.java:57-71, which stores any Skins.Named key verbatim (lowercased) with no rejection of the reserved word "default".~~[found 2026-09-24, 0.5.0 @ 9fa25c3] handle() computes `String requested = skinName.equalsIgnoreCase("default") ? null : skinName;` then calls `weapon.setSelectedSkin(requested)`. Passing null clears the selection back to the state-driven default (per the class javadoc). Nothing in the Skins.Named YAML parser or SkinsData rejects or warns about an admin naming one of their own skins "default" (no validation found in the skins parsing path), and the tab-completer (tabComplete(), lines 63-73) even offers whatever named keys exist plus a literal "default" without checking for a collision. If an admin authors a Skins.Named entry called default (a plausible, unremarkable choice of name), `/bartizan weapon skin default` can never select it - the command always interprets that token as "clear selection" instead, silently doing the opposite of what the admin configured and the player typed.~~High
