# BZ-EF effects-status — verified findings (Bartizan 0.5.0 @ 9fa25c3, hunt 2026-09-24)
1~~P1~~StatusEffectService.expire() never sets EffectContext.source, so any On_Status_Expire effect targeting the shooter silently never fires~~In expire(), look up the shooter the same way rollContagion() does (line 350): `Player shooter = status.getShooterId() != null ? Bukkit.getPlayer(status.getShooterId()) : null;` Then add `.source(shooter)` to the EffectContext builder at line 243. An offline shooter stays null, which is fine. Update the syringe_gun.yml comment at lines 60-61 to say %shooter% is empty on Message_Expire only when the shooter is offline, not that no shooter is known. Add an ArgumentCaptor<EffectContext> assertion on getSource() to the expire test in StatusEffectServiceTest (around line 223). Separately, settings.yml:25 still calls On_Status_Expire "reserved - fired by later gates", which is out of date.~~StatusEffectServiceTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/status/StatusEffectService.java:237-245 (expire); the comment in bartizan-plugin/src/main/resources/weapon/syringe_gun.yml:59-61 describes the same limitation~~[found 2026-09-24, 0.5.0 @ 9fa25c3] apply() builds its EffectContext with `.source(shooter)` (line 124), but expire() (called from tick()-driven natural expiry, cure(), and clear() on quit/death) builds its context as `EffectContext.builder().weapon(status.getWeapon()).victim(victim).level(status.getLevel()).build()` (line 243) — source is never set, even though `status.getShooterId()` is known on every ActiveStatus and rollContagion() (line 350) shows the exact one-line pattern (`Bukkit.getPlayer(status.getShooterId())`) to resolve it. EffectContext.targets("source", radius) (EffectContext.java:135) returns List.of() whenever source is null, and "source" is the *default* Target for most effect types (message, action_bar, title, sound, boss_bar, cooldown, camera_shake, command all default Target to "source" when the YAML entry omits it). weapons-roadmap.md explicitly documents that 'servers that want something exotic write the hook list directly' under Effects:, so an admin authoring a custom `Effects: On_Status_Expire:` entry to notify the infector that their target's status wore off (a message/sound/action_bar with `Target: source`, or simply omitting Target since that's the type's own default) gets silent no-ops forever — no warning, no exception, nothing in the log. Trigger: configure any biological weapon with `Effects: { On_Status_Expire: [{Type: message, Text: "&7%victim% cured"}] }` (Target omitted → defaults to source), let a status you applied expire/get cured/die/quit — the message never reaches you. The shipped Feedback: sugar (StatusSectionParser.lowerFeedback) happens to only ever emit victim-targeted specs for On_Status_Expire, which is why this has stayed invisible in the default configs; it only breaks the documented 'write the hook list directly' escape hatch. The existing StatusEffectServiceTest only asserts the hook fires once with `any()` context (line 223), never inspecting ctx.getSource(), so nothing pins this.~~High
2~~P3~~BossBarHookEffect bars are orphaned on plugin disable because their only removal is a scheduler task~~Confirmed reachable and un-mitigated; the reported fix is right-sized. Give BossBarHookEffect a small live-bar set (Set<BossBar> or Map<UUID,BossBar> if per-player tracking is wanted) it adds to on run() and removes from in the scheduled callback, and make EffectRunner implement BeanLifecycle with onShutdown() that iterates its registry and clears any effect exposing live state (or, simplest: give BossBarHookEffect itself an onShutdown()-style method and have EffectRunner forward shutdown to any registry entry that is a BeanLifecycle). Mirrors the exact pattern already used by HudService.onShutdown()/StatusEffectService.onShutdown() next to it.~~BossBarHookEffectTest, EffectRunnerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/effect/impl/BossBarHookEffect.java:46-49 (run()); root cause is structural — BossBarHookEffect is built inline inside EffectRunner.buildRegistry() (EffectRunner.java:47-65) and neither EffectRunner nor any effect implements org.luckyraven.keystone.bean.BeanLifecycle, so nothing in this path is visited by BartizanContext.shutdownBeans() → BeanFactory.shutdownLifecycleBeans() (called from Bartizan.onDisable(), Bartizan.java:56).~~[found 2026-09-24, 0.5.0 @ 9fa25c3] run() creates a BossBar, adds the target players, and relies only on Bukkit.getScheduler().runTaskLater(plugin, bar::removeAll, duration) to take it down. Nothing tracks live bars, and neither EffectRunner nor the effect is lifecycle-managed. HudService and StatusEffectService, by contrast, remove their bars in onShutdown. Trigger: a weapon with an On_Hit Boss_Bar effect (Duration 100) hits a player, and Bartizan is disabled within 5 s (/reload, a plugin manager, an update). The pending removal is cancelled with the plugin, and the bar stays on the victim's screen until they relog. Latent: no shipped weapon uses the Boss_Bar effect type.~~High
3~~P3~~A status's boss bar keeps its first infector's Color/Style forever, even after a different weapon with different Boss_Bar styling re-stacks the same status~~In updateBossBar() (line 280), also call bar.setColor(parseEnum(BarColor.class, statusData.getBossBar().color(), BarColor.WHITE)) and bar.setStyle(...) each call, re-deriving from the current statusData exactly like title/progress already do — same fix the report proposes.~~StatusEffectServiceTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/status/StatusEffectService.java:269-290 (ensureBossBar/updateBossBar); root cause enabled by apply() at lines 105-117, which keys `active` solely by victim UUID and unconditionally does `status.setWeapon(weapon)` on restack with no check that the new weapon matches the one that created the bar~~[found 2026-09-24, 0.5.0 @ 9fa25c3] ensureBossBar() (line 269) only creates and colours the BossBar the first time a victim is infected (`if (status.getBossBar() != null) return;` guards every later call), reading `statusData.getBossBar().color()/style()` once at that moment. Every subsequent apply() re-stack calls updateBossBar() (line 280), which only touches `progress` and `title` (line 288-289) — Color/Style are never revisited. Since apply() unconditionally does `status.setWeapon(weapon)` on every re-stack (line 114) regardless of which weapon landed the new hit, a victim who is first infected by Weapon A (e.g. GREEN/SOLID bar) and then, before A's status expires, hit by Weapon B (a different biological weapon with e.g. RED/SEGMENTED_10 configured) keeps A's bar colour/style while the title text switches to B's Name/Icon (since formatBossBarText reads the freshly-passed statusData each call). Reachable on any server running more than one biological weapon (the roadmap's own example ships two: syringe_gun.yml and ray_gun.yml).~~High
4~~P0~~CommandHookEffect dispatches player-controlled entity names as console command text (selector/argument injection)~~Fix it in CommandHookEffect only, not in ctx.placeholders(). Build the Command substitution from ctx.getSource()/ctx.getVictim() directly: if the entity is a Player, use player.getName() (Mojang restricts it to [A-Za-z0-9_]). Otherwise use entity.getUniqueId().toString(). A UUID is still a valid vanilla entity argument and can never parse as a selector or add extra arguments, so a template like 'effect give %victim% glowing 3' still works on mobs. Optionally, strip '&' from entity names in EffectContext.format() so a mob name cannot inject color codes into Message, Title or Boss_Bar text. That part is only P3 cosmetic.~~CommandHookEffectTest, CommandHookEffectTest, EffectContextTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/effect/impl/CommandHookEffect.java:29,37,41-45 (substitute reads ctx.placeholders()); source of the raw name is EffectContext.java:81 (victim.getName()). Mob-victim entry points: WeaponRaytracerImpl.java:583-593 (On_Hit / On_Critical / zone hooks), BeamAction.java:226, StatusEffectService.java:124/243, WearableService.java:371 (On_Hit_Taken). NOT On_Kill: WeaponDeathListener.java:107-199 only handles PlayerDeathEvent, so On_Kill's victim is always a Player.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] CommandHookEffect.substitute() replaces %player%/%victim% with LivingEntity#getName() verbatim, and with the default As: console the whole result goes to Bukkit.dispatchCommand(Bukkit.getConsoleSender(), resolved) with no escaping. getName() on a non-player LivingEntity returns its player-settable custom name (any player can name-tag a mob anything via an anvil). A weapon designer's normal Command effect such as 'give %victim% totem_of_undying 1' or 'lp user %victim% parent add vip' on On_Hit/On_Kill will, the moment the victim is a name-tagged mob, run the console command against whatever %victim% expands to: name a mob '@a' and the console command executes against every online player instead of the one killed mob (mass item grant / permission change / command run server-wide), because vanilla commands like /give, /lp, /effect accept target selectors. The same unsanitized substitution feeds EffectContext.format()'s BartizanChatUtil.color() call for Message/Title/Boss_Bar text, so a maliciously named mob can also inject &-color codes to spoof system-looking chat.~~High
5~~P0~~FireworkHookEffect spawns a real, undamage-protected Firework that deals live splash damage~~Tag the spawned firework (for example a PDC byte under a bartizan NamespacedKey, or metadata) before detonate(). In ProjectileDamageListener.onProjectileEntityDamage, also cancel when the damager is a Firework carrying that tag. Do not use the report's weaponVisualSpawner.registerCosmetic(id) fix as written. A lifetime detonation never fires ProjectileHitEvent, and that event is the only place unregisterCosmetic runs for this path, so every burst would leave its id behind in cosmeticEntityIds and the set would keep growing. If registerCosmetic is used anyway, schedule an unregisterCosmetic 2 ticks later. EffectRunner would also need the spawner injected, because it currently builds FireworkHookEffect with a no-arg constructor.~~FireworkHookEffectTest, ProjectileDamageListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/effect/impl/FireworkHookEffect.java:27-36 (spawn + detonate); no guard in bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/projectile/ProjectileDamageListener.java:31-39~~[found 2026-09-24, 0.5.0 @ 9fa25c3] run() spawns a real Firework entity with an effect and calls firework.detonate(), which is vanilla Bukkit behaviour that deals real explosion damage (~7 HP within ~5 blocks) to every living entity nearby, including the shooter. This firework is never passed to WeaponVisualSpawner (the registration ProjectileDamageListener checks with isCosmetic() before cancelling EntityDamageByEntityEvent for Bartizan's other purely-visual entities), so its damage reaches vanilla combat resolution untouched. It also never goes through DamageRules (unlike ExplosionHandler, which explicitly reads Owner_Immunity/Ignore_Teams off ExplosionData per its own javadoc) or CombatEligibility, and never raises WeaponEntityDamageEvent, so a kill from it is invisible to WeaponDeathListener's kill-credit/assist tracking. Any weapon using the documented Firework effect type (e.g. an On_Kill celebration burst) will actually hurt or kill the shooter, teammates, or bystanders with none of Bartizan's protection or stats systems applying.~~High
6~~P0~~EffectContext's Target: nearby never filters through DamageRules, so Push/Potion/Ignite/Command(As: player) hit protected entities~~Add filtering at the EffectContext level so it happens once for every current and future effect type, not per-hook-effect: give EffectContext access to the shooter-protection check for its own `weapon` (resolve DamageData/ThrowableData/ExplosionData by `instanceof` on `weapon`, exactly the pattern WeaponRaytracerImpl.java:341-344 and ExplosionHandler.isEligible already use) and apply `DamageRules.isProtected(data, source, candidate)` inside `nearby()` before returning the list. Leave `source`/`victim` single-entity resolution alone — `victim` already only ever holds an entity that passed DamageRules upstream (WeaponRaytracerImpl only calls ON_HIT for non-protected candidates; ExplosionHandler.isEligible gates the damage loop the same way), and `Target: source` is a self-reference, not a protection question, so filtering it would incorrectly suppress a weapon's own self-directed effects. Add one test (e.g. in a new PushHookEffectTest/EffectContextTest) asserting a nearby teammate is excluded under `Ignore_Teams: true` and the shooter itself is excluded under `Owner_Immunity: true`, and that a non-DamageRules-bearing weapon (or wearable context with `weapon == null`) still returns the full unfiltered list so nothing else regresses.~~EffectContextTest, EffectContextTest (pins the fix)~~Root cause: bartizan-plugin/src/main/java/org/luckyraven/bartizan/effect/EffectContext.java, `nearby()` (lines 142-152) and `targets()` (lines 131-140) — collects every LivingEntity in radius / returns victim/source with zero DamageRules check. Consumed unfiltered by effect/impl/PushHookEffect.java:22, PotionHookEffect.java:30, IgniteHookEffect.java:19, CommandHookEffect.java:26 (all four call `ctx.targets(target, radius)` directly). Fix belongs in EffectContext (the one shared chokepoint all four — and every future Target:-consuming effect — route through), not in each hook effect.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] nearby() collects every LivingEntity within radius of the impact/source location with zero filtering. Every hook effect that documents and supports Target: nearby (settings.yml explicitly lists it for all effect types, 'required for Target: nearby') — Push, Potion, Ignite, and Command with As: player — funnels straight through ctx.targets(...)/nearby() with no DamageRules check. A weapon's Target: nearby Potion/Ignite/Push effect (a normal shape for a gas grenade, stun mine, or knockback blast) will poison/ignite/knock back the shooter's own teammates even with the weapon's Ignore_Teams: true, and the shooter itself even with Owner_Immunity: true — unlike every real damage path in the codebase: WeaponRaytracerImpl's candidate filter (line ~344) and ExplosionHandler both run victims through DamageRules.isProtected first. This is a real, reachable protection bypass any time a shipped or authored weapon uses the documented nearby target on one of these four effect types.~~Medium
