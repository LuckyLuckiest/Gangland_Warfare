# WM — weapon-model findings. Bartizan @ 0e18356, branch master.
1~~P0~~WeaponService.getWeapon(String) mints a registry entry and DB row on every read lookup~~Route every read-only caller through getWeaponTemplate/getWeaponTemplates/createTransientWeapon; make getWeapon(String) delegate to createTransientWeapon or require an explicit uuid/newInstance=true~~WeaponServiceTest (getWeaponTemplate_registersNothing, createTransientWeapon_registersNothing, getWeapon_actualGive_stillRegisters — pinning: the safe methods are proven safe, the vulnerable getWeapon(String)/getWeapon(Player,String) overloads are still exercised by nothing that proves them fixed)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/WeaponService.java:172-241~~getWeapon(@Nullable String type) at line 173 forwards to the 4-arg overload with uuid=null; that overload's `Weapon finalWeapon = weaponAddon.copyWithUUID(finalUuid); weapons.put(finalUuid, finalWeapon);` at lines 232-234 unconditionally mints a fresh Weapon and registers it in `weapons`, which WeaponManager.initialize() wires as `repository.setDataSupplier(() -> getWeapons().values())` — so a display-name lookup, death-message lookup, or sign check that still calls the 1-arg getWeapon(String) instead of getWeaponTemplate grows the live registry and the `weapon` table by one row per call, unbounded. was Gangland WP-6.~~High
2~~P0~~ProjectileState.canPenetrateBlock/canPenetrateEntity/canRicochet NPE when the weapon has no Modifiers section~~Null-check weapon.getModifiersData() in all three methods (mirror the null guards already present in RecoilManager.applyRecoil/SpreadManager.applySpread) and return false~~ProjectileStateTest.canPenetrateAndRicochet_nullModifiersData_throwsNpe (pinning: asserts NPE is thrown today)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/ProjectileState.java:66-93~~`var modifiers = weapon.getModifiersData(); if (!modifiers.hasPenetration()) return false;` in canPenetrateBlock/canPenetrateEntity, and the equivalent in canRicochet, call `.hasPenetration()`/`.hasRicochet()` on the result of getModifiersData() with no null check. ModifiersData is a mutable, optional (Nullable-by-omission) field only populated when a weapon's yml carries a `Modifiers:` section — the pinning test explicitly notes "no Modifiers: section" is what an ordinary admin config with no advanced modifiers looks like, making every shot from such a weapon that reaches the penetration/ricochet checks throw. was Gangland WP-7.~~High
3~~P1~~SpreadManager.checkSpreadReset compares a tick-authored Time value against a millisecond delta~~Convert spreadData.getResetTime() from ticks to ms (×50) before comparing against the currentTimeMillis() delta, or switch the delta itself to ticks~~SpreadManagerTest.applySpread_resetTimeInTicks_resetsFarEarlierThanIntended (pinning)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/spread/SpreadManager.java:66-75~~`long timeSinceLastShot = currentTime - lastShotTime; if (timeSinceLastShot >= spreadData.getResetTime()) { currentSpread = spreadData.getStart(); }` compares a millisecond delta against a value the yml author writes as `Time: 5` meaning 5 ticks (250ms at 20 TPS). Any real firing cadence exceeds the raw numeric value of a small tick count read as milliseconds, so the spread resets to Starting_Spread on effectively every shot instead of accumulating toward Bounds.Max. was Gangland WP-8.~~High
4~~P1~~WeaponService.weapons map is never pruned per-player; only a full clear() on bean reload removes entries~~Remove a player's minted weapon uuids from `weapons` on PlayerQuitEvent/death (or move to a WeakHashMap/TTL-based store) instead of relying solely on the reload-time clear()~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/WeaponService.java:32,266-268~~`private final Map<UUID, Weapon> weapons;` only shrinks via `public void clear() { weapons.clear(); }`, which WeaponManager.onClear() (a BeanLifecycle reload hook) is the only caller of — there is no removal path tied to a player quitting, dying, or an item being destroyed. Combined with finding #1 (every non-template lookup mints a new entry), the map and the backing `weapon` table both grow for the lifetime of the server between reloads. was Gangland WP-9.~~High
5~~P1~~ReloadType.amount is a single mutable field shared by every weapon using that reload type~~Move Reload_Amount off the shared ReloadType enum constant and onto ReloadData/AmmunitionData (per-weapon), passing it explicitly into createInstance instead of reading enum state~~TypeEnumParsingTest.reloadType_amountIsSharedMutableState (pinning)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/reload/ReloadType.java:11-32~~`private int amount;` is declared as an instance field on the `ReloadType` enum, but ONE/NUM are JVM-wide singletons — `case ONE, NUM -> new NumberedReload(weapon, ammunition, amount);` in createInstance() reads whatever the last weapon's parser call to `ReloadType.NUM.setAmount(...)` left there. Two configured weapons that both use `num` reload with different `Reload_Amount` values silently race: whichever was parsed last wins for every weapon of that type constructed afterward. was Gangland WP-16.~~High
6~~P1~~Throwables share one deterministic UUID per type, so getWeapon() overwrites one player's registered instance with another's~~Key registered throwable instances by (player, type) or a per-give random uuid instead of reusing the type-deterministic uuid for anything that gets put() into `weapons`; keep the deterministic uuid only for createTransientWeapon's stacking behaviour~~WeaponServiceTest.createTransientWeapon_throwable_usesDeterministicUuid only pins the uuid determinism for the never-registered createTransientWeapon path, not the registered-collision scenario below — not covered~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/WeaponService.java:225,232-234,283-286~~`mintUuid` returns `UUID.nameUUIDFromBytes(("throwable:" + type)...)` for every THROWABLE-category weapon regardless of caller, and `getWeapon(player, uuid=null, type, false)` (the path any "give a grenade" call takes without an explicit uuid) always does `weapons.put(finalUuid, finalWeapon)` with that same deterministic uuid. A second player being given the same throwable type overwrites the first player's already-registered Weapon object at that uuid in the shared map, so any code keying off that uuid (press-lock/scope/spread state, ammo tracking) now resolves to the second player's instance instead of the first's. was Gangland WP-32.~~Medium
7~~P2~~GunWeapon.consumeShot with Consumed_Amount defaulting to 0 gives an infinite magazine~~Reject/parse-warn a configured or defaulted Projectile.Consumed_Amount of 0 for gun weapons instead of silently subtracting 0 every shot~~WeaponConsumeShotTest.gunWeapon_consumedAmountZero_infiniteMagazine (pinning)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/GunWeapon.java:46-51~~`setCurrentMagCapacity(Math.max(0, getCurrentMagCapacity() - projectileData.getConsumed()));` never reduces the magazine when `ProjectileData.consumed` is left at its unset default of 0 (no `@Builder.Default` on that field), so a gun yml that omits `Consumed_Amount` fires forever without ever needing a reload while `consumeShot()` keeps returning true. was Gangland WP-13.~~High
8~~P3~~DurabilityCalculator divides by Weapon.durability with no zero guard, silently yielding 0 instead of erroring~~Treat a configured Durability.Base of 0 the same way calculateWeaponDurabilityFromItem already treats itemMaxDurability==0 — an explicit early-return instead of relying on NaN/Infinity narrowing to 0~~DurabilityCalculatorTest.getWeaponDurability_zeroWeaponMax_divisionByZero_yieldsZeroNotCrash (pinning)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/durability/DurabilityCalculator.java:22-33~~`double scale = itemMaxDurability / weaponMaxDurability;` in getWeaponDurability divides by `weapon.getDurability()` with no zero check (unlike calculateWeaponDurabilityFromItem two methods below, which does guard `itemMaxDurability == 0`). A weapon yml with `Durability.Base: 0` produces `0/0` or `finite/0`, and the JLS-defined narrowing of the resulting NaN/Infinity to `(short)` silently yields 0 rather than surfacing the misconfiguration. Weapon.buildItem() (Weapon.java:207-209) performs the same unguarded division independently. was Gangland WP-33.~~High
9~~P3~~RecoilManager.clone() calls Object.clone() without the class implementing Cloneable~~Either implement Cloneable on RecoilManager or delete the dead clone() override, since Weapon.initClone() already replaces recoil with `new RecoilManager(this)` instead of calling this method~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/recoil/RecoilManager.java:10,74-80~~`public class RecoilManager {` carries no `implements Cloneable`, yet it overrides `public RecoilManager clone() { return (RecoilManager) super.clone(); }` — `Object.clone()` on a non-Cloneable type always throws `CloneNotSupportedException`, which this method wraps and rethrows as `PluginException` on every call. Currently unreachable dead code (Weapon.initClone() at Weapon.java:415 constructs a fresh RecoilManager rather than cloning the source's), but it is a landmine for the next caller who reasonably expects `clone()` to work. was Gangland WP-34.~~Medium
10~~P2~~Weapon.tags is a final Map shared by reference across every clone descended from the same template~~Reassign a fresh TreeMap in initClone() (drop `final` from the tags field) instead of clearing the map the clone still shares with its source~~WeaponCloneTest.clone_bug_tagsMapIsSharedNotCopied (pinning; the test's own name and javadoc call this out as a bug beyond the docket's numbered observations)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/Weapon.java:46,110,298-306,403-404~~`private final Map<WeaponTag, Object> tags;` is set once in the constructor (`this.tags = new TreeMap<>();`) and, being final, can never be reassigned — `clone()`'s `Object.clone()` therefore copies only the reference, and every subsequent clone descended from the same object (ultimately the shared config template every WeaponAddon entry keeps) points at the exact same TreeMap forever. `initClone()`'s `this.tags.clear();` clears that one shared map, wiping whatever the source (or any sibling clone) had cached. initializeTags()/updateTag() do re-derive the four tag values from the weapon's own instance fields before writing to an ItemStack's NBT, which keeps the actual item data correct, but `Weapon.getTags()` is a public getter and any caller reading it directly gets another instance's or an already-cleared map's contents.~~High
11~~P1~~Weapon.applyPush dereferences recoilData with no null check~~Guard both branches of applyPush the same way scope()/unScope() guard scopeData, returning early when recoilData is null~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/Weapon.java:324-339~~`push(player, recoilData.getPushPowerUp(), recoilData.getPushVelocity());` (not-sneaking branch) and `push(player, recoilData.getPushPowerUp() / 2, recoilData.getPushVelocity() / 2);` (sneaking+scoped branch) both read `recoilData` directly with no null guard, unlike every other optional-DTO consumer in this same class (scope(), unScope(), isMagazineFull(), applyOnHitDurability all null-check their DTO first). RecoilData is a mutable field only populated when a weapon's yml carries a `Recoil:` section; any weapon type that calls applyPush() without one configured throws an NPE on every successful, grounded, non-sneaking shot.~~Medium
