# BZ-HU hud-stats — verified findings (Bartizan 0.5.0 @ 9fa25c3, hunt 2026-09-24)
1~~P1~~StatsService.save() clears the dirty flag before the write succeeds, so a failed quit-save permanently drops that session's stats~~Make save(UUID) return a boolean. Clear the dirty flag only after the write succeeds, and return false when statsDir.mkdirs() fails or the writer throws. In onQuit, drop `loaded` and `dirty` only when save() returns true. On failure, keep both so the autosave timer and onShutdown retry the write. When the player rejoins, stats() then returns the newer in-memory copy instead of the stale file. To stop retried entries for offline players from staying in `loaded` forever, have autosave() remove an id from `loaded` after a successful save when Bukkit.getPlayer(id) == null. Re-adding the id to `dirty` in save()'s catch block, as the report proposes, is not enough: onQuit lines 172-173 then delete `loaded` and `dirty` unconditionally, so the quit-time loss still happens.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/stats/StatsServiceTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/stats/StatsServiceTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/stats/StatsService.java:170-174 (onQuit), 220-245 (autosave/save)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] save(UUID playerId) opens with `if (!dirty.remove(playerId)) return;` (line 229) - the dirty bit is cleared immediately, before the actual `Files.newBufferedWriter(...)` write is attempted a few lines later (line 240). If that write throws IOException (stats directory deleted by an external backup/cleanup job, permission issue, full disk, file locked by AV/backup software - all realistic on a real server), the catch block only logs a warning (line 243); nothing re-adds playerId to `dirty`. For a player who is still online, this self-heals on their very next recordShot/recordDamage/recordKill (markDirty runs again), but StatsService.onQuit(playerId) (lines 170-174) calls save(playerId) and then unconditionally does `loaded.remove(playerId)` right after, discarding the in-memory PlayerStats regardless of whether the save succeeded. A disk hiccup at the exact moment a player disconnects therefore permanently loses every stat (shots/hits/kills/damage) accumulated since the last successful autosave for that session, with only a log line and no admin-facing signal - matching the tier rubric's "silent failures hidden from admins."~~High
2~~P2~~HudShotRefreshListener re-creates a HUD boss bar for a player who already quit; the entry leaks and the reused bar is never shown to them after rejoin~~In HudShotRefreshListener.onShoot, check `player.isOnline()` inside the deferred `runTask` lambda (not just at schedule time, since offline-ness is what changes over that 1-tick gap) before calling hudService.refresh. Belt-and-suspenders in HudService.updateBossBar: when reusing an existing bar (`bar != null`), call `bar.addPlayer(player)` unconditionally (BossBar.addPlayer is documented idempotent - CraftBossBar no-ops if the player is already tracked) so a bar surviving under a UUID from a prior session always has the current session's Player instance attached.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/hud/HudShotRefreshListenerTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/hud/HudShotRefreshListenerTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/hud/HudServiceTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/hud/HudShotRefreshListener.java:31-36 (missing isOnline guard on deferred refresh); bartizan-plugin/src/main/java/org/luckyraven/bartizan/hud/HudService.java:122-139 (updateBossBar reuses a stale-keyed bar without re-adding the current viewer)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] onShoot schedules hudService.refresh(player) one tick later with no isOnline check. HudService.updateBossBar then creates a bar, calls addPlayer, and stores it in bossBars under the player's UUID. WeaponQuitCleanupListener removes the entry at quit. Any WeaponShootEvent for that player after quit re-creates it: a shot in the same tick as a kick, the remaining rounds of a BURST SequenceTimer (GunFireDispatcher:90-97), or FullAutoTask rounds fired after quit (docket EV-01). HudService.tick only iterates online players, so the entry is never removed, and the CraftBossBar keeps a reference to the dead ServerPlayer until restart. On rejoin, updateBossBar finds the stale bar and reuses it without calling addPlayer for the new player, so the HUD boss bar never appears. It only reappears after the player holds a non-HUD item (removeBossBar) and switches back. Trigger: the shipped rifle (AUTO, HUD.Boss_Bar); the player holds RMB and disconnects, then rejoins holding the rifle and has no ammo boss bar.~~High
3~~P0~~The PlaceholderAPI expansion calls validateAndGetWeapon from whatever thread PAPI uses, so an async caller writes the live weapon's ammo, durability and fire mode from stale item NBT and puts into the plain HashMap registry~~Make the expansion read-only. Add a non-minting, non-syncing lookup to WeaponService, for example `@Nullable Weapon peekWeapon(ItemStack item) { UUID u = getWeaponUUID(item); return u == null ? null : weapons.get(u); }`, and use it in BartizanExpansion.onPlaceholderRequest instead of validateAndGetWeapon. If the uuid is not registered yet, fall back to getWeaponTemplate(getHeldWeaponName(item)) for the static fields and read ammo-left, selective fire and durability from the item NBT. Main-thread paths (the HUD ticker, interact and give) already keep the live instance synced, so the expansion never needs to write. Change `weapons` to `new ConcurrentHashMap<>()` so the remaining cross-thread reads (isWeapon, peek) are safe. WeaponPlaceholders.resolve is already read-only.~~WeaponServiceTest, WeaponServiceTest, BartizanExpansionTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/hud/BartizanExpansion.java:56-61 (calls validateAndGetWeapon); bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/WeaponService.java:46 (plain HashMap), :289 (weapons.put on the mint path), :329 + :365-387 (setWeaponData overwrites the shared live Weapon from item NBT); race partner bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/GunAction.java:59 (consumeShot) through :88/:96 (updateWeaponData/updateWeapon)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] BartizanExpansion.onPlaceholderRequest is called on the caller's thread. It reads `player.getInventory().getItemInMainHand()` and passes it to `weaponService.validateAndGetWeapon(player, item)`. That lookup is not read-only. (1) When the item's uuid is not registered yet (its first use since boot or /bartizan reload), getWeapon mints a clone and calls `weapons.put(finalUuid, finalWeapon)`. `weapons` is a plain `new HashMap<>()` that the main thread also puts into on every first-use interact, HUD tick and give. (2) On every call it runs `setWeaponData(weapon, new ItemBuilder(heldItem))`, which overwrites the shared live Weapon's currentMagCapacity, currentDurability, currentSelectiveFire, loaded ammo type and skin with whatever the item NBT holds at that moment. None of these fields is volatile or guarded. PlaceholderAPI expansions are routinely resolved off the main thread. Chat plugins (DeluxeChat, VentureChat, ChatControl, LPC and others) apply PAPI to the chat format inside AsyncPlayerChatEvent, and TAB and scoreboard plugins refresh on their own threads. Trigger: a chat format containing `%bartizan_ammo_left%` (or any bartizan param), and a player who chats while AUTO-firing. On the main thread, GunAction.weaponShoot runs `weapon.consumeShot()` (mag 30 -> 29, GunAction.java:59), then fires WeaponShootEvent and raytraces (:79-86), and only then writes the magazine back to NBT (`updateWeaponData`/`updateWeapon`, :88/:96). If the async chat thread's setWeaponData lands inside that window, it reads the item's old NBT (30) and does `setCurrentMagCapacity(30)`. The main thread then persists 30: the shot was fired and no round was used. Scoreboard/TAB refreshes every few hundred ms make the refund recur (ammo creation). The same overwrite during a numbered reload (a round added in memory but not yet persisted) loses the round after the ammo item was already taken from the inventory. A racing `weapons.put` can also drop an entry or leave two Weapon instances for one uuid. The main thread then holds an instance that is not in the registry, so its reload flag and scope state split from what the next lookup returns.~~High
4~~P1~~PlayerStats.deaths (and the recentDamage assist-window cleanup) only advance when a Bartizan weapon or status claims the kill, so ordinary deaths — fall, drowning, lava, void, unarmed melee, mob attacks — never increment the death counter~~Confirmed as designed by the reporter: repo-wide grep shows `stats.deaths++`/`PlayerStats.deaths` is written at exactly StatsService.java:145 and nowhere else, and that line is reached only from recordKill, which is reached only from StatsListener.onKill(WeaponKillEntityEvent), which is fired only by WeaponDeathListener when a weapon/throwable claim or an active biological status claims the kill. Any death with neither (fall/drown/lava/void/starvation, unarmed PvP, vanilla/other-plugin mob kill) never calls recordKill. Fix direction matches the report: add a StatsService.recordDeath(Player victim) that does the unconditional `deaths++` + `recentDamage.remove(victim)`, invoked from a new StatsListener @EventHandler(priority = MONITOR) on PlayerDeathEvent (MONITOR runs after WeaponDeathListener's HIGH-priority handler, so it observes the final state) fired for every player death; then delete the `deaths++` line from recordKill (its `recentDamage.remove` can stay for the assist loop's own use — the generic handler's removal becomes a harmless no-op when a weapon kill already consumed it) so weapon-attributed deaths aren't double-counted.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/stats/StatsServiceTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/stats/StatsServiceTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/stats/StatsServiceTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/stats/StatsServiceTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/stats/StatsServiceTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/stats/StatsService.java:127-149 (recordKill — the only site that does `stats.deaths++` and the only site that removes a victim's `recentDamage` entry); bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/death/WeaponDeathListener.java:106-169 (onPlayerDeath — fires WeaponKillEntityEvent, and thus reaches recordKill, only when `weapon != null || throwableName != null`, i.e. line 145's early return) and :176-204 (creditStatusKill — the only other path to recordKill, gated on an active biological status within its kill-credit window)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] `stats.deaths++` exists at exactly one call site, StatsService.recordKill line 145, and that's the only place recentDamage's per-victim entry is ever removed (line 130). recordKill is only ever invoked from StatsListener.onKill (StatsListener.java:51-56), which only ever runs when a WeaponKillEntityEvent is actually constructed and fired. WeaponDeathListener is the only place that constructs one, and it only does so (a) in onPlayerDeath when a weapon or a recorded throwable claim resolves to a non-null Weapon (guarded by the early `if (weapon == null && throwableName == null) return;` at line 145), or (b) in creditStatusKill when an active biological status is still within its kill-credit window (lines 194-195). Any death that is neither claimed by a currently/recently-held Bartizan weapon nor by an active biological status — falling, drowning, lava, void, starvation, an unarmed player punching another to death, a vanilla/other-plugin mob attacking a player — never reaches recordKill at all. `/bartizan stats`'s 'Deaths' field (StatsCommand.java:166, PlayerStats.deaths) therefore silently undercounts real deaths on any server with normal (non-weapon) PvE/PvP alongside Bartizan combat, and any recentDamage entry recorded for that victim from an earlier non-fatal Bartizan hit is never cleaned up by this path either (compounding the already-documented `// ponytail` leak at StatsService.java:60-64).~~Medium
