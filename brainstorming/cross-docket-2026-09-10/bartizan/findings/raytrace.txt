# BZ-RT raytrace — verified findings (Bartizan @ 0e18356)
1~~P0~~Weapon block breaking calls block.setType(AIR) without ever firing BlockBreakEvent, bypassing every protection plugin~~Fire a cancellable BlockBreakEvent (or the region-plugin equivalent) in BlockDamageManager before removing the block and honour its cancellation~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManager.java:206-218~~BlockDamageManager removes blocks with a bare `block.setType(Material.AIR);` at two sites, reached from WeaponRaytracerImpl.applyBlockBreak (line 500-508) on every raytraced block hit that matches a Break_Blocks modifier. A repo-wide grep for BlockBreakEvent finds only an import and a listener in WeaponInteract — nothing in the plugin ever fires one. Five shipped weapons configure Break_Blocks (rifle ships `- GLASS-3`, `- ICE-5`, `- TERRACOTTA-10`, plus shotgun, minigun, steyr_aug, golden_ak47), so out of the box any player holding a rifle can destroy glass, ice and terracotta inside a WorldGuard or GriefPrevention claim with no plugin able to observe or veto it.~~High
2~~P0~~Cosmetic ROCKET Fireball is spawned without setYield(0), so vanilla explosion physics fire alongside the configured blast~~Call setYield(0f) and setIsIncendiary(false) on the spawned projectile inside WeaponVisualSpawner.spawnCosmetic~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/raytrace/WeaponVisualSpawner.java:49-64~~WeaponShooting.fireSlow picks `Class<? extends Projectile> visualClass = type == ProjectileType.ROCKET ? Fireball.class : Firework.class;` and spawnCosmetic does only `projectile.setSilent(true); projectile.setGravity(false); projectile.setShooter(shooter); projectile.setVelocity(velocity);` before returning. A grep for setYield, setIsIncendiary, EntityExplodeEvent and ExplosionPrimeEvent across both modules returns nothing, so the Fireball keeps its default yield and explodes on contact through vanilla physics with real block destruction and entity damage. That fires in addition to SteppedProjectileTask's own configured fireExplosion, so every rocket shot produces one uncontrolled vanilla explosion that ignores Explosion_Radius and Explosion_Damage entirely, contradicting the spawner's javadoc that cosmetic entities never drive damage logic.~~High
3~~P1~~CombatEligibility.canBeHit is never consulted for the entity being shot, only for the shooter and reloader~~Gate the raytracer's entity filter on CombatEligibility.resolve().canBeHit(player) for Player targets, in WeaponShooting or as the RaytraceRequest default~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:291-296~~advanceRay's entity predicate is `e -> e != shooter && !(e instanceof ItemFrame || e instanceof ArmorStand) && request.getEntityFilter().test(e)`, and RaytraceRequest.Builder defaults `entityFilter = e -> true` (RaytraceRequest.java:69), which WeaponShooting never overrides. All six canBeHit call sites in the repo gate the acting player: WeaponInteract.java:147 and :224 gate the shooter, InstantReload and NumberedReload gate the reloader, and WiringConfig.java:162 only builds the holder bean. CombatEligibility's javadoc says it replaces Gangland's DownedPlayerRegistry and returns false for a player who is downed or otherwise protected, so the contract's primary purpose — making those players un-shootable — is not implemented on the damage path.~~High
4~~P2~~handleEntityImpact calls setInvulnerable(false) on every hit target and never restores the previous flag~~Capture living.isInvulnerable() before the call and restore it after damage is applied~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:440-443~~The default damage branch runs `living.setNoDamageTicks(0); living.setInvulnerable(false);` unconditionally before `living.damage(event.getDamage(), shooter)`, and nothing later in the method restores the flag. Any entity that was deliberately invulnerable — an admin in god mode, a spawn-protected mob, an NPC inside a plugin-driven invulnerability window — has that flag permanently cleared the first time a weapon shot touches it, long after the shot resolves. Was Gangland WP-22.~~High
5~~P2~~SteppedProjectileTask and its cosmetic entity leak on plugin disable, leaving orphaned visuals in the world~~Track live SteppedProjectileTask instances and call terminate() on each from a BeanLifecycle shutdown hook~~SteppedProjectileTaskTest~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/SteppedProjectileTask.java:68-115~~WeaponShooting constructs the task fire-and-forget as `new SteppedProjectileTask(...).start()` with no registry holding a reference. start() schedules a Keystone RepeatingTimer through runTaskTimer, and Bukkit cancels that BukkitTask on disable, but cancelling the tick callback never invokes terminate(), which is what removes the cosmetic Fireball/Firework and unregisters its id from WeaponVisualSpawner.cosmeticEntityIds. A rocket in flight during a reload or shutdown therefore leaves a permanent gravity-free projectile entity in the world with no owner to despawn it.~~Medium
6~~P2~~BlockDamageManager's delayed regeneration tasks throw on Objects.requireNonNull(location.getWorld()) after a world unload~~Null-check location.getWorld() in sendBlockDamage and clearBlockDamage and abandon the block instead of throwing~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManager.java:128-147~~Both sendBlockDamage (line 131) and clearBlockDamage (line 142) open with `World world = Objects.requireNonNull(location.getWorld());`. They are invoked from scheduleRegeneration, startSmoothRegeneration and breakAndScheduleRestore, all of which are Bukkit delayed or repeating tasks that can fire after the block's world has unloaded, and this repo's own house rule states Bukkit getWorld() is nullable and must be checked before use. Unloading a world with a weapon-cracked block mid-regeneration throws an uncaught NPE inside the scheduler callback, aborting that block's cleanup and leaving a stale damagedBlocks entry with a permanent crack overlay.~~Medium
7~~P3~~WeaponKillEntityEvent is declared in bartizan-api but never constructed or fired anywhere~~Fire it from handleEntityImpact when living.isDead() becomes true after damage, or delete the dead event class~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:446-451~~handleEntityImpact applies `living.damage(event.getDamage(), shooter)` and never re-checks isDead() to signal a kill. A repo-wide grep for WeaponKillEntityEvent returns only its own class file and constructor, with no callEvent site in either module, so the event is a dead public API a consumer plugin can register a listener for and never receive. WeaponEntityDamageEvent, the other half of the original pairing, is now genuinely fired from ThrowableAction, so only the kill event remains dead. Was Gangland WP-18, half-fixed.~~High
8~~P3~~Hitscan rays start at the shooter's eye while the rendered tracer starts at the muzzle offset~~Draw the tracer from the same origin the ray uses, or raytrace from WeaponMuzzle.compute consistently~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:572-574~~WeaponShooting.fireHitscan builds the request with `Location origin = shooter.getEyeLocation();` while drawTracerLine seeds `Location previous = WeaponMuzzle.compute(ctx.getRequest().getShooter(), ctx.getRequest().getDirection());` before walking the segments. The first rendered leg therefore runs from the muzzle offset (right of the body, below the eye) to the first real ray point, producing a visible kink that does not match the path the shot actually took. The same split exists in fireSlow, which spawns the visual at the muzzle but raytraces from the eye. Was Gangland WP-26.~~High
9~~P3~~BlockGroupResolver.resolve falls back to raw Material.valueOf instead of XMaterial~~Resolve names through XMaterial.matchXMaterial(...).map(XMaterial::parseMaterial)~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/util/BlockGroupResolver.java:82-87~~The single-material fallback is `try { Material material = Material.valueOf(upperName); return Set.of(material); } catch (IllegalArgumentException e) { return Collections.emptySet(); }`. CLAUDE.md requires version-drifting enums to resolve through XSeries and never raw valueOf, so a material renamed between Minecraft versions in a weapon's Break_Blocks list silently resolves to an empty set and that block group stops breaking, with no warning to the admin.~~High
10~~P3~~SteppedProjectileTask plays the explosion sound through the raw Sound enum instead of SoundEffect~~Route the call through org.luckyraven.keystone.sound.SoundEffect as WeaponRaytracerImpl already does~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/SteppedProjectileTask.java:172-172~~The explosion effect plays `world.playSound(loc, Sound.ENTITY_GENERIC_EXPLODE, 2.0f, 1.0f);` against the raw Bukkit Sound enum, which CLAUDE.md bans in favour of XSeries-backed SoundEffect. The surrounding lines of the same method already resolve particles correctly through XParticle.EXPLOSION/SMOKE/FLAME.get(), so this is the one version-fragile call in an otherwise convention-following block.~~High
11~~P3~~BlockDamageManager's break effect uses the raw Particle enum instead of XParticle~~Resolve the particle through XParticle to match the rest of the raytrace and block-damage code~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManager.java:255-255~~The break effect calls `world.spawnParticle(Particle.BLOCK, location.clone().add(0.5, 0.5, 0.5), 25, 0.3, 0.3, 0.3, 0.05, blockData);` with a raw org.bukkit.Particle constant. Particle constant names have drifted across Minecraft versions (BLOCK_CRACK to BLOCK), which is exactly why CLAUDE.md requires XParticle here, and both WeaponRaytracerImpl's tracer and SteppedProjectileTask's explosion already use XParticle.X.get().~~Medium
12~~P3~~PenetrationModifier.calculateDamage is dead code; the real reduction math lives in ProjectileState~~Delete calculateDamage, or wire it in and remove the duplicate logic in ProjectileState.applyPenetrationReduction~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/action/PenetrationModifier.java:12-14~~`public double calculateDamage(double baseDamage, int penetrationCount) { return baseDamage * Math.pow(1.0 - damageReduction, penetrationCount); }` has no call site anywhere in either module. The per-hit reduction actually applied during a shot comes from ProjectileState.applyPenetrationReduction's cumulative multiplier, so this method is unreferenced modifier logic that duplicates, and could silently diverge from, the live formula.~~Medium
