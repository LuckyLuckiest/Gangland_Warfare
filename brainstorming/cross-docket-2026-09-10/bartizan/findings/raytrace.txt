# BZ-RT raytrace — verified findings (Bartizan @ 0e18356)
1~~P0~~Weapon block breaking calls block.setType(AIR) without ever firing BlockBreakEvent, bypassing every protection plugin~~In BlockDamageManager.destroyBlock and breakAndScheduleRestore, fire a cancellable org.bukkit.event.block.BlockBreakEvent (with a synthetic/no-op player if none is available for a weapon-caused break) before block.setType(Material.AIR), and return without breaking when event.isCancelled(); BlockDamageManager is JavaPlugin-scoped already so Bukkit.getPluginManager().callEvent is reachable directly.~~bartizan-api/src/test/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManagerTest, bartizan-api/src/test/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManagerTest, bartizan-api/src/test/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManagerTest, bartizan-api/src/test/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManagerTest (pins the fix)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManager.java:224-235 (destroyBlock) and :242-269 (breakAndScheduleRestore)~~BlockDamageManager removes blocks with a bare `block.setType(Material.AIR);` at two sites, reached from WeaponRaytracerImpl.applyBlockBreak (line 500-508) on every raytraced block hit that matches a Break_Blocks modifier. A repo-wide grep for BlockBreakEvent finds only an import and a listener in WeaponInteract — nothing in the plugin ever fires one. Five shipped weapons configure Break_Blocks (rifle ships `- GLASS-3`, `- ICE-5`, `- TERRACOTTA-10`, plus shotgun, minigun, steyr_aug, golden_ak47), so out of the box any player holding a rifle can destroy glass, ice and terracotta inside a WorldGuard or GriefPrevention claim with no plugin able to observe or veto it.~~High
2~~P0~~Cosmetic ROCKET Fireball is spawned without setYield(0), so vanilla explosion physics fire alongside the configured blast~~Call setYield(0f) and setIsIncendiary(false) on the spawned projectile inside WeaponVisualSpawner.spawnCosmetic~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/raytrace/WeaponVisualSpawner.java:49-64~~WeaponShooting.fireSlow picks `Class<? extends Projectile> visualClass = type == ProjectileType.ROCKET ? Fireball.class : Firework.class;` and spawnCosmetic does only `projectile.setSilent(true); projectile.setGravity(false); projectile.setShooter(shooter); projectile.setVelocity(velocity);` before returning. A grep for setYield, setIsIncendiary, EntityExplodeEvent and ExplosionPrimeEvent across both modules returns nothing, so the Fireball keeps its default yield and explodes on contact through vanilla physics with real block destruction and entity damage. That fires in addition to SteppedProjectileTask's own configured fireExplosion, so every rocket shot produces one uncontrolled vanilla explosion that ignores Explosion_Radius and Explosion_Damage entirely, contradicting the spawner's javadoc that cosmetic entities never drive damage logic.~~High
3~~P1~~CombatEligibility.canBeHit is never consulted for the entity being shot, only for the shooter and reloader~~Gate the raytracer's entity predicate in WeaponRaytracerImpl.advanceRay on CombatEligibility.resolve().canBeHit(player) for Player targets (mirroring the existing DamageRules.isProtected clause), or set it as RaytraceRequest.Builder's default entityFilter so both fireHitscan and fireSlow inherit it automatically.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/weapon/DamageRulesTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:336-345 (advanceRay entity predicate); bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponShooting.java (RaytraceRequest.Builder never sets entityFilter)~~advanceRay's entity predicate is `e -> e != shooter && !(e instanceof ItemFrame || e instanceof ArmorStand) && request.getEntityFilter().test(e)`, and RaytraceRequest.Builder defaults `entityFilter = e -> true` (RaytraceRequest.java:69), which WeaponShooting never overrides. All six canBeHit call sites in the repo gate the acting player: WeaponInteract.java:147 and :224 gate the shooter, InstantReload and NumberedReload gate the reloader, and WiringConfig.java:162 only builds the holder bean. CombatEligibility's javadoc says it replaces Gangland's DownedPlayerRegistry and returns false for a player who is downed or otherwise protected, so the contract's primary purpose — making those players un-shootable — is not implemented on the damage path.~~High
4~~P2~~handleEntityImpact calls setInvulnerable(false) on every hit target and never restores the previous flag~~Capture `boolean wasInvulnerable = living.isInvulnerable();` before the two calls and restore it with `living.setInvulnerable(wasInvulnerable);` right after `living.damage(...)` returns (in a finally, alongside the existing WeaponRaytracer.setRaytraceDamageInProgress try/finally).~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImplInvulnerabilityTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:526-527~~The default damage branch runs `living.setNoDamageTicks(0); living.setInvulnerable(false);` unconditionally before `living.damage(event.getDamage(), shooter)`, and nothing later in the method restores the flag. Any entity that was deliberately invulnerable — an admin in god mode, a spawn-protected mob, an NPC inside a plugin-driven invulnerability window — has that flag permanently cleared the first time a weapon shot touches it, long after the shot resolves. Was Gangland WP-22.~~High
5~~P2~~SteppedProjectileTask and its cosmetic entity leak on plugin disable, leaving orphaned visuals in the world~~Track live SteppedProjectileTask instances and call terminate() on each from a BeanLifecycle shutdown hook~~SteppedProjectileTaskTest~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/SteppedProjectileTask.java:68-115~~WeaponShooting constructs the task fire-and-forget as `new SteppedProjectileTask(...).start()` with no registry holding a reference. start() schedules a Keystone RepeatingTimer through runTaskTimer, and Bukkit cancels that BukkitTask on disable, but cancelling the tick callback never invokes terminate(), which is what removes the cosmetic Fireball/Firework and unregisters its id from WeaponVisualSpawner.cosmeticEntityIds. A rocket in flight during a reload or shutdown therefore leaves a permanent gravity-free projectile entity in the world with no owner to despawn it.~~Medium
6~~P2~~BlockDamageManager's delayed regeneration tasks throw on Objects.requireNonNull(location.getWorld()) after a world unload~~Change both call sites to `World world = location.getWorld(); if (world == null) { state.cancelRegeneration(); damagedBlocks.remove(location); return; }` (or equivalent early-return) so a regen/restore task on an unloaded world abandons cleanly instead of throwing inside the Bukkit scheduler callback.~~bartizan-api/src/test/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManagerTest (pins the fix)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManager.java:133 (sendBlockDamage) and :144 (clearBlockDamage)~~Both sendBlockDamage (line 131) and clearBlockDamage (line 142) open with `World world = Objects.requireNonNull(location.getWorld());`. They are invoked from scheduleRegeneration, startSmoothRegeneration and breakAndScheduleRestore, all of which are Bukkit delayed or repeating tasks that can fire after the block's world has unloaded, and this repo's own house rule states Bukkit getWorld() is nullable and must be checked before use. Unloading a world with a weapon-cracked block mid-regeneration throws an uncaught NPE inside the scheduler callback, aborting that block's cleanup and leaving a stale damagedBlocks entry with a permanent crack overlay.~~Medium
7~~P3~~WeaponKillEntityEvent is declared in bartizan-api but never constructed or fired anywhere~~Fire it from handleEntityImpact when living.isDead() becomes true after damage, or delete the dead event class~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:446-451~~handleEntityImpact applies `living.damage(event.getDamage(), shooter)` and never re-checks isDead() to signal a kill. A repo-wide grep for WeaponKillEntityEvent returns only its own class file and constructor, with no callEvent site in either module, so the event is a dead public API a consumer plugin can register a listener for and never receive. WeaponEntityDamageEvent, the other half of the original pairing, is now genuinely fired from ThrowableAction, so only the kill event remains dead. Was Gangland WP-18, half-fixed.~~High
8~~P3~~Hitscan rays start at the shooter's eye while the rendered tracer starts at the muzzle offset~~Either raytrace from WeaponMuzzle.compute(...) consistently in both fireHitscan and fireSlow (matching the rendered tracer/visual spawn point), or have drawTracerLine seed `previous` from ctx.getRequest().getOrigin() instead of recomputing the muzzle offset, so the ray and its visible tracer agree on where the shot started.~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponShooting.java:72-73 (fireHitscan), :128-142 (fireSlow); bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:859-861 (drawTracerLine)~~WeaponShooting.fireHitscan builds the request with `Location origin = shooter.getEyeLocation();` while drawTracerLine seeds `Location previous = WeaponMuzzle.compute(ctx.getRequest().getShooter(), ctx.getRequest().getDirection());` before walking the segments. The first rendered leg therefore runs from the muzzle offset (right of the body, below the eye) to the first real ray point, producing a visible kink that does not match the path the shot actually took. The same split exists in fireSlow, which spawns the visual at the muzzle but raytraces from the eye. Was Gangland WP-26.~~High
9~~P3~~BlockGroupResolver.resolve falls back to raw Material.valueOf instead of XMaterial~~Replace the try/catch with `return XMaterial.matchXMaterial(upperName).map(XMaterial::parseMaterial).map(Set::of).orElse(Collections.emptySet());` so a renamed material (e.g. a future Mojang rename) still resolves instead of silently dropping the whole block group.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/util/BlockGroupResolverTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/util/BlockGroupResolverTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/util/BlockGroupResolverTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/util/BlockGroupResolver.java:82-87~~The single-material fallback is `try { Material material = Material.valueOf(upperName); return Set.of(material); } catch (IllegalArgumentException e) { return Collections.emptySet(); }`. CLAUDE.md requires version-drifting enums to resolve through XSeries and never raw valueOf, so a material renamed between Minecraft versions in a weapon's Break_Blocks list silently resolves to an empty set and that block group stops breaking, with no warning to the admin.~~High
10~~P3~~SteppedProjectileTask plays the explosion sound through the raw Sound enum instead of SoundEffect~~Route the call through org.luckyraven.keystone.sound.SoundEffect as WeaponRaytracerImpl already does~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/SteppedProjectileTask.java:172-172~~The explosion effect plays `world.playSound(loc, Sound.ENTITY_GENERIC_EXPLODE, 2.0f, 1.0f);` against the raw Bukkit Sound enum, which CLAUDE.md bans in favour of XSeries-backed SoundEffect. The surrounding lines of the same method already resolve particles correctly through XParticle.EXPLOSION/SMOKE/FLAME.get(), so this is the one version-fragile call in an otherwise convention-following block.~~High
11~~P3~~BlockDamageManager's break effect uses the raw Particle enum instead of XParticle~~Resolve the particle through XParticle to match the rest of the raytrace and block-damage code~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManager.java:255-255~~The break effect calls `world.spawnParticle(Particle.BLOCK, location.clone().add(0.5, 0.5, 0.5), 25, 0.3, 0.3, 0.3, 0.05, blockData);` with a raw org.bukkit.Particle constant. Particle constant names have drifted across Minecraft versions (BLOCK_CRACK to BLOCK), which is exactly why CLAUDE.md requires XParticle here, and both WeaponRaytracerImpl's tracer and SteppedProjectileTask's explosion already use XParticle.X.get().~~Medium
12~~P3~~PenetrationModifier.calculateDamage is dead code; the real reduction math lives in ProjectileState~~Delete calculateDamage, or wire it in and remove the duplicate logic in ProjectileState.applyPenetrationReduction~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/action/PenetrationModifier.java:12-14~~`public double calculateDamage(double baseDamage, int penetrationCount) { return baseDamage * Math.pow(1.0 - damageReduction, penetrationCount); }` has no call site anywhere in either module. The per-hit reduction actually applied during a shot comes from ProjectileState.applyPenetrationReduction's cumulative multiplier, so this method is unreferenced modifier logic that duplicates, and could silently diverge from, the live formula.~~Medium
13~~P2~~Break_Blocks hits of 0 crashes block damage with a division by zero on every hit~~Add `.min(1)`-equivalent validation in ModifiersSectionParser.applyBreakBlocks: reject/skip a Break_Blocks entry whose hits token parses to <= 0 (mirroring the `.asInt().min(1)` pattern used for every other bounded numeric field in the other Section/Weapon parsers, e.g. GunWeaponParser's Pellets/Per_Shot, ChargeSectionParser's Max_Level, BeamWeaponParser's Ammo_Per_Level), and/or defensively clamp `hitsRequired` to at least 1 in BlockDamageManager.applyDamage before the division as defense-in-depth for any other/future caller that constructs a BlockBreakModifier directly (BeamAction.java:260 and ExplosionHandler.java:227 already hardcode `1`, but the record itself has no compact-constructor guard).~~BlockBreakModifierTest, BlockBreakModifierTest, BlockBreakModifierTest, ModifiersSectionParserTest (pins the fix)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManager.java:75 (applyDamage); root cause bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/parser/ModifiersSectionParser.java:68 (applyBreakBlocks, no lower-bound check on the parsed `hits` token)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] BlockDamageManager.applyDamage computes `int damageStage = Math.min(MAX_DAMAGE_STAGE, (currentHits * MAX_DAMAGE_STAGE) / hitsRequired);` with hitsRequired taken directly from the BlockBreakModifier record, with no lower-bound guard. ModifiersSectionParser.applyBreakBlocks (bartizan-plugin/.../configuration/parser/ModifiersSectionParser.java:68) parses the hits token with `Integer.parseInt(parts[1].trim())` and has no `>= 1` validation (unlike other numeric parses elsewhere in the codebase that use `.min(...)` bounds) — so a weapon YAML entry like `Modifiers: Break_Blocks: ["GLASS-0"]` (a plausible typo, or a misguided attempt to mean 'breaks in one hit') parses cleanly to BlockBreakModifier(materials, 0, RESTORE). The very first time that weapon's raytrace hits a matching block, applyBlockBreak -> BlockDamageManager.applyDamage divides by hitsRequired=0 and throws ArithmeticException, which propagates up through advanceRay/fireInstant/advanceSegment uncaught. Because state.incrementHits() (line 70) already ran before the division, every subsequent hit on that block repeats the crash — the block-break feature for that weapon is permanently non-functional (crack stage, break, and regeneration all silently stop working) while the console is spammed with a stack trace on every shot, a silent failure that is easy to miss admin-side beyond the noise.~~High
14~~P0~~Cancelling WeaponRaytraceImpactEvent does not stop a block from being cracked/broken~~Make handleBlockImpact return whether the event went through: `return false` at the isCancelled check, `return true` at the end. In advanceRay, replace the two calls with `if (handleBlockImpact(impactPt, block, face, ctx)) { applyBlockBreak(block, request.getWeapon()); }`. That fires the event before anything changes in the world, the same order the entity branch uses. A listener then also sees the real block instead of AIR after a DESTROY/RESTORE break. Update the handleBlockImpact javadoc to say that cancelling it also suppresses the Break_Blocks damage. Ricochet and penetration still run after the break, so their behaviour does not change.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerBlockImpactTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerBlockImpactTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:404-405 (advanceRay block branch) and :661-685 (handleBlockImpact)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] In advanceRay's block-hit branch, `applyBlockBreak(block, request.getWeapon())` (which calls BlockDamageManager.applyDamage -> crack progression / BreakMode.DESTROY|RESTORE, i.e. block.setType(AIR) etc.) runs BEFORE `handleBlockImpact(impactPt, block, face, ctx)`, which is the method that actually constructs and fires WeaponRaytraceImpactEvent and checks `if (event.isCancelled()) return;` (line 667-669). So by the time any listener gets a chance to cancel the event, the block has already been damaged/broken. WeaponRaytraceImpactEvent's own class javadoc explicitly promises 'Cancelling this event suppresses the actual damage application' (bartizan-api/.../WeaponRaytraceImpactEvent.java:26-27) — for blocks that promise is false. Contrast with the entity path: handleEntityImpact fires the event and returns on cancellation BEFORE calling living.damage(...), so entities are correctly protected but blocks are not. Trigger: any consumer plugin (e.g. a claim/region-protection integration) that listens to WeaponRaytraceImpactEvent and cancels it for a block hit inside a protected area still sees the block crack and eventually break/regenerate, because BlockDamageManager.applyDamage already ran two lines earlier — a genuine protection hole distinct from the already-known missing-vanilla-BlockBreakEvent issue (different event, different code path).~~High
15~~P1~~A server stop or plugin disable leaves blocks broken in RESTORE mode as AIR for good: BlockDamageManager.clearAll() is never called~~Add `blockDamageManager.clearAll();` to WeaponRaytracerImpl.onShutdown() next to visualSpawner.removeAll(). It runs on the main thread during Bartizan.onDisable -> context.shutdownBeans(), before Bukkit cancels the plugin's tasks. clearAll already cancels each restore task and writes the original BlockData back into any cell that is still AIR. Do not make BlockDamageManager itself a BeanLifecycle, because it lives in bartizan-api and would pull keystone-bean into the api.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerShutdownTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:184-186 (onShutdown); bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManager.java:112-125 (clearAll, no caller), 242-269 (breakAndScheduleRestore)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] breakAndScheduleRestore sets the block to AIR and depends only on a runTaskLater (Block_Regeneration.Restore_Delay_Ticks, default 100) to write the original BlockData back. BlockDamageManager is a plain @Bean (WiringConfig:74-77) and not a BeanLifecycle. Its clearAll() (javadoc: "useful for plugin disable ... Restores any blocks that are mid-restore") has no caller anywhere in either module. WeaponRaytracerImpl.onShutdown is the only lifecycle bean that holds the manager, and it only calls visualSpawner.removeAll(). On disable Bukkit cancels the pending restore task and nothing else restores the block. RESTORE is the default Break_Blocks mode (ModifiersSectionParser:69). The shipped rifle (GLASS-3, ICE-5, TERRACOTTA-10), shotgun, steyr_aug and golden_ak47 use it, and minigun sets ICE-9-RESTORE. ExplosionHandler.applyBlockDamage also breaks up to 512 blocks per blast in RESTORE mode when Block_Damage: true. Trigger: a player hits a glass window 3 times with the rifle, so the glass becomes AIR and the restore is scheduled 5 s out. The admin runs /stop (or /reload, or updates the plugin) inside those 5 s. The task is cancelled, clearAll never runs, and the window is saved to the world as permanent AIR. Players' builds lose blocks on every restart that lands inside a gunfight's restore window.~~High
16~~P1~~Armor_Piercing damage compensation formula is mathematically wrong — barely works at high armor~~Line 63: replace `return baseDamage + (piercingDamage - normalDamage);` with `return piercingDamage / (1 - normalReduction);`, which is the same as `baseDamage * (1 - piercingReduction) / (1 - normalReduction)`. normalReduction is at most 0.8 because it is capped by min(20, armor)/25, so this never divides by zero. Update ModifierHandlerTest line 70 from 28.0 to 60.0 (20 * 0.6 / 0.2). The flat-armor model still ignores Armor_Toughness and Protection enchantments. With high toughness, vanilla takes off less at large damage values, so the fixed ratio can deliver somewhat more than intended. That is still much closer than the current additive version. If exact numbers matter, add a toughness-aware inverse later. MeleeAction has its own separate AP split and is not affected.~~bartizan-api/src/test/java/org/luckyraven/bartizan/api/weapon/modifiers/ModifierHandlerTest (pins the fix)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/ModifierHandler.java:62-63 (called from bartizan-plugin/.../raytrace/WeaponRaytracerImpl.java:468, damage applied at :538); test pinning the wrong value at bartizan-api/src/test/.../modifiers/ModifierHandlerTest.java:66-70~~[found 2026-09-24, 0.5.0 @ 9fa25c3] calculateArmorPiercingDamage tries to compensate for the fact that the value it returns will be reduced again by Minecraft's own armor formula when handleEntityImpact later calls living.damage(...) (comment on line 62 says this explicitly). It computes normalDamage/piercingDamage using the assumed vanilla formula damage*(1-min(20,armor)/25), then returns `baseDamage + (piercingDamage - normalDamage)` (an additive correction). But since Minecraft will multiply whatever is returned by (1-normalReduction) again, the correct inverse requires dividing, not adding: returnedValue should be piercingDamage/(1-normalReduction). Concretely, with baseDamage=10, armor=20 (full diamond), armorBypass=0.5: normalReduction=0.8, intended piercingDamage=6.0, but the current formula returns 14, which after vanilla's own reduction (14*0.2) actually lands only 2.8 damage on the target — less than half the intended 6.0, and barely more than the 2.0 the target would take with no Armor_Piercing modifier at all. The error grows with armor: verified numerically for armor=0/5/10/15/20 (0/8.8/7.2/5.2/2.8 actual vs 10/9/8/7/6 intended). Armor_Piercing is one of the six 'action' modifiers weapons can configure (bartizan-api/.../modifiers/action/ArmorPiercingModifier.java) and is exercised on every hit against an armored LivingEntity by any weapon that sets it, so this is reachable in completely normal play — the modifier simply doesn't deliver anywhere near its configured bypass against well-armored targets, which is exactly when it matters most.~~High
17~~P1~~ProjectileHitEvent unregisters cosmetic status before that same hit's own EntityDamageByEntityEvent, so the impact deals real uncontrolled vanilla damage~~In onProjectileHit, cancel the event for a cosmetic id (`event.setCancelled(true)`) and remove the `unregisterCosmetic` call. SteppedProjectileTask.terminate() and WeaponVisualSpawner.removeAll() already handle unregistering. ProjectileHitEvent is Cancellable from 1.16.5 onward. On 1.21 a cancelled ENTITY hit skips onHit entirely, so no fireball damage event fires and the visual stays alive for the task. A cancelled BLOCK hit still runs onHit, which is harmless because yield is 0. Optional hardening: have spawnProjectile skip the live velocity (or zero it) so vanilla never gets a moving tick, since the task teleports the visual anyway.~~ProjectileDamageListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/projectile/ProjectileDamageListener.java:41-48 (onProjectileHit). The live vanilla tick comes from bartizan-api/.../raytrace/WeaponVisualSpawner.java:135 (setVelocity(launchVelocity)) and SteppedProjectileTask.java:116 (RepeatingTimer, first run is a no-op).~~[found 2026-09-24, 0.5.0 @ 9fa25c3] SteppedProjectileTask drives a real, live Bukkit Fireball/Firework entity (spawned by WeaponVisualSpawner.spawnProjectile) that is still ticked by vanilla physics every server tick, independently of the plugin's own once-per-tick teleport-and-zero-velocity loop and its own manual world.rayTraceBlocks-based collision detection. ProjectileDamageListener exists specifically because vanilla can still detect its own collision on this entity and fire a real ProjectileHitEvent/EntityDamageByEntityEvent pair (its own javadoc says so). onProjectileHit(ProjectileHitEvent) unconditionally calls visualSpawner.unregisterCosmetic(projectileId) the instant a ProjectileHitEvent fires on a cosmetic entity — it never cancels the event and never tells SteppedProjectileTask to stop. NMS processes a projectile hit synchronously in one call: ProjectileHitEvent fires and its listeners (including this one, at LOWEST) run first, and only afterward does the same onHit call proceed to onHitEntity, which is what fires EntityDamageByEntityEvent for that same collision. Because the cosmetic id is already stripped from WeaponVisualSpawner's set by the time onProjectileEntityDamage(EntityDamageByEntityEvent) checks visualSpawner.isCosmetic(damager.getEntityId()), that check now returns false and the event is not cancelled — so the very hit that triggered the unregister (and any later vanilla collision on the same still-flying entity, since SteppedProjectileTask never learns the hit happened and keeps teleporting it through the world for up to Alive_Ticks more ticks) lands as real, uncancelled vanilla Fireball/Firework damage. That path bypasses every one of Bartizan's own damage rules for that hit — Owner_Immunity, Ignore_Teams, CombatEligibility, the weapon's configured Base_Damage — because it's raw vanilla `entity.hurt()`, not anything Bartizan computed. Trigger: fire a ROCKET/FLARE weapon (or a cluster/airstrike sub-munition) near any living entity (a mob at the muzzle, a crowd the projectile arcs through) so vanilla's own collision detection beats or duplicates Bartizan's manual raytrace on the same tick -> that living entity (which could be the shooter's own teammate, or the shooter itself) takes an extra, un-attributed, un-immune vanilla hit outside Bartizan's damage model.~~High
18~~P0~~WeaponVisualSpawner's DROPPED_ITEM (and FALLING_BLOCK) cosmetic visuals are real Bukkit entities that can leak into vanilla item/block state with no event guard~~In ProjectileDamageListener, add `@EventHandler(priority = EventPriority.LOWEST, ignoreCancelled = true) public void onHopperPickup(InventoryPickupItemEvent event)` that calls `event.setCancelled(true)` when `visualSpawner.isCosmetic(event.getItem().getEntityId())`. This one event covers both hoppers and hopper minecarts. An EntityPickupItemEvent handler is not needed, because pickupDelay 32767 already stops players and mobs (Mob.aiStep checks hasPickUpDelay). Drop the FALLING_BLOCK half as a required fix. An EntityChangeBlockEvent cancel for cosmetic ids is optional hardening only. This guard does not cover ThrowableAction's grenade display Item, which is never registered as cosmetic; that is the separate finding.~~ProjectileDamageListenerTest, ProjectileDamageListenerTest (pins the fix)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/raytrace/WeaponVisualSpawner.java:176-187 (spawnItem). The missing guard belongs in bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/projectile/ProjectileDamageListener.java:30-50, which only has EntityDamageByEntityEvent and ProjectileHitEvent handlers.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] Projectile.Visual.Type is a fully supported, admin-facing config key (GunWeaponParser.parseVisual, VisualData.VisualType) that accepts DROPPED_ITEM and FALLING_BLOCK on any gun, not just the shipped fireball/firework defaults. spawnItem sets item.setPickupDelay(Integer.MAX_VALUE) to keep players off it, but that only affects direct player/mob pickup — vanilla hoppers, hopper minecarts and droppers ignore an Item entity's pickup delay entirely when sucking it into a container (the same vanilla mechanic the already-accepted 'thrown grenade display Item entity' finding relies on), and there is no InventoryPickupItemEvent/EntityPickupItemEvent handler anywhere in the codebase (grep confirms none) to reject a cosmetic-tagged item. spawnFallingBlock similarly only sets setDropItem(false)/setHurtEntities(false) — dropItem only suppresses the 'couldn't survive' debris drop, it does nothing to stop the FallingBlock's separate landing-placement logic from placing a real block via EntityChangeBlockEvent if the entity is ever detected onGround by vanilla physics (e.g., right after the 0.05-block-off-the-face reposition SteppedProjectileTask does on a bounce) before Bartizan's own terminate()/remove() runs first. Trigger: configure any weapon's Shoot.Projectile.Visual.Type: DROPPED_ITEM (e.g. Item: DIAMOND) and place a hopper under its flight path or muzzle -> every shot mints one real, pickupable-by-hopper diamond item out of a purely cosmetic visual, with zero ammo/currency cost.~~High
19~~P2~~ExplosionHandler fires WeaponEntityDamageEvent before calling target.damage(), unlike every other damage-kind path, so a blocked/no-op explosion hit still records stats and a false 5-second kill claim~~Mirror WeaponRaytracerImpl.handleEntityImpact (lines ~524-546) and IncendiaryAction.applyIncendiaryImpact: before calling target.damage(damage, shooter), also call target.setNoDamageTicks(0) (ExplosionHandler currently never resets this, unlike all three sibling paths — this alone lets a Cluster/Airstrike sub-munition's second bomblet get vanilla-blocked by i-frames from the first). Capture `double healthBefore = target.getHealth();` before damage(), compute `boolean damageBlocked = target.isValid() && !target.isDead() && target.getHealth() >= healthBefore;` after it, and move the `Bukkit.getPluginManager().callEvent(new WeaponEntityDamageEvent(...))` call to after target.damage(), guarded by `if (!damageBlocked && shooter instanceof Player playerShooter) { ... }`. Also gate applyKnockback/fire-ticks on !damageBlocked to fully match the sibling pattern.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/raytrace/ExplosionHandlerDamageVictimTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/ExplosionHandler.java:111-124 (explode()'s per-entity loop)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] In explode()'s per-entity loop, once `damage > 0` (a pure distance-falloff computation from ExplosionMath, gated only by DamageRules.isProtected's owner-immunity/same-team check — see DamageRules.java:40-46, which has no invulnerability or third-party-plugin awareness), the code calls `Bukkit.getPluginManager().callEvent(new WeaponEntityDamageEvent(..., DamageKind.EXPLOSION))` at lines 112-115 and only afterward calls `target.damage(damage, shooter)` at line 121, with no check of whether the damage actually landed. Every other DamageKind path in this codebase does the opposite: WeaponRaytracerImpl.handleEntityImpact (DIRECT, lines 543-566) computes `damageBlocked` from a before/after health comparison and skips firing the event when blocked; BeamAction (lines 199-205) and IncendiaryAction (lines 224-239) both compute the same `damageBlocked` check post-damage before firing. ExplosionHandler alone skips this. Concretely: a player standing in spawn protection, wearing an invulnerability effect, or protected by an unrelated plugin's EntityDamageEvent listener takes a grenade/rocket blast that vanilla protection silently no-ops — StatsListener.onDamage (registered at default priority, not MONITOR, and reading a WeaponEntityDamageEvent that is not Cancellable) still records a hit and full damage into that player's stats, and WeaponDeathListener.onWeaponEntityDamage still inserts a 5-second explosion-kill claim for that victim (line 98). If the victim then dies to any unrelated cause within that 5s window with a non-null Bukkit killer (e.g. a completely different player's melee hit), WeaponDeathListener.onPlayerDeath (lines 130-142) prefers the stale, never-actually-landed grenade claim over the real killer's held weapon, misattributing the death message and crediting the wrong player's stats with the kill.~~High
20~~P1~~ExplosionHandler never consults CombatEligibility.canBeHit for blast victims, so a downed/protected player still takes explosion damage, fire and knockback~~Put the check once in the shared victim filter, not only in ExplosionHandler. In DamageRules' private isProtected, add this as the first line, before the `shooter == null` early return: `if (victim instanceof Player p && !CombatEligibility.resolve().canBeHit(p)) return true;`. The lookup is resolved lazily at each call, per the convention. The same guard also closes BZ-RT-03 (the hitscan filter already routes through DamageRules.isProtected at WeaponRaytracerImpl:344), so there is no need for a separate fix in the raytracer. It is safe in unit tests: resolve() returns DEFAULT when there is no server, and DEFAULT is !isDead(), which is false on a mock, so the existing isEligible tests do not change. Melee, Beam and Incendiary do not go through DamageRules and still need their own guard (sibling gap). Separately: explode() applies knockback and fire even when the consumer cancelled the damage event.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/weapon/DamageRulesTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/DamageRules.java:40-46 (private isProtected, the shared victim filter). ExplosionHandler.isEligible:155-164 and the hitscan filter at WeaponRaytracerImpl.java:343-344 both call it. Symptom site: ExplosionHandler.explode:104-134.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] The docket already flags the raytracer's own hitscan entity filter (WeaponRaytracerImpl.java:291-296) for never calling CombatEligibility.canBeHit, but that's a distinct, narrower gap than this one: ExplosionHandler.explode does not go through the raytracer's entity filter at all — it does its own direct world.getNearbyEntities bounding-box scan and filters candidates only through isEligible(data, shooter, target, centre, this::hasLineOfSight), which calls DamageRules.isProtected (Owner_Immunity/Ignore_Teams only) and, for LINE_OF_SIGHT exposure, a raycast — nothing in ExplosionHandler, DamageRules, or ExplosionMath ever references CombatEligibility (confirmed by a repo-wide grep of the raytrace package: zero hits). CombatEligibility's stated purpose (per BZ-RT-03's own observation) is to replace Gangland's DownedPlayerRegistry and make a downed/protected player un-hittable — a contract every gun raytrace, reload and throwable-press path already tries to honour for the shooter/reloader side, but no path honours it for a victim standing in a blast radius. Trigger: a player is downed/marked ineligible for combat (a consumer plugin registers a CombatEligibility that returns false for them) standing within a rocket's or grenade's Explosion.Radius -> ExplosionHandler.explode still damages them, sets them on fire (Fire_Ticks), and applies blast knockback, with no eligibility check anywhere in the call path.~~High
21~~P2~~BeamAction permanently strips invulnerability from every entity it hits~~Widen `WeaponRaytracerImpl.damageIgnoringInvulnerability` from package-private to `public static` (a plugin-only class), and in BeamAction replace `living.setInvulnerable(false); living.damage(dmg, player);` with `WeaponRaytracerImpl.damageIgnoringInvulnerability(living, dmg, player);`. Keep setNoDamageTicks(0). Remove the BeamAction row from docket-fix-wave-0.5.1.md §4.~~Capture the RaytraceRequest that BeamAction.fire passes to a mocked WeaponRaytracer and invoke its impactHandler with a WeaponRaytraceImpactEvent whose hit entity is a LivingEntity mock with isInvulnerable() == true. Verify damage(dmg, player) was called and that setInvulnerable(true) was called last (InOrder). The existing damageIgnoringInvulnerability unit test already covers the helper itself.~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/BeamAction.java:201-203~~Deferred from the 0.5.1 fix wave (descends from BZ-RT-04): BeamAction's custom impactHandler calls `living.setNoDamageTicks(0); living.setInvulnerable(false); living.damage(dmg, player);` and never restores the flag. The first beam hit on a deliberately invulnerable entity (an admin in god mode, a spawn-protected mob, an NPC inside a plugin-driven invulnerability window) clears its invulnerability for good. This is the same defect BZ-RT-04 fixed in WeaponRaytracerImpl, which now uses damageIgnoringInvulnerability to save and restore the flag in a finally block. docket-fix-wave-0.5.1.md §4 lists it as 'flagged, not fixed'.~~high
22~~P2~~Non-player NPC shooters still break protected blocks without any check~~In BlockDamageManager, add an overload or parameter that takes @Nullable LivingEntity. When the shooter is a Player, fire the WeaponBlockBreakEvent as now. Otherwise fire `new EntityChangeBlockEvent(entity, block, Material.AIR.createBlockData())` and abort the break if it is cancelled. Change applyBlockBreak to pass request.getShooter() through. This is additive to the bartizan-api surface. The user decides, since it reverses a documented ruling.~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:847~~Final review of the 0.5.1 fix wave (2026-09-25), deferred as a product decision: The implementer's report rules on this explicitly: 'player == null ... the break proceeds unchecked', because a fake Player could crash third-party listeners. It is still a remaining hole. NpcWeaponControllerImpl fires through WeaponShooting.fire with a non-Player LivingEntity as shooter (a consumer's mob NPC). applyBlockBreak turns that into player = null, and fireBlockBreakEvent returns true without firing anything. Example: a consumer's gang/police mob NPC holding the shipped minigun (STONE-9-DESTROY) shoots at a player in a WorldGuard-protected spawn. The stone is permanently destroyed and no protection plugin ever sees it. The ruling's objection (no real Player to put in a BlockBreakEvent) does not apply to Bukkit's EntityChangeBlockEvent. That event takes a real Entity and already exists on 1.16.5 as (Entity, Block, BlockData). WorldGuard and GriefPrevention already use it to veto mob grief.~~high
23~~P2~~Explosions fire one player BlockBreakEvent per block (up to 512); blasts with no shooter stay unchecked~~In applyBlockDamage, first build the list of eligible blocks. Then fire one `new BlockExplodeEvent(centre.getBlock(), candidates, 0f)` (1.16.5 API, no entity needed, so it also covers blasts with no shooter). Only if it is not cancelled, call applyDamage(block, mod) with the 2-arg form, so no second per-block event fires, for each block still in event.blockList().  NOTE: BlockExplodeEvent's constructor differs between 1.16.5 and 1.21 (ExplosionResult), so the fire must be reflective or version-split.~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/ExplosionHandler.java:251~~Final review of the 0.5.1 fix wave (2026-09-25), deferred as a product decision: applyBlockDamage calls applyDamage(block, RESTORE hits=1, player) once per block, up to MAX_BLOCKS_PER_EXPLOSION = 512. Each call is its own WeaponBlockBreakEvent with the shooter as player. Scenario: Block_Damage: true on a rocket or grenade, fired into a GriefPrevention claim the shooter is not trusted in. From my reading of GriefPrevention's own source (a third-party plugin, not checked in this repo), its BlockEventHandler.onBlockBreak sends the no-build message and cancels on every event. The shooter would then get up to 512 chat lines in one tick. WorldGuard rate-limits its message to one per 500 ms. Vanilla explosions do not work this way: they go through one EntityExplodeEvent/BlockExplodeEvent, and protection plugins quietly drop protected blocks from its blockList. The per-block design also cannot cover a blast with no Player (shooter null or a non-Player NPC), so those stay unchecked.~~high
