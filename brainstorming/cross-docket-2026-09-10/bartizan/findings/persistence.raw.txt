1~~P1~~deleteAll() bypasses backend Diagnostics routing, wraps op in an unused legacy Database~~Route deleteAll() through AbstractRepository's own backend-async path (mirroring save/saveAll/delete's runBackendAsync + reportBackendFailure) instead of a fresh DatabaseHelper wrapping an unused legacy Database connection.~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/database/WeaponRepository.java:38-41~~deleteAll() builds `DatabaseHelper helper = new DatabaseHelper(getPlugin(), getDatabaseHandler());` then calls `helper.runQueriesAsync(database -> tableBackend().delete(null));` - the lambda ignores the injected legacy `database` parameter entirely and calls `tableBackend()` (the DatabaseBackend/HikariCP path) instead, so DatabaseHelper.runQueries' `synchronized(database)` plus its `database.connect()`/`disconnect()` bracketing protects nothing real. Per DatabaseHelper.runQueries, `if (database == null) return;` - if the legacy Database handle is ever null the whole delete silently no-ops with zero logging. Any SQLException tableBackend().delete(null) throws is caught only by DatabaseHelper's generic `log.warn(UnhandledError.SQL_ERROR, ...)`, never reaching Diagnostics.active() - unlike every other backend-path mutation in AbstractRepository (save/saveAll/delete), which reports SQLExceptions through reportBackendFailure.~~High
2~~P2~~Malformed uuid row crashes doLoadAll() with an uncaught IllegalArgumentException~~Wrap UUID.fromString in a try/catch per row, log+skip the bad row (route through Diagnostics) instead of letting it propagate past loadAll()'s SQLException-only catch.~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/database/WeaponRepository.java:44-62~~doLoadAll() runs `UUID uuid = UUID.fromString(String.valueOf(result[0]));` for every row tableBackend().selectAll() returns. UUID.fromString throws unchecked IllegalArgumentException on any malformed string, and AbstractRepository.loadAll() only catches SQLException around doLoadAll() (`catch (SQLException exception) { reportBackendFailure(...); return Collections.emptyList(); }`), so a single corrupted uuid cell (partial write, manual DB edit, migration artifact) propagates uncaught out of WeaponManager.initialize(), aborting the whole repository load instead of just skipping that row.~~High
3~~P2~~doLoadAll() silently discards rows whose weapon type no longer exists in the catalogue, no log~~Log a warning (uuid + type) when weaponAddon.getWeapon(type) returns null or when weaponAddon itself is unset, so admins can see persisted weapon identities were dropped on load instead of a silent skip.~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/database/WeaponRepository.java:48-59~~doLoadAll()'s loop does `if (weaponAddon == null) continue;` and, further down, `if (template == null) continue;` for every row, with zero logging in either branch. If a weapon YAML file is renamed or deleted from the catalogue, every persisted row of that type is permanently and silently skipped on every future load with no admin-visible signal that data was dropped; if weaponAddon itself has not been set yet when loadAll() runs, doLoadAll() silently returns an empty collection for the entire table instead of surfacing that the addon wiring is missing.~~High
4~~P2~~Cleanup wipes the whole weapon table and in-memory cache on a blind timer, not scoped to orphans~~Scope the wipe to entries that are actually stale/orphaned (e.g. not backing any online player's currently-held item), or at minimum evict a weapon's map entry on drop/quit/death so the periodic full wipe is not the only bound on unbounded map growth.~~WeaponDataCleanupTaskTest (mocks WeaponRepository, so it only pins call order, not the real deleteAll()/table consequence)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/data/WeaponDataCleanupTask.java:49-60~~cleanup() unconditionally calls `repo.deleteAll()` (DELETE FROM weapon, no WHERE clause) then `weaponManager.clear()`, wiping every persisted (uuid,type) row and every in-memory Weapon instance for the whole server on a fixed schedule (Clean_Up.Time, default 30 days per settings.yml) - including weapons players are actively holding, since nothing ever removes a single map entry on drop/quit/death (WeaponQuitCleanupListener only unscopes/stops reloading the held weapon, it never touches weaponManager's backing map). Physical items keep working afterwards because WeaponService.getWeapon/validateAndGetWeapon re-mint an instance under the item's own uuid and resync ammo/durability/selective-fire from the item's own NBT on next interaction, but every weapon that is not actively touched between wipes loses its DB identity row until then, and any admin tooling reading weaponManager.getWeapons() (DebugCommand) reports the population as zero immediately after every wipe even though every physical weapon in the world still exists.~~High
5~~P2~~WeaponTableImportTask never imports a MySQL-backed legacy Gangland weapon table~~Detect Database.Type=mysql (or otherwise probe the legacy connection generically) and either connect to the MySQL gangland schema for the one-shot import or log at WARN/INFO that a MySQL migration needs a manual import, instead of a DEBUG-only skip that permanently marks the import done.~~WeaponTableImportTaskTest (covers only the SQLite-file-absent and SQLException-during-read cases, never a MySQL-backed source)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/database/WeaponTableImportTask.java:56-65~~run() only ever looks for `plugins/Gangland_Warfare/database/gangland.db` on disk: `File ganglandDatabase = new File(bartizan.getDataFolder().getParentFile(), "Gangland_Warfare" + File.separator + "database" + File.separator + "gangland.db"); if (!ganglandDatabase.exists()) { log.debug(...); writeMarker(marker); return; }`. A Gangland install running Database.Type: mysql never has that file on disk, so this branch always fires, logs only at DEBUG (invisible by default since Debug.Enable defaults false), and permanently writes the marker file - the one-shot import opportunity for a MySQL-backed fleet migrating to Bartizan is silently and irreversibly skipped with no admin-visible message and no retry path.~~High
