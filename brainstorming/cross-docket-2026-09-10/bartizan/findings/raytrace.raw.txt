# RT — raytrace system, Bartizan repo, HEAD 0e18356. One finding per line: num~~tier~~title~~fix~~tests~~location~~observation~~confidence
1~~P0~~Cosmetic ROCKET Fireball is never yield-zeroed, so vanilla explosion physics fire alongside the custom blast~~Call visual.setYield(0f) and setIsIncendiary(false) (or cancel EntityExplodeEvent for cosmetic ids) right after spawnCosmetic returns a Fireball~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/raytrace/WeaponVisualSpawner.java:49-64~~WeaponShooting.fireSlow spawns `Class<? extends Projectile> visualClass = type == ProjectileType.ROCKET ? Fireball.class : Firework.class;` (WeaponShooting.java:86) and WeaponVisualSpawner.spawnCosmetic only does `projectile.setSilent(true); projectile.setGravity(false);` before returning it — no `setYield`/`setIsIncendiary` call anywhere in the repo (grepped bartizan-plugin+bartizan-api, zero hits) and no listener anywhere handles `EntityExplodeEvent`/`ExplosionPrimeEvent`. A Bukkit `Fireball` (Ghast-style large fireball) explodes on block/entity contact via vanilla physics with default yield ~1.0, independently of SteppedProjectileTask's own `fireExplosion()`. Every rocket shot therefore also triggers an uncontrolled, unconfigurable vanilla explosion (real block destruction, no falloff, ignores Explosion_Radius/Explosion_Damage) on top of the plugin's own AOE — contradicting WeaponVisualSpawner's own javadoc that cosmetic entities "never drive damage logic."~~High
2~~P0~~WeaponRaytracerImpl never consults CombatEligibility before damaging a raytraced entity target~~Default RaytraceRequest.entityFilter to also gate on CombatEligibility.resolve().canBeHit(entity) (for Player targets) inside WeaponShooting/RaytraceRequest, or add the check directly in advanceRay's entityHit predicate~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:291-296~~advanceRay's entity filter is `e -> e != shooter && !(e instanceof ItemFrame || e instanceof ArmorStand) && request.getEntityFilter().test(e)`, and RaytraceRequest.Builder defaults `entityFilter = e -> true` (RaytraceRequest.java:69); WeaponShooting never overrides it. Every `canBeHit` call site in the repo (WiringConfig, WeaponInteract, InstantReload, NumberedReload) gates the shooter's own fire/reload action, none gate the target being shot. CombatEligibility's own javadoc says it "replaces the Gangland-only DownedPlayerRegistry static gate" and returns false when a player is "downed, dead, otherwise protected" — but a downed/protected player can still be hit and killed by someone else's gunfire through this raytracer.~~High
3~~P0~~applyBlockBreak mutates/destroys the block before the WeaponRaytraceImpactEvent cancellation check runs~~Move applyBlockBreak's call inside handleBlockImpact, after `if (event.isCancelled()) return;`~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:346-347~~In advanceRay: `applyBlockBreak(block, request.getWeapon()); handleBlockImpact(impactPt, block, face, ctx);` — applyBlockBreak (which can crack, RESTORE-break, or permanently DESTROY the block via BlockDamageManager) runs unconditionally before handleBlockImpact fires the WeaponRaytraceImpactEvent and checks `event.isCancelled()` (WeaponRaytracerImpl.java:490-493). Unlike the entity path (handleEntityImpact fires the event and checks cancellation before calling `.damage()`), a plugin/region-protection listener cancelling this event to stop weapon damage in a claim cannot prevent the block mutation — it already happened.~~High
4~~P2~~setInvulnerable(false) permanently clears a hit LivingEntity's invulnerable flag with no restore~~Save the target's prior invulnerable state before the call and restore it after damage is applied (or only clear it when it was actually the cause of no-damage)~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:440-443~~In handleEntityImpact's default damage branch: `living.setNoDamageTicks(0); living.setInvulnerable(false);` runs unconditionally on every raytrace hit and nothing in the method (lines 440-478) ever restores a previously-true invulnerable flag afterward. Any admin god-mode player, spawn-protected entity, or plugin-driven invulnerability window hit by a weapon shot has that flag permanently flipped off. was Gangland WP-22.~~High
5~~P3~~Hitscan raytrace origin is the shooter's eye while the rendered tracer line starts at the muzzle offset~~Either raytrace from WeaponMuzzle.compute(...) consistently, or start the tracer's first segment from the eye location to match the real ray path~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:572-574~~WeaponShooting.fireHitscan builds the RaytraceRequest with `Location origin = shooter.getEyeLocation();` (WeaponShooting.java:57) — the actual ray starts at the eye. But drawTracerLine seeds `Location previous = WeaponMuzzle.compute(ctx.getRequest().getShooter(), ctx.getRequest().getDirection());` before walking the tracer segments, so the rendered dust line's first leg runs from the muzzle offset (right of body, below eye) to the first real-ray point, producing a visible kink that doesn't match where the shot actually traveled. Same origin/muzzle split exists in fireSlow (WeaponShooting.java:82 vs :96). was Gangland WP-26.~~High
6~~P3~~WeaponKillEntityEvent is declared but never fired anywhere in the codebase~~Fire WeaponKillEntityEvent from the raytrace kill path (WeaponRaytracerImpl.handleEntityImpact, after living.damage() when living.isDead() becomes true) or delete the dead event class~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:446-451~~handleEntityImpact applies damage via `living.damage(event.getDamage(), shooter)` and never checks `living.isDead()` afterward to fire a kill event. A repo-wide grep for `WeaponKillEntityEvent` only turns up its own class file and constructor — no `Bukkit.getPluginManager().callEvent(new WeaponKillEntityEvent(...))` exists anywhere in bartizan-plugin or bartizan-api. WeaponEntityDamageEvent, by contrast, is now fired from ThrowableAction, so this event class is the sole remaining dead half of the old WP-18 pair. was Gangland WP-18.~~Medium
7~~P3~~BlockGroupResolver.resolve falls back to raw Material.valueOf instead of XMaterial~~Resolve unknown/single-material names through XMaterial.matchXMaterial(...).map(XMaterial::parseMaterial) instead of Material.valueOf~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/util/BlockGroupResolver.java:82-87~~`try { Material material = Material.valueOf(upperName); return Set.of(material); } catch (IllegalArgumentException e) { return Collections.emptySet(); }` uses the raw enum lookup the repo's own CLAUDE.md bans ("Material/Particle/Sound/PotionEffectType resolve through XSeries... never raw valueOf"), so a legacy/renamed material name in a weapon's Break_Blocks config silently resolves to an empty set instead of the intended block on a different Minecraft version.~~High
8~~P2~~SteppedProjectileTask's explosion sound uses the raw Sound enum instead of Keystone's SoundEffect~~Play the explosion sound via org.luckyraven.keystone.sound.SoundEffect (as WeaponRaytracerImpl already does for impact/crit sounds) instead of world.playSound(..., Sound.X, ...)~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/SteppedProjectileTask.java:172~~`world.playSound(loc, Sound.ENTITY_GENERIC_EXPLODE, 2.0f, 1.0f);` is a direct `player.playSound(..., Sound.X, ...)` call — exactly the pattern Bartizan's CLAUDE.md names as forbidden ("never raw valueOf and never player.playSound(..., Sound.X, ...)"). The rest of this same method already uses XParticle.EXPLOSION/SMOKE/FLAME.get() for particles, so the sound line is the one inconsistent, version-drift-fragile call in the explosion effect.~~High
9~~P3~~BlockDamageManager's break-effect particle uses the raw Particle enum instead of XParticle~~Use XParticle (e.g. XParticle.BLOCK_CRACK/BLOCK.get()) to match the XParticle usage everywhere else in the raytrace/block-damage code~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManager.java:255~~`world.spawnParticle(Particle.BLOCK, location.clone().add(0.5, 0.5, 0.5), 25, 0.3, 0.3, 0.3, 0.05, blockData);` references the raw org.bukkit.Particle enum constant directly, unlike WeaponRaytracerImpl's tracer rendering and SteppedProjectileTask's explosion particles, which both go through `XParticle.X.get()` per house convention.~~Medium
10~~P1~~Delayed block-damage/regeneration tasks NPE via Objects.requireNonNull(location.getWorld()) if the world unloads first~~Null-check location.getWorld() in sendBlockDamage/clearBlockDamage and bail out (treating the block as no longer trackable) instead of throwing~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/BlockDamageManager.java:128-147~~`World world = Objects.requireNonNull(location.getWorld());` appears in both sendBlockDamage (line 131) and clearBlockDamage (line 142), which are invoked from scheduleRegeneration/startSmoothRegeneration/breakAndScheduleRestore — all Bukkit delayed/repeating tasks that can fire after the block's world has been unloaded (Bukkit's Location#getWorld() is documented nullable once its world unloads, per this repo's own house rule). A world unload while a weapon-cracked block's regen/restore timer is pending throws an uncaught NPE inside a runTaskTimer/runTaskLater callback, aborting that block's cleanup — repeating regen tasks then either stop updating (stale damagedBlocks entry, permanent crack overlay) or spam the console every tick depending on Bukkit's per-tick task exception handling.~~Medium
11~~P3~~PenetrationModifier.calculateDamage is dead code — the real damage math lives in ProjectileState instead~~Delete calculateDamage from PenetrationModifier, or wire it in and delete ProjectileState.applyPenetrationReduction if it was meant to replace it~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/modifiers/action/PenetrationModifier.java:12-14~~`public double calculateDamage(double baseDamage, int penetrationCount) { return baseDamage * Math.pow(1.0 - damageReduction, penetrationCount); }` has zero call sites anywhere in bartizan-api or bartizan-plugin main source (grepped `.calculateDamage(` repo-wide, no hits) — the actual per-hit reduction is applied via ProjectileState.applyPenetrationReduction's cumulative multiplier instead, leaving this method as unused, unreferenced modifier logic.~~Medium
12~~P2~~A rocket/flare's SteppedProjectileTask + cosmetic visual entity leak on plugin disable~~Track active SteppedProjectileTask instances (or cosmetic entity ids) in a registry and terminate()/remove() them from a BeanLifecycle shutdown hook~~SteppedProjectileTaskTest~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/SteppedProjectileTask.java:68-115~~`new SteppedProjectileTask(...).start()` (WeaponShooting.java:116-117) is fire-and-forget — no registry keeps a reference. start() schedules a Keystone RepeatingTimer via runTaskTimer(plugin, ...); Bukkit auto-cancels that BukkitTask on plugin disable, but that only stops the tick callback — it never invokes SteppedProjectileTask.terminate(), so the cosmetic Fireball/Firework entity is never removed and stays registered in WeaponVisualSpawner.cosmeticEntityIds. If any weapon was mid-flight during a `/reload` or module disable, its visual outlives the plugin with no owner to track or despawn it.~~Medium
