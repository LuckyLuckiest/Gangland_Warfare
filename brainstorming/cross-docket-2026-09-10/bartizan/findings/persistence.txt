# BZ-DB persistence — verified findings (Bartizan @ 0e18356)
1~~P2~~WeaponRepository.deleteAll wraps a backend call in an unused legacy Database and bypasses Diagnostics routing~~Route deleteAll through AbstractRepository's own backend-async path so failures reach reportBackendFailure like save, saveAll and delete~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/database/WeaponRepository.java:38-41~~The whole method is `DatabaseHelper helper = new DatabaseHelper(getPlugin(), getDatabaseHandler()); helper.runQueriesAsync(database -> tableBackend().delete(null));` — the lambda ignores the injected legacy `database` parameter entirely and calls tableBackend(), the HikariCP DatabaseBackend path, so DatabaseHelper's synchronized block and connect/disconnect bracketing guard nothing that is actually used. DatabaseHelper.runQueries opens with `if (database == null) return;`, so a null legacy handle makes the entire table delete a silent no-op with no logging. Any SQLException from the backend delete is swallowed by DatabaseHelper's generic warn and never reaches Diagnostics.active(), unlike every other mutation path in AbstractRepository.~~High
2~~P2~~doLoadAll calls UUID.fromString per row, so one malformed uuid aborts the entire repository load~~Wrap the per-row parse in try/catch, log the bad row through Diagnostics and skip it~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/database/WeaponRepository.java:44-62~~The load loop runs `UUID uuid = UUID.fromString(String.valueOf(result[0]));` for every row returned by tableBackend().selectAll(). UUID.fromString throws an unchecked IllegalArgumentException, while doLoadAll is declared `throws SQLException` and AbstractRepository.loadAll catches only SQLException around it. A single corrupted uuid cell — from a partial write, a manual edit or a migration artifact — therefore propagates uncaught out of WeaponManager.initialize() and aborts the whole weapon load rather than costing one row.~~High
3~~P2~~doLoadAll silently discards every row whose weapon type left the catalogue, and returns empty if weaponAddon is unset~~Log a warning naming the uuid and type when the template is missing, and treat an unset weaponAddon as an error rather than an empty result~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/database/WeaponRepository.java:48-59~~The loop contains `if (weaponAddon == null) continue;` and `if (template == null) continue;` with no logging in either branch. Renaming or deleting a weapon YAML makes every persisted row of that type silently vanish on every subsequent load, with no signal that stored weapon identities were dropped. Worse, if weaponAddon has not been wired at the moment loadAll runs, the first guard fires for every row and the method returns an empty collection for the entire table, which is indistinguishable from a genuinely empty database.~~High
4~~P2~~WeaponDataCleanupTask wipes the entire weapon table on a blind timer instead of scoping to orphaned rows~~Scope the delete to rows no longer backing any live weapon item, rather than an unconditional full-table DELETE~~WeaponDataCleanupTaskTest (mocks the repository, so it pins call order only, not the real table consequence)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/data/WeaponDataCleanupTask.java:49-60~~cleanup() unconditionally runs `repo.deleteAll()` followed by `weaponManager.clear()`, deleting every persisted uuid and type row and every cached Weapon for the whole server on the Clean_Up.Time schedule, which settings.yml ships at 30 days. There is no WHERE clause and no staleness test, so weapons players are actively carrying are wiped alongside genuinely dead rows. The blast radius is bounded rather than catastrophic because WeaponService re-mints an instance under the item's own uuid and resyncs ammo, durability and fire mode from the item's NBT on next interaction, but every untouched weapon loses its database identity until someone handles it, and DebugCommand reports a population of zero straight after each wipe. The related in-memory leak this wipe is currently the only bound on is filed separately as BZ-WM-04.~~High
5~~P2~~WeaponTableImportTask only looks for a legacy SQLite file, so a MySQL Gangland install is skipped and marked done~~Detect Database.Type mysql and either import from that schema or log at warn that a manual migration is required, before writing the marker~~WeaponTableImportTaskTest (covers the file-absent and SQLException cases, never a MySQL source)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/database/WeaponTableImportTask.java:56-65~~The one-shot import resolves `File ganglandDatabase = new File(bartizan.getDataFolder().getParentFile(), "Gangland_Warfare" + File.separator + "database" + File.separator + "gangland.db");` and, when absent, logs at debug that this is "a fresh install or a MySQL deployment, not an error" and calls writeMarker(marker) before returning. A Gangland server running Database.Type mysql never has that file, so the branch always fires, the message is invisible because Debug.Enable defaults to false, and the marker permanently records the import as complete. The migration opportunity is silently and irreversibly lost with no admin-facing message and no retry path.~~High
