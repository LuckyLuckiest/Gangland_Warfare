# EV system findings - Bartizan events (listeners + api/event/*), scanned at HEAD 0e18356
1~~P2~~Non-sneaking left click on scope-less weapons is swallowed by the scope-toggle branch~~Guard the scope branch with `scopeData != null` (drop the `validateScope` true-default for null scopeData) before consuming the click~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:154-176~~`validateScope` defaults to `true` when `weapon.getScopeData()` is null (`boolean validateScope = true; if (scopeData != null) { validateScope = scopeData.getLevel() > 0; }`), so the branch `if (leftClick && !player.isSneaking() && validateScope && !weapon.isReloading())` is entered for ANY weapon lacking scope data, including melee weapons. Inside it calls `weapon.unScope(player, true)` (since `scopeData` is null, the ternary falls to the else) and returns before the code ever reaches `handleNonGunInteract`'s `MeleeWeapon`/`leftClick` branch a few lines below. Consequence: a scope-less melee weapon swung at air/a block with a plain (non-sneaking) left click never calls `MeleeAction`; only sneak-left-clicking reaches the melee swing path (entity hits still work via the separate `EntityDamageByEntityEvent` handler). was Gangland WP-5~~High
2~~P1~~WeaponInteract's per-weapon fire/press state maps are never cleared on player quit~~Add a PlayerQuitEvent handler (or extend WeaponQuitCleanupListener) that stops and removes every WeaponInteract-owned map entry for the weapon(s) the quitting player was operating, not just weapon.stopReloading()/unScope()~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:80-120~~WeaponInteract owns eight per-weapon-UUID maps (`continuousFire`, `pressLockUntilTick`, `pressHoldState`, `releaseCallbacks`, `autoTasks`, `activeTasks`, `meleeCooldowns`, `lastMeleeSwingMs`), all cleared only from `onWeaponHeld` (weapon swap). `WeaponQuitCleanupListener.onPlayerQuit` only calls `weapon.stopReloading()`/`weapon.unScope(player, true)` on the main-hand weapon (`ItemStack item = player.getInventory().getItemInMainHand();`) and touches none of these maps. If a player disconnects mid-AUTO-fire or mid-throwable-charge, the `FullAutoTask`/`RepeatingTimer` entries in `autoTasks`/`activeTasks`/`continuousFire` keep running and calling Bukkit Player APIs on an offline Player object until their own watchdog eventually times out or errors. was Gangland WP-9 (scoped here to the quit path; death/shutdown paths belong to other listener files)~~High
3~~P2~~Unguarded UUID.fromString on the weapon NBT tag can throw out of every listener, including the interact handler~~Wrap `UUID.fromString(value)` in a try/catch that logs via `@CustomLog` and returns null on `IllegalArgumentException`~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/WeaponService.java:39-55~~`getWeaponUUID(ItemStack item)` reads the raw NBT string via `tempItem.getStringTagData(tagProperName)` and does `if (!(value == null || value.equals("null") || value.isEmpty())) { uuid = UUID.fromString(value); }` with no try/catch. This is the first thing `WeaponInteract.onPlayerInteract` does via `weaponService.validateAndGetWeapon(player, item)` at WeaponInteract.java:143, and every other listener in this system (`WeaponDroppedListener`, `WeaponItemSpawnListener`, `WeaponQuitCleanupListener`, `WeaponSelectiveFireChangeListener`, `WeaponReloadListener`) calls the same path. A corrupted or hand-edited UUID tag string throws `IllegalArgumentException` out of the `@EventHandler` method for every one of these events. was Gangland WP-24~~High
4~~P3~~shootInterval schedules a new Consume_On_Time CountdownTimer on every shot instead of resetting one~~Track a single CountdownTimer per weapon and restart it, or cancel the previous one before scheduling a new one~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:678-698~~`shootInterval` unconditionally does `CountdownTimer timer = new CountdownTimer(plugin, 0L, 0L, consumeOnTime, null, null, time -> weapon.removeWeapon(player, player.getInventory().getHeldItemSlot())); timer.start(false);` on every call when `Consume_On_Time` (`consumeOnTime`) is configured (`> -1`). Firing multiple shots before the first timer elapses (BURST rounds, or rapid SINGLE presses) stacks independent timers that each independently call `weapon.removeWeapon(...)` on whatever item now occupies that hotbar slot, which can remove an unrelated item once the slot has been reused. Dormant because `Consume_On_Time` is rarely configured. was Gangland WP-29~~High
5~~P1~~Throwables share one deterministic UUID per type, so WeaponInteract's press/scope state maps are shared across all players holding that throwable~~Key the WeaponInteract per-weapon maps by (player UUID, weapon UUID) for throwables, or mint a per-item UUID and rely on a separate stack-merge key for inventory stacking~~WeaponServiceTest.createTransientWeapon_throwable_usesDeterministicUuid (pins the shared-uuid root cause as intentional for stacking; does not cover the WeaponInteract cross-player consequence)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/WeaponService.java:278-292~~`mintUuid` explicitly documents: "Throwables share one deterministic uuid per type so identical items stack in the inventory" and returns `UUID.nameUUIDFromBytes(("throwable:" + type).getBytes(...))` for every throwable of a given type, regardless of player. Since `WeaponInteract.handleThrowablePress` (line 437) gates via `isPressGated(weaponUuid)` (line 523) and `engagePressHoldWatchdog(weaponUuid, ...)` (line 544), all keyed by this shared UUID, one player throwing a grenade locks out every other online player's throw of the same grenade type until the press-hold watchdog clears. was Gangland WP-32~~High
6~~P3~~GunWeapon Per_Shot count inflates the SINGLE-mode press-lock window meant for one shot~~Compute lockTicks from numberOfShots (1 for SINGLE, perShot for BURST) instead of unconditionally using getPerShot()~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:584-602~~`shootOtherModes` handles both SINGLE and BURST (the `else` branch of the AUTO check in `onPlayerInteract`) and computes `long lockTicks = Math.max((long) projectileData.getPerShot() * projectileData.getCooldown(), MIN_PRESS_LOCK_TICKS);` unconditionally, then calls `shoot(player, weapon)` which correctly only fires `perShot` rounds when `weapon.getCurrentSelectiveFire() == SelectiveFire.BURST` (defaulting to 1 shot otherwise). A weapon configured with Per_Shot > 1 for its BURST mode gets an unnecessarily long press-lock window in SINGLE mode even though only one round is fired. was Gangland WP-35~~High
7~~P1~~WeaponChangeSelectiveFireEvent.getHandlerList() is private, breaking Bukkit event registration for any listener~~Make getHandlerList() public static, matching every other event class in this package~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/event/WeaponChangeSelectiveFireEvent.java:19-21~~`private static HandlerList getHandlerList() { return handler; }` — every sibling event class (`WeaponReloadEvent`, `WeaponEntityDamageEvent`, `WeaponRaytraceImpactEvent`, `WeaponReloadStartEvent`/`CompleteEvent`, `WeaponShootEvent`, `WeaponKillEntityEvent`) declares this method `public static`. Bukkit's `SimplePluginManager` resolves each event's HandlerList reflectively via `getMethod("getHandlerList")`, which only finds public methods; a private declaration makes that lookup fail with `IllegalPluginAccessException`/"Unable to find handler list" the instant any plugin (in-repo or a consumer) registers an `@EventHandler` for `WeaponChangeSelectiveFireEvent`. Currently dormant because no listener in the repo subscribes to this event, but it is public API in `bartizan-api` meant for cross-plugin consumption.~~High
8~~P3~~WeaponKillEntityEvent is dead code - never constructed anywhere in the codebase~~Either fire it from the melee/gun kill paths (mirroring WeaponEntityDamageEvent) or delete it as unshipped API~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/event/WeaponKillEntityEvent.java:11-25~~`grep -rn "new WeaponKillEntityEvent" bartizan-plugin bartizan-api` returns only the class's own declaration — no action class (`GunAction`, `MeleeAction`, `ThrowableAction`, `IncendiaryAction`, `BiologicalAction`) constructs it. This is a public, Cancellable event with correct `public static getHandlerList()`/`getHandlers()` wiring, but it ships as dead API. Note: WeaponEntityDamageEvent, the other half of WP-18, IS fired (from ThrowableAction.java:208,242), so that half of WP-18 is refuted. was Gangland WP-18 (partial: only the kill-event half confirmed)~~High
9~~P2~~WeaponQuitCleanupListener only inspects the main-hand item, leaving an off-hand weapon's reload/scope state uncleaned on quit~~Check both EquipmentSlot.HAND and EquipmentSlot.OFF_HAND (mirroring WeaponService.getHeldWeaponItem's own main-then-offhand fallback) before returning~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/player/WeaponQuitCleanupListener.java:27-40~~`onPlayerQuit` does `ItemStack item = player.getInventory().getItemInMainHand(); Weapon weapon = weaponManager.validateAndGetWeapon(player, item); if (weapon == null) return;` — it never looks at `getItemInOffHand()`. `WeaponService.getHeldWeaponItem` (WeaponService.java:107-121) proves off-hand weapons are a real supported case: it falls back to `EquipmentSlot.OFF_HAND` when the main hand isn't a weapon, and `WeaponSelectiveFireChangeListener` relies on that fallback. A player who quits while reloading or scoped with a weapon in their off-hand keeps `reloading`/`scoped` state stuck on that weapon instance across the disconnect.~~Medium
10~~P1~~Held-trigger watchdog timers mutate shared WeaponData.shooting / RecoilManager state from an async thread while the main-thread interact handler writes the same fields~~Run these watchdogs with start(false) like handleBiologicalCharge already does, or make WeaponData.shooting volatile/AtomicBoolean and audit RecoilManager.playerPatternIndex the same way~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:544-570~~`engagePressHoldWatchdog` (SINGLE/BURST/throwable/incendiary press-lock) and `shootFullAuto`'s AUTO watchdog (line 625) both schedule their `RepeatingTimer` with `.start(true)` (async) with the comment "Pure flag-flipping + map mutation, so safe to run async", mutating `WeaponData.shooting` (a plain, non-volatile `boolean` field, WeaponInteract.java:716) via `stillHeld.get().shooting = false;`. The SAME field is written from the main thread inside `isPressGated`/`onPlayerInteract` (`held.get().shooting = true;`) and `shootFullAuto`'s else-branch. `shootFullAuto`'s async watchdog also calls `weapon.getRecoil().resetRecoilPattern()` (mutates `RecoilManager.playerPatternIndex`, a plain int) concurrently with `applyRecoil` on the main thread during firing. `handleBiologicalCharge`'s equivalent watchdog explicitly uses `.start(false)` with a comment noting main-thread-only APIs, showing the async choice elsewhere is an inconsistent, unguarded assumption rather than a deliberate safe design.~~Medium
