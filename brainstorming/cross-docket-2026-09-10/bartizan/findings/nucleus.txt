# BZ-NU nucleus — verified findings (Bartizan @ 0e18356)
1~~P1~~Bartizan.onDisable calls the test-only PacketBridge.reset, wiping the server-global packet adapter every other Keystone plugin shares~~Remove the PacketBridge.reset() call from onDisable, or replace it with a scoped uninstall that only reverts an adapter Bartizan itself installed~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/Bartizan.java:47-51~~onDisable runs `getServer().getServicesManager().unregisterAll(this); PacketBridge.reset();` before tearing down the context. Keystone's PacketBridge holds `private static volatile PacketAdapter adapter = NoOpAdapter.INSTANCE;` — one server-global instance shared by every consumer plugin on the shared Keystone classloader — and its reset() carries the javadoc "Reset to the no-op fallback. Test-only." Disabling or reloading Bartizan therefore silently downgrades recoil and packet handling to a no-op for every other Keystone-powered plugin still running, with no log line. The same path also runs when bootstrap fails, since onEnable's catch calls disablePlugin(this).~~High
2~~P3~~Bartizan.dependencyHandler logs "Found X, linking" and "Linked X" while always passing a null Runnable~~Either pass the real linking Runnable for each soft dependency or drop the misleading linking log lines~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/Bartizan.java:116-146~~dependencyHandler calls `viaVersion.validate(null)`, `placeholderApi.validate(null)` and `nbtApi.validate(null)`, and Dependency.validate is `if (Bukkit.getPluginManager().getPlugin(name) == null) return; log.info("Found {}, linking...", name); if (runnable != null) runnable.run(); log.info("Linked {}", name);`. Since the Runnable is always null nothing is ever linked, yet the console reports a successful link for each plugin found. NBTAPI is additionally a hard `depend:` entry in plugin.yml, so Bukkit guarantees it is present before onEnable and routing it through a soft-dependency helper makes that branch permanently dead.~~High
3~~P3~~BartizanContext declares both a Lombok @Getter and a hand-written getContainer(), leaving one dead~~Delete the hand-written getContainer() and keep the @Getter, or drop the annotation~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/bootstrap/BartizanContext.java:41-43~~The field is declared `@Getter private final DependencyContainer container;` while the class also defines `public DependencyContainer getContainer() { return container; }` at lines 179-181. Lombok skips generating an accessor when one already exists, so this compiles cleanly and behaves correctly, but one of the two declarations is dead and a reader cannot tell which accessor is authoritative or whether the annotation is load-bearing.~~High
