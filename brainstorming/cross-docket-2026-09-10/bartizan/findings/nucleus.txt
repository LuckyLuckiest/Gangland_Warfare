# BZ-NU nucleus — verified findings (Bartizan @ 0e18356)
1~~P2~~Bartizan.onDisable calls the test-only PacketBridge.reset, wiping the server-global packet adapter every other Keystone plugin shares~~Bump keystone.version in the root pom to 1.11.2 (the first release with owner-scoped reset) and update the Keystone-1.9.0 references in README.md and documentation/migration.md. The Bartizan-side reset() call can stay, because it is now the correct scoped withdrawal. Optionally, have bootstrap log a warning when the running Keystone version is below 1.11.2. Also correct the KernelConfig javadoc so it says reset withdraws only Bartizan's own install.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/BartizanDisableTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/Bartizan.java:49 (PacketBridge.reset() in onDisable); install at bartizan-plugin/src/main/java/org/luckyraven/bartizan/config/KernelConfig.java:74-79; pin at pom.xml:64 (keystone.version 1.9.0); README.md:9,139 and documentation/migration.md:16,20 tell admins to deploy Keystone-1.9.0.jar~~onDisable runs `getServer().getServicesManager().unregisterAll(this); PacketBridge.reset();` before tearing down the context. Keystone's PacketBridge holds `private static volatile PacketAdapter adapter = NoOpAdapter.INSTANCE;` — one server-global instance shared by every consumer plugin on the shared Keystone classloader — and its reset() carries the javadoc "Reset to the no-op fallback. Test-only." Disabling or reloading Bartizan therefore silently downgrades recoil and packet handling to a no-op for every other Keystone-powered plugin still running, with no log line. The same path also runs when bootstrap fails, since onEnable's catch calls disablePlugin(this).~~High
2~~P3~~Bartizan.dependencyHandler logs "Found X, linking" and "Linked X" while always passing a null Runnable~~Delete the Dependency inner class and dependencyHandler(). If an admin-facing line is wanted, replace them with a single log.info like "Soft dependencies detected: ViaVersion=yes, PlaceholderAPI=no" (no 'linking' wording, no NBTAPI entry). Also fix the javadoc so it names NBTAPI as a hard depend.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/BartizanSoftDependencyTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/Bartizan.java:82-112 (dependencyHandler + inner Dependency.validate)~~dependencyHandler calls `viaVersion.validate(null)`, `placeholderApi.validate(null)` and `nbtApi.validate(null)`, and Dependency.validate is `if (Bukkit.getPluginManager().getPlugin(name) == null) return; log.info("Found {}, linking...", name); if (runnable != null) runnable.run(); log.info("Linked {}", name);`. Since the Runnable is always null nothing is ever linked, yet the console reports a successful link for each plugin found. NBTAPI is additionally a hard `depend:` entry in plugin.yml, so Bukkit guarantees it is present before onEnable and routing it through a soft-dependency helper makes that branch permanently dead.~~High
3~~P3~~BartizanContext declares both a Lombok @Getter and a hand-written getContainer(), leaving one dead~~Delete the hand-written getContainer() and keep the @Getter, or drop the annotation~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/bootstrap/BartizanContext.java:41-43~~The field is declared `@Getter private final DependencyContainer container;` while the class also defines `public DependencyContainer getContainer() { return container; }` at lines 179-181. Lombok skips generating an accessor when one already exists, so this compiles cleanly and behaves correctly, but one of the two declarations is dead and a reader cannot tell which accessor is authoritative or whether the annotation is load-bearing.~~High
4~~P1~~NpcWeaponControllerImpl.onDestroy() does not cancel an in-flight burst SequenceTimer, so a killed/despawned NPC keeps firing shots after death~~Make each burst round check that the shooter is still alive before it fires. At the top of the scheduleBurst lambda, add `if (!shooter.isValid()) { timer.stop(); return; }`. Entity.isValid() is false for both a dead entity and a despawned or removed one, so this one guard covers death and every destroy path, and onDestroy needs no field. Optionally, also keep a `volatile boolean destroyed` that onDestroy() sets, and check it in the same guard. That covers destroy() being called on a still-valid entity. Calling stop() from inside the body is safe, because SequenceTimer runs the body outside its monitor. NpcWeaponCadenceTest stubs scheduleBurst, so the change does not affect it.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/npc/NpcWeaponCadenceTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/npc/NpcWeaponControllerImpl.java:157-173 (scheduleBurst lambda), :240-244 (no-op onDestroy)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] performBurstFire() schedules a SequenceTimer that fires `perShot` rounds over several ticks (interval 0 for the first, `cooldown` thereafter), each callback invoking fireRound(gun) which consumes ammo, fires a WeaponShootEvent with `shooter`, drives WeaponRaytracerImpl, and refreshes the held item. onDestroy() is an explicit no-op whose javadoc assumes the timer 'self-expires after its last interval' harmlessly, matching the original NpcCombatDelegate. But Keystone's AbstractNpc.destroy() (keystone-npc/.../AbstractNpc.java:165-186) calls rangedAttack.onDestroy() and then npc.despawn()/npc.destroy() in the SAME call, before any pending burst intervals elapse. Trigger: an NPC holding a BURST-selective-fire gun starts a burst (tryFire -> performBurstFire -> scheduleBurst schedules e.g. 4 more fireRound calls over the next several ticks), then the NPC is killed or otherwise destroy()'d mid-burst. The still-running SequenceTimer keeps calling fireRound(gun) against the now-despawned `shooter`: it keeps consuming ammunition from an orphaned weapon, still calls Bukkit.getPluginManager().callEvent(new WeaponShootEvent(weapon, shooter)) and still fires raytraces/effects sourced from the dead entity's last-known position, so the dead NPC can visibly keep 'shooting' (and landing hits) for the remainder of the burst window after it has already been removed from the world. This is a normal-play trigger (any burst-weapon NPC kill), not a misconfiguration.~~High
