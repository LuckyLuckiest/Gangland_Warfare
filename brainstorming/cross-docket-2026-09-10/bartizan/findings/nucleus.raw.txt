# SYSTEM: NU — nucleus (Bartizan repo, branch master, HEAD 0e18356)
1~~P1~~Bartizan.onDisable() calls the Keystone-documented test-only PacketBridge.reset(), wiping the server-global adapter every consumer shares~~Replace the unconditional reset() with a no-op (or only reset if Bartizan's own adapter instance is still the currently-installed one) so disabling/crashing Bartizan cannot silently degrade another Keystone consumer's already-installed PacketAdapter~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/Bartizan.java:47-51~~Bartizan.onDisable() runs `getServer().getServicesManager().unregisterAll(this); PacketBridge.reset();` unconditionally, and KernelConfig.java:76-81's own comment says "every consumer plugin installs its own adapter exactly once at bootstrap. Bartizan.onDisable() calls PacketBridge.reset()." Keystone's PacketBridge (keystone-common/src/main/java/org/luckyraven/keystone/nms/PacketBridge.java:19-37) is a single `private static volatile PacketAdapter adapter` field shared across the whole server/classloader ("a single shared instance across consumers is safe"), and its own javadoc labels `reset()` "Reset to the no-op fallback. Test-only." Because there is no per-plugin adapter slot and no check for "is the currently-installed adapter still mine", any bootstrap failure in `Bartizan.onEnable()` (caught Throwable -> `disablePlugin(this)` -> `onDisable()`) or any manual disable/`/reload` of Bartizan while another Keystone-based plugin has already installed its own PacketAdapter will silently downgrade that other plugin's camera-kick/inventory-packet features to `NoOpAdapter` with zero log output, until that other plugin itself re-enables and reinstalls.~~High
2~~P3~~Bartizan.dependencyHandler() logs "Found X, linking..."/"Linked X" for ViaVersion/PlaceholderAPI/NBTAPI but every call passes a null Runnable so nothing is ever linked, and NBTAPI (a hard `depend:`) is checked through the same soft-dependency code path~~Either pass a real linking Runnable per dependency or drop the misleading "linking"/"Linked" wording (and drop the NBTAPI check entirely since Bukkit already guarantees it's present before onEnable runs)~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/Bartizan.java:116-146~~`dependencyHandler()` calls `viaVersion.validate(null); placeholderApi.validate(null); nbtApi.validate(null);` and `Dependency.validate` does `if (runnable != null) runnable.run();` after logging `log.info("Found {}, linking...", name); ... log.info("Linked {}", name);` — since every call site passes `null`, these three log lines fire with no actual wiring behind them, which is misleading to an admin reading logs to confirm an integration actually works. NBTAPI is additionally declared under plugin.yml's hard `depend:` list (plugin.yml:7-9), so `Bukkit.getPluginManager().getPlugin("NBTAPI") == null` can never be true when this code runs, making that branch of the soft-dependency helper permanently dead for NBTAPI and conflating a mandatory dependency with the two genuinely-optional ones in both code path and wording.~~High
3~~P3~~BartizanContext double-declares a getContainer() accessor: Lombok's @Getter on the container field plus a hand-written method of the same name~~Drop either the `@Getter` on the `container` field or the hand-written getContainer() method — keep one~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/bootstrap/BartizanContext.java:41-43~~`@Getter private final DependencyContainer container;` (line 42) asks Lombok to generate `getContainer()`, and the class separately hand-writes `public DependencyContainer getContainer() { return container; }` at lines 179-181. Lombok silently skips generating its own accessor when one with the same signature already exists in source, so this compiles fine, but it is dead/confusing duplication that could mislead a future edit (e.g. someone adding logic to one copy while the other, or a re-generated one, stays plain) — `beanFactory` right next to it only has the `@Getter` form, showing the intended pattern.~~Medium
