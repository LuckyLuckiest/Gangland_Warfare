# BZ-EV events — verified findings (Bartizan @ 0e18356)
1~~P1~~WeaponInteract's eight per-weapon state maps are never cleared on player quit, leaving timers running for an offline player~~Add an onPlayerQuit path (either in WeaponQuitCleanupListener, given WeaponInteract's own instance, or a package-private cleanup method WeaponInteract exposes) that removes the quitting player's currently-held weapon's UUID from continuousFire, pressHoldState, releaseCallbacks (stopping any FullAutoTask/RepeatingTimer found), plus equipDelayUntil/meleeCooldowns/lastMeleeSwingMs so those don't linger indefinitely.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/WeaponInteractTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/player/WeaponQuitCleanupListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:99-137 (eight maps: continuousFire, equipDelayUntil, pressHoldState, releaseCallbacks, autoTasks, activeTasks, meleeCooldowns, lastMeleeSwingMs); bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/player/WeaponQuitCleanupListener.java:46-67~~WeaponInteract owns continuousFire, pressLockUntilTick, pressHoldState, releaseCallbacks, autoTasks, activeTasks, meleeCooldowns and lastMeleeSwingMs, all keyed by weapon UUID and all cleared only from onWeaponHeld on a weapon swap. WeaponQuitCleanupListener.onPlayerQuit reads only `ItemStack item = player.getInventory().getItemInMainHand();` and then calls stopReloading() and unScope(player, true), touching none of the maps. A player who disconnects mid-AUTO-fire or mid-throwable-charge leaves the FullAutoTask and RepeatingTimer entries running and calling Bukkit Player APIs against an offline Player until their own watchdog eventually times out. Was Gangland WP-9, quit path.~~High
2~~P1~~Async press-hold watchdogs write the non-volatile WeaponData.shooting flag the main-thread interact handler also writes~~Change the two remaining async watchdogs (engagePressHoldWatchdog line 752, shootFullAuto's AUTO watchdog line 858, and handleIncendiaryAuto's line 656) to .start(false) to match handleChargeHold's already-fixed pattern, since all of them only flip a flag/mutate maps and (for the AUTO watchdog) call the main-thread-only resetRecoilPattern/raytracer path; that removes the cross-thread write entirely rather than papering over it with volatile.~~WeaponInteractInputTest, WeaponInteractInputTest, WeaponInteractInputTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:728-753 (engagePressHoldWatchdog, .start(true) at 752), :834-858 (shootFullAuto's AUTO watchdog, .start(true) at 858), :893-897 (WeaponData.shooting still non-volatile); bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/recoil/RecoilManager.java:17 (playerPatternIndex still a plain int)~~engagePressHoldWatchdog and shootFullAuto's AUTO watchdog both schedule a RepeatingTimer with `.start(true)` under the comment "Pure flag-flipping + map mutation, so safe to run async", and mutate `stillHeld.get().shooting = false;`. WeaponData declares `private boolean shooting;` with no volatile, and the same field is set true from the main thread at lines 396, 478, 527 and 653 during PlayerInteractEvent handling. The eight surrounding maps are all ConcurrentHashMap so there is no map corruption, but the flag itself has no memory barrier, so a release can be missed for a cycle and the press-lock either lingers or re-arms late. shootFullAuto's async watchdog additionally calls resetRecoilPattern(), mutating RecoilManager.playerPatternIndex concurrently with applyRecoil on the main thread.~~Medium
3~~P2~~The scope-toggle branch swallows a non-sneaking left click on every weapon that has no scope configured~~Enter the scope branch only when scopeData != null (drop the `scopeData == null` default-true clause), same fix direction as originally filed, now at WeaponInteract.java:196.~~WeaponInteractInputTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:192-213 (scope-toggle branch, validateScope at line 196); bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/ScopeToggle.java:25-45~~onPlayerInteract computes `boolean validateScope = true; if (scopeData != null) { validateScope = scopeData.getLevel() > 0; }`, so a weapon with no ScopeData keeps validateScope true and enters `if (leftClick && !player.isSneaking() && validateScope && !weapon.isReloading())`. That branch denies the interaction, calls unScope, plays the scope sound and `return`s before execution can reach handleNonGunInteract's melee branch below. A scope-less melee weapon swung at air or a block with a plain left click therefore never reaches MeleeAction; only sneak-left-click does, though entity hits still land through the separate damage handler. Was Gangland WP-5.~~High
4~~P2~~WeaponService.getWeaponUUID calls UUID.fromString on raw NBT with no try/catch, throwing out of every listener~~Wrap `UUID.fromString(value)` in try/catch at WeaponService.java:61, log at warn (include the item/tag) and return null on IllegalArgumentException, exactly as originally proposed.~~WeaponServiceTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/WeaponService.java:49-65 (getWeaponUUID, unguarded UUID.fromString at line 61); called unguarded from validateAndGetWeapon:313-332 (line 320) and isWeapon:79-97 (line 84)~~getWeaponUUID reads the tag with `String stringTagData = tempItem.getStringTagData(tagProperName);` and then does `if (!(value == null || value.equals("null") || value.isEmpty())) { uuid = UUID.fromString(value); }` with no guard. This is the first thing every listener in the system reaches through validateAndGetWeapon — WeaponInteract, WeaponDroppedListener, WeaponItemSpawnListener, WeaponQuitCleanupListener, WeaponReloadListener and WeaponSelectiveFireChangeListener all route through it. A hand-edited or corrupted UUID tag throws IllegalArgumentException out of the @EventHandler method for all of them, and because the item stays in the inventory the exception repeats on every interaction. Was Gangland WP-24.~~High
5~~P2~~WeaponQuitCleanupListener inspects only the main hand, so an off-hand weapon's reload and scope state survives the disconnect~~Check OFF_HAND as well, mirroring WeaponService.getHeldWeaponItem's own main-then-offhand fallback~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/player/WeaponQuitCleanupListener.java:27-40~~onPlayerQuit does `ItemStack item = player.getInventory().getItemInMainHand(); Weapon weapon = weaponManager.validateAndGetWeapon(player, item); if (weapon == null) return;` and never looks at getItemInOffHand(). WeaponService.getHeldWeaponItem explicitly falls back to EquipmentSlot.OFF_HAND when the main hand is not a weapon, and WeaponSelectiveFireChangeListener depends on that fallback, so off-hand weapons are a supported case. A player quitting while reloading or scoped with an off-hand weapon leaves the reloading and scoped flags stuck on that weapon instance.~~Medium
6~~P3~~shootInterval schedules a fresh Consume_On_Time CountdownTimer on every shot instead of restarting one~~Keep one CountdownTimer per weapon UUID (e.g. a ConcurrentHashMap<UUID, CountdownTimer> alongside GunFireDispatcher's existing pressLockUntilTick map) and cancel/replace the previous timer before scheduling a new one in shootInterval.~~GunFireDispatcherTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/GunFireDispatcher.java:100-119 (shootInterval, moved out of WeaponInteract), CountdownTimer built unconditionally at lines 114-118~~Whenever Consume_On_Time is configured above -1, shootInterval unconditionally builds `CountdownTimer timer = new CountdownTimer(plugin, 0L, 0L, consumeOnTime, null, null, time -> weapon.removeWeapon(player, player.getInventory().getHeldItemSlot()));` and starts it. Firing several shots before the first timer elapses stacks independent timers that each call removeWeapon on whatever item then occupies that hotbar slot, so a slot reused after the weapon is gone can have an unrelated item removed. Dormant in practice because no shipped weapon configures Consume_On_Time. Was Gangland WP-29.~~High
7~~P3~~shootOtherModes derives the SINGLE-mode press-lock window from Per_Shot, which only applies to BURST~~Have lockTicksFor branch on weapon.getCurrentSelectiveFire(): use perShot×cooldown only when the mode is BURST, otherwise just cooldown (1 shot), same fix direction as originally filed, now centralized in GunFireDispatcher.lockTicksFor since WeaponSelectiveFireChangeListener's scoped F fire shares the same call.~~GunFireDispatcherTest, GunFireDispatcherTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:769-794 (shootOtherModes, call site at line 783); bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/GunFireDispatcher.java:73-76 (lockTicksFor)~~shootOtherModes serves both SINGLE and BURST and computes `long lockTicks = Math.max((long) projectileData.getPerShot() * projectileData.getCooldown(), MIN_PRESS_LOCK_TICKS);` unconditionally, while the shoot() call it then makes only fires perShot rounds when the mode is BURST and otherwise fires one. A weapon whose BURST mode is configured with Per_Shot above 1 therefore gets that inflated press-lock in SINGLE mode too, so single fire is slower than its configured cooldown implies. Was Gangland WP-35.~~High
8~~P3~~WeaponChangeSelectiveFireEvent declares getHandlerList() private, unlike all seven sibling event classes~~Make it `public static HandlerList getHandlerList()` to match all thirteen siblings and the Bukkit event contract, same fix as originally filed.~~bartizan-api/src/test/java/org/luckyraven/bartizan/api/event/WeaponChangeSelectiveFireEventTest, bartizan-api/src/test/java/org/luckyraven/bartizan/api/event/WeaponChangeSelectiveFireEventTest (pins the fix)~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/event/WeaponChangeSelectiveFireEvent.java:19-21~~The class declares `private static HandlerList getHandlerList() { return handler; }` while WeaponReloadEvent, WeaponEntityDamageEvent, WeaponRaytraceImpactEvent, WeaponReloadStartEvent, WeaponReloadCompleteEvent, WeaponShootEvent and WeaponKillEntityEvent all declare it public static. Registration itself still works, because Bukkit's SimplePluginManager resolves the handler list with getDeclaredMethod plus setAccessible(true) rather than getMethod — so this is not the registration break it first appears to be. The real cost is that this is a bartizan-api class meant for cross-plugin consumption and a consumer cannot call WeaponChangeSelectiveFireEvent.getHandlerList() to unregister, which every other event in the package permits.~~High
9~~P1~~Swapping a SLOWNESS-scoped weapon to the off-hand (F key) leaves the player permanently slowed with no cleanup path~~In onSwapHand, whenever the event will go through uncancelled and the main-hand `weapon` is non-null, call `weapon.unScope(player, false)` before returning. That call does nothing unless the weapon is scoped. It has to run on all three uncancelled exits: not sneaking (line 81), no Selective_Fire (line 85), and a cancelled WeaponChangeSelectiveFireEvent (line 90). If the scope skin should also revert, route the call through ScopeToggle/skin refresh, or fire ON_SCOPE_OUT the way ScopeToggle.apply does. The alternative is to cancel the swap for any scoped weapon, as the code already does for the spyglass case. A plain Q drop of a scoped weapon (WeaponDroppedListener.onPlayerDrop, non-sneak path) leaks the same way, so the same unScope guard belongs there.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/selective/WeaponSelectiveFireChangeListenerTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/reload/WeaponDroppedListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/selective/WeaponSelectiveFireChangeListener.java:59-108 (onSwapHand uncancelled exits at lines 81, 85, 90). The root cause is that scope cleanup lives only in WeaponInteract#onWeaponHeld (WeaponInteract.java:365-409, PlayerItemHeldEvent).~~[found 2026-09-24, 0.5.0 @ 9fa25c3] onSwapHand only intervenes on PlayerSwapHandItemsEvent for two cases: Cancel.Swap_Hands=true, and a spyglass-scoped gun while not sneaking (redirected into fireScoped). For every other weapon - including any gun using the default Scope.Type: SLOWNESS (e.g. the shipped awp.yml, which has no Type: spyglass key) - the branch at line 81 (`if (!player.isSneaking()) return;`) lets the event proceed uncancelled whenever the player is not sneaking, regardless of whether the weapon is currently scoped. Concretely: scope in with AWP (Weapon#scope applies XPotion.SLOWNESS with no expiry, only Weapon#unScope removes it), then press F without sneaking. The event is never cancelled, so the weapon physically moves to the off-hand while ScopeData.scoped stays true and SLOWNESS stays applied. WeaponInteract#onWeaponHeld (the only place that calls previousWeapon.unScope on a swap) only fires on PlayerItemHeldEvent (hotbar number/scroll changes), never on PlayerSwapHandItemsEvent, and it only inspects the main-hand hotbar slot, so a weapon that is now sitting in the off-hand is never revisited by it. The player is stuck slowed (and the gun stuck 'scoped', blocking scope-in on other weapons only if they re-scope) until they manually re-select the weapon and click to toggle scope off themselves - most players will not realize the off-hand item is the cause.~~High
10~~P1~~WeaponQuitCleanupListener#onPlayerDeath never stops an in-progress reload, so isReloading() stays stuck true (blocking fire and scope) for up to a full reload stage after respawn~~In onPlayerDeath, do what onPlayerQuit does before unScope: `if (weapon.isReloading()) { weapon.stopReloading(); effectRunner.run(weapon, EffectHook.ON_RELOAD_CANCEL, EffectContext.builder().weapon(weapon).source(player).build()); }`. The simplest way is to pull lines 59-66 into one private helper and call it from both handlers. stopReloading() then goes through endReloading(player, true), which records the resume point that migration.md:295 promises for a death. It also clears the item-cooldown overlay through WeaponReloadListener.onReloadEnd.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/player/WeaponQuitCleanupListenerTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/player/WeaponQuitCleanupListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/player/WeaponQuitCleanupListener.java:69-77 (onPlayerDeath). The fallback it relies on is the isDead() check at InstantReload.java:109/129/181 and NumberedReload.java:134/181.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] onPlayerQuit explicitly calls weapon.stopReloading() before unScope when the weapon is reloading (lines 59-64), but the sibling onPlayerDeath handler only calls weapon.unScope(player, true) - it never calls stopReloading(). Reload#reloading is only cleared by InstantReload/NumberedReload's own per-stage timer callbacks noticing player.isDead() (InstantReload.java / NumberedReload.java: every stage boundary checks `player.isDead() || !CombatEligibility...canBeHit(player)` before calling stopReloading()), which only runs at the NEXT stage boundary (openTicks/insertTicks/closeTicks ticks away, i.e. up to several seconds for a slow-cooldown weapon). Concretely: a player dies mid-reload (e.g. right after the open-stage sound, before the insert commit) on a server with keepInventory (common for a combat plugin like this), respawns within that window, and finds gunWeapon.isReloading() still true - WeaponInteract#onPlayerInteract denies both the fire branch (`if (gunWeapon.isReloading()) { ...; return; }`) and the scope-toggle branch (`!weapon.isReloading()` guard) until the reload's own delayed self-check finally fires.~~High
11~~P0~~An off-hand throwable is never consumed: ThrowableAction takes one item from the main hand instead, so the off-hand stack gives unlimited grenades~~Root fix: pass the hand the throwable came from. In handleThrowablePress, forward event.getHand() to ThrowableAction.activate(player, hand). In decrementHeldStack, use inv.getItem(hand) and inv.setItem(hand, ...) instead of the main-hand getter and setter (PlayerInventory.getItem/setItem(EquipmentSlot) are available in Spigot 1.9+). ThrowableAction has only this one caller. You could also add `if (event.getHand() == EquipmentSlot.OFF_HAND) return;` right after the DENY at WeaponInteract:180, since Dual_Wield is Missing (gate HN). But that guard also turns off off-hand gun firing, which persistHeldWeapon partly supports today, so treat it as a product decision and not the minimal fix.~~WeaponInteractOffHandTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:163-180 (no event.getHand() check), :445-454, :575-583; bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/ThrowableAction.java:99-101, 244-254 (decrementHeldStack reads and writes the main hand only)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] WeaponInteract.onPlayerInteract never checks event.getHand(). It resolves the weapon from event.getItem(), which for an OFF_HAND interact is the off-hand item. It then goes handleNonGunInteract -> handleThrowablePress -> ThrowableAction.activate. activate() calls decrementHeldStack(player), and that method always reads and rewrites player.getInventory().getItemInMainHand(). Repro with the shipped grenade (LIME_DYE): 1. Put a stack of grenades in the off hand and leave the main hand empty. 2. Right-click air. The client finds nothing to use in the empty main hand, so it sends a use-item packet for OFF_HAND. 3. The server fires PlayerInteractEvent(RIGHT_CLICK_AIR, grenade, OFF_HAND) and the grenade is thrown and detonates. 4. decrementHeldStack sees an AIR main hand: getAmount() > 1 is false, so it calls setItemInMainHand(AIR), which does nothing. Result: the off-hand stack never shrinks, so one grenade gives unlimited throws, limited only by the 4-tick press lock. The same works for the molotov (HONEY_BOTTLE), flashbang (ENDER_PEARL), smoke grenade and tomahawk. If the main hand holds any non-weapon stack (blocks, ammo), each off-hand throw deletes one of those items instead. Dual_Wield is listed as Missing (gate HN) in weapons-roadmap.md, so off-hand firing is not a designed path.~~High
12~~P0~~An off-hand gun or flamethrower shot is written into the main-hand slot: this duplicates the weapon and deletes the main-hand stack, and with two guns held the off-hand gun never uses ammo~~1) In WeaponInteract.onPlayerInteract, return early when event.getHand() == EquipmentSlot.OFF_HAND. Dual wield is listed as Missing/HN in the roadmap, so off-hand firing is not a feature. 2) The guard alone is not enough. A running FullAutoTask keeps calling GunAction after an F swap: Swap_Hands is not cancelled by default, and onWeaponHeld does not fire on a swap. So GunAction.weaponShoot and IncendiaryAction.sprayFire must also resolve the item by the firing weapon's uuid. Use the main hand if its uuid equals weapon.getUuid(), otherwise the off hand if that uuid matches, otherwise return without consuming anything. Then write back to that same hand, the way persistHeldWeapon does (setItemInOffHand for the off hand, updateWeapon(heldSlot) for the main hand). Apply the same hand choice to the removeWeapon calls in GunAction:110 and GunFireDispatcher:108/115.~~GunActionHeldWeaponTest, GunActionHeldWeaponTest, WeaponInteractOffHandTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:162-168 (no event.getHand() check); bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/GunAction.java:43,88,96,110; bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/IncendiaryAction.java:103-121; bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/WeaponService.java:146-159 (getHeldWeaponItem falls back to the off hand); bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/GunFireDispatcher.java:108,115 (same getHeldItemSlot removal)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] OFF_HAND PlayerInteractEvents reach the gun and incendiary fire paths because onPlayerInteract has no hand check. GunAction.weaponShoot then picks its write-back target with weaponService.getHeldWeaponItem(shooter): the main-hand item if it is any weapon, otherwise the off-hand item. It then does weapon.updateWeaponData(heldWeapon) and weapon.updateWeapon(shooter, heldWeapon, getHeldItemSlot()), which always writes to the main-hand hotbar slot. IncendiaryAction.sprayFire does the same. (a) Duplication and deletion: hold a rifle in the off hand and, for example, 32 of its ammo items in the main hand, then right-click air. The ammo item passes, so the client sends an OFF_HAND use. The rifle fires, and the main-hand slot is set to a copy of the rifle. The 32 ammo items are deleted, and there are now two rifles carrying the same uuid; the second can be traded or sold. Destroy_When_Empty (GunAction:110) then removes the main-hand copy, while the real off-hand weapon survives. (b) Unlimited ammo with two guns: hold gun A (a pickaxe, hoe or shovel material, which passes on air) in the main hand and gun B in the off hand. Each right-click fires A (HAND event) and then B (OFF_HAND event). B's shot decrements only the in-memory B instance and is stamped onto A's item. B's own item is never written, so validateAndGetWeapon re-syncs B from its unchanged NBT on the next click. B never empties, and A's AMMO_LEFT is overwritten with B's count minus one on every click.~~High
13~~P0~~Charge release and incendiary-AUTO spray are torn down only by PlayerItemHeldEvent, so dropping/moving/emptying the hand mid-charge still fires a live shot and writes its ammo onto whatever is currently held~~Fix it once by matching the uuid. Add a WeaponService helper that returns the hand (main first, then off) whose item's getWeaponUUID equals weapon.getUuid(), or null if neither hand holds it. (1) In the handleChargeHold watchdog, when the helper returns null: stop and remove the activeTasks charge timer, drop releaseCallbacks and continuousFire, and do not run the callback. Do the same in the handleIncendiaryAuto sprayLoop: stop and remove from activeTasks/continuousFire. (2) persistHeldWeapon and sprayFire should build from that matched hand and write back only to it, and do nothing when there is no match. That stops a weapon's state being stamped onto a different weapon, and it also fixes sprayFire copying an off-hand flamethrower into the main-hand slot. The watchdog already runs every 4 ticks, so this covers Q-drop, F-swap and inventory moves without new PlayerDropItemEvent, InventoryClickEvent or PlayerSwapHandItemsEvent handlers.~~UnheldWeaponFireTest, UnheldWeaponFireTest, UnheldWeaponFireTest, UnheldWeaponFireTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:532-566 (handleChargeHold watchdog), 608-657 (handleIncendiaryAuto spray loop and watchdog). The damage lands in bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/WeaponService.java:169-183 (persistHeldWeapon writes to whatever weapon is in the main hand) and bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/IncendiaryAction.java:102-118 (sprayFire writes the resolved item into getHeldItemSlot() whichever hand it came from). The only teardown is onWeaponHeld at WeaponInteract.java:366-427.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] handleChargeHold (shared by biological and beam) and handleIncendiaryAuto register their state (continuousFire, releaseCallbacks, activeTasks) only, and the only code that ever clears it is onWeaponHeld, which runs exclusively on PlayerItemHeldEvent (a hotbar-slot change). Nothing in WeaponInteract handles PlayerDropItemEvent or InventoryClickEvent, and WeaponDroppedListener (listener/reload/WeaponDroppedListener.java) only starts a reload or cancels the drop per Cancel.Drop_Item — it never touches these maps either. Crucially, onPlayerInteract unconditionally DENYs the vanilla item-use for every right click (`if (rightClick) event.setUseItemInHand(Event.Result.DENY);`, line 180, reinforced in handleNonGunInteract), so the client's hand is never actually 'in use' while charging — the player is free to open their inventory (E) and shift-click the charging weapon out of the hotbar, or simply press Q to drop it (for any weapon that doesn't set Cancel.Drop_Item), while RMB is still held. Trigger: hold RMB on a shipped charge weapon (syringe_gun.yml, Category: biological; or arc_lance.yml, Category: beam) to start charging, then move the item out of the hand (drop it, or shift-click it into the backpack via an open inventory) before releasing RMB. Because no more PlayerInteractEvents arrive for that weapon, handleChargeHold's own release watchdog (RepeatingTimer every 4 ticks) sees the `shooting` flag go stale within one cycle and calls the release callback — controller.release(player) -> BiologicalAction.fire(player, level) or BeamAction.fire(player, level) — which fires a real raytrace shot from the player's current eye location with no check that the weapon is still held. BiologicalAction.fire (line 64-65) and BeamAction.fire (line 141-143) then both call weaponService.persistHeldWeapon(weapon, player), which (per the already-known persistHeldWeapon-writes-onto-whatever-is-held behavior) stamps this now-orphaned weapon's post-shot ammo/state onto whichever item is currently sitting in the player's main hand — a different weapon if the player already swapped to one. The same gap applies to handleIncendiaryAuto's spray RepeatingTimer (activeTasks), which likewise keeps calling IncendiaryAction.fireOnce every tick-rate ticks until its own watchdog notices the stale flag; sprayFire (IncendiaryAction.java:102-104) does bail out once weaponService.getHeldWeaponItem(player) stops resolving to a weapon at all, but still runs at least once more against whatever the hand still holds.~~High
14~~P1~~PlayerInteractEntityEvent bypasses the Trigger: left_click / scope-toggle swap onPlayerInteract applies, so right-clicking a target fires instead of scoping~~In onPlayerInteractWithEntity, right after `if (!(weapon instanceof GunWeapon gunWeapon)) return;`, add `if (isLeftClickTrigger(gunWeapon)) return;` so the entity click never fires. Keep the cancel. Do NOT call ScopeToggle.apply here, as the report proposes. For a zombie or a player the client follows the INTERACT packet with a USE_ITEM packet, which reaches onPlayerInteract as RIGHT_CLICK_AIR and already runs the scope branch. cycleScope toggles, so scoping in both handlers would scope in and then straight back out. Also rewrite the ponytail comment at :186-187 to match the new behaviour.~~WeaponInteractInputTest, WeaponInteractInputTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:287-316 (onPlayerInteractWithEntity), plus the stale ponytail note at :186-187~~[found 2026-09-24, 0.5.0 @ 9fa25c3] onPlayerInteract computes `gunLeftTrigger`/`scopeClick`/`fireClick` and routes a scope click into ScopeToggle.apply before ever considering firing. onPlayerInteractWithEntity has none of that logic: after the isWeapon/isDead/canBeHit/isReloading checks it unconditionally treats any right-click-on-entity as a fire input (`shootFullAuto`/`shootOtherModes`), regardless of Shoot.Trigger. scout.yml — the shipped Scope.Type: spyglass reference weapon — configures `Trigger: left_click` specifically so right-click is the scope input and left-click fires. Bukkit dispatches PlayerInteractEntityEvent instead of PlayerInteractEvent whenever the click lands on an entity, so a player holding scout who right-clicks directly at a mob or another player (i.e. exactly when they'd want to scope onto their target) never reaches the scope branch at all and instead fires a live shot, wasting ammo/revealing position and making it impossible to scope while already aiming at the intended target — the normal, most common use case for a sniper rifle.~~Medium
15~~P1~~Equip_Delay is seeded only on a hotbar-slot change, so swap-hands (F), join/respawn, and pickup-into-the-held-slot all skip the anti-quickscope delay a shipped weapon relies on~~Move the new-weapon half of onWeaponHeld (ON_EQUIP plus the equipDelayUntil.put) into a private helper such as `equip(Player, ItemStack)`. Then add `@EventHandler(priority = MONITOR, ignoreCancelled = true) onSwapHands(PlayerSwapHandItemsEvent e)` in WeaponInteract, which is where the private map lives. It calls `equip(e.getPlayer(), e.getMainHandItem())`; in Spigot, getMainHandItem() is the item that lands in the main hand after the swap. The outgoing weapon, e.getOffHandItem(), should get the same holster cleanup, because today F skips the unscope and the auto-task stop too. Join, respawn and pickup are minor edge cases, so leave them out unless wanted.~~WeaponInteractInputTest, WeaponInteractInputTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:411-425 (onWeaponHeld is the only seeding site) and bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/selective/WeaponSelectiveFireChangeListener.java:59-81 (onSwapHand lets F swap a weapon into the main hand and never seeds the delay)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] equipDelayUntil is written exactly once in the whole class, inside onWeaponHeld's 'new slot' branch, which only runs off PlayerItemHeldEvent (a hotbar index change via scroll/number key). scout.yml ships `Equip_Delay: 3` specifically as an anti-quickscope mechanic (paired with Scope.Type: spyglass and Trigger: left_click). WeaponSelectiveFireChangeListener.onSwapHand handles the F key (PlayerSwapHandItemsEvent) and never calls onWeaponHeld or seeds equipDelayUntil; scout has no Cancel.Swap_Hands set, so a plain F swap is not blocked. A player can therefore keep scout in the off-hand and F-swap it into the main hand only at the instant they want to fire, permanently skipping the 3-tick delay the weapon's own config was written to enforce — the same gap applies to a player who simply joins or respawns with the weapon already selected in their held slot (no PlayerItemHeldEvent fires for that), or who has it dropped/looted directly into the currently-selected slot.~~High
16~~P1~~onPlayerInteract's null-weapon early return skips the unconditional crossbow-pose DENY, so a CROSSBOW weapon that fails to resolve fires a real vanilla arrow and keeps restocking one on every rebuild~~Do not apply the proposed fix as written. Moving `if (rightClick) event.setUseItemInHand(DENY)` above the null check makes it run for every PlayerInteractEvent on the server, because validateAndGetWeapon returns null for air and for every non-weapon item. That would stop block placing, eating, bows, pearls and so on. Gate the deny on the Bartizan weapon tag instead. Before line 168 add: `if (rightClick && weaponService.getHeldWeaponName(item) != null) event.setUseItemInHand(Event.Result.DENY);` (getHeldWeaponName already handles null and air). For a narrower change, also require `item.getType() == XMaterial.CROSSBOW.get()`. After that, the existing unconditional DENY at line 180 can stay or be dropped.~~WeaponInteractInputTest, WeaponInteractInputTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:166-180 (the `if (weapon == null) return;` at line 168 runs before the crossbow DENY at line 180)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] Weapon#buildItem always calls applyCrossbowChargedProjectile, which stamps `CrossbowMeta.setChargedProjectiles(List.of(ARROW))` onto any Material: CROSSBOW weapon item, purely cosmetic (Bartizan's own firing path never reads it) and by design meant to be neutralised by WeaponInteract always DENYing vanilla item-use on right click. But onPlayerInteract's own code reads: `Weapon weapon = weaponService.validateAndGetWeapon(player, item); if (weapon == null) return;` before the `if (rightClick) event.setUseItemInHand(Event.Result.DENY);` line the surrounding comment says must run 'unconditionally' to prevent exactly this. If validateAndGetWeapon returns null for an item that still carries the Bartizan weapon NBT tag and CrossbowMeta's charged arrow (e.g. the weapon's type entry was removed/renamed from config, or the addon hadn't finished loading when the item was last built), the method returns before ever reaching the DENY line, so the client's right click fires the item as a genuine loaded vanilla crossbow — a real ProjectileLaunchEvent arrow with real damage, entirely outside Bartizan's raytrace/ammo system. Because the meta gets re-stocked with a fresh charged arrow on every subsequent buildItem() call for that same (still-broken) item, each later rebuild hands the player another free real arrow shot the next time the same failure path is hit.~~Medium
17~~P2~~Shoot.Circumstance and Information.Equip_Delay are never enforced for biological/beam charge weapons or incendiary AUTO fire~~Drop the Circumstance/firstDenied part of the proposed fix entirely (including for handleThrowablePress) — that behavior is working as designed. Fix only the Equip_Delay gap: in handleChargeHold add `if (isEquipDelayActive(weaponUuid)) return;` right after the `existing != null` early return (before `start.getAsBoolean()` is invoked), and in handleIncendiaryAuto add the same guard before the first `action.fireOnce(player)` call. Prefer a bare isEquipDelayActive check over reusing isPressGated — these two paths use continuousFire, not pressHoldState/GunFireDispatcher, so pulling in the rest of isPressGated's logic works (both would always be false/no-op for non-gun UUIDs) but tests irrelevant state; a direct isEquipDelayActive call is the smaller, more honest diff.~~WeaponInteractInputTest, WeaponInteractInputTest, WeaponInteractInputTest, WeaponInteractInputTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/WeaponInteract.java:532-539 (handleChargeHold, biological+beam cold start) and :608-623 (handleIncendiaryAuto cold start) — both gate purely on `continuousFire.get(weaponUuid) != null`, never calling isEquipDelayActive/isPressGated. The Shoot.Circumstance half of the report is NOT a defect: WeaponAddon.java:319 explicitly documents "Trigger/Circumstance only have an effect for GUN weapons (WeaponInteract/GunAction)", and CircumstanceRules.firstDenied's only 3 call sites all sit behind the `weapon instanceof GunWeapon` branch at WeaponInteract.java:216 (handleNonGunInteract, reached for every non-gun category including throwables, never calls it). The Information.Equip_Delay half is real: the equip-delay gate is category-agnostic everywhere else it's checked — the scope-toggle branch (line 198-199, any weapon with a scope), handleThrowablePress (578), handleIncendiaryPress/SINGLE-BURST (593), and shootFullAuto's own cold start (803, via isPressGated) all check it — but the two continuous-fire cold starts that structurally mirror shootFullAuto (same continuousFire/AtomicReference/RepeatingTimer watchdog pattern) skip it, with no comment anywhere scoping Equip_Delay to guns the way Circumstance is explicitly scoped.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] CircumstanceRules.firstDenied is called from exactly three places in the class: the reload-deny branch, shootOtherModes, and shootFullAuto's cold-start branch — all GunWeapon-only paths. handleChargeHold (shared by biological and beam) and handleIncendiaryAuto never call it at all, and also never call isPressGated (which is what folds in the Equip_Delay check via isEquipDelayActive) — they gate purely on `continuousFire.containsKey(weaponUuid)`. handleThrowablePress does call isPressGated (so Equip_Delay is respected there) but likewise never calls firstDenied, so Shoot.Circumstance is a no-op for throwables too. HandlingData (Circumstance rules and Equip_Delay) is a generic Weapon-level model, not gun-specific, so an admin configuring either block on a biological, beam, incendiary-AUTO or throwable weapon YAML gets silently ignored behaviour with no warning. No shipped biological/beam/incendiary weapon currently configures Equip_Delay or Circumstance (only the three gun weapons awp.yml/rifle.yml/scout.yml do), so this is a latent trap rather than a shipped-content break today.~~Medium
18~~P2~~HUD.Reload_Item_Cooldown keys off Material, so two weapons sharing a base Material blank each other's overlay~~Cheapest fix matching the ladder: at weapon-registration time (WeaponAddon, where every weapon file is already parsed), keep a static Map<Material, String> of "which weapon name first claimed this Material with Reload_Item_Cooldown: true" and emit a ConfigReport WARNING naming both files when a second one collides — a few lines in the existing per-file loader, no new abstraction, no behavior change for the non-colliding case. Do not attempt to move the overlay off vanilla Material cooldown (that's a bigger redesign the report itself offers only as an alternative, not the lazy fix).~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/configuration/WeaponAddonTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/reload/WeaponReloadListener.java:46-51 (onReloadStart) and :75-77 (onReloadEnd's interrupted branch); root cause is `Weapon#getMaterial()` being the sole cooldown key for `Player#setCooldown`, with no cross-weapon-file uniqueness check in bartizan-plugin/src/main/java/org/luckyraven/bartizan/configuration/WeaponAddon.java. scout.yml:12 and arc_lance.yml:12 both `Material: SPYGLASS`; same Material-keying pattern also used at effect/impl/CooldownHookEffect.java:30.~~[found 2026-09-24, 0.5.0 @ 9fa25c3] isReloadItemCooldownEnabled gates a call to vanilla Player#setCooldown(Material, ticks) (WeaponReloadListener.java:50, cleared at line 76), which is a per-Material client overlay, not per-weapon — Bukkit has no CustomModelData-scoped cooldown API. Bartizan already ships two distinct guns on the exact same base Material: scout.yml:12 and arc_lance.yml:12 both use `Material: SPYGLASS`, differentiated only by CustomModelData/skin. Neither currently sets HUD.Reload_Item_Cooldown: true, so there is no live collision today, but the moment an admin turns it on for either one (the same one-line opt-in shotgun.yml/rifle.yml already use), reloading that weapon would also grey out the other weapon's hotbar icon for its full (or resumed) duration, and finishing/cancelling one reload would prematurely zero the other's overlay via the same setCooldown(material, 0) call, even though that other weapon was never touched.~~High
19~~P1~~Kill credit for slow-travelling and burn-based hits falls back to whatever the killer currently holds, because only DamageKind.EXPLOSION is recorded as a claim~~The proposed fix (also record a claim for DIRECT/FIRE in onWeaponEntityDamage) does not work for the fatal slow hit. handleEntityImpact fires the DIRECT WeaponEntityDamageEvent AFTER living.damage(), and the PlayerDeathEvent fires synchronously inside that damage() call, so the claim would be recorded after the death is already handled. Suggested fix instead: around living.damage(...) in handleEntityImpact, set a ThreadLocal with the weapon name next to the existing setRaytraceDamageInProgress flag, and clear it in the same finally block. Do the same around the damage() calls in ExplosionHandler, MeleeAction, BeamAction and IncendiaryAction. In WeaponDeathListener.onPlayerDeath, use that weapon first when it is set, because it names the weapon that dealt the fatal blow regardless of what the killer now holds. This covers slow rockets and flares and also removes the need for the EXPLOSION pre-record. For delayed burn deaths: also record a claim for DamageKind.FIRE, and for gun hits that set Fire_Ticks > 0. Consult that claim only when victim.getLastDamageCause().getCause() is FIRE_TICK or FIRE, so a later knife or rifle finish is still credited to the held weapon.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/death/WeaponDeathListenerTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/death/WeaponDeathListenerTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/death/WeaponDeathListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/death/WeaponDeathListener.java:95 and :137-145; bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/WeaponRaytracerImpl.java:536-566 (handleEntityImpact: living.damage() runs before the DIRECT event fires); bartizan-plugin/src/main/java/org/luckyraven/bartizan/raytrace/SteppedProjectileTask.java:163 (the slow projectile reaches handleEntityImpact through advanceSegment); bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/IncendiaryAction.java:221-239~~[found 2026-09-24, 0.5.0 @ 9fa25c3] onWeaponEntityDamage only records a 5s throwable-claim entry when `event.kind() == DamageKind.EXPLOSION` (line 95); every other kind (DIRECT, FIRE, BIOLOGICAL, MELEE) is ignored. onPlayerDeath then resolves the credited weapon from the recorded claim, and only if that's null falls back to `killer.getInventory().getItemInMainHand()` at the moment the death event fires (lines 137-142). This 'held at death time' fallback is only safe when the fatal hit and the death are effectively simultaneous. Two real weapon classes break that assumption: (1) WeaponShooting.java:163 `explode = type == ProjectileType.ROCKET && ed.getRadius() > 0` — a ProjectileType.FLARE weapon, or a ROCKET with no Explosion_Radius configured, travels slowly via SteppedProjectileTask but deals damage through the default DIRECT path (WeaponRaytracerImpl.handleEntityImpact), not EXPLOSION; a shooter who fires such a slow projectile and then swaps weapons before it lands and kills the target gets no claim recorded, so the kill is attributed to whatever they're now holding (or dropped entirely if that's not a valid weapon, since onPlayerDeath returns at line 145 without ever firing WeaponKillEntityEvent). (2) IncendiaryAction.applyIncendiaryImpact fires DamageKind.FIRE once at the moment of the spray hit (with a token 0.001 'attributed' damage — see accepted docket item on the near-zero-damage biological pattern) and calls `target.setFireTicks(data.getFireDuration())`; the target's actual death almost always comes later from ongoing vanilla fire-tick damage, well after the FIRE event fired and often after the shooter has stopped firing or swapped weapons — the exact same race the class's own javadoc explains was the reason EXPLOSION got special-cased, but FIRE and slow-DIRECT hits were left out.~~High
20~~P1~~Kill credit (WeaponKillEntityEvent, stats.kills, assists, and the On_Kill effect hook) never fires for a non-player kill, because the only firing site is gated on PlayerDeathEvent~~In WeaponDeathListener, add an `@EventHandler(priority = MONITOR) onEntityDeath(EntityDeathEvent e)` that returns early when `e.getEntity() instanceof Player`, because onPlayerDeath already covers players. The killer comes from `victim.getKiller()`, which is set because `WeaponRaytracerImpl:538` calls `living.damage(dmg, shooter)` with a Player source. Resolve the weapon the same way onPlayerDeath does: an explosion claim first, then the held weapon through `validateAndGetWeapon`. Then fire `WeaponKillEntityEvent` and `effectRunner.run(weapon, ON_KILL, ctx)`, and do not set a death message. Pull the shared resolve/fire/ON_KILL steps into one private helper that both handlers call, so there is still only one kill path. To make explosion kills of mobs count, widen the `recentThrowableKills` guard at :96 to LivingEntity; the existing sweep already expires stale entries. The assist loop is optional: it only works for mobs if the Player-only guard at StatsService:111 is also dropped. A mob that despawns without dying would then leave an entry behind, so that needs the same piggyback sweep.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/death/WeaponDeathListenerTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/death/WeaponDeathListenerTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/death/WeaponDeathListenerTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/death/WeaponDeathListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/death/WeaponDeathListener.java:106-169 (onPlayerDeath, the only direct-kill site; creditStatusKill at :176-204 is also reached only from it) and :95-96 (the throwable claim is recorded for Player victims only); knock-on in bartizan-plugin/src/main/java/org/luckyraven/bartizan/stats/StatsService.java:109-114 (recentDamage is Player-only because of this)~~[found 2026-09-24, 0.5.0 @ 9fa25c3] A repo-wide grep for `EntityDeathEvent` in bartizan-plugin returns nothing, and `WeaponKillEntityEvent(` is constructed only at WeaponDeathListener.java:155 (inside onPlayerDeath) and :194 (inside creditStatusKill), both reachable only from a PlayerDeathEvent. Bukkit never fires PlayerDeathEvent for a non-player LivingEntity — mobs, NPCs, and any other living target fire EntityDeathEvent instead, which nothing in the plugin listens for. WeaponEntityDamageEvent itself fires for any Entity (ExplosionHandler/WeaponRaytracerImpl/BeamAction/IncendiaryAction all take a generic Entity/LivingEntity hit target), so StatsService.recordDamage does bucket shots/hits/damageDealt for a mob kill correctly — but StatsService.recordKill, the `stat.kills++`/assist loop/WeaponAssistEvent, and every weapon's EffectHook.ON_KILL (a general-purpose hook documented for all kills in settings.yml:21, not scoped to player kills) are unreachable for that same mob. A player who mows down 100 zombies with a Bartizan weapon sees `/bartizan stats` report zero kills for that weapon, and any weapon configured with an On_Kill effect (heal-on-kill, particle burst, sound) silently never triggers against mobs.~~High
21~~P1~~creditStatusKill's `getKiller() == null` gate misses the common case of an unrelated, non-fatal player hit inside Bukkit's own last-hurt-by-player window, so a poison/status kill silently loses credit to whoever grazed the victim last~~Don't gate creditStatusKill on Bukkit's getKiller() at all — gate it on the actual fatal-damage cause. Bukkit already exposes this precisely: victim.getLastDamageCause() (an EntityDamageEvent) has getCause() == DamageCause.POISON / DamageCause.WITHER when a status DoT tick is what actually zeroed the victim's health, independent of the separate/looser lastHurtByPlayer field that backs getKiller(). Change the branch to call creditStatusKill(event, victim) when killer == null OR the last damage cause is POISON/WITHER (return early if it credited), falling through to the existing killer/weapon-held-item path only otherwise. This is strictly more correct than a timing-heuristic comparison against Kill_Credit_Window: a genuinely lethal hit from C still correctly credits C (last damage cause would be ENTITY_ATTACK/PROJECTILE, not POISON/WITHER), while a graze-then-poison-death correctly credits the status's shooter.~~bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/death/WeaponDeathListenerTest, bartizan-plugin/src/test/java/org/luckyraven/bartizan/listener/death/WeaponDeathListenerTest (pins the fix)~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/death/WeaponDeathListener.java:122-128 (the `if (killer == null)` gate in onPlayerDeath) together with creditStatusKill at :176-204~~[found 2026-09-24, 0.5.0 @ 9fa25c3] onPlayerDeath only calls creditStatusKill(...) inside `if (killer == null)` (lines 123-127), reasoning (per the inline comment) that a null Bukkit killer means 'no entity dealt the final blow.' But `Player#getKiller()` is not 'whoever dealt the fatal hit' — it returns the last Player that damaged the victim within Minecraft's own lastHurtByPlayerTime window (~100 ticks / 5s), regardless of whether that hit was even close to lethal. So: Player A poisons Player B with a biological weapon (StatusEffectService.apply records shooterId=A, appliedTick=now). Within the next 5 real-world seconds — the length of an ordinary firefight — Player C lands any non-fatal hit on B (their own weapon, a stray arrow, vanilla melee). B then dies from the poison DoT tick. `victim.getKiller()` returns C (non-null) purely because of Minecraft's own combat-tracker timing, even though C's hit did not kill B and B's active status (still well inside its own Kill_Credit_Window, tracked via the same tickClock) is what actually did. Because killer != null, creditStatusKill is never invoked: A gets no kill credit, no stats.kills, no ON_KILL effect, and instead the code falls through to crediting C's currently-held item (if any) with the kill C did not actually land.~~High
