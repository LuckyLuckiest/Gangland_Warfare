1~~P0~~WearableEquipListener never checks permission on right-click equip (only handles InventoryClickEvent)~~Add a PlayerInteractEvent handler that re-validates the Wearable permission after Bukkit auto-equips armor via right-click, and add HOTBAR_SWAP to the recognized armor-slot actions~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListener.java:39-92~~onArmorEquip only listens to InventoryClickEvent, and resolveArmorBeingEquipped only treats `PLACE_ALL`, `PLACE_ONE`, `PLACE_SOME`, `SWAP_WITH_CURSOR` plus shift-click as equip actions. Right-clicking an armor piece in hand to wear it (the default vanilla equip method) never fires InventoryClickEvent at all, and pressing a hotbar number key while hovering the armor slot fires action HOTBAR_SWAP, so neither path ever reaches `if (permission != null && !permission.isEmpty() && !player.hasPermission(permission))`. Any player can therefore equip a permission-gated wearable such as heavy_vest by simply right-clicking it on, bypassing bartizan.wearables.<key> entirely.~~High
2~~P2~~Unknown/misspelled Traits: keys in wearables.yml silently contribute zero effect with no warning~~In WearableAddon.loadWearables validate each Traits: key against Wearable's known trait set and log.warn on an unmapped key instead of silently storing it~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/wearable/WearableAddon.java:130-138~~Every key under a wearable's Traits: section is accepted unconditionally via `traits.put(traitKey.toLowerCase(Locale.ROOT), level);` with no check against Wearable's TRAIT_TABLE. Wearable.traitBonus() later does `double[] definition = TRAIT_TABLE.get(key); if (definition == null) return 0;`, so a typo such as REINFORCE instead of REINFORCED loads without error and silently grants no protection. Unlike the Material/Name validation earlier in the same method, no log.warn fires here, so an admin has no signal that the armor piece is doing nothing.~~High
3~~P3~~WearableEquipListener javadoc promises drop-cancellation for non-droppable wearables that does not exist~~Delete the stale "On drop" javadoc paragraph, or implement a PlayerDropItemEvent handler honoring a real Droppable flag~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListener.java:17-31~~The class javadoc states `<p>On drop: if the wearable is marked as non-droppable the drop event is cancelled.` but the class declares only one @EventHandler, onArmorEquip(InventoryClickEvent), and neither Wearable nor wearables.yml has any droppable field. No PlayerDropItemEvent handler exists anywhere in the wearable package, so the documented drop protection never runs in Bartizan and any wearable can be dropped freely regardless of the (nonexistent) flag.~~High
4~~P3~~WearableEquipListener uses raw Keystone ChatUtil instead of the house BartizanChatUtil~~Import org.luckyraven.bartizan.util.BartizanChatUtil and call BartizanChatUtil.color(...) instead of Keystone's ChatUtil~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/listener/wearable/WearableEquipListener.java:13-58~~The file imports `org.luckyraven.keystone.util.ChatUtil` and sends `player.sendMessage(ChatUtil.color("&cYou are not authorized to equip this armor."));` instead of BartizanChatUtil, which wraps Keystone's ChatUtil with the %money_symbol% substitution and is what every other bartizan-plugin listener/command (WeaponDeathListener, ReloadCommand, the wearable command classes) uses to send messages. This is a direct violation of CLAUDE.md's stated chat-color convention for bartizan-plugin code.~~High
5~~P3~~Wearable.buildItem() stamps wt_<trait>/wr_base NBT that nothing in the wearable pipeline reads back~~Drop the per-trait and base-reduction NBT stamps from buildItem() unless an out-of-scope consumer needs them, since WearableService always resolves live values from the registry by wearableKey~~-~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/wearable/Wearable.java:276-284~~buildItem() stamps `builder.addTag(NBT_BASE_REDUCE, baseDamageReduction);` and, per trait, `builder.addTag(NBT_TRAIT_PREFIX + entry.getKey(), entry.getValue());`, but WearableService.resolveWearable() only ever reads the single NBT_KEY tag and looks everything else up live in the wearables map, and WearableConverter/WearableItemSerializer/WearableRefresher likewise only touch NBT_KEY. These per-trait/base tags are written onto every wearable item and never read anywhere in this scan's scope, so a later wearables.yml edit and an already-crafted item's stamped values can silently diverge with no functional consumer on either side.~~Medium
