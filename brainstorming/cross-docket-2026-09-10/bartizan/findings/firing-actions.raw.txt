# System: FA - firing-actions (Bartizan @ 0e18356)
1~~P1~~BiologicalAction.fireRay computes damage but the impact handler never applies it~~In fireRay's impactHandler call target.damage(damage, player) for LivingEntity hits (mirroring IncendiaryAction.applyIncendiaryImpact), or route through the raytracer's default damage path by not supplying a custom impactHandler~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/BiologicalAction.java:133-153~~fire() computes `double damage = data.getBaseDamage() * level + flatBonus;` and passes it into fireRay, which builds a RaytraceRequest with `.baseDamage(damage)` but a custom `.impactHandler(event -> { if (event.getHitEntity() instanceof LivingEntity target) { for (PotionEffect effect : effects) target.addPotionEffect(effect); } })`. RaytraceRequest's own javadoc states a custom impactHandler is "invoked ... instead of the default damage application" (bartizan-api/.../RaytraceRequest.java:20-22), and every other action type (Incendiary, Melee, Throwable) that supplies a custom handler explicitly calls target.damage(...) inside it - Biological's handler never does. Every charged biological shot therefore only applies potion effects and deals zero health damage on any normal server, regardless of Base_Damage/charge level configured. Confirmed at the Bartizan location - was Gangland WP-3.~~High
2~~P1~~ThrowableAction EXPLOSIVE detonation applies damage twice: vanilla createExplosion plus the manual entity loop~~Pass a damage-suppressing path (0 power or a custom no-entity-damage explosion helper) to createExplosion, or drop the manual target.damage loop and rely solely on the WeaponEntityDamageEvent/WeaponRaytraceImpactEvent pipeline for the configured amount~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/ThrowableAction.java:213-247~~detonate() for ThrowableType.EXPLOSIVE calls `world.createExplosion(center.getX(), center.getY(), center.getZ(), (float) data.getExplosionRadius(), false, false, player);` which is vanilla Bukkit explosion damage to every living entity in radius (the trailing booleans only suppress fire/block-breaking, not entity damage), and then immediately loops `world.getNearbyEntities(...)` and calls `target.damage(impactEvent.getDamage(), player)` on every LivingEntity in the same radius. Any player or mob within a thrown EXPLOSIVE weapon's blast radius takes vanilla explosion damage plus the full configured Explosion_Damage a second time. Confirmed at the Bartizan location - was Gangland WP-4.~~High
3~~P1~~GunWeaponParser/GunWeapon: Consumed_Amount defaulting to 0 gives an infinite magazine~~Reject or clamp Consumed_Amount to a minimum of 1 in GunWeaponParser (min(1) instead of min(0)), or have GunWeapon.consumeShot() treat a 0 consume rate as an authoring error and fall back to 1~~WeaponConsumeShotTest.gunWeapon_consumedAmountZero_infiniteMagazine (pins today's wrong behaviour, per its own javadoc "Pins Observation #13")~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/GunWeapon.java:46-51~~`public boolean consumeShot() { if (isMagazineEmpty()) return false; setCurrentMagCapacity(Math.max(0, getCurrentMagCapacity() - projectileData.getConsumed())); return true; }` is called from GunAction.java:46 on every shot. GunWeaponParser reads `projectile.get("Consumed_Amount").asInt().min(0).orDefault(0)`, so any gun YAML that omits Consumed_Amount gets projectileData.getConsumed() == 0, making the subtraction a no-op while consumeShot() keeps returning true - an unconfigured gun therefore never runs out of ammo. Confirmed at the Bartizan location, and already pinned by a red-to-flip unit test - was Gangland WP-13.~~High
4~~P1~~MeleeAction and ThrowableAction never call weapon.consumeShot(), so configured ammo/magazines never deplete~~Call weapon.consumeShot() (and gate on isMagazineEmpty()/getAmmunitionData()) in MeleeAction.activate() and ThrowableAction.activate() the same way GunAction/IncendiaryAction/BiologicalAction do~~WeaponConsumeShotTest.baseConsumeShot_decrementsByOne_forMeleeThrowableBiological (javadoc explicitly notes "this test does not claim melee/throwable weapons consume ammunition in real play")~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/MeleeAction.java:55-63~~MeleeAction.activate() only guards with `if (weapon.getReloadData() != null && weapon.isMagazineEmpty())` and never calls consumeShot() anywhere in the class (grep of the action package confirms only GunAction/IncendiaryAction/BiologicalAction call it). ThrowableAction.activate() (ThrowableAction.java:61-86) never references ammunitionData/reloadData/consumeShot at all - it only decrements the physical held ItemStack via decrementHeldStack(). Any melee or throwable weapon configured with a magazine (Ammunition:/Reload: sections) silently has unlimited uses because isMagazineEmpty() reads a currentMagCapacity that is never decremented. Confirmed for the melee/throwable half at the Bartizan location - was Gangland WP-14 (the Biological-ammo-NBT half of that finding is a reload/config-serialization concern outside this file set and was not re-verified here).~~High
5~~P2~~GunAction refunds exactly 1 round on a cancelled WeaponShootEvent regardless of the weapon's actual Consumed_Amount~~Refund weapon.getProjectileData().getConsumed() (or read back the amount actually subtracted by consumeShot()) instead of the hardcoded 1~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/GunAction.java:46-64~~`boolean consumed = weapon.consumeShot();` deducts projectileData.getConsumed() rounds (see GunWeapon.consumeShot(), GunWeapon.java:46-51), but the cancel branch a few lines later does `if (shootEvent.isCancelled()) { weapon.addAmmunition(1); return; }` - always exactly 1, not the amount just subtracted. For any weapon configured with Consumed_Amount > 1 (e.g. a shotgun), a plugin that cancels WeaponShootEvent leaves the shooter net-down ammo compared to before the (non-)shot. Confirmed at the Bartizan location - was Gangland WP-12.~~High
6~~P2~~BiologicalAction.fire() throws IndexOutOfBoundsException(-1) when Effects_Per_Level is configured empty~~Guard with `if (data.getEffectsPerLevel().isEmpty()) return;` (mirroring ThrowableAction's stun/smoke effects.isEmpty() guards) before indexing~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/BiologicalAction.java:115-116~~`List<PotionEffect> effects = PotionEffectParser.parseList(List.of(data.getEffectsPerLevel().get(Math.min(level, data.getEffectsPerLevel().size()) - 1)));` - when Effects_Per_Level is empty, size() is 0, so `Math.min(level, 0) - 1` evaluates to -1 and `.get(-1)` throws IndexOutOfBoundsException on every completed charge-and-release for that weapon, right after ammo/durability/recoil have already been applied. Confirmed at the Bartizan location - was Gangland WP-15.~~Medium
7~~P2~~PluginFireRegistry.onShutdown() clears the tracking set without reverting the placed FIRE blocks it tracks~~Iterate the tracked BlockVectors per world and set each still-FIRE block back to AIR before (or instead of) clearing the map, or register the block-revert task itself as shutdown-safe~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/fire/PluginFireRegistry.java:72-75~~`@Override public void onShutdown() { tracked.clear(); }` only forgets the bookkeeping; any Material.FIRE block placed by IncendiaryAction/ThrowableAction that is still burning at plugin shutdown (server stop/reload) is left in the world as a real fire block. Once tracked.clear() runs, PluginFireProtectionListener (the companion class referenced in this file's own javadoc) can no longer recognise those blocks as plugin-cosmetic, so on the next start any leftover fire behaves as vanilla fire able to spread/burn structures. Confirmed at the Bartizan location - was Gangland WP-38.~~High
8~~P2~~The five weapon action types diverge on durability wear and pre-fire cancellability, not just ammo~~Either give BiologicalAction/MeleeAction/ThrowableAction a Durability_On_Shot hook symmetric with Gun/Incendiary, and fire a cancellable pre-fire event from every action, or document the asymmetry explicitly in weapons.md so it is a decision, not an oversight~~WeaponConsumeShotTest (javadoc cites "weapons.md's divergence table, Observation #42")~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/BiologicalAction.java:92-126~~GunAction.java:71-74 and IncendiaryAction.java:99-101 both call `weapon.decreaseDurability(heldWeapon, onShot)` from `weapon.getDurabilityData().getOnShot()`; BiologicalAction.fire() (lines 92-126) and MeleeAction.activate() never call decreaseDurability anywhere in their classes, so a Durability_On_Shot value configured for a biological or melee weapon is silently never applied. Separately, only GunAction constructs and fires a cancellable `WeaponShootEvent` before the shot (GunAction.java:58-64) - Incendiary/Biological/Melee/Throwable have no equivalent pre-fire cancellation hook, so a listener that wants to veto a shot (e.g. a region-protection plugin) can only do it for guns. Confirmed at the Bartizan location - was Gangland WP-42.~~Medium
9~~P3~~BiologicalAction emits a literal section-sign color code instead of using BartizanChatUtil's &-code convention~~Replace the literal "§6"/"§e" characters with "&6"/"&e" and route the string through BartizanChatUtil.color() per house convention~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/BiologicalAction.java:72-73~~`ActionBarManager.send(player, "§6Charging... §e[" + "■".repeat(charge[0]) + "□".repeat(data.getMaxChargeLevel() - charge[0]) + "]");` hardcodes literal `§` bytes, while the same class's release message two lines away correctly uses `&`-codes ("&aReleased at charge level " + level, line 125). CLAUDE.md's house rule is explicit: colored chat/action-bar text must go through the ChatUtil/BartizanChatUtil `&`-code path, never a literal `§`. Confirmed at the Bartizan location - was Gangland WP-27.~~High
10~~P3~~ThrowableAction plays a raw Sound enum constant instead of going through SoundEffect/XSeries~~Replace with SoundEffect.playSounds/playSoundsAtLocation (or an XSound-resolved constant) so the sound survives version drift~~-~~bartizan-plugin/src/main/java/org/luckyraven/bartizan/weapon/action/ThrowableAction.java:309-313~~`world.playSound(center, Sound.ENTITY_GENERIC_EXPLODE, 2.0f, 1.8f);` in detonateStun() calls the raw Bukkit `Sound` enum directly, unlike every other sound call in this same file (activate()/detonate() use `SoundEffect.playSounds(...)`), and unlike CLAUDE.md's house rule that version-drifting enums (including Sound) must resolve through XSeries/SoundEffect, never a raw enum constant. Confirmed at the Bartizan location - was Gangland WP-28.~~High
