1~~P1~~RequirementHook minimum=1 fires onSuccess for every passing set, unlike Mode.ANY~~Make evaluateMinimum(min=1) short-circuit and dispatch onSuccess only for the first passing set, matching evaluateAny, or document the divergence explicitly~~RequirementHookTest (covers minimum=2 dispatch, not minimum=1)~~menu-core/src/main/java/org/luckyraven/oriel/core/requirement/RequirementHook.java:195-215~~evaluateMinimum() evaluates every RequirementSet (no short-circuit) then, when `passed.size() >= min`, runs `for (RequirementSet set : passed) { set.dispatchSuccess(dispatchCtx); }` for every passing set. evaluateAny() (the mode the class javadoc says minimum=1 "naturally generalises") instead returns on the *first* passing set and fires onSuccess for only that one. A hook built with `RequirementHook.of(sets, mode, 1)` (DeluxeMenus-style `minimum_requirements: 1`, semantically "any one of these") therefore fires onSuccess on every set that happens to pass, not just one. Since RequirementSet.onSuccess is documented as "where consumable gates spend their resource" (cooldown start, item consumption), a click satisfying two alternative "pay with A OR B" sets under minimum=1 double-charges instead of charging once. RequirementHookTest only exercises minimum=2, so this min=1 divergence is untested.~~High
2~~P2~~MenuRegistry.openPrevious() drops args, silently no-ops on a required argument_processor~~Have NavigationHistory retain (or MenuOpener re-derive) the args a menu was opened with, or skip/warn when the target menu's ArgumentProcessorSpec has a required processor instead of firing its fail chain out of a back-nav context~~-~~menu-core/src/main/java/org/luckyraven/oriel/core/registry/InMemoryMenuRegistry.java:77-86~~openPrevious() always re-opens with `new String[0]`: `return doOpen(player, previous.get(), new String[0]);`. doOpen() looks up the target menu's ArgumentProcessorSpec and, if a processor has a `required` action chain, `spec.bind()` fails on the empty args and `spec.fireRequired(player, result)` runs that chain via a HeadlessClickContext, then doOpen returns false — no menu (re)opens. So `menu_back`/`ClickContext.openPrevious()` into any menu registered with a required `argument_processor:` entry silently fails and fires that menu's "you must supply X" fail chain out of context, even though the player is just navigating backward, not opening fresh. The doc at line 84 acknowledges args are dropped but not that this can abort the open entirely.~~Medium
3~~P1~~ItemHoldingComponent contract never says whether COMMAND/NAVIGATION closes return held items~~Extend the ItemHoldingComponent/CloseReason contract to explicitly cover COMMAND and NAVIGATION (return items on both, since neither is a Bukkit-driven cursor-pickup close), and have menu close() implementations follow it~~-~~menu-core/src/main/java/org/luckyraven/oriel/core/component/ItemHoldingComponent.java:10-19~~The interface javadoc says the owning Menu "is responsible for returning `heldItems(Player)` to the player on shutdown closes (`PLUGIN_DISABLE` / `RELOAD` / `PLAYER_DISCONNECT`)" and that "User-initiated closes (`USER`) skip the return — Bukkit's normal cursor-pickup flow will surface the cursor stack". `CloseReason` also defines `COMMAND` ("a `close: true` YAML action or `ctx.closeMenu()`") and `NAVIGATION` ("a MenuFlow switched panels"), neither of which goes through Bukkit's own inventory-close cursor logic (they are programmatic closes), yet neither is listed among the "return items" reasons or the "skip" reason. A `DepositSlotComponent`-style implementation that follows this contract literally (treats "not one of the three listed reasons" as skip, like USER) would strand a player's deposited items when they click a "Cancel"/"Back" button or a MenuFlow panel switches away from a deposit slot — an item-loss path for exactly the consumable-slot use case this interface exists for.~~Medium
4~~P3~~AnimationTicker's int globalTick overflows after ~3.4 years of continuous uptime~~Use a long counter for globalTick (widen the Animatable/AnimatedComponent int contract or wrap/reset it deliberately) instead of letting int wrap negative~~-~~menu-core/src/main/java/org/luckyraven/oriel/core/animation/AnimationTicker.java:29,63-64~~`private int globalTick;` is incremented every tick forever via `globalTick++` in tickOnce(), which runs on a 1-tick BukkitTask for the plugin's whole uptime with no reset except `/menu reload`. After roughly 2^31 ticks (~3.4 years at 20 tps) it overflows to a negative int; AnimationMath.frameIndex() then does `Math.max(0, globalTick)`, clamping every animated component to frame 0 for the entire negative half of the int range (another ~3.4 years) before counting resumes. A long-running server that never issues `/menu reload` would see every animated menu title/lore/item freeze on frame 0 for years. Low likelihood given typical restart cadences, but deterministic and unguarded.~~Medium
5~~P2~~NavigationHistory.back() consumes the trail step even when the popped-to menu no longer exists~~Have openPrevious() (or NavigationHistory itself) verify the target name is still registered before consuming the step, or push it back on a failed doOpen so a stale entry doesn't desync the trail~~NavigationHistoryTest (covers normal pop order, not a since-unregistered target)~~menu-core/src/main/java/org/luckyraven/oriel/core/registry/NavigationHistory.java:53-60~~back() unconditionally does `trail.removeLast(); return Optional.ofNullable(trail.peekLast());` with no knowledge of whether the returned name is still a registered menu. InMemoryMenuRegistry.openPrevious() (registry.java:77-86) takes that name straight into doOpen(), and if the menu was removed by an intervening `/menu reload`/`unregister()` (registry.java:45-48, which only clears `factories`/`argSpecs`, never touches any player's NavigationHistory), doOpen returns false with no menu opening — but the trail has already permanently dropped that step. A player mid-back-navigation loses a step of their trail silently instead of getting a working back-nav or a clear failure, and repeated `menu_back` clicks will skip past the gap rather than retry it.~~Medium
