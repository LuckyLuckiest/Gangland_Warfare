1~~P2~~Legacy player_uuid->actor_uuid migration silently no-ops and loses data on MySQL~~Quote identifiers via each backend's own quoteIdentifier (backtick on MySQL) instead of hardcoding double quotes, or detect ANSI_QUOTES before relying on it~~HistoryServiceLegacyMigrationTest (SQLite only, does not cover MySQL)~~menu-data/database/src/main/java/org/luckyraven/oriel/database/history/HistoryService.java:131-168~~migrateLegacyColumn probes with `backend.query("SELECT player_uuid FROM " + quote(tableName) + " LIMIT 1", ...)` and quote() hardcodes `return '"' + identifier.replace("\"", "\"\"") + '"';`, on the stated assumption "both SQLite and MySQL (in ANSI_QUOTES mode) accept them" (line 32-33 javadoc). MySQL's default sql_mode does NOT include ANSI_QUOTES, so a double-quoted identifier is parsed as a string literal, making the probe throw a syntax-error SQLException for a real legacy table too, not just for "table/column absent"; that exception is caught and the menu is marked migrated without renaming anything. The next `delegate.registerLog(menuName)` schema diff then runs `ADD COLUMN actor_uuid` and `DROP COLUMN` for any live column not in the declared schema (confirmed in Keystone's `AbstractJdbcBackend.applySchema`, ` " DROP COLUMN " + quoteIdentifier(drop)`), which drops the still-present `player_uuid` column and its data. Keystone's own `MysqlBackend.quoteIdentifier` already uses backticks (`'`' + name...+'`'`) for its own generated SQL, confirming double-quote is wrong for this backend; root cause of the flawed assumption is shared with Keystone's `AuditLogService` (same caveat comment, same gap). Only SQLite is exercised by tests, per the task brief.~~High
2~~P1~~Audit-row insert failures never reach Diagnostics/oriel_faults, unlike the migration path~~Wrap delegate.logEvent (or its Keystone AuditLogService.logEvent) with a Diagnostics.report call on SQLException, mirroring what migrateLegacyColumn already does one method up~~HistoryServiceSqliteTest / DatabaseFaultSinkSqliteTest (neither covers a failing INSERT)~~menu-data/database/src/main/java/org/luckyraven/oriel/database/history/HistoryService.java:85-93~~logEvent() has no try/catch and no return value: `delegate.logEvent(menuName, playerUuid, actionType, payload);` runs straight through to Keystone's AuditLogService.logEvent, whose catch block only does `log.warn("Failed to write audit row for {}/{}: {}", ...)` with no Diagnostics report (root cause in Keystone AuditLogService.logEvent). Contrast with migrateLegacyColumn in the same file, which explicitly does `Diagnostics hub = Diagnostics.active(); if (hub != null) hub.report(rename, "history.migrate");` on failure so admins see it via the DatabaseFaultSink-backed oriel_faults table this very system ships. A dropped deposit/return audit row (e.g. transient MySQL pool exhaustion) is therefore invisible anywhere admins are told to look, only in the raw console log. DepositActions.maybeLog has no way to know the write failed either.~~Medium
3~~P2~~Failed item-snapshot serialization is persisted as an indistinguishable empty payload instead of being skipped~~encode() should return null (or a distinct sentinel) on IOException so maybeLog treats it like the documented null case, or skip logEvent entirely on failure~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/action/DepositActions.java:89-119~~`maybeLog` calls `history.logEvent(menuName, ctx.player().getUniqueId(), actionType, encode(snapshot));` unconditionally, but `encode()` catches IOException and returns `""` rather than propagating failure: `catch (IOException ex) { log.warn(...); return ""; }`. HistoryRow's own javadoc defines `payload` as either the Base64 blob or `null` for "no item payload" — a serialization failure instead writes a real row with an empty-string payload that is indistinguishable from a legitimate zero-item snapshot when an admin later reads the table, silently corrupting that row's meaning even though a warning is logged to console.~~Medium
