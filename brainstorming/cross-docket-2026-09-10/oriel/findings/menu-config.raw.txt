1~~P1~~ImportService.writeTree overwrites existing imported YAML with no exists check~~check Files.exists(outFile) before writing in writeTree; warn and skip (or version-suffix) on a same-run name collision or a pre-existing hand-edited file instead of silently truncating it~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/importer/ImportService.java:158-174~~writeTree() opens `Files.newBufferedWriter(outFile, StandardCharsets.UTF_8)` (default CREATE+TRUNCATE_EXISTING) with no prior existence check, called from the per-menu loop at lines 126-133 where `outFile = menuOutDir.resolve(safeFileName(menu.name()) + ".yml")`. Re-running `/menu import` after an admin hand-edited a previously-imported file under `menus/imported/<source>/` silently discards those edits with no warning. Two source menus whose names both sanitize to the same `safeFileName` (e.g. "Shop A" and "Shop-A" both to "shop_a") in one import run also silently clobber each other, losing one imported menu with zero report entry.~~High
2~~P2~~4 bundled demo menus missing from MenuConfigService's addExpectedFile list~~add addExpectedFile(new FileHandler(plugin, "<name>", MENUS_FOLDER, "yml")) calls for the 4 missing names alongside the existing 25~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/MenuConfigService.java:113-137~~The constructor registers exactly 25 `addExpectedFile(new FileHandler(plugin, "<name>", MENUS_FOLDER, "yml"))` calls that FolderLoader uses to seed `plugins/Oriel/menus/` on a fresh install, but `menu-plugin/src/main/resources/menus/` ships 29 yml files. `gangland_demo.yml`, `item_requirement_demo.yml`, `vault_economy_demo.yml` and `vault_group_demo.yml` are absent from that list and grep finds no other Java reference to any of the four names anywhere in the repo, so FolderLoader never extracts them on first run — they ship inside the jar but a fresh admin install can never see them.~~High
3~~P2~~AnvilClickContext's %input% reaches elevated op/permission commands unfiltered~~document the interaction in ActionParser's op/permission javadoc, or strip/escape a literal %input% before it reaches an elevated command's resolved string~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/loader/AnvilClickContext.java:36-50~~parseAnvilInput (ActionParser.java:430) dispatches on_confirm/on_cancel through a fresh AnvilClickContext whose placeholders() wrapper does `raw.replace("%input%", safeInput)` (line 47) before any other resolution. runElevated (ActionParser.java:648-673) resolves the op/permission command string via `ctx.placeholders().resolve(...)` with no distinction between an outer context and an anvil-fed one, so a menu author writing `anvil_input: { on_confirm: { op: "%input%" } }` hands the anvil-typing player the literal command that runs under temp-op or a granted PermissionAttachment. Neither this class's nor ActionParser's javadoc mentions the interaction and no test exercises anvil_input combined with op/permission.~~Medium
4~~P2~~give_slot has no upper-bound check unlike SlotLoader's slot-range validation~~clamp/validate give_slot against the chest's real slot count at parse time (mirror SlotLoader's slot>=size check) and emit a config.slot.range-style report instead of reaching the live inventory~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/action/DepositActions.java:70-75~~give_slot is parsed as `NodeReader.of(mapping, report).get("give_slot").asInt().min(0).required().orElse(-1)` (line 71) with only a lower bound, then used directly as `chest.bukkitInventory().getItem(slot)` (line 75) at click time with no upper-bound check. Every other slot-index path in this system validates against the menu's real size — SlotLoader.load reports `config.slot.range` and skips when `slot < 0 || slot >= size` (SlotLoader.java:56-60) — but give_slot bypasses that convention entirely, so an admin typo like `give_slot: 99` in a 1-row (9-slot) chest hands an out-of-range index straight to the live Bukkit inventory.~~Medium
