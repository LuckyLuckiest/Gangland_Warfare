1~~P0~~allow_elevated_actions toggle stays stale for one full /menu reload cycle (elevated-action gate lag)~~Reload config.yml before the FILE-phase menu re-parse runs, e.g. call plugin.reloadConfig() at the top of OrielContext.reloadBeans()/bootstrap() instead of inside ManagedDatabaseBackend.onInitialize~~-~~menu-plugin/src/main/java/org/luckyraven/oriel/plugin/config/PersistenceConfig.java:164-169~~PersistenceConfig.ManagedDatabaseBackend (a DATABASE-phase BeanLifecycle bean) is the ONLY place that calls `plugin.reloadConfig()` on reload: `if (delegate != null && delegate.isConnected()) return; plugin.reloadConfig(); open();`. BeanFactory.reloadLifecycleBeans() (Keystone) runs onInitialize(false) forward by Phase order FILE, then DATABASE — and FileManager (FILE phase, ConfigLoaderConfig.java) cascades into MenuConfigService.initialize() first, which re-parses every menu YAML through ActionParser's elevatedActionsGate, itself OrielSettings.allowElevatedActions() reading plugin.getConfig() live. Because that FILE-phase parse happens strictly before this DATABASE-phase reloadConfig() call in the same reload pass, an admin who edits config.yml's allow_elevated_actions and runs /menu reload gets menus re-parsed against the file's PRE-edit value; the new value only takes effect on the NEXT reload. Disabling the flag to lock down op:/permission: menu actions therefore silently fails to take effect for one full reload cycle, while /menu reload reports success — a real, always-reproducible permission-gate staleness bug with no admin-visible warning.~~High
2~~P1~~Failed /menu reload DB reconnect aborts the whole lifecycle cascade and permanently stops AnimationTicker~~Catch and log the SQLException/IllegalStateException inside ManagedDatabaseBackend.open() instead of rethrowing, or have Keystone's BeanFactory.reloadLifecycleBeans() wrap each bean's onClear/onInitialize call in try/catch so one failure doesn't skip the rest~~-~~menu-plugin/src/main/java/org/luckyraven/oriel/plugin/config/PersistenceConfig.java:110-129~~ManagedDatabaseBackend.open() rethrows as `throw new IllegalStateException("Database backend '" + type + "' could not connect", ex);` when the configured backend (e.g. a mistyped mysql host after switching database.type) fails to connect. Keystone's BeanFactory.reloadLifecycleBeans() runs every BeanLifecycle bean's onInitialize(false) in one forward loop with no per-bean try/catch, so this exception aborts the loop entirely; AnimationConfig's AnimationTicker (Phase.LIFECYCLE, ordered after Phase.DATABASE) already had its BukkitTask cancelled by the earlier onClear() pass (`stopTask(); task = null;`) and never gets its onInitialize(false) call to restart it. Every animated menu (title-frame/burn-bar animations) silently stops advancing for every viewer, with no error shown beyond ReloadCommand's generic "Reload failed: <message>", until an admin runs another /menu reload that succeeds end to end. This is a genuine uptime leak triggered by an ordinary admin mistake (bad database.type config) that has nothing to do with animations.~~High
3~~P2~~UpdateChecker resource id is hardcoded to 0 with no config key, making /menu download and the startup check permanently dead~~Add a resource-id (or "spigot_resource_id") key to config.yml and pass OrielSettings' value into KernelConfig.updateChecker(...) instead of the literal 0, or remove DownloadCommand/UpdateCheckBootstrap until Oriel is published~~-~~menu-plugin/src/main/java/org/luckyraven/oriel/plugin/config/KernelConfig.java:193-196~~`return new UpdateChecker(plugin, permissionManager, pluginName, 0);` hardcodes resourceId to 0. Keystone's UpdateChecker treats any id <= 0 as a sentinel that short-circuits every method (getLatestVersion, updateAvailable, downloadLatestVersion, checkAtStartup) to a no-op, and there is no config.yml key or constructor path in KernelConfig to override it. Consequently DownloadCommand's `/menu download` always reports "Oriel is already on the latest version" and UpdateCheckBootstrap's startup check never actually queries SpigotMC — a fully wired, help-documented command and boot-time check that can never do anything on any server running this jar, with nothing telling the admin it is inert.~~High
4~~P2~~DevPaginatedMenuConfig's @Configuration is not actually pinned to Phase.DATABASE as its own doc claims, re-opening the race it says it prevents~~Add phase = Phase.DATABASE to DevPaginatedMenuConfig's @Configuration annotation to match the documented invariant (or delete the demo config, since it is already marked for pre-release removal)~~-~~menu-plugin/src/main/java/org/luckyraven/oriel/plugin/config/DevPaginatedMenuConfig.java:23-31~~The class javadoc states: "Phase note: pinned to {@link Phase#DATABASE} (between KERNEL and CONFIG) so the source is in the registry before MenuConfigService (CONFIG phase) fires its onInitialize pass... Without this, a CONFIG-phase intra-phase ordering race could cause the YAML loader to log a spurious config.paginated.source warning." The actual annotation is bare `@Configuration` with no phase argument (defaults to Phase.CONFIG, the same phase as ConfigLoaderConfig's MenuConfigService), unlike DemoComponentsConfig/DemoItemProvidersConfig which do carry the explicit `@Configuration(phase = Phase.DATABASE)` the comment describes. Since devPaginated(MenuRegistry) has no bean-parameter dependency edge to menuConfigService(...), their relative construction order within Phase.CONFIG depends on incidental package-scan order rather than the documented safeguard, so the paginated_demo.yml source registration is exposed to exactly the race the comment says was solved.~~High
5~~P2~~ImportCommand triggers a full context.reloadBeans() instead of the lighter reloadMenus() path, closing every player's open menu server-wide~~Change ImportService's reload callback in ConfigLoaderConfig.importService(...) from context::reloadBeans to context::reloadMenus, matching the narrower path CreateCommand/EditCommand already use via MenuReloader~~-~~menu-plugin/src/main/java/org/luckyraven/oriel/plugin/config/ConfigLoaderConfig.java:66-81~~`return new ImportService(importers, menusDir, templatesDir, context::reloadBeans);` wires the import flow to OrielContext.reloadBeans(), which runs the full BeanLifecycle cascade (bounces the database pool, restarts the animation ticker, and — via DefaultOpenMenuTracker.onPreClear() — force-closes every online player's currently open menu with CloseReason.RELOAD) purely to make newly-imported YAML menus visible. EditorConfig's MenuReloader bean deliberately calls the narrower context.reloadMenus() for the same "a menu file just appeared/changed" scenario specifically "so the editor takes this narrower path" instead of bouncing the database pool. Running /menu import therefore disrupts every other online player's unrelated menu session and briefly drops the DB connection, when reloadMenus() would make the imported menus available with no such side effects.~~Medium
