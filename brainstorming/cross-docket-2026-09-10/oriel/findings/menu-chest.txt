1~~P0~~ChestMenu.close skips returnHeldItems on CloseReason.USER, destroying DepositSlotComponent items on Escape~~Return (or drop at the player's feet) held ItemHoldingComponent items on USER closes too — Bukkit only restores the cursor stack, never a custom inventory's slot contents~~DepositSlotComponentTest and DepositSlotMultiSlotStressTest both assert today's wrong behaviour and must be flipped with the fix~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/ChestMenu.java:380-386~~close(Player,CloseReason) drains held items only for non-user reasons: `if (reason != CloseReason.USER) { returnHeldItems(player, reason, true); }`. A DepositSlotComponent's stack physically occupies a slot in the plugin-created Bukkit Inventory, not the player's cursor, so the javadoc's rationale ("Bukkit's normal close flow surfaces the cursor stack to the player") does not apply to it — Bukkit discards a custom inventory's slot contents on close. Verified that no other path compensates: ChestMenuCloseListener.java:70 only calls `target.onClose(event, player)`, and ChestMenu.onClose (ChestMenu.java:422-429) merely runs the user's on_close handler, never returnHeldItems. A player who deposits an item and presses Escape loses it permanently.~~High
2~~P0~~MenuFlow.switchInternal and end() abandon DepositSlotComponent items on every panel switch~~Close the outgoing panel's menu with a non-USER CloseReason before building the next panel, and pass an explicit reason in end(), so ItemHoldingComponent contents drain to the player~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/flow/MenuFlow.java:165-185~~switchInternal builds a fresh ChestMenu for the target panel and swaps it in without ever closing the outgoing one: `ChestMenu newMenu = builder.build(); currentMenu = newMenu; currentPanelId = panelId; newMenu.open(viewer);`. Any item sitting in a DepositSlotComponent slot on the old panel is abandoned inside the discarded Inventory. end() is no better — MenuFlow.java:153 calls `currentMenu.close(viewer)`, the reason-less overload, which routes to the USER path that finding 1 shows never returns held items. So a payment, bid, or appraisal slot inside a flow panel loses its contents on every switchTo, back, and end, and no MenuFlow code path transfers them.~~High
3~~P1~~ChestMenuCloseListener fires the outgoing menu's on_close handler during internal navigation~~Move the `target.onClose(event, player)` dispatch inside the same `currentMenuOf(player) == target` guard already used for the veto and untrack branches~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/listener/ChestMenuCloseListener.java:56-70~~ChestMenu.open() tracks the incoming menu before calling player.openInventory(), so when a navigation triggers Bukkit's implicit close of the outgoing inventory, `MenuTracking.tracker().currentMenuOf(player) == target` is already false for that outgoing menu. The listener correctly skips the veto and untrack block in that case, but `target.onClose(event, player);` sits after the closing brace at line 70 and runs unconditionally. Every navigation into a submenu therefore fires the parent menu's YAML on_close actions — message, sound, command — as though the player had genuinely left the menu, when they only clicked through to a child screen.~~High
4~~P2~~PaginatedListPageState.itemAt never clamps a stale page after the backing list shrinks~~Clamp the stored page against pageCount(player, ctx) - 1 inside itemAt or currentPage, mirroring the clamp PaginatedChestMenu.render already applies~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/internal/PaginatedListPageState.java:56-61~~itemAt uses whatever page was last stored with no upper bound against the freshly fetched list: `int idx = page * slotsPerPage + regionIndex; return idx < items.size() ? items.get(idx) : null;`. nextPage clamps on the way up, but nothing re-clamps when the list later shrinks — so after invalidate(player) runs from a cycle_sort, cycle_filter or set_search action while the viewer sits on a high page, every region cell renders null and the player sees a blank page instead of snapping back to the last valid one. PaginatedChestMenu's whole-menu pagination avoids exactly this with an explicit clamp on every render, so the two pagination systems diverge on the same trigger.~~Medium
5~~P2~~PaginatedControlState's four per-viewer maps never evict a departed player~~Clear a UUID's sortIndex, filterIndex, searchQuery and descending entries on menu close or PlayerQuitEvent instead of only on the whole-state clear driven by /menu reload~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/internal/PaginatedControlState.java:25-28~~The viewer state is four plain HashMaps — `private final Map<UUID, Integer> sortIndex = new HashMap<>();` plus filterIndex, searchQuery and descending — written by cycleSort, cycleFilter, setSearchQuery and toggleDirection and never removed per player. The only bulk removal is PaginatedControlRegistry.unregister/clear, which fires on /menu reload and drops the whole menu's state. Since the registry holds one state object per registered menu name for the life of the process, a public server accumulates four stale entries for every unique visitor who ever touched sort, filter or search on that menu. Growth is tied to player turnover rather than concurrent player count, so it never plateaus between reloads.~~Medium
6~~P2~~ChestMenu.rerenderSlot omits the wasTouched blanking that renderAll performs~~Have rerenderSlot check view.wasTouched() after renderOne and blank the slot and its handlers when the component rendered nothing, matching renderAll~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/ChestMenu.java:452-463~~renderAll treats an untouched ChestSlotView as a deliberate no-render and clears the slot: `if (!view.wasTouched()) { inventory.setItem(entry.getKey(), null); clearHandlers(entry.getKey()); }`. rerenderSlot has no equivalent — it calls `renderOne(slot, component, viewer);` then applyFillIfEmpty and returns, never inspecting wasTouched(). PaginatedNavComponent.renderInto returns early without touching the SlotView at a page boundary by design, so refreshing such a slot through the public rerenderSlot API leaves the previous render's arrow item and its click handler in place, while the framework's own rerender() path would have cleared them. Consumers of the published builder API hit the inconsistent behaviour; internal callers use rerender() and do not.~~Medium
7~~P0~~ChestMenu.adoptComponentsFrom clears a slot and its handlers for a component the fresh render dropped without returning that component's held items first, destroying deposits on every rerender that drops a slot~~Before inventory.setItem(oldSlot, null), if the outgoing component holds items for the viewer, run the return path for that slot~~ChestMenuTest (fix round 1: verifies returnHeldItemsForSlot runs before the slot is cleared, no double-return path)~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/ChestMenu.java:466-478 (adoptComponentsFrom)~~adoptComponentsFrom (:466-478) clears a slot and its handlers for any component the fresh render dropped; if that component is an ItemHoldingComponent holding a stack, it leaves components with no return path, so returnHeldItems (:673, the close-time drain) can never reach it - the item is gone from the inventory and unreturnable the moment MenuFlow.rerender() drops a slot. Found in the Oriel 0.8.0 G1+G2 review (Critical 1) alongside the deposit-persistence work (menu-chest #1/#2, this docket's rows 1-2); FIXED in the same fix round - adoptComponentsFrom now returns via the new private returnHeldItemsForSlot (:713-740) before clearing, with no double-return path against the normal close drain.~~High
8~~P1~~Deposit-slot items are not recovered after an ungraceful shutdown (kill -9) or crash - there is no on-disk/startup-recovery path for held items, only the in-session return paths~~Accepted as out of scope for the 2026-09-14 decoupling wave (ruling W21): Gangland's WS2 workstream no longer parks items in Oriel menus (re-targeted onto Keystone's own keystone-inventory, R10), so no Gangland gate depends on this; tracked as an Oriel backlog item for its own roadmap~~-~~docs/USE-CASE-ENABLERS.md:77-86 (A3 requirement); menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/flow/MenuFlow.java, ChestMenu.java (in-session return paths only)~~The Oriel 0.8.0 G1+G2 review found deposit persistence had three halves: menu-chest #1 (Escape close, this docket's row 1) and #2 (panel switch, row 2) are covered by the in-session return paths and both fixed; the third half - USE-CASE-ENABLERS.md's A3, surviving a kill -9 or crash via an on-disk write plus startup recovery of any still-held deposit - was never built and has no in-session mechanism that could cover it. Orchestrator ruling W21 (2026-09-17) accepted this gap in writing rather than scheduling it inside the decoupling wave; this row exists so the acceptance is recorded, not only in a chat/ruling log.~~High
9~~P0~~Oriel's own ChestMenuCloseListener/ChestMenu pair loses held items on the same veto+disconnect and programmatic-close/menu-switch paths Keystone's keystone-inventory E6.1 found and fixed in its base MenuListener/ChestMenu~~Port Keystone's E6.1 round-2/3 fix shape to Oriel's own listener pair: on the vetoed tracked-close branch record a pending reopen per service/consumer, consume it on PlayerQuitEvent and return the held items with a disconnect-equivalent reason (quit fires before player data is saved); drain any pending reopen from closeAll/shutdown/reload too, not only from onQuit; give programmatic close(Player) / menu-switch a documented, tested contract for whether it returns held items (Oriel's MenuFlow already routes switches through a real CloseReason - verify it does not skip the veto/disconnect race the same way)~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/listener/ChestMenuCloseListener.java; menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/ChestMenu.java (close/veto path)~~Keystone's keystone-inventory E6.1 review (2026-09-17, Critical F1 + a fix-round-1 re-review finding) found that a menu veto followed by the player disconnecting before the scheduled reopen fires loses any held ItemHoldingComponent contents - CraftBukkit closes the inventory before PlayerQuitEvent (SPIGOT-5799), so onQuit finds nothing tracked and the item is gone; Keystone fixed this in keystone-inventory (InventoryService per-service pendingReopen, consumed atomically on quit and drained on closeAll/shutdown, E6.1 fix rounds 2-3, commit 53de9e6) but the same veto+disconnect shape exists in Oriel's own, structurally similar ChestMenuCloseListener/ChestMenu pair and was never independently checked or fixed there - this is the Oriel-track twin row the Keystone review's orchestrator ruling (W26) named for the docket step. Not exercised by any Oriel test; needs its own red-first case mirroring Keystone's InventoryServiceLifecycleTest/MenuListenerTest coverage.~~Medium
10~~P1~~MenuFlow.suspend()/resume() keep the old inventory handle; after an anvil/chat detour the next switchTo(same panel)/rerender() adopts into the inventory the detour already Bukkit-closed, leaving the player with no open window~~Track that a detour happened (a `detoured`-style flag set in suspend(), cleared only once a genuine reopen happens) and force the full close+reopen path instead of the in-place adopt/rerender path while it is set - port Keystone keystone-inventory 1.11.0's MenuFlow fix shape (commit 6ea96e5) to Oriel's own flow/MenuFlow.java~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/flow/MenuFlow.java:184-192 (suspend/resume)~~Found by the Keystone keystone-inventory E6.2 review (2026-09-20, Critical 1) while porting Oriel's own flow package: suspend()/resume() only toggle a `suspended` boolean, never null or otherwise flag the live `currentMenu` handle. An external detour (anvil, sign, etc.) Bukkit-closes that inventory out from under the flow; when the caller then calls switchTo(<same panel>) (the documented re-render-in-place pattern, mirrored in Gangland's own `ShopAdminView.java:426-429`-style usage before WS1's scoreboard-only edits), MenuFlow's in-place branch (or rerender()) calls adoptComponentsFrom on an inventory that no longer has an open viewer - the player is left with no window while the flow object still thinks it is live. Keystone independently found and fixed the same bug in its own keystone-inventory base (commit 6ea96e5, a `detoured` flag): Oriel's shipped code has the identical shape at MenuFlow.java:184-192 and was never independently checked or fixed on this track. Cross-reference: this docket's row 9 (OR-CH-09, the veto+disconnect twin) - both are Oriel-track ports of Keystone keystone-inventory fixes not yet applied here. Not exercised by any Oriel test.~~High
