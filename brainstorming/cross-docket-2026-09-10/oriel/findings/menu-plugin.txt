1~~P1~~A failed database reconnect on /menu reload aborts reloadLifecycleBeans and leaves AnimationTicker cancelled~~Catch and log the connection failure inside ManagedDatabaseBackend.open() rather than rethrowing, so the reload cascade continues to the LIFECYCLE phase~~-~~menu-plugin/src/main/java/org/luckyraven/oriel/plugin/config/PersistenceConfig.java:110-129~~ManagedDatabaseBackend.open() rethrows a connection failure as `throw new IllegalStateException("Database backend '" + type + "' could not connect", ex);` at line 125. Keystone's BeanFactory.reloadLifecycleBeans (BeanFactory.java:333-350) runs three bare loops — onPreClear, onClear, then `for (BeanLifecycle bean : forward) { bean.onInitialize(false); }` — with no per-bean try/catch, so that exception aborts the forward loop. AnimationConfig is `@Configuration(phase = Phase.LIFECYCLE)`, ordered after PersistenceConfig's DATABASE phase, so its AnimationTicker has already had its BukkitTask cancelled by the earlier onClear pass and never receives the onInitialize call that would restart it. An ordinary admin mistake such as a mistyped mysql host therefore silently freezes every animated menu for every viewer until a later reload succeeds end to end, with only ReloadCommand's generic failure message shown.~~High
2~~P2~~UpdateChecker is constructed with a hardcoded resourceId of 0, making /menu download and the startup check permanently inert~~Add a resource-id key to config.yml and pass it through KernelConfig.updateChecker, or remove DownloadCommand and UpdateCheckBootstrap until Oriel is published~~-~~menu-plugin/src/main/java/org/luckyraven/oriel/plugin/config/KernelConfig.java:193-196~~The bean method returns `new UpdateChecker(plugin, permissionManager, pluginName, 0);` with the resource id as a literal zero and no config key anywhere to override it. Keystone's UpdateChecker documents that value as a sentinel — "resourceId is treated as a sentinel: anything <= 0 short-circuits every method to a safe no-op" (UpdateChecker.java:31) — and gates on `return resourceId > 0;` (UpdateChecker.java:183). So /menu download always reports that Oriel is already up to date and UpdateCheckBootstrap never queries SpigotMC, on every server running this jar. A fully wired, help-documented command and a boot-time check are shipped dead with nothing telling the admin they are inert.~~High
3~~P2~~ImportService is wired to context::reloadBeans, so /menu import closes every online player's open menu~~Wire ImportService's reload callback to context::reloadMenus, the narrower path EditorConfig's MenuReloader already uses for the same scenario~~-~~menu-plugin/src/main/java/org/luckyraven/oriel/plugin/config/ConfigLoaderConfig.java:66-81~~The bean is built as `return new ImportService(importers, menusDir, templatesDir, context::reloadBeans);`. OrielContext.reloadBeans() runs the full BeanLifecycle cascade, which bounces the database pool, restarts the animation ticker, and force-closes every online player's open menu with CloseReason.RELOAD via DefaultOpenMenuTracker.onPreClear — all merely to make newly imported YAML visible. EditorConfig's MenuReloader deliberately calls the narrower context.reloadMenus() for the same "a menu file just changed" case. An admin running /menu import therefore interrupts every unrelated player's menu session and briefly drops the database connection.~~Medium
4~~P3~~DevPaginatedMenuConfig's javadoc claims a Phase.DATABASE pin its bare @Configuration annotation does not apply~~Add phase = Phase.DATABASE to the annotation to match the documented invariant, or delete the demo config as its own comment already proposes~~-~~menu-plugin/src/main/java/org/luckyraven/oriel/plugin/config/DevPaginatedMenuConfig.java:23-31~~The class javadoc states the class is "pinned to Phase.DATABASE (between KERNEL and CONFIG) so the source is in the registry before MenuConfigService (CONFIG phase) fires its onInitialize pass", and warns that without it "a CONFIG-phase intra-phase ordering race could cause the YAML loader to log a spurious config.paginated.source warning". The actual annotation at line 30 is a bare `@Configuration`, which defaults to Phase.CONFIG — the same phase as ConfigLoaderConfig's MenuConfigService — while the sibling DemoComponentsConfig and DemoItemProvidersConfig do carry the explicit `@Configuration(phase = Phase.DATABASE)` the comment describes. Since devPaginated(MenuRegistry) has no bean-parameter dependency edge on menuConfigService, their relative order within CONFIG falls to incidental scan order. Impact is limited to a spurious warning for a dev-only demo menu already marked for pre-release removal.~~High
