1~~P1~~ImportService.writeTree truncates an existing imported YAML with no existence check~~Check Files.exists(outFile) before writing and warn-and-skip or version-suffix on a collision, instead of silently truncating~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/importer/ImportService.java:158-174~~writeTree opens the destination with `Files.newBufferedWriter(outFile, StandardCharsets.UTF_8)`, whose default CREATE plus TRUNCATE_EXISTING semantics overwrite unconditionally, and no existence check precedes it. The per-menu loop at lines 126-133 resolves `outFile = menuOutDir.resolve(safeFileName(menu.name()) + ".yml")`. Re-running /menu import therefore discards any hand-edits an admin made to a previously imported file under menus/imported/<source>/ with no warning and no report entry. Worse, safeFileName collapses every non-alphanumeric character to an underscore, so two source menus named "Shop A" and "Shop-A" both resolve to shop_a and silently clobber each other inside a single import run — one imported menu is simply lost.~~High
2~~P2~~Four bundled demo menus are missing from MenuConfigService's addExpectedFile list and never reach a fresh install~~Add addExpectedFile entries for gangland_demo, item_requirement_demo, vault_economy_demo and vault_group_demo alongside the existing 25~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/MenuConfigService.java:113-137~~The constructor registers exactly 25 `addExpectedFile(new FileHandler(plugin, "<name>", MENUS_FOLDER, "yml"))` calls, which FolderLoader uses to seed plugins/Oriel/menus/ on a fresh install, but menu-plugin/src/main/resources/menus/ ships 29 yml files — I confirmed both counts directly. gangland_demo.yml, item_requirement_demo.yml, vault_economy_demo.yml and vault_group_demo.yml are absent from the list, and a repo-wide grep finds zero Java references to any of the four names. They ship inside the jar but are never extracted, so a fresh admin install can never see them — including both Vault demos, the only shipped examples of the economy and group integrations.~~High
3~~P2~~AnvilClickContext substitutes raw player-typed %input% into elevated op and permission command strings~~Refuse or escape a %input% token inside an elevated op/permission command string, and document the interaction in ActionParser's elevated-action javadoc~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/loader/AnvilClickContext.java:36-50~~The constructor wraps the placeholder provider so the anvil's typed text is substituted first: `String withInput = raw.contains("%input%") ? raw.replace("%input%", safeInput) : raw;`, where `String safeInput = input == null ? "" : input;` performs no sanitization at all — the surrounding comment shows "safe" means only that the input is not itself re-expanded as a placeholder. ActionParser.runElevated (ActionParser.java:648-673) resolves an op or permission action's command through `ctx.placeholders().resolve(player, command)` without distinguishing an anvil-fed context, so a menu author writing an anvil_input on_confirm of `op: "%input%"` lets the typing player dictate the literal command executed under temporary op. The temp-op and PermissionAttachment grants are themselves correctly revoked in finally blocks, and allow_elevated_actions defaults to false, which bounds this to a trusted-author footgun rather than an open hole — but nothing in the code or docs flags the combination.~~Medium
4~~P2~~give_slot validates only a lower bound and hands an out-of-range index to the live Bukkit inventory~~Validate give_slot against the menu's real slot count at parse time, mirroring SlotLoader's config.slot.range report~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/action/DepositActions.java:70-75~~give_slot is parsed with a lower bound only — `NodeReader.of(mapping, report).get("give_slot").asInt().min(0).required().orElse(-1)` — and guarded at runtime only for negatives via `if (slot < 0) return NO_OP;`. The click handler then calls `chest.bukkitInventory().getItem(slot)` with no upper-bound check. Every other slot-index path in this system validates against the menu's real size: SlotLoader.load reports config.slot.range and skips when `slot < 0 || slot >= size` (SlotLoader.java:56-60). An admin typo such as give_slot 99 in a one-row nine-slot chest therefore reaches the live inventory and throws out of the click handler instead of being caught as a config error at load time.~~Medium
5~~P1~~MenuConfigService.registerMenu never calls the factory, so ChestMenuBuilder.build validation is deferred to first open~~Invoke factory.get() once during registerMenu so build()'s IllegalStateExceptions surface as ConfigReport entries with file and field at reload time~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/MenuConfigService.java:265-275~~ChestMenuLoader returns a Supplier<Menu> whose lambda calls builder.build() fresh per open, by design so per-player state cannot leak. registerMenu only does `registry.register(name, factory);` and never calls factory.get(), so ChestMenuBuilder's own range checks — `if (slotIndex < 0 || slotIndex >= size) { throw new IllegalStateException("slot " + slotIndex + " out of range [0, " + size + ")"); }` at ChestMenuBuilder.java:314-315, plus the region row/col throws at lines 393 and 398 — never run during /menu reload. A malformed pagination button slot or region value passes reload with zero report entries and throws only when a player opens the menu, where InMemoryMenuRegistry.doOpen catches it generically and logs a warning carrying no file, line or field. That directly contradicts PLAN section 7's "Builder validation runs on .build(), not on first open. Fail early." Reload reports success while the menu is unopenable.~~High
6~~P1~~Paginated previous_button, next_button and search_button slots skip the upper-bound check SlotLoader enforces~~Validate those three slots against rows times 9 in parseSlotMaterial and parseSearchButton, mirroring SlotLoader's config.slot.range report~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/loader/ChestMenuLoader.java:196-207~~parseSlotMaterial (used for paginated.previous_button and next_button) and parseSearchButton both read the slot with a lower bound only — `int slot = r.get("slot").asInt().min(0).required().orElse(-1);` at line 200 and the identical line at 484 — then guard only with `if (slot < 0 || matName.isEmpty()) return null;`. SlotLoader.load, which every menu type's main slots block routes through, instead reports config.slot.range and skips whenever `slot < 0 || slot >= size` (SlotLoader.java:56-60). A typo such as previous_button slot 999 in a three-row chest passes the loader silently and only surfaces through finding 5's deferred build, so three of the loader's own slot-reading call sites bypass the range convention the rest of the system follows.~~High
7~~P1~~ItemDefinitionWriter.isNamespacedReference misses dash-prefixed MaterialSource refs, so the editor flattens dynamic heads~~Extend isNamespacedReference to treat a registered MaterialSource dash prefix as dynamic, mirroring ItemDefinitionLoader's dash dispatch~~ItemDefinitionWriterRoundTripTest does not cover this case~~menu-data/config/src/main/java/org/luckyraven/oriel/config/loader/ItemDefinitionWriter.java:139-145~~ItemDefinitionLoader resolves a scalar item value with a dash before any colon through the MaterialSourceRegistry — `MaterialSource source = dash > 0 && sourceRegistry != null ? sourceRegistry.lookup(itemRef.substring(0, dash)) : null;` (ItemDefinitionLoader.java:413-416) — so head-, basehead-, texture- and the soft-dep prefixes resolve per player at render time exactly as dynamically as a namespace:key reference. ItemDefinitionWriter.isNamespacedReference recognises only the colon form: `int colon = text.indexOf(':'); return colon > 0 && colon < text.length() - 1;`. A dash reference has no colon, so it returns false. When an admin drops a physical item into the in-game editor to reset a slot icon, writeInto sees neither animated nor namespaced and overwrites `item: head-%player_name%` with the resolved literal material, permanently destroying the per-player skin reference on save — the same loss the writer's javadoc says it avoids for the colon form.~~High
8~~P2~~RequirementParser.parseItem uses raw Material.matchMaterial instead of the house MaterialLookup convention~~Resolve the item requirement's material through MaterialLookup.parse so it matches every other material lookup in the system~~RequirementParserTest~~menu-data/config/src/main/java/org/luckyraven/oriel/config/loader/RequirementParser.java:344-356~~parseItem resolves the material with `Material material = Material.matchMaterial(materialName.trim());` and reports config.requirement.item.material when it returns null. Every other material lookup in this system — ChestMenuLoader's slot buttons, ItemDefinitionLoader, TraderMenuLoader — goes through MaterialLookup.parse, which is deliberately XMaterial-backed precisely because material enum names drift across Minecraft versions (MaterialLookup's javadoc at line 17 states this rationale). A type item requirement naming a material whose constant was renamed across versions therefore fails to resolve on a server where the identical name inside a slot's item: field resolves fine, so a gate copy-pasted from a working slot definition can be permanently unsatisfiable while looking correct.~~Medium
9~~P2~~NbtApplier silently narrows an int_array value that NbtSchemaValidator already reported as an ERROR~~Gate the nbtApplier construction on the report having no errors for the nbt block, so a value the validator rejected is never written~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/loader/NbtApplier.java:82-91~~NbtSchemaValidator reports config.item.nbt.list.range and returns when an int_array entry exceeds int range (NbtSchemaValidator.java:58-64). ItemDefinitionLoader nonetheless builds the applier from the same mapping unconditionally: `NbtSchemaValidator.validate(nbtMapping, reader.report()); if (isNbtApiAvailable()) { nbtApplier = NbtApplier.applierFor(nbtMapping, reader.report()); }` (ItemDefinitionLoader.java:508-510). NbtApplier's int_array branch re-reads the value as a Long and narrows without any range recheck — `out[i] = v.intValue();` — and byte_array does the same with byteValue(). The rendered item therefore carries a silently wrapped garbage integer on every render even though /menu reload already told the admin the value was invalid; the class javadoc says it "trusts that validation and just writes", but nothing gates on whether validation actually passed.~~Medium
