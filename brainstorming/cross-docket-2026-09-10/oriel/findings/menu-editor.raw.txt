1~~P1~~MenuEditor.open() silently discards a player's unsaved editor session via previous.exit()~~In MenuEditor.open(), check previous.session().dirty() before replacing the controller and route through previous.requestExit() (which opens ExitPromptScreen) instead of calling previous.exit() directly~~-~~menu-editor/src/main/java/org/luckyraven/oriel/editor/MenuEditor.java:117-121~~When a player who already has an editor session open runs /menu edit again (same or a different menu), MenuEditor.open() calls `EditorController previous = active.remove(...); if (previous != null) { previous.exit(); }` - exit() unregisters the listener and closes the inventory unconditionally with no dirty check, unlike requestExit() which opens ExitPromptScreen when session.dirty(). Any unsaved edits in the first session (material, lore, actions, requirements, properties, etc.) are lost with no warning or prompt, even though the very same EditorController has a working "unsaved changes" guard on every other exit path (ESC, Hub's Exit button, back() at the root).~~High
2~~P1~~GridScreen's stale grabbed-slot marker lets SlotOpScreen's Move/Copy blank an occupied destination slot~~Clear GridScreen.grabbed whenever the grabbed cell's own content is edited/cleared (or re-check occupancy before rendering the grab-state lore), and make SlotOpScreen.apply() treat a null resolved `from` as a cancelled no-op for MOVE/COPY instead of writing null / an empty deep-copy over the destination~~-~~menu-editor/src/main/java/org/luckyraven/oriel/editor/screen/SlotOpScreen.java:65-82~~Grabbing a populated cell in GridScreen only changes the cell's name()/lore() inside `if (isGrab) { cell.name(...).lore(...); }`; the click handlers assigned earlier in the same cellFor() call are untouched, so a plain (non-shift) left-click on the still-grabbed cell still opens SlotEditing.open(controller, target) and lets the admin clear/delete that slot via the normal "Clear slot"/"Delete slot" button, returning to the same GridScreen instance with `grabbed` unchanged. Shift-clicking a different, occupied destination then opens SlotOpScreen, whose apply() re-reads `Object from = slots.get(src)` fresh - now null - and for MOVE does `slots.remove(src); put(slots, dst, from);`, for COPY does `put(slots, dst, EditorTrees.deepCopy(from));`; `put()` treats a null value as `slots.remove(dst)`, so the destination's occupied item definition is deleted even though the screen's own "&c⚠ Overwrites slot N" warning implied a real item was about to be moved/copied there.~~High
3~~P2~~GridScreen.duplicate() and SlotOpScreen ignore CRAFTING's named result/grid schema and silently no-op~~Route GridScreen.duplicate() and SlotOpScreen.apply() through the same crafting-aware lookup GridScreen.targetFor() already uses (result/grid vs. slots.<n>) instead of session.rawSlot()/session.slots() directly, or at least message the admin when a cell isn't backed by the indexed slots: map~~-~~menu-editor/src/main/java/org/luckyraven/oriel/editor/screen/GridScreen.java:241-257~~GridScreen.targetFor() already knows a crafting menu may store its content under named result:/grid: keys instead of indexed slots.<n> (the WorkbenchMenuLoader alternative schema documented in the class javadoc) and routes editing through MapKeyTarget in that case. duplicate(int index) was never updated to match: it reads `Object source = controller.session().rawSlot(index)` and writes via `controller.session().slots().put(...)`, both of which only ever look at slots.<n>. On a crafting menu using the named schema, right-clicking a visibly-populated result/grid cell to duplicate it reads rawSlot(index) as null and returns immediately via `if (source == null) { return; }` with no message, so "duplicate" silently does nothing; SlotOpScreen.apply()'s exclusive use of controller.session().slots() gives grab/move/copy/swap the identical blind spot on such cells.~~Medium
