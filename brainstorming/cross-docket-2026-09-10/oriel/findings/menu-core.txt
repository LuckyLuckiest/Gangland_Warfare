1~~P1~~RequirementHook.evaluateMinimum dispatches onSuccess for every passing set, double-spending consumable gates~~Make evaluateMinimum stop dispatching success once `min` sets have passed (matching evaluateAny for min=1), or dispatch success to exactly the sets that were needed~~RequirementHookTest (exercises minimum=2 only, does not pin minimum=1 dispatch count)~~menu-core/src/main/java/org/luckyraven/oriel/core/requirement/RequirementHook.java:195-215~~evaluateMinimum() evaluates all sets, then on a pass runs `for (RequirementSet set : passed) { set.dispatchSuccess(dispatchCtx); }` for every set that passed. evaluateAny() (RequirementHook.java:217-228) instead fires success for only the first passing set and returns, and the evaluateMinimum javadoc claims to be "the natural generalisation of evaluateAll (min = M) and evaluateAny (min = 1)" — so min=1 contradicts its own documented contract. `minimum_requirements: 1` is reachable from user YAML (RequirementParser.java:155-156 clamps with `.asInt().min(1)`) and is emitted directly by the DeluxeMenus importer (DeluxeMenusImporter.java:961-965, whose javadoc at line 906 shows `minimum_requirements: 1`). RequirementSet's javadoc calls onSuccess "load-bearing for consumable gates ... the cooldown is spent only" there, so an imported N-of-M gate where two alternative sets both pass spends both resources on a single click instead of one.~~High
2~~P2~~InMemoryMenuRegistry.openPrevious re-opens with empty args and aborts on a required argument_processor~~Retain the args a menu was opened with in NavigationHistory, or skip the required-chain abort when the open originates from back-navigation~~-~~menu-core/src/main/java/org/luckyraven/oriel/core/registry/InMemoryMenuRegistry.java:78-86~~openPrevious() always re-opens the popped menu with no arguments: `return doOpen(player, previous.get(), new String[0]);`. doOpen() then looks up that menu's ArgumentProcessorSpec and, when a processor is required, the empty args fail the bind and it runs `spec.fireRequired(player, result); return false;` (InMemoryMenuRegistry.java:96-102). A `menu_back` click into any menu registered with a required `argument_processor:` therefore opens nothing and instead fires that menu's "you must supply X" fail chain through a HeadlessClickContext, out of context for a backward navigation. The inline comment at line 84 acknowledges args are dropped but not that the open can abort entirely.~~High
3~~P2~~NavigationHistory.back consumes the trail step even when the popped-to menu is no longer registered~~Have openPrevious push the step back onto the trail when doOpen returns false, or verify the name is still registered before removing it~~NavigationHistoryTest (covers normal pop order, not a since-unregistered target)~~menu-core/src/main/java/org/luckyraven/oriel/core/registry/NavigationHistory.java:53-60~~back() unconditionally does `trail.removeLast(); return Optional.ofNullable(trail.peekLast());` with no check that the returned name still resolves. openPrevious() feeds that name straight into doOpen(), which returns false at `Supplier<Menu> factory = factories.get(name); if (factory == null) return false;` when the menu was dropped by MenuConfigService.reload() -> `registry.unregister(name)` (MenuConfigService.java:147); unregister() only clears `factories`/`argSpecs` and never touches any player's trail, and NavigationHistory.clear() is called only per-viewer from DefaultOpenMenuTracker.java:104. The player's back-navigation silently does nothing while the trail has already lost that step, so a second `menu_back` skips past the gap instead of retrying it.~~Medium
