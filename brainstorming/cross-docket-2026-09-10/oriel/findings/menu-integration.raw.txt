1~~P0~~give_permission/take_permission bypass the allow_elevated_actions gate~~Gate PermissionActions' two registrations behind the same allow_elevated_actions flag (ActionParser.elevatedActionsGate) the built-in op/permission actions use, warning + no-op when disabled~~-~~menu-dependencies/vault/src/main/java/org/luckyraven/oriel/vault/permission/PermissionActions.java:34-49~~PermissionActions registers `give_permission`/`take_permission` straight into the ActionRegistry with only a blank-node check: `return ctx -> permissions.add(ctx.player(), node);`. ActionParser.java (config.loader, read as context) shows the built-in `op`/`permission` elevated actions are hard-gated by `allow_elevated_actions` (default false, config.yml:8) via `elevatedActionsGate`, refusing with a warning until an admin opts in (ActionParser.java:614-646, "Both are no-ops ... unless allow_elevated_actions is enabled"). PermissionActions never consults that gate or any equivalent flag, so any menu author who can write an on_left_click action (in-game menu editor included, per this repo's PLAN) can permanently grant or revoke arbitrary permission nodes (`give_permission: "*"`, group-admin nodes, etc.) with zero opt-in, while the functionally equivalent core path is locked down by default. This is a real asymmetric privilege-elevation hole, not a hypothetical.~~High
2~~P0~~take_money/give_money ignore Vault's transactionSuccess result, so a failed charge still lets the reward fire~~Check the boolean economy.withdraw/deposit result in the action lambda and call ctx.cancel() (and/or send a failure message) when it is false instead of discarding it~~-~~menu-dependencies/vault/src/main/java/org/luckyraven/oriel/vault/economy/EconomyActions.java:39-46~~`return ctx -> economy.deposit(ctx.player(), amount);` and `return ctx -> economy.withdraw(ctx.player(), amount);` are statement-expression lambdas against `void onClick(ClickContext ctx)` (ClickHandler.java), so `EconomyProvider.withdraw/deposit`'s `transactionSuccess()`-derived boolean (VaultEconomyProvider.java:54-61 in Keystone) is silently thrown away. vault_economy_demo.yml's documented "check-then-charge" pattern puts `take_money` in a `type: money` requirement's on_success (EconomyRequirements.java:32-39 only ever calls the read-only `has()`), and RequirementHook.evaluateAll (menu-core RequirementHook.java) already decided the gate is open and will run the wrapped reward handler before/independently of whether on_success's take_money actually succeeded — dispatchSuccess has no way to veto the gate result. So if the Vault withdraw fails for any reason (backend error, another plugin reserving funds, a delayed action chain letting the balance change between the `has()` check and the charge), the diamond sword (or any reward) is still handed out for free. ClickHandler's own javadoc notes `ctx.cancel()` exists precisely for this kind of signal, and EconomyActions never calls it.~~High
3~~P0~~give_money/take_money accept non-finite amounts (Infinity/NaN), letting a YAML value mint or corrupt a balance~~Add a Double.isFinite(amount) check (report a config error / treat as invalid) alongside the existing amount<=0 guard in both action registrations~~-~~menu-dependencies/vault/src/main/java/org/luckyraven/oriel/vault/economy/EconomyActions.java:35-46~~Both actions parse with `NodeReader.of(mapping, report).get(...).asDouble().min(0).required().orElse(0)`. Keystone's `NodeReader.asDouble()` calls `Double.parseDouble(normalizeNumeric(scalar.value()))` on the raw YAML scalar with no numeric-shape restriction, so a literal `give_money: Infinity` or `take_money: NaN` parses successfully. `DoubleAccess.min(double min)` only rejects via `value < min` (Keystone NodeReader.java:492-498), and by IEEE 754 rules every comparison against NaN is false, so NaN sails through untouched; `Infinity < 0` is also false so `+Infinity` sails through too. EconomyActions' own follow-up guard `if (amount <= 0) return NO_OP;` likewise never triggers for NaN or +Infinity (both comparisons are false). The result: `give_money: Infinity` reaches `economy.deposit(ctx.player(), Double.POSITIVE_INFINITY)` uncapped — an unlimited-money mint on any server whose Vault economy plugin doesn't itself special-case the value — and a NaN take_money/give_money amount reaches the Vault call with undefined balance-corruption behaviour.~~High
