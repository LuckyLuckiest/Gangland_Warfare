1~~P1~~ChestMenuBuilder.build() validation runs on first open, not at /menu reload, so a bad slot/region loads silently~~call factory.get() once (throwaway) inside MenuConfigService.registerMenu / loadMenuFile right after resolveFactory so build()'s IllegalStateExceptions become ConfigReport entries with file/field at reload time instead of a runtime log.warn at first open~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/MenuConfigService.java:252-296~~ChestMenuLoader.load returns a `Supplier<Menu>` whose lambda calls `builder.build()` (ChestMenuLoader.java:397) fresh per open (by design, per the class javadoc, so per-player state doesn't leak). `MenuConfigService.registerMenu` (line 271) only does `registry.register(name, factory)` — it never calls `factory.get()`, so `ChestMenuBuilder.build()`'s own range checks (`if (slotIndex < 0 || slotIndex >= size) throw new IllegalStateException(...)` at ChestMenuBuilder.java:314-316, and the same for `applyRegion`'s row/col checks at ChestMenuBuilder.java:392-399) never run during `/menu reload`. A malformed `paginated.previous_button.slot`, `next_button.slot`, `search_button.slot` or `paginated.region` value (all parsed with only a lower-bound check — see finding 2) passes `/menu reload` with zero report entries, then throws only when a player actually opens the menu; `InMemoryMenuRegistry.doOpen` catches it generically (`log.warn("Menu factory '{}' threw while building for {}: {}", ...)`, InMemoryMenuRegistry.java:110-114) with no file path, line, or field name — directly contradicting PLAN §7's "All loader errors include file path, line number (if available), and field name" and "Builder validation runs on .build(), not on first open. Fail early." This is a silent-failure-hidden-from-admins shape: `/menu reload` reports success and `/menu debug` shows nothing wrong, but the menu is unopenable and the only trace is a server log line the admin has to notice.~~High
2~~P1~~Paginated previous/next/search-button slots skip SlotLoader's upper-bound range check, unlike the docket's give_slot~~clamp/validate the slot against `rows * 9` in parseSlotMaterial/parseSearchButton (mirror SlotLoader's `slot >= size` skip-and-report) instead of only checking `min(0)`~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/loader/ChestMenuLoader.java:196-207~~`parseSlotMaterial` (used for `paginated.previous_button`/`next_button`) and `parseSearchButton` (`paginated.search_button`) both read `slot` as `r.get("slot").asInt().min(0).required().orElse(-1)` (ChestMenuLoader.java:200, :484) with no upper bound, unlike `SlotLoader.load` which reports `config.slot.range` and skips whenever `slot < 0 || slot >= size` (SlotLoader.java:56-60). An admin typo like `previous_button: {slot: 999, item: ARROW}` in a 3-row chest passes the loader with no warning, is stored via `ChestMenuBuilder.slot(999, ...)` with no bound check either (ChestMenuBuilder.java:118-125), and only surfaces as the IllegalStateException described in finding 1 when a player opens the menu — the exact same shape as the already-filed `give_slot` bug but in a different code path (pagination nav/search buttons, not deposit actions), so six of the loader's own slot-reading call sites (SlotLoader itself, plus every loader that routes through it) enforce the range and these three do not.~~High
3~~P1~~ItemDefinitionWriter overwrites a dash-prefixed dynamic material reference (head-/basehead-/texture-/soft-dep) as if it were static~~extend isNamespacedReference to also treat a MaterialSource dash-prefix (mirror ItemDefinitionLoader's dash>0 dispatch: head-/basehead-/texture-/hdb-/itemsadder-/oraxen-/nexo-) as dynamic, not just colon-syntax~~ItemDefinitionWriterRoundTripTest (does not cover this case)~~menu-data/config/src/main/java/org/luckyraven/oriel/config/loader/ItemDefinitionWriter.java:139-145~~ItemDefinitionLoader.loadItem dispatches a scalar item: value with a dash before any colon through MaterialSourceRegistry (head-Notch, basehead-<b64>, texture-<hash>, and the soft-dep prefixes hdb-/itemsadder-/oraxen-/nexo- at ItemDefinitionLoader.java:412-431) exactly as dynamically as a namespace:key ItemProvider reference - both resolve per-player at render time. ItemDefinitionWriter.isNamespacedReference only recognises the colon form: `int colon = text.indexOf(':'); return colon > 0 && colon < text.length() - 1;` (lines 143-144) - a dash reference has no colon, so it returns false. When an admin drops a physical item into the in-game editor to reset a slot's icon (SlotScreen.java calls ItemDefinitionWriter.writeInto), writeInto sees isAnimated(existingItem)==false and isNamespacedReference(existingItem)==false for `item: head-%player_name%` and overwrites it with `slot.put("item", XMaterial.matchXMaterial(stack).name())` - the literal resolved material (e.g. PLAYER_HEAD) - permanently destroying the per-player dynamic head-skin reference on save, the same class of loss the writer's own javadoc says it takes care to avoid for the colon form.~~High
4~~P2~~RequirementParser's item requirement uses raw Material.matchMaterial instead of the house MaterialLookup/XMaterial convention~~replace Material.matchMaterial(materialName.trim()) with MaterialLookup.parse(materialName, report, setMapping.location(), setMapping.path()+".material") so item requirements resolve materials the same cross-version way every other loader does~~RequirementParserTest~~menu-data/config/src/main/java/org/luckyraven/oriel/config/loader/RequirementParser.java:344-359~~Every other material lookup in this system (ChestMenuLoader slot buttons, ItemDefinitionLoader, TraderMenuLoader, DepositSlotComponent's accepts list) resolves through MaterialLookup.parse, which is deliberately XMaterial-backed for cross-version material name drift (MaterialLookup's own javadoc says so). parseItem instead does `Material material = Material.matchMaterial(materialName.trim());` (line 350) and reports `config.requirement.item.material` when it returns null. A `type: item` view/close/click requirement naming a material whose enum constant was renamed across Minecraft versions (XMaterial's whole purpose) fails to resolve on a server where the same name typed inside `item:` on a chest slot resolves fine, so a `has_pickaxe` gate an admin copy-pasted from a working slot definition can silently never be satisfiable while looking identical to working config elsewhere in the same file.~~Medium
5~~P2~~NbtApplier silently truncates an out-of-range int_array/byte_array value that NbtSchemaValidator already flagged as an ERROR~~have ItemDefinitionLoader check report.hasErrors() (or an equivalent severity gate) after NbtSchemaValidator.validate and skip building the nbtApplier when the nbt: block itself failed validation, instead of building it unconditionally from the same untrusted mapping~~-~~menu-data/config/src/main/java/org/luckyraven/oriel/config/loader/NbtApplier.java:82-91~~NbtSchemaValidator.validateTypedList reports `config.item.nbt.list.range` ("value 99999999999 out of int range — use list: long_array") and returns when an int_array entry doesn't fit an int (NbtSchemaValidator.java:58-64), but ItemDefinitionLoader builds the nbtApplier unconditionally right after calling the validator regardless of what was reported: `NbtSchemaValidator.validate(nbtMapping, reader.report()); if (isNbtApiAvailable()) { nbtApplier = NbtApplier.applierFor(nbtMapping, reader.report()); }` (ItemDefinitionLoader.java:508-517). NbtApplier.applyTypedList's own int_array/byte_array cases never re-check the range — they parse the same value as a Long and do `out[i] = v.intValue();` (line 88) or `.byteValue()` (line 106), a silent narrowing conversion — so the live item's NBT ends up with a silently wrapped/garbage integer every time the item renders, even though `/menu reload` already told the admin the value was invalid; the class's own javadoc says it "trusts that validation and just writes" but nothing actually gates on whether validation passed.~~Medium
