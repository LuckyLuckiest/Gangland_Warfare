1~~P0~~TradeOffer.maxUses resets to zero on every TraderMenu.open(), bypassable by closing and reopening the menu~~Persist per-(player,offer) use counts (or the built MerchantRecipe instances) outside of open() instead of calling offer.toRecipe() fresh on every open; only rebuild recipes when the offer set actually changes~~-~~menu-inventories/trader/src/main/java/org/luckyraven/oriel/trader/TraderMenu.java:72-78~~TraderMenu.open() is called every time a player (re)opens the merchant and always builds brand-new MerchantRecipe objects via `MerchantRecipe recipe = offer.toRecipe();` inside `for (TradeOffer offer : offers) {`, and TradeOffer.toRecipe() (TradeOffer.java:43-45) does `public MerchantRecipe toRecipe() { MerchantRecipe recipe = new MerchantRecipe(result.clone(), maxUses);` without ever restoring a prior uses count, so every fresh MerchantRecipe starts at uses=0 regardless of how many times this offer was already bought. Vanilla Bukkit/Minecraft only enforces maxUses within the lifetime of one MerchantRecipe instance, so any admin-configured "buy this once" or "limited stock" trade can be bought unlimited times simply by closing the trader menu (walking away from the NPC, or clicking a UI button that reopens it) and opening it again — no special client action or exploit tooling required. TraderMenuStressTest's rapidOpenCloseCyclesPreserveOfferDispatch (trader/src/test/.../TraderMenuStressTest.java:159-176) reopens the same menu 20 times and only asserts identity-map dispatch survives, never that uses/maxUses are preserved, so this is unpinned.~~High

2~~P1~~TraderMenu.open() registers in TraderRegistry before player.openMerchant(), so a chained open's implicit close deletes the fresh entry~~Call player.openMerchant(...) first and registry.register(...) after it returns, or have TraderClickListener/TraderRegistry only unregister when the removed value is identity-equal to the menu whose close is being processed~~-~~menu-inventories/trader/src/main/java/org/luckyraven/oriel/trader/TraderMenu.java:84-87~~open() runs `TraderRegistry registry = TraderSupport.registry(); registry.register(player, this); player.openMerchant(fresh, true); MenuTracking.tracker().track(player, this);` — registering the new menu before calling openMerchant. Bukkit's openMerchant implicitly closes whatever inventory the player currently has open, firing an InventoryCloseEvent synchronously inside that same call; TraderClickListener.onClose (TraderClickListener.java:103-111) unconditionally does `registry.unregister(player)` for any MERCHANT-type close. When a player already has a TraderMenu-backed merchant open and either the same or a different TraderMenu is opened for them again, the sequence is: overwrite registry[player] with the NEW menu, then openMerchant closes the OLD merchant screen, whose close event deletes the registry entry that was just written for the NEW menu — so the very next purchase on the new screen finds `registry.lookup(player) == null` in TraderClickListener.onClick and onTrade/onPurchase silently never fire, even though the vanilla item exchange still completes. TraderMenuStressTest.registryIsUpdatedOnEveryOpen (TraderMenuStressTest.java:44-67) asserts "second open should overwrite registry entry" but never registers TraderClickListener as a live Bukkit listener, so it cannot see the close-event-driven deletion this bug depends on.~~High

3~~P1~~WorkbenchMenu/DispenserMenu/HopperMenu track the menu after renderAll+openInventory, unlike FurnaceMenu (and ChestMenu per its own comment) which track first~~Move MenuTracking.tracker().track(player, this) before renderAll(player) in WorkbenchMenu.open, DispenserMenu.open and HopperMenu.open, matching FurnaceMenu.open's order~~-~~menu-inventories/crafting/src/main/java/org/luckyraven/oriel/crafting/WorkbenchMenu.java:177-180~~WorkbenchMenu.open() runs `ensureTitleFor(player); renderAll(player); player.openInventory(inventory); MenuTracking.tracker().track(player, this);` — tracking happens only after the first render and after the inventory is already shown to the player; DispenserMenu.java:166-170 and HopperMenu.java:170-174 are byte-for-byte identical in this respect. FurnaceMenu.open() (FurnaceMenu.java:219-222) does the opposite on purpose, with an explicit comment: "Track before the first render so PlaceholderProviders that look up the player's current menu resolve against this furnace on open, not just after a click — mirrors ChestMenu.open." Any PlaceholderProvider or on_open handler that calls MenuTracking.tracker().currentMenuOf(player) during the very first render of a workbench/dispenser/hopper menu therefore sees the player's previous menu (or null) instead of the menu now being opened, while the identical lookup works correctly for furnace and (per the comment) chest menus, and self-corrects on any later rerender(). No workbench/dispenser/hopper test exercises MenuTracking/currentMenuOf, so this divergence is unpinned.~~High

4~~P2~~FurnaceProgress.max has no upper bound, letting an admin-configured value overflow the short-typed furnace window property~~Clamp or validate max against Short.MAX_VALUE (32767) in FurnaceProgress's compact constructor, matching the range the ContainerSetData/window-property protocol actually carries~~-~~menu-inventories/furnace/src/main/java/org/luckyraven/oriel/furnace/FurnaceProgress.java:19-27~~FurnaceProgress's compact constructor only checks `public FurnaceProgress { if (max < 0) { throw new IllegalArgumentException("max must be >= 0, was " + max); }` and that ticksPerCycle >= 1 — there is no upper bound. FurnaceMenuBuilder.fuelProgress/cookProgress (FurnaceMenuBuilder.java:171-180) pass max straight through to `new FurnaceProgress(max, ticksPerCycle)` with no validation either, and FurnaceMenu.advanceProgress/pushProgressDenominators (FurnaceMenu.java:374-389, 416-420) forward that value into PacketAdapter.setProperty, which ultimately becomes Minecraft's ContainerSetData packet field — a `short`. An admin who configures fuelProgress/cookProgress with a max above 32767 gets a silently wrapped/negative burn or cook bar on the client with no warning anywhere in this module. FurnaceProgressTest covers ascending/descending math and negative-tick/invalid-args cases but never a large-max case, so this gap is untested.~~Medium

5~~P2~~AnvilMenu has no guard for AnvilGUI being absent, so a consumer of the published oriel-anvil artifact crashes with a raw NoClassDefFoundError~~Wrap the AnvilBuilderFactory.create()/AnvilGUI usage in AnvilMenu.Builder.open with a check (or try/catch on NoClassDefFoundError/ClassNotFoundException) that throws a clear "AnvilGUI is required on the runtime classpath" IllegalStateException instead of letting the JVM's raw class-loading error surface~~-~~menu-inventories/anvil/src/main/java/org/luckyraven/oriel/anvil/AnvilMenu.java:99-106~~AnvilMenu.Builder.open() calls `getBuilder(...)` which does `return AnvilBuilderFactory.create().plugin(AnvilSupport.plugin())...`, and AnvilBuilderFactory.create() (AnvilBuilderFactory.java:20-22) directly returns `factory.get()` where the default factory is `AnvilGUI.Builder::new` — a hard reference to the net.wesjd.anvilgui.AnvilGUI class with no presence check anywhere in this module. Per this repo's own CLAUDE.md, AnvilGUI is compileOnly in :menu-inventories:anvil specifically so the published oriel-anvil Maven Central artifact never bundles it (menu-plugin holds the implementation/shaded copy instead); any external consumer who adds oriel-anvil from Central without separately shading AnvilGUI themselves gets a bare NoClassDefFoundError the first time AnvilMenu.builder()...open(player) runs, with no Oriel-authored message pointing at the missing dependency. docs/PLAN.md's open questions already track replacing AnvilGUI with a first-party impl, but until then this is a live crash path for any third-party consumer of the published module, not just a documentation gap.~~Medium

6~~P2~~WorkbenchMenu renders into a live InventoryType.WORKBENCH inventory, so vanilla recipe matching can silently overwrite the admin's configured result-slot item~~Either suppress recipe recomputation (cancel PrepareItemCraftEvent for WorkbenchMenuHolder-backed inventories) or switch the decorative/static workbench menu to a CHEST-backed inventory shaped like a workbench, since no code in this module currently defends the result slot~~-~~menu-inventories/crafting/src/main/java/org/luckyraven/oriel/crafting/WorkbenchMenu.java:325-331~~WorkbenchMenu is built on a real `Bukkit.createInventory(holder, InventoryType.WORKBENCH, ...)` (WorkbenchMenuBuilder.java:189) and renderAll writes admin-configured components straight into it: `private void renderAll(Player player) { handlers.clearAll(); ItemStack[] empty = new ItemStack[SIZE]; inventory.setContents(empty);` followed by per-slot renders including the result at slot 0 and the 3x3 grid at slots 1-9. The class javadoc states interactive recipe matching is "out of scope" and clicks are cancel-by-default, which correctly stops a *player* from placing real items, but nothing in this module intercepts or cancels PrepareItemCraftEvent/recipe recomputation that a real CraftingInventory performs whenever its 1-9 grid slots are populated — if an admin decorates the grid with real ingredient ItemStacks that happen to match a vanilla recipe, the live crafting container can recompute and overwrite the admin's configured result-slot component with the vanilla-crafted item. No test in WorkbenchMenuTest/WorkbenchMenuStressTest exercises real recipe matching (MockBukkit's CraftingInventory does not model this), so the risk is untested either way.~~Medium

7~~P3~~TraderMenu.returnInputItems logs wrong returned/dropped counts because dropped accumulates across both input slots but is re-subtracted per slot~~Compute a per-stack droppedThisStack local (reset each loop iteration) and use that for the returned += stack.getAmount() - droppedThisStack line, only adding into the running dropped total afterward~~-~~menu-inventories/trader/src/main/java/org/luckyraven/oriel/trader/TraderMenu.java:167-171~~Inside returnInputItems's loop over the two merchant input slots, leftovers are dropped with `player.getWorld().dropItemNaturally(player.getLocation(), leftover); dropped += leftover.getAmount(); } returned += stack.getAmount() - dropped;` — `dropped` is the cumulative total across both loop iterations, not the amount dropped for the current stack, so on the second slot's iteration `returned` is under-counted by whatever was already dropped from the first slot (and can even go negative for the debug log). The actual item movement (addItem + dropItemNaturally) is unaffected and correct — only the `log.debug("Returned {} item(s) to {} from trader inputs...")` diagnostic at the end of the method reports wrong numbers, which would mislead an admin reading logs after a plugin-disable/reload item-return event. No test in trader/src/test covers returnInputItems at all.~~High
