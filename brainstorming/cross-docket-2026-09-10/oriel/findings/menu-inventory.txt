1~~P0~~TraderMenu.open rebuilds MerchantRecipes each open, resetting maxUses so limited-stock trades are unlimited~~Persist per-player-per-offer use counts (or reuse the built MerchantRecipe instances) across opens instead of calling offer.toRecipe() fresh in every open()~~TraderMenuStressTest exercises repeated open/close but never asserts uses or maxUses, so the behaviour is unpinned~~menu-inventories/trader/src/main/java/org/luckyraven/oriel/trader/TraderMenu.java:68-78~~open() constructs a brand-new Merchant and a brand-new recipe list on every invocation: `for (TradeOffer offer : offers) { MerchantRecipe recipe = offer.toRecipe(); recipes.add(recipe); recipeIndex.put(recipe, offer); }`. TradeOffer.toRecipe (TradeOffer.java:43-50) does `MerchantRecipe recipe = new MerchantRecipe(result.clone(), maxUses);` and never restores a prior use count, so each new instance starts at uses = 0. Minecraft enforces maxUses only within the lifetime of one MerchantRecipe instance, so an admin-configured "limited stock" or "buy once" trade is bought again simply by closing and reopening the trader — no exploit tooling required, just walking away and coming back. Every purchase beyond the intended cap is item and currency creation.~~High
2~~P1~~TraderMenu.open registers in TraderRegistry before openMerchant, whose implicit close deletes the new entry~~Call player.openMerchant(...) first and register afterwards, or make the close handler unregister only when the stored menu is identity-equal to the one being closed~~TraderMenuStressTest asserts the registry is overwritten on reopen but never registers TraderClickListener as a live listener, so it cannot observe the close-driven deletion~~menu-inventories/trader/src/main/java/org/luckyraven/oriel/trader/TraderMenu.java:83-86~~open() runs `TraderRegistry registry = TraderSupport.registry(); registry.register(player, this); player.openMerchant(fresh, true); MenuTracking.tracker().track(player, this);` — the registry write precedes openMerchant. Bukkit's openMerchant implicitly closes whatever the player currently has open and fires InventoryCloseEvent synchronously inside that call, and TraderClickListener.onClose (TraderClickListener.java:103-111) unconditionally does `registry.unregister(player);` for any MERCHANT-type close. Opening a trader menu while one is already open therefore writes the new entry, then immediately deletes it when the old screen's close event fires. The next purchase finds no registry entry, so onTrade and onPurchase never fire while the vanilla item exchange still completes — the player trades but the menu's handlers silently do not run.~~High
3~~P2~~WorkbenchMenu, DispenserMenu, HopperMenu and TraderMenu track the menu after opening, contradicting FurnaceMenu's documented order~~Move MenuTracking.tracker().track(player, this) before the first render and the open call in all four types, matching FurnaceMenu~~-~~menu-inventories/crafting/src/main/java/org/luckyraven/oriel/crafting/WorkbenchMenu.java:177-180~~WorkbenchMenu.open() runs `ensureTitleFor(player); renderAll(player); player.openInventory(inventory); MenuTracking.tracker().track(player, this);`, tracking only after the inventory is already shown; DispenserMenu.java:166-170, HopperMenu.java:170-174 and TraderMenu.java:83-86 do the same. FurnaceMenu.open (FurnaceMenu.java:219-222) deliberately does the opposite with an explicit comment: "Track before the first render so PlaceholderProviders that look up the player's current menu resolve against this furnace on open, not just after a click — mirrors ChestMenu.open." A PlaceholderProvider or on_open handler calling currentMenuOf(player) during the first render of those four types resolves the player's previous menu or null instead of the one opening, and self-corrects only on a later rerender. Note the majority of types use the later ordering, so the intended convention should be settled before the fix.~~Medium
4~~P2~~FurnaceProgress.max has no upper bound and overflows the short-typed furnace window property~~Clamp or reject max above Short.MAX_VALUE in FurnaceProgress's compact constructor, matching what the window-property protocol carries~~FurnaceProgressTest covers ascending/descending math and invalid args but never a large max~~menu-inventories/furnace/src/main/java/org/luckyraven/oriel/furnace/FurnaceProgress.java:19-27~~The compact constructor validates only the lower bound: `if (max < 0) { throw new IllegalArgumentException("max must be >= 0, was " + max); }` plus a ticksPerCycle check. FurnaceMenuBuilder.fuelProgress and cookProgress (FurnaceMenuBuilder.java:171-180) pass the value through unvalidated, and FurnaceMenu.advanceProgress / pushProgressDenominators (FurnaceMenu.java:374-389, 416-420) forward it into PacketAdapter.setProperty, which becomes Minecraft's ContainerSetData field — a short. A configured max above 32767 wraps to a negative value, so the client shows a garbled or inverted burn/cook bar with no warning logged anywhere.~~Medium
5~~P2~~AnvilMenu dereferences AnvilGUI with no presence check, so consumers of the published oriel-anvil artifact get a bare NoClassDefFoundError~~Guard the AnvilBuilderFactory.create() call with a class-presence check and throw an IllegalStateException naming AnvilGUI as a required runtime dependency~~-~~menu-inventories/anvil/src/main/java/org/luckyraven/oriel/anvil/AnvilMenu.java:99-106~~AnvilMenu.Builder.open() routes through getBuilder(...), which does `return AnvilBuilderFactory.create().plugin(AnvilSupport.plugin())...`, and AnvilBuilderFactory.create() (AnvilBuilderFactory.java:20-22) returns `factory.get()` where the default factory is `AnvilGUI.Builder::new` — a hard reference to net.wesjd.anvilgui.AnvilGUI with no presence check in the module. This repo's CLAUDE.md states AnvilGUI is deliberately compileOnly in menu-inventories:anvil so the published oriel-anvil Central artifact never bundles it, with menu-plugin holding the shaded copy. Any third-party consumer who depends on oriel-anvil from Central without shading AnvilGUI themselves gets a raw JVM class-loading error on the first open(player), with no Oriel-authored message identifying the missing dependency.~~Medium
6~~P3~~TraderMenu.returnInputItems under-counts returned because dropped accumulates across both input slots~~Track a per-stack dropped count inside the loop and use that in the returned calculation, adding it to the running total afterwards~~-~~menu-inventories/trader/src/main/java/org/luckyraven/oriel/trader/TraderMenu.java:167-171~~Inside the loop over the merchant's two input slots, leftovers are dropped and tallied into a method-scoped counter, then the return count is computed against that cumulative total: `dropped += leftover.getAmount(); } returned += stack.getAmount() - dropped;`. On the second slot's iteration, dropped already includes whatever the first slot dropped, so returned is under-counted and can go negative. Only the closing `log.debug("Returned {} item(s) to {} from trader inputs...")` diagnostic is affected — the addItem and dropItemNaturally calls move the right items — but an admin reading logs after a plugin-disable item return sees wrong numbers. No test in trader/src/test covers returnInputItems.~~High
