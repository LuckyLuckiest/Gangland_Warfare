1~~P0~~ChestMenu.close(player, CloseReason.USER) never returns DepositSlotComponent items~~Also drain/return (or drop-at-feet) held ItemHoldingComponent items on CloseReason.USER instead of assuming Bukkit's cursor-return covers slot contents; e.g. call returnHeldItems unconditionally, or transfer the physical slot item to the player before the natural close completes.~~DepositSlotComponentTest#closeWithUserSkipsReturnSoBukkitCursorPickupHandlesIt (pins today's wrong behaviour), DepositSlotMultiSlotStressTest#closeOnUserSkipsReturnSoBukkitCursorPickupHandlesIt~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/ChestMenu.java:380-386~~close(Player,CloseReason) only drains held items when the reason isn't USER: `if (reason != CloseReason.USER) { returnHeldItems(player, reason, true); }`. A DepositSlotComponent's held stack physically lives in the shared Bukkit Inventory slot, not on the player's cursor, so when a player just presses Escape or walks away (CloseReason.USER — the only reason ChestMenuCloseListener's normal path ever produces) the item is neither returned nor dropped; the backing Inventory is simply discarded (garbage collected) the moment the menu is next opened as a fresh instance. The class's own javadoc rationale ("Bukkit's normal close flow surfaces the cursor stack to the player") conflates the mouse-cursor item with items sitting in inventory slots — vanilla Bukkit never auto-returns slot contents of a closed custom inventory. Two tests explicitly assert the item stays only in heldByViewer and never reaches the player's inventory on a USER close, confirming this is the shipped (and wrong) behaviour rather than a hypothetical.~~High
2~~P0~~MenuFlow panel switch/back/end drops DepositSlotComponent items with no return path~~Before building/opening the next panel (or ending the flow), call currentMenu.close(viewer, CloseReason.RELOAD) (or a dedicated non-USER reason) so any ItemHoldingComponent contents drain to the player instead of the Inventory being silently replaced or discarded.~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/flow/MenuFlow.java:165-185~~switchInternal always builds a brand-new ChestMenu/Inventory for the target panel and reassigns `currentMenu = newMenu;` then `newMenu.open(viewer);` — it never calls close() on the outgoing currentMenu, so any physical item sitting in a DepositSlotComponent slot on the old panel is abandoned inside the discarded Inventory object. end() is no better: `if (currentMenu != null) currentMenu.close(viewer);` uses the reason-less close(Player) overload, which per ChestMenu.close(Player,CloseReason) only drains held items for non-USER reasons — since no reason is passed here either, nothing is ever returned. Any flow panel that places a DepositSlotComponent (a payment slot, a bid slot, an item-appraisal slot) loses whatever is sitting in it the instant switchTo/back/openAt(sameId)/end() runs, and no code path anywhere in MenuFlow transfers or returns it.~~High
3~~P1~~ChestMenuCloseListener fires on_close during internal navigation, not just real closes~~Guard the target.onClose(event, player) dispatch behind the same currentMenuOf(player) == target check used for the veto/untrack branch, so a navigation-triggered close of the outgoing menu doesn't also fire its on_close actions.~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/listener/ChestMenuCloseListener.java:56-70~~ChestMenu.open() tracks the new menu before calling player.openInventory(...), so when a navigation (ctx.openMenu(name), a MenuFlow panel switch, any re-open while a menu is already showing) triggers Bukkit's implicit close of the outgoing inventory, `MenuTracking.tracker().currentMenuOf(player) == target` is already false for the outgoing menu. The listener's comment explains this exists so navigation "preserves the new registration," and it does correctly skip the veto/untrack block in that case — but `target.onClose(event, player);` on the line right after the closing brace runs unconditionally regardless of that check. Every internal navigation therefore fires the OUTGOING ChestMenu's YAML on_close handler (message/sound/command actions per ChestMenu's onCloseHandler docstring) exactly as if the player had genuinely left, even though they only clicked through to a child menu.~~High
4~~P2~~PaginatedListPageState.itemAt doesn't clamp a stale page after the source list shrinks~~Clamp the stored page to pageCount(player,ctx)-1 inside itemAt (or currentPage), mirroring the clamp PaginatedChestMenu.render() already applies for whole-menu pagination, instead of letting every cell render null.~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/internal/PaginatedListPageState.java:56-61~~itemAt computes `int idx = page * slotsPerPage + regionIndex; return idx < items.size() ? items.get(idx) : null;` using whatever page index was last stored for the player, with no upper-bound check against the freshly-fetched list size. If invalidate(player) runs (a cycle_sort/cycle_filter/set_search action, or a live source shrinking between renders) while the player sits on a high page, every region cell renders null — a blank page — instead of snapping back to the last valid page. PaginatedChestMenu's own whole-menu pagination avoids exactly this with an explicit `page = Math.min(pageIndex.getOrDefault(...,0), max)` clamp on every render, but the region-based PaginatedListPageState/PaginatedListSlotComponent path has no equivalent, so the two pagination systems behave inconsistently on the same shrink trigger.~~Medium
5~~P2~~PaginatedControlState never evicts a viewer's sort/filter/search entries once added~~Add a per-player removal hook (e.g. invoked from a PlayerQuitEvent listener or on menu close) that clears sortIndex/filterIndex/searchQuery/descending for that UUID, instead of relying solely on the whole-state clear() driven by /menu reload.~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/internal/PaginatedControlState.java:25-28~~The four per-viewer maps — `sortIndex`, `filterIndex`, `searchQuery`, `descending` — only ever grow: cycleSort/cycleFilter/setSearchQuery/toggleDirection all `.put(viewer, ...)`, and the only bulk-removal paths are PaginatedControlRegistry.unregister/clear (whole menu-name state, driven by /menu reload). PaginatedControlRegistry documents itself as "one state object per registered menu name" that survives across every player who ever opens that menu for the life of the process, so a public server accumulates one stale UUID entry per map for every unique visitor who ever touched sort/filter/search on that menu, with no code anywhere in this class or its registry to purge a single departed player's entries between reloads. Low per-entry cost, but genuinely unbounded growth tied to player turnover rather than concurrent player count.~~Medium
6~~P2~~ChestMenu.rerenderSlot skips the untouched-component clear that renderAll applies~~Have rerenderSlot check view.wasTouched() the same way renderAll does, and explicitly call inventory.setItem(slot, null) / clearHandlers(slot) when the component left the view untouched.~~-~~menu-inventories/chest/src/main/java/org/luckyraven/oriel/chest/ChestMenu.java:452-463~~renderAll treats an untouched ChestSlotView as "this component deliberately rendered nothing" and explicitly blanks the slot: `if (!view.wasTouched()) { inventory.setItem(entry.getKey(), null); clearHandlers(entry.getKey()); }`. rerenderSlot has no equivalent — it just calls `renderOne(slot, component, viewer);` and returns without ever inspecting wasTouched(). PaginatedNavComponent.renderInto is exactly the component this matters for: at a page boundary it returns early without calling any SlotView method by design, so a composited nav button doesn't erase an underlying decoration. If rerenderSlot is ever invoked directly on a standalone (non-composited) nav button's slot — e.g. custom click-handler code calling ctx.rerenderSlot(navSlot) — the stale arrow item and its click handler from the previous render are left in place instead of being cleared, unlike the framework's own click-driven path (PaginatedNavComponent always calls the fully-clamped ctx.rerender()).~~Medium
