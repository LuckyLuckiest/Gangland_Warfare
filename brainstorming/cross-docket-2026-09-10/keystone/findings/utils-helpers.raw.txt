1~~P2~~Tree.size()/getAllNodes()/clone() NPE on an empty Tree~~Null-check root in getNumberOfDescendants, buildTreeInfo and clone() and return 0/empty/no-op instead of dereferencing~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/Tree.java:47-49~~`Tree<E>` supports an empty state (`isEmpty()` exists and checks `root == null`), but `size()` calls `getNumberOfDescendants(root) + 1`, and that method immediately does `root.getChildren().size()` with no null guard. `new Tree<Integer>().size()` throws NullPointerException instead of returning 0. The same missing-root NPE hits `getAllNodes()` (`buildTreeInfo(root, nodes)` does `list.add(node)` then `node.getChildren()` unconditionally) and `clone()` (`tree.root = this.root.clone()`), which cascades into `toString()` and `iterator()` since both call `getAllNodes()`. Verified by extracting the method and running it against a null root, which threw `NullPointerException: Cannot invoke "Node.getChildren()" because "<parameter1>" is null`.~~High
2~~P1~~SpellChecker's optimized Damerau-Levenshtein transposition math is wrong~~Track the row from two iterations back (a real prev-prev row or a small ring buffer) instead of reading currentRow[i-2] from the row currently being written~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/SpellChecker.java:107-124~~`damerauLevenshteinDistance`'s inner loop keeps only one `currentRow` array plus scalar `leftValue`/`upperLeftValue`/`temp`, then checks `word1.charAt(i-1)==word2.charAt(j-2) && word1.charAt(i-2)==word2.charAt(j-1)` and applies `currentRow[i] = Math.min(currentRow[i], currentRow[i-2] + cost)` — but `currentRow[i-2]` at that point holds a value from the *current* row (already overwritten this same j-iteration), not the row from two iterations back that the transposition term requires. Extracting the method and comparing against a reference OSA implementation confirms real mismatches on ordinary transposition typos: `damerauLevenshteinDistance("teh","the")` returns 2 (should be 1), `("ab","ba")` returns 2 (should be 1), `("ca","ac")` returns 2 (should be 1). This directly feeds `ChatUtil.suggestCommand`'s "Did you mean" ranking (`suggestions.merge(length, ...)` picks the smallest edit distance), so a transposed-letter typo — one of the most common typing mistakes — is scored as a 2-edit distance and can lose to (or wrongly tie with) an unrelated word that's actually farther away, degrading the command-suggestion feature silently. No SpellChecker test exists in the repo.~~High
3~~P2~~LinkedList.clone() is shallow: cloned list shares the same Node chain as the original~~Deep-clone the chain in LinkedList.clone() (reuse Node.clone() per node and relink) instead of relying on Object.clone()'s field copy~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/LinkedList.java:169-175~~`public LinkedList<E> clone() { return (LinkedList<E>) super.clone(); }` only shallow-copies the `head`/`tail` object references; the `Node<E>` instances themselves (whose `next`/`previous` fields are mutable) remain shared between the original and the clone. `Node<T>` even has its own overridden `clone()` (lines 193-200) that is never called from `LinkedList.clone()`. Calling `add`/`remove`/`addSorted` on a "cloned" list mutates the shared `Node.next`/`previous` links in place and corrupts the original list's traversal order/size bookkeeping, defeating the entire purpose of exposing `clone()`. No LinkedListTest exists; the class has no internal callers in this repo but is public API shared by every consumer plugin.~~High
4~~P2~~JsonFormatter.formatToJson mis-scopes brace handling due to operator precedence, corrupting quoted values containing `{`/`}`~~Parenthesize the conditions: `(c == '{' || c == '[') && !hasQuotes` and the matching `}`/`]` branch~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/JsonFormatter.java:25,28~~`} else if (c == '{' || c == '[' && !hasQuotes) {` — Java's `&&` binds tighter than `||`, so this parses as `c == '{' || (c == '[' && !hasQuotes)`. The `hasQuotes` guard, which every other structural branch (`[`, `:`, `,`, and the matching `}`/`]` at line 28) correctly applies, is silently skipped for `{`. A JSON-like string containing a literal `{` or `}` inside a quoted value (e.g. formatting `{"msg": "use {token}"}`) makes `formatToJson` treat the in-string brace as a real structural token, incrementing/decrementing `indentLevel` and injecting a newline + indent into the middle of the quoted string, corrupting the output. No JsonFormatterTest exists and no internal caller was found, but the class is public API.~~High
5~~P2~~ScientificCalculator only registers the factorial operator and logb/neg/ln functions when a non-empty variables map is supplied~~Always call .operator(operators()).functions(functions()) on the builder, independent of whether variables is null/empty~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/ScientificCalculator.java:20-31~~`if (!(variables == null || variables.isEmpty())) builder.variables(variables.keySet()).operator(operators()).functions(functions());` gates registration of the custom `!` (factorial) operator and the `logb`/`neg`/`ln` functions behind whether a variables map was passed — a condition unrelated to whether the formula actually uses those symbols. `new ScientificCalculator("5!")` (the single-arg constructor, `variables == null`) never registers the `!` operator, so exp4j's `ExpressionBuilder.build()` fails to parse the token and throws, even though the class exists specifically to support these operators/functions.~~High
6~~P2~~Color enum has two duplicate colour-code pairs, leaving two of the 16 legacy chat codes unused~~Assign BROWN and ORANGE, and PURPLE and MAGENTA, distinct legacy codes (e.g. use the unused &9/&c) instead of reusing &6 and &5~~-~~keystone-common/src/main/java/org/luckyraven/keystone/color/Color.java:9-23~~The 16 `Color` constants are meant to mirror Minecraft's 16 dye colours (matching `MaterialType`/`ColorUtil`'s per-colour material lookups) but `BROWN("&6", ...)` and `ORANGE("&6", ...)` share the gold code, and `PURPLE("&5", ...)` and `MAGENTA("&5", ...)` share the dark-purple code, while legacy codes `&9` (blue) and `&c` (red) are never used by any constant. `ColorUtil.getColorCode("brown")` and `getColorCode("orange")` therefore return the identical string, so any consumer rendering a colour-coded label for two visually distinct dye colours (e.g. "Brown Wool" vs "Orange Wool") gets the same chat colour for both.~~Medium
7~~P2~~NumberUtil.resizeLinear divides by zero when shrinking a multi-element list to size 1, silently returning 0~~Special-case targetSize == 1 (return a single representative value, e.g. the average or original.get(0)) before the general interpolation loop~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/NumberUtil.java:219-257~~For `targetSize == 1` and `original.size() > 1`, the loop body computes `double position = (double) i * (originalSize - 1) / (targetSize - 1);` with `i == 0` and `targetSize - 1 == 0`, i.e. `0 / 0`, which in floating point yields `NaN` (no exception is thrown). `(int) Math.floor(NaN)` and `Math.round(NaN)` both evaluate to `0` in Java, so `resizeLinear(List.of(10, 20, 30), 1)` silently returns `[0]` instead of any value derived from the input list — a wrong-but-bounded result with no error signal. No NumberUtilTest exists in the repo.~~High
8~~P2~~DurationParser.parse throws an uncaught ArithmeticException instead of returning null on an overflowing duration~~Wrap the Duration.ofX(amount) calls in parseToken in a try/catch for ArithmeticException and return null, consistent with the documented "returns null on anything else" contract~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/DurationParser.java:71-82~~The class javadoc promises "Returns null on anything else; the caller owns the error report," and `parseToken` does catch `NumberFormatException` and reject negative amounts, but the final `switch` calls `Duration.ofHours/ofMinutes/ofDays(amount)` directly; those methods use `Math.multiplyExact` internally and throw `ArithmeticException` on overflow rather than returning a value. Verified directly: `Duration.ofHours(Long.parseLong("999999999999999999"))` throws `ArithmeticException: long overflow`. A YAML-authored duration string with an oversized numeric prefix (a plausible typo, e.g. an extra digit) crashes the caller instead of yielding the documented null. No overflow case is covered by DurationParserTest.~~High
9~~P2~~ReflectionUtil.newInstance(Class, Object...) NPEs immediately on any null constructor argument~~Skip the getClass()/primitive-unboxing step for a null argument and instead resolve its declared parameter type via the target constructor's own parameter types~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/ReflectionUtil.java:27-42~~`for (int i = 0; i < parameters.length; i++) { classes[i] = parameters[i].getClass();` dereferences every varargs element unconditionally before looking up the constructor. `ReflectionUtil.newInstance(SomeType.class, null, "x")` — a plausible call when reflectively constructing an object that has a nullable field — throws `NullPointerException` from this line rather than resolving to the declared parameter type or producing the documented `InternalError` wrapping a reflective failure. No test exercises a null-parameter call.~~Medium
10~~P1~~PdcPlayerMetaService silently drops set()/remove() for offline players, diverging from the InMemoryPlayerMetaService fallback for the same interface~~Either document + surface a boolean/Result return on PlayerMetaService.set so callers can detect the offline no-op, or route offline writes through OfflinePlayer PDC access if available~~-~~keystone-common/src/main/java/org/luckyraven/keystone/meta/PdcPlayerMetaService.java:39-46~~`PlayerMetaService.set(UUID, String, String)` is declared as `void set(...)` with the javadoc "Store (or overwrite) value under key" and no offline caveat (`PlayerMetaService.java:27-30`). The production implementation, `PdcPlayerMetaService.set`, does `Player online = Bukkit.getPlayer(player); if (online == null) { return; }` — a completely silent no-op with no exception, log line, or return signal. The test/fallback implementation, `InMemoryPlayerMetaService.set` (`InMemoryPlayerMetaService.java:72-75`), always succeeds regardless of online status (`store.computeIfAbsent(player, ...).put(key, value)`), so code exercised against the in-memory fake in tests will not catch the production data loss when a caller sets a stateful toggle/counter for an offline player (e.g. an admin command run against an offline target). Neither implementation has a dedicated test class.~~High
11~~P2~~PermissionWorker.permissionRefactor uses a bare startsWith check with no dot boundary, so a same-prefixed foreign permission bypasses canonicalisation~~Check `permission.equals(indicator) || permission.startsWith(indicator + ".")` instead of a raw startsWith(indicator)~~-~~keystone-common/src/main/java/org/luckyraven/keystone/permission/PermissionWorker.java:186-192~~`return (!permission.startsWith(this.indicator) ? this.indicator + "." : "") + permission;` treats any string that textually begins with the indicator as already-namespaced and passes it through unchanged. With indicator `"cop"`, a raw permission `"copsandcrooks.spawn"` (which is not actually namespaced under `cop.`) satisfies `startsWith("cop")` and is returned as-is instead of becoming `"cop.copsandcrooks.spawn"`, producing a canonical permission string that doesn't match the intended `<indicator>.` namespace. No test exists for PermissionWorker/PermissionManager.~~Medium
12~~P2~~YamlMessageProvider.getString can return null with no fallback found, and ChatUtil.color() NPEs on a null message~~Have YamlMessageProvider.getString fall back to a non-null placeholder (or the raw path) when both primary and fallback lack the key, or have ChatUtil.color null-check and return an empty/placeholder string~~-~~keystone-common/src/main/java/org/luckyraven/keystone/message/YamlMessageProvider.java:44-49~~`getString` returns `null` whenever the key is absent from both the primary YAML and the (optional or key-missing) fallback. The obvious, everywhere-used consumption pattern is `ChatUtil.color(messages.getString(path))`, and `ChatUtil.color` immediately does `Objects.requireNonNull(message)` (`ChatUtil.java:53-54`), so any config-drift scenario — a message key referenced by newly-shipped code before the admin's on-disk YAML and the jar's pristine default are both updated, or an admin accidentally deleting a key with no jar fallback configured — turns a missing translation into an uncaught NPE crash instead of a degraded/placeholder message.~~Medium
13~~P1~~ResourcePackTracker.active is one process-wide static shared by every consumer plugin, so each plugin installing its own tracker clobbers the others~~Key the active tracker by owning plugin (e.g. a Map<Plugin, ResourcePackTracker>) instead of a single static field, or require SoundEffect callers to pass their own tracker explicitly~~-~~keystone-common/src/main/java/org/luckyraven/keystone/sound/ResourcePackTracker.java:24,59-69~~`private static volatile ResourcePackTracker active;` with `install()`/`active()` as plain get/set is explicitly documented as "process-wide... one tracker per server," but Keystone's own repo rule is that "every consumer plugin sees ONE loaded copy of each Keystone class... a static singleton/registry/cache holding per-plugin data is a bug." A resource-pack-loaded flag is inherently per-plugin (each of Gangland/Bartizan/Oriel ships its own distinct resource pack), so if two of the three consumer plugins on one server each construct and `install()` their own tracker at bootstrap, the second installer's tracker silently becomes the sole "active" one system-wide; `SoundEffect.playSound`/`playAtLocation` for the first plugin's custom sounds then check the wrong tracker (or none, if uninstalled), so its resource-pack gate either wrongly blocks players who do have the pack or wrongly plays sounds to players who don't.~~High
14~~P2~~StaticResets omits EconomyHandler.vaultEconomy, a confirmed real process-wide static, forcing every test to hand-roll its own save/restore~~Add clearVaultEconomy()/a setVaultEconomy(null)-plus-restore helper to StaticResets (and include it in resetAll()) for EconomyHandler.vaultEconomy~~-~~keystone-testkit/src/main/java/org/luckyraven/keystone/testkit/StaticResets.java:1-76~~`StaticResets` documents itself as covering "Keystone's process-wide static caches" (NmsVersion, CraftBukkitRevision, NbtBridge, PacketBridge, ResourcePackTracker) so suites "don't leak state into the next test class," but it has no method for `EconomyHandler.vaultEconomy` (`keystone-hooks/src/main/java/org/luckyraven/keystone/economy/EconomyHandler.java:27`, `private static Economy vaultEconomy;`), which is exactly the same kind of process-wide static this class exists to manage. `EconomyHandlerTest` is aware of the risk and manually saves/restores the static itself via `EconomyHandler.getVaultEconomy()`/`setVaultEconomy(...)` in `@BeforeEach`/`@AfterEach` — but any other test in the suite that indirectly sets `vaultEconomy` (e.g. a Vault-detection integration test) and doesn't replicate that dance leaks Vault economy state into whichever test class runs next in the same JVM, since `StaticResets.resetAll()` silently does not touch it.~~High
15~~P2~~DbFiles.release relies solely on a fixed retry loop for Windows SQLite file-lock cleanup, contradicting this ecosystem's own documented lesson that retries alone are insufficient~~Add an explicit WAL checkpoint / connection-pool-closed synchronization step (or a longer, backoff-based wait) before/alongside the delete retries, matching the sibling repo's documented guidance~~-~~keystone-testkit/src/main/java/org/luckyraven/keystone/testkit/DbFiles.java:19-42~~`release(Path dir)` deletes `*.db`/`*-wal`/`*-shm` files in a loop of "up to 20 times with 50 ms pauses" (1 second total) as its only strategy for the documented problem that "HikariCP background threads may briefly hold OS file handles after dataSource.close() returns." The sibling Gangland Warfare repo, which hit this exact HikariCP/SQLite/Windows issue in production test runs, explicitly records as an institutional rule: "Do not use retry loops as the primary cleanup strategy — Windows file locks can persist for several seconds and retries alone are insufficient." `DbFiles.release` is precisely such a retry-only strategy with a fixed ~1 s budget, so it can leave `.db`/`-wal`/`-shm` files locked and undeleted on a slower CI/Windows box, silently returning without signaling that cleanup failed (the loop just falls through after 20 attempts).~~Medium
16~~P2~~ActionBarManager's static per-player maps never evict entries for players who disconnect, growing without bound over server uptime~~Remove a player's entries from foregroundUntil/backgroundSlot on PlayerQuitEvent (expose a cleanup(UUID) method consumers wire to their quit listener)~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/ActionBarManager.java:49,55~~`private static final Map<UUID, Long> foregroundUntil = new ConcurrentHashMap<>();` and `private static final Map<UUID, BackgroundSlot> backgroundSlot = new ConcurrentHashMap<>();` are populated on every `send`/`sendBackground` call (`foregroundUntil.put(...)`, `backgroundSlot.put(...)`) but nothing in this class ever calls `.remove(uuid)` for a departed player, and there is no listener or lifecycle hook exposed to do so. Being static, these maps are shared and persist for the entire server process across every consumer plugin; on a long-running public server with many distinct joiners over weeks/months, both maps grow monotonically for the lifetime of the process.~~Medium
17~~P3~~ServerEnvironment's javadoc names the consumer plugin "Oriel" inside Keystone~~Drop the consumer name from the javadoc (describe the use case generically, e.g. "a Brigadier registrar picking between a Paper listener and a Commodore-shared-tree backend")~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/ServerEnvironment.java:102-103~~The class javadoc reads `"Used today by Oriel's Brigadier registrar to pick between the Paper event listener backend and the Spigot Commodore-shared-tree backend."` The repo's own CLAUDE.md layering rule states "Keystone must not know its consumers. Any Gangland/Oriel/Bartizan product concept or symbol inside Keystone is a bug (javadoc-only mentions are P3)" — this is exactly that: a bare javadoc mention of a specific consumer plugin by name.~~High
18~~P2~~ChatUtil.capitalize throws on an empty string instead of returning it unchanged~~Return the input unchanged (or empty string) when text.isEmpty() before calling substring~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/ChatUtil.java:144-146~~`public static String capitalize(@NotNull String text) { return text.substring(0, 1).toUpperCase() + text.substring(1); }` has no length check; `ChatUtil.capitalize("")` throws `StringIndexOutOfBoundsException` instead of returning `""`. This is a plausible admin-input path (an empty category/name string from config or a command argument being title-cased for display). No test in ChatUtilTest exercises capitalize at all.~~Medium
