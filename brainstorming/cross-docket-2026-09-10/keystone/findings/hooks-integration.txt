1~~P0~~EconomyHandler.setAmount discards both Vault EconomyResponses, creating or destroying money when either leg fails~~Check transactionSuccess() on the withdraw before depositing, and abort (or compensate) when either response fails~~EconomyHandlerTest (userMode_setAmountWithdrawsEverythingThenDepositsTheNewBalance pins the call order without asserting response handling)~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/EconomyHandler.java:58-66~~setAmount implements "set the balance" as a withdraw-everything-then-deposit-the-new-value pair and throws away both return values: `vaultEconomy.withdrawPlayer(offlinePlayer, vaultEconomy.getBalance(offlinePlayer)); vaultEconomy.depositPlayer(offlinePlayer, this.amount.doubleValue());`. Vault reports failure through the returned EconomyResponse rather than by throwing, so when the backing economy plugin refuses the withdraw — a frozen or bank-backed account, a plugin-side limit, a race with another transaction — the deposit still runs and the player keeps their old balance plus the new amount, creating money out of nothing. The mirror case destroys it: a successful withdraw followed by a rejected deposit leaves the account at zero. The pair is also non-atomic, so a server crash or exception between the two calls loses the whole balance, and every consumer that sets a balance through this class inherits the fault.~~High
2~~P1~~EconomyHandler.depositAmount accepts a negative delta, bypassing withdrawAmount's insufficient-funds guard~~Reject negative deltas in depositAmount (throw EconomyException) so every debit routes through withdrawAmount's balance check~~-~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/EconomyHandler.java:68-70~~depositAmount is simply `setAmount(getAmount().add(Currency.of(delta)));` with no sign validation, while the sibling withdrawAmount explicitly guards with `if (normalised.compareTo(current) > 0) throw new EconomyException("Amount exceeding balance");`. Currency.of only rescales the value (`return raw.setScale(SCALE, ROUNDING_MODE);`) and never rejects a negative, so a mis-signed refund or penalty delta debits the account through the path that has no balance check and can drive the computed balance below zero. setAmount then withdraws everything and calls depositPlayer with a negative amount, which most Vault economies reject outright, leaving the player at zero with their original balance gone. Filed P1 rather than P0 because it takes a caller passing a negative delta, but the consequence is real money loss.~~High
3~~P2~~EconomyHandler gates on vaultEconomy != null but never on vaultEconomy.isEnabled()~~Check isEnabled() alongside the null check, matching VaultEconomyProvider.isAvailable()~~-~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/EconomyHandler.java:51-62~~getAmount() and setAmount() both gate purely on `useUserInfo() && vaultEconomy != null`, treating a non-null reference as a working economy. VaultEconomyProvider.isAvailable() treats isEnabled() as the real availability signal for the very same Economy type (VaultEconomyProvider.java:39-41), so the two classes disagree about what "the economy is usable" means. If the economy plugin behind Vault is present but disabled, balance reads return whatever the disabled provider yields and writes go into a provider that is not accepting them, with no error surfaced to the caller.~~High
4~~P2~~VaultEconomyProvider, VaultGroupProvider and VaultPermissionProvider do not fail closed on a throwing Vault backend~~Wrap the Vault calls in try/catch that reports through Diagnostics and returns the fail-closed value, as VaultOfflinePermissionService already does~~-~~keystone-hooks/src/main/java/org/luckyraven/keystone/vault/economy/VaultEconomyProvider.java:53-61~~withdraw and deposit call `economy.withdrawPlayer(...)` / `economy.depositPlayer(...)` with no guard, and VaultGroupProvider.inGroup/primaryGroup plus VaultPermissionProvider.add/remove do the same. The one class in this package that does defend itself is VaultOfflinePermissionService, which wraps its lookups in try/catch and reports through Diagnostics — so the fail-closed pattern is established house style and these four are the gap. A third-party permission or economy plugin throwing from its Vault implementation therefore propagates out of Keystone into whatever consumer call site was running, instead of degrading to a denied permission or a reported fault.~~High
5~~P3~~EconomyHandler.vaultEconomy is a non-volatile static read and written without synchronization~~Mark the field volatile so a runtime setVaultEconomy is visible to other threads~~EconomyHandlerTest (saveStatic/restoreStatic treat it as a server-global fact but do not exercise cross-thread visibility)~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/EconomyHandler.java:25-27~~`private static Economy vaultEconomy;` carries Lombok-generated static accessors and is read on every getAmount()/setAmount() while being settable at any time via setVaultEconomy(...). The static itself is legitimate under the shared-classloader rule — the server's Vault economy is a server-global fact, the explicitly permitted case — so the only defect is the missing volatile, which leaves a late or re-registered economy potentially invisible to a thread that already cached the field. Economy calls are expected on the main thread, so this is a memory-model tidiness issue rather than an observed fault.~~Medium
6~~P1~~EconomyHandler.withdrawAmount accepts a negative delta and credits the account~~Reject normalised.signum() < 0 in withdrawAmount (and in depositAmount per #2) with EconomyException~~EconomyHandlerTest~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/EconomyHandler.java:72-77~~[found 2026-09-24, master 4192baa] withdrawAmount only checks `if (normalised.compareTo(current) > 0) throw new EconomyException("Amount exceeding balance");`, which a negative delta always passes, then runs `setAmount(current.subtract(normalised))`, so withdrawing -100 adds 100. This is the mirror of #2 (a negative deposit), and #2's fix direction only touches depositAmount. A concrete trigger exists today: Gangland's GangWithdrawCommand parses `Currency.parse(args[2])` from the player's command, checks only `gang.getEconomy().getAmount().compareTo(argAmount) < 0` (false for a negative amount), then calls `gang.getEconomy().withdrawAmount(argAmount)`, so `/gang withdraw -1000` inflates the gang vault. Keystone's balance guard is the only sign check on this path.~~High
7~~P1~~Currency.parse accepts huge exponents, so one command argument can freeze the server~~Reject exponent notation (or bound precision()/scale() before setScale) and return a USER_ERROR instead of computing the value~~CurrencyTest~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/Currency.java:48-55~~[found 2026-09-24, master 4192baa] parse does `new BigDecimal(cleaned).setScale(SCALE, ROUNDING_MODE)` on raw player input. BigDecimal accepts `1e20000000`, and setScale(2) then materialises a 66-million-bit unscaled integer: measured at about 6 s of CPU for 1e20000000 and 0.8 s for 1e5000000, with cost rising faster than linearly, so a larger exponent reaches the 60 s Spigot watchdog. A negative exponent such as `1e-999999999` instead throws `ArithmeticException: BigInteger would overflow supported range`, an unchecked exception that callers catching only NumberFormatException do not handle. Gangland passes command arguments straight in (`BigDecimal argAmount = Currency.parse(args[2]);` in GangWithdrawCommand, and the deposit path does the same), so any player who can run the command can stall the server.~~High
8~~P1~~Bank.accrueInterest advances the clock when interest rounds to 0.00, losing it for good~~Only advance lastInterestAt when Currency.of(actual) is non-zero (or carry the fractional remainder), and return the rounded amount actually deposited~~BankTest (daily-granularity cases only)~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/bank/Bank.java:77-98~~[found 2026-09-24, master 4192baa] Interest is computed as a double, `double interest = balance * ratePerDay * elapsedDays;`, then deposited through `economy.depositAmount(Currency.of(actual))`, which rounds HALF_UP to cents, and `lastInterestAt = now;` runs regardless. Gangland's GanglandBankerEconomy.maintain calls accrueInterest on every banker interaction (documented as safe to call every interaction), so a 1000 balance at 1%/day accrues 0.00023 per interaction two seconds apart, which rounds to 0.00 while the elapsed time is consumed: active players never earn interest. In the opposite band, where each call's interest lands between 0.005 and 0.01, HALF_UP credits 0.01 each time and creates money. The method also returns the unrounded `actual`, so the caller reports interest that differs from what was credited.~~High
9~~P2~~PapiExpansionAdapter has no getRequiredPlugin, so PAPI keeps it after the owner disables~~Store the plugin name and override getRequiredPlugin() to return it (PAPI then unregisters it on PluginDisableEvent)~~-~~keystone-hooks/src/main/java/org/luckyraven/keystone/papi/PapiExpansionAdapter.java:61-92~~[found 2026-09-24, master 4192baa] The adapter overrides getIdentifier/getAuthor/getVersion and `public boolean persist() { return true; }` but not getRequiredPlugin(), so it inherits null. PlaceholderAPI's LocalExpansionManager.onPluginDisable (verified in the 2.11.3 bytecode) unregisters an expansion only when `expansion.getRequiredPlugin()` equalsIgnoreCase the disabling plugin's name, and because the adapter class is loaded by Keystone's classloader, nothing else ties it to the consumer. Gangland registers `new PapiExpansionAdapter(this, FULL_PREFIX, placeholder)` and never unregisters it, so after Gangland disables (a failed enable or a plugin-manager reload), `%gangland_*%` keeps routing into the dead plugin's delegate and pins its classloader.~~High
