1~~P1~~NpcCombatDelegate.attackEntity() never consults the target-filter SPI that attack(Player) enforces~~Add the same `!owner.targetFilter().isAttackable(target)` short-circuit at the top of attackEntity() that attack(Player) has~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcCombatDelegate.java:72-90~~attack(Player) opens with `if (player.isDead() || !owner.targetFilter().isAttackable(player)) return;` but attackEntity() opens with only `if (target.isDead()) return;` and never calls `owner.targetFilter()` anywhere in its body. AbstractNpc.attack(Player)'s javadoc explicitly promises "Does nothing if the target fails the target filter" while attackEntity() carries no such guarantee and doesn't deliver one, so any consumer's pluggable filter (e.g. a downed-player or friendly-fire check) is silently bypassed for every NPC-vs-LivingEntity attack path. NpcCombatDelegateTest only exercises attack(Player), leaving attackEntity() with zero coverage of this gap.~~High
2~~P1~~AbstractNpc.destroy() has no fault isolation between rangedAttack.onDestroy(), the despawn/onDespawn callback and cleanup/npc.destroy()~~Wrap rangedAttack.onDestroy() and cleanupTransientState() each in their own try/catch (or route through Diagnostics) so one thrower can't skip npc.despawn()/onDespawn or npc.destroy()~~AbstractNpcDestroyTest (covers the happy path only, not a failure path)~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/AbstractNpc.java:160-175~~`rangedAttack.onDestroy();` runs before the `if (npc.isSpawned())` block, so if a consumer's ranged-attack SPI implementation throws there, `npc.despawn()` and the `onDespawn` consumer callback (documented as the caller's chance to "release entity-keyed state (marks, registries, ...)") never fire, leaking that state forever. Separately, the finally block is `cleanupTransientState(); npc.destroy();` with no try/catch between them, so a throwing cleanupTransientState() (subclass-defined, not under Keystone's control) skips `npc.destroy()` entirely, leaving the NPC alive in Citizens' registry. Repo convention requires faults to flow through Result/Pipeline+Diagnostics rather than an unguarded throw that can skip cleanup.~~Medium
3~~P1~~NpcNavigationDelegate never clears ladderClimbActive if the NPC's entity goes transiently null mid-climb, permanently stalling navigation~~In updateNavigationProgress()/tickLadderClimb(), reset ladderClimbActive (and restore gravity if possible) on the null-entity path instead of returning with the flag still set~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcNavigationDelegate.java:414-426~~`updateNavigationProgress()` does `LivingEntity entity = owner.getEntity(); if (entity == null) { return; }` before ever checking `ladderClimbActive`, and `tickLadderClimb()` (called only when entity is non-null) has the same shape: `if (entity == null) { return; }` with no flag reset. If Citizens momentarily reports a null entity while a climb is active (chunk unload/reload cycling the backing entity, since the same AbstractNpc/delegate instance persists), `ladderClimbActive` is stuck `true` forever. Every future call to navigateTo() then hits `if (ladderClimbActive) { navPlanTarget = destination.clone(); return; }` at line 103-106 and never issues another Citizens navigation command, freezing that NPC's pathing permanently even after its entity comes back.~~Medium
4~~P2~~NpcCombatDelegate.faceTarget/faceTargetEntity and the melee knockback calc call Vector.normalize() on a possibly zero-length vector~~Guard with a lengthSquared() check before normalize(), mirroring the pattern already used throughout NpcNavigationDelegate (resolveLateralExploration, findLineApproachLocation, isFrontedByImmediateGap)~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcCombatDelegate.java:111-120~~`faceTarget` computes `player.getEyeLocation().toVector().subtract(shooterEye.toVector()).normalize()` with no length check, and it runs unconditionally at the top of every attack() call via `faceTarget(player);`. If the NPC and target ever occupy coincident eye coordinates (teleport collision, both pathed onto the same spot), Bukkit's Vector.normalize() throws IllegalArgumentException("Cannot normalize a vector of zero length"), crashing whatever AI tick invoked attack(). The same unguarded pattern repeats in faceTargetEntity (~122-130) and in the knockback vectors of performMeleeAttack (192-199) and performMeleeAttackOnEntity (211-218) — notably inconsistent with NpcNavigationDelegate, which checks `lengthSquared() <= 0.0001` before every normalize() it performs.~~High
5~~P2~~EntitySpawner.findSpawnLocation's phase-2 shrink loop can spin forever if getSpawnRadiusShrinkStep() is 0 or negative~~Validate/clamp the configured shrink step to a positive minimum before using it as a loop decrement, or bound the loop by iteration count instead of the radius value~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/entity/EntitySpawner.java:190-196~~`for (double max = maxDist; max >= minDist; max -= shrinkStep)` decrements `max` by `config.getSpawnRadiusShrinkStep()` each iteration with no lower bound check on that value. A misconfigured `NPC_Navigation`/spawn YAML shrink-step of 0 (or a negative typo) makes `max` never decrease below `minDist`, spinning the main thread forever on every call to findSpawnLocation() and hanging the server — this is reachable from ordinary NPC-spawn attempts, not just admin tooling.~~Medium
6~~P3~~resetNavigationTracking()'s ladderClimbActive branch is dead code~~Either drop the dead branch or change stopNavigation() to call resetNavigationTracking() even while ladderClimbActive, whichever was actually intended~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcNavigationDelegate.java:216-220~~`stopNavigation()` is `if (ladderClimbActive) return; owner.npc.getNavigator().cancelNavigation(); resetNavigationTracking();` — the only call site of resetNavigationTracking() already returns early whenever ladderClimbActive is true. Yet resetNavigationTracking() itself (lines 494-499) still contains `if (ladderClimbActive) { ...entity.setGravity(true); }`, a branch that can never execute given its single caller's guard. The asymmetry suggests the intended behavior (being able to force-cancel a ladder climb via stopNavigation()) was never wired up.~~High
7~~P3~~NpcMetadata bakes Gangland-specific product vocabulary (trader/banker/turf-powerup) into shared keystone-npc infrastructure~~Move TRADER_ID/BANKER_ID/TURF_ID out of Keystone into Gangland's own constants class now that they aren't referenced anywhere inside keystone-npc; keep only the generic MARK_KEY here~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcMetadata.java:11-25~~`TRADER_ID = "gangland.trader.id"`, `BANKER_ID = "gangland.banker.id"` and `TURF_ID = "gangland.turfpowerup.turfid"` are declared in `org.luckyraven.keystone.npc` — a module also consumed by Bartizan and Oriel — but are not referenced anywhere in keystone-npc's own sources (only MARK_KEY is, by NpcMarkManager). This is exactly the "trader shops... must stay in the consumer, reached through the SPI" case the module's own layering rule calls out; the class javadoc defends it as a deliberate backward-compat exception, but the constants could be published from Gangland itself (as NpcMarkDefaults already models for per-EntityType marks) without breaking the persisted keys, since NamespacedKey strings don't require living in this module to stay stable.~~Medium
