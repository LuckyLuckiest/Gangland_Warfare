1~~P2~~DefaultPlaceholderProvider reads world and chunk state with no documented main-thread contract~~Document the provider as main-thread-only and resolve %biome% from a cached value, or guard the chunk-loading reads~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/provider/DefaultPlaceholderProvider.java:62-71~~resolve() reaches into live world state for several tokens, the sharpest being `s = replace(s, "%biome%", () -> online.getLocation().getBlock().getBiome().name());` — getBlock() touches the chunk and can trigger a chunk load — alongside `online.getWorld().getName()`, the location reads and `Bukkit.getOnlinePlayers().size()`. Placeholder resolution is exactly the kind of text rendering consumers commonly drive from async scoreboard or timer tasks, and neither this class nor the PlaceholderProvider interface states a threading requirement anywhere. No async caller exists inside Keystone today, so this is filed as a latent trap rather than a proven main-thread violation — but it becomes a P0 the moment any of the three consumers renders a board off-thread, and nothing in the API warns them.~~High
2~~P2~~CompositePlaceholderProvider.resolve isolates nothing, so one throwing provider breaks the whole chain~~Wrap each provider call in try/catch that reports through Diagnostics and passes the previous value through~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/CompositePlaceholderProvider.java:63-67~~The chain is a bare loop: `for (PlaceholderProvider provider : providers) { value = provider.resolve(player, value); }`. The PlaceholderProvider contract does say implementations should never throw, but this class exists precisely to chain providers it does not control — including ones registered by a consumer plugin — so a single misbehaving provider propagates its exception out of the whole resolve call and takes down the chat message, scoreboard line or GUI title being rendered. The repo's own house pattern elsewhere (VaultOfflinePermissionService) is to catch, report and degrade, which is what the composite should do here.~~High
3~~P2~~PlaceholderHandler inherits PlaceholderRequest.onRequest returning null, so direct use silently resolves nothing~~Make PlaceholderHandler abstract on onRequest so a subclass must supply it, instead of inheriting a null-returning default~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/PlaceholderHandler.java:14-27~~PlaceholderHandler extends PlaceholderRequest but declares no onRequest override, so it inherits the base implementation's `return null;`. CharReplacer.apply treats a null result as unresolved and reconstructs the original `%token%` text verbatim, so a consumer instantiating PlaceholderHandler directly — which its public constructor invites — gets a handler that quietly resolves nothing and leaves raw tokens in player-facing text. There is no compile-time signal and no runtime warning that the class must be subclassed to do anything.~~Medium
4~~P2~~Built-in placeholder providers NPE on a null OfflinePlayer that the sibling effect SPI accepts~~Null-check player at the top of each provider's resolve and skip the player-scoped tokens~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/provider/CooldownPlaceholderProvider.java:81~~CooldownPlaceholderProvider dereferences the player unconditionally once its quick contains-check passes (`UUID uuid = player.getUniqueId();`), and MetaPlaceholderProvider plus DefaultPlaceholderProvider have the same gap (`player.getName()` at DefaultPlaceholderProvider.java:56). The placeholder effect SPI alongside them explicitly supports a null player for server-scoped text, so the two halves of the same subsystem disagree on whether null is legal. A consumer rendering server-wide text with no player context therefore gets an NPE out of a provider rather than the server-scoped tokens it expected.~~Medium
5~~P2~~The flash placeholder effect layer is fully built and tested but wired to nothing, and holds classloader-wide static state~~Decide it: either wire the effect layer into CharReplacer/PlaceholderHandler and namespace its static state per plugin, or delete it~~FlashEffectTest, FlashPlaceholderWrapperTest~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/effect/FlashPlaceholderWrapper.java:1-89~~A repo-wide grep for processFlash, processConditionalFlash, isFlashPlaceholder, FlashEffect and FlashPlaceholderWrapper outside the effect package and its own tests returns zero hits — nothing in PlaceholderHandler, CharReplacer, PlaceholderProvider or CompositePlaceholderProvider ever calls into it, so no consumer's `%flash...%` token can ever resolve despite the feature being documented, implemented and unit-tested. It also carries state that would breach the shared-classloader rule the moment it were wired: `private static final Map<String, FlashState> flashStates = new HashMap<>();` keyed on raw placeholder text with no per-plugin namespace, plus a JVM-wide `@Setter private static long currentTick`. The two halves are one decision, so they are filed together rather than as separate findings.~~High
6~~P3~~PlaceholderHandler recompiles its Pattern and format string on every call~~Hoist the compiled Pattern into a static final field and precompute the prefix format per instance~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/PlaceholderHandler.java:37-47~~containsPlaceholder builds a fresh matcher each time with `Pattern.compile(PLACEHOLDER_PATTERN).matcher(text).find()` even though PLACEHOLDER_PATTERN is a static final constant, and replacePlaceholder recomputes `String.format("%%%s_", prefix)` although prefix is fixed per instance. Placeholder resolution runs per line per player per scoreboard tick, so this recompiles a regex thousands of times a second on a populated server. Pure waste rather than incorrect output, hence P3.~~High
7~~P3~~Placeholder effect javadoc names Gangland domain concepts (wanted level, gang bounty, GTA-style)~~Rewrite the examples with neutral placeholder names~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/effect/ConditionalFlashWrapper.java:11-21~~The class javadoc teaches the feature with `%flashif:user_wanted>0:5:5:user_wanted%` and `%flashif:gang_bounty>0:user_bounty%`, and FlashEffect.java:9 describes the whole thing as "a GTA-style flash effect". Wanted levels and gang bounties are Gangland Warfare's own domain entities, so shared infrastructure is documenting itself in one consumer's vocabulary — the same layering rule broken by the diagnostics javadoc filed under DG, but in different files with a different owner. Comments only, so nothing misbehaves; it misleads the other two consumers reading the same API.~~High
