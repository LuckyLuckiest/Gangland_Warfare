1~~P1~~NpcCombatDelegate.attackEntity never consults the targetFilter SPI that attack(Player) enforces~~Add the same owner.targetFilter().isAttackable(target) guard to attackEntity that attack(Player) already applies~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcCombatDelegate.java:72-74~~attack(Player) opens with `if (player.isDead() || !owner.targetFilter().isAttackable(player)) return;` but the entity-typed sibling opens with only `if (target.isDead()) return;` and never calls owner.targetFilter() anywhere in its body. NpcTargetFilter is the SPI through which a consumer expresses what its NPC may not hit — allied gang members, civilians, protected mobs — so any consumer path that routes through attackEntity rather than attack(Player) has its protection silently skipped. The NPC then damages entities the consumer explicitly marked unattackable, which is exactly the product decision the layering rules push out to the SPI.~~High
2~~P2~~AbstractNpc.destroy skips despawn and the onDespawn callback when rangedAttack.onDestroy throws~~Wrap rangedAttack.onDestroy() in its own try/catch so a failing SPI cannot skip the despawn callback~~AbstractNpcDestroyTest (happy path only)~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/AbstractNpc.java:160-175~~destroy() runs `rangedAttack.onDestroy();` as the first statement inside the try, before the `if (npc.isSpawned())` block that fires the onDespawn consumer callback and `npc.despawn()`. A consumer's ranged-attack SPI implementation that throws there skips both, and the finally block only covers cleanupTransientState() and npc.destroy(). The onDespawn callback is documented one line above as the caller's chance to "release entity-keyed state (marks, registries, ...)", so a throwing SPI permanently leaks that consumer state while the NPC itself is still destroyed — an inconsistent half-teardown rather than a clean failure.~~High
3~~P3~~NpcNavigationDelegate leaves ladderClimbActive set when the entity is transiently null, stalling navigation permanently~~Check and clear ladderClimbActive before the entity==null early return in updateNavigationProgress~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcNavigationDelegate.java:414-426~~updateNavigationProgress reads `LivingEntity entity = owner.getEntity(); if (entity == null) { return; }` and only afterwards tests `if (ladderClimbActive) { tickLadderClimb(); return; }`, so a null entity during a climb skips the tick that would normally end it and the flag stays true. That flag then disables all recovery: stopNavigation is `if (ladderClimbActive) return; owner.npc.getNavigator().cancelNavigation(); resetNavigationTracking();` (line 216-219), so it returns without cancelling anything. Citizens reporting a momentarily null entity — a chunk unload or a despawn/respawn cycle — therefore leaves that NPC unable to navigate for the rest of its life, and its gravity restoration in resetNavigationTracking never runs either.~~Low
4~~P1~~EntitySpawner.findSpawnLocation loops forever on the main thread when getSpawnRadiusShrinkStep returns zero or negative~~Validate the shrink step is greater than zero before the loop and fall back to a sane default~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/entity/EntitySpawner.java:190-196~~The phase-2 search is `for (double max = maxDist; max >= minDist; max -= shrinkStep)` where `double shrinkStep = config.getSpawnRadiusShrinkStep();` comes straight from the consumer's SpawnConfigProvider with no bounds check. A value of 0 never decrements max, and a negative value increases it, so the loop never terminates while running on the main server thread — the whole server hangs and the watchdog eventually kills it. The trigger is a single mistyped spawn-radius value in a consumer's NPC YAML, and nothing between the config read and the loop rejects it.~~High
5~~P2~~NpcCombatDelegate.faceTarget normalizes a possibly zero-length vector, producing a NaN facing direction~~Guard with lengthSquared() == 0 (or use Vector#isZero) before normalize and skip the facing update~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcCombatDelegate.java:111-120~~faceTarget computes `Vector direction = player.getEyeLocation().toVector().subtract(shooterEye.toVector()).normalize();` with no length check, then feeds it to `entity.getLocation().setDirection(direction)` and teleports the NPC there. Bukkit's normalize divides by the vector length, so coincident eye positions — an NPC and target teleported onto the same spot, or both pathed into one block — yield NaN components and a teleport to a non-finite location, which Bukkit rejects with IllegalArgumentException. faceTarget runs unconditionally at the top of every attack, and faceTargetEntity plus the melee knockback calculation repeat the same unguarded normalize.~~High
6~~P3~~resetNavigationTracking's ladderClimbActive branch is unreachable from its only caller~~Restore gravity where the climb actually ends, or drop the unreachable branch once finding 3 is fixed~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcNavigationDelegate.java:494-500~~resetNavigationTracking contains `if (ladderClimbActive) { LivingEntity entity = owner.getEntity(); if (entity != null) { entity.setGravity(true); } }`, but its caller stopNavigation returns early precisely when that flag is set, so the branch cannot run from that path. The gravity restoration it performs is therefore not reachable where it was intended to matter, which is the same root confusion as finding 3 — the flag guards both the cleanup and the caller that would perform it. Cosmetically dead today; it should be resolved together with finding 3 rather than deleted blindly.~~Medium
7~~P1~~EntitySpawner behind/visibility checks treat Bukkit yaw as a math angle, off by 90 degrees~~Compare against the Minecraft facing angle (Math.toRadians(yaw) + PI/2, or a dot product with Location#getDirection()) in both isSpawnBehindPlayer and isVisibleToOtherPlayers~~EntitySpawnerTest (no yaw coverage)~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/entity/EntitySpawner.java:308-313~~[found 2026-09-24, master 4192baa] isSpawnBehindPlayer does `double playerYaw = Math.toRadians(playerLoc.getYaw());` and `double toSpawnAngle = Math.atan2(spawnLoc.getZ() - playerLoc.getZ(), spawnLoc.getX() - playerLoc.getX());` and treats angleDiff > PI/2 as behind. atan2(dz, dx) is measured from +X, but Bukkit yaw 0 faces +Z (direction (-sin yaw, cos yaw)), so the reference is off by exactly 90 degrees: for a player facing south the "behind" half-plane is the -X half, and half of that is in front of the player. isVisibleToOtherPlayers (lines 119-123) has the identical error, so a location straight ahead of another player is 90 degrees off its 60-degree cone and reported unseen. In normal play, phase-1 spawns land in the player's field of view and despawns happen in plain sight of bystanders. NpcNavigationDelegate.navigateTo (line 207) and findForwardWanderDestination (lines 375-376) already use the correct `-sin`/`cos` convention, which confirms the intent.~~High
8~~P1~~isNavigationHopeless is always false after the first navigateTo; navPlan never empties~~Test plan exhaustion (navPlanStep >= navPlan.size()) instead of navPlan.isEmpty(), and clear navPlan in resetNavigationTracking and on exhaustion~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcNavigationDelegate.java:235-241~~[found 2026-09-24, master 4192baa] isNavigationHopeless opens with `if (!navPlan.isEmpty()) return false;`. navPlan changes only through `navPlan.clear();` followed by `navPlan.addAll(buildNavPlan(destination));` on replan (lines 117-121), and buildNavPlan always appends at least the final NONE step (line 1094). Plan exhaustion only nulls navPlanTarget, and resetNavigationTracking never touches navPlan. After the first navigateTo the list is therefore permanently non-empty, and the method always returns false whatever consecutiveStuckChecks says. Gangland's PursuingBehavior (`if (cop.isNavigationHopeless()) { cop.transitionTo(CopState.RETURNING); ...`) and the civilian combat/flee behaviours rely on it, so an NPC chasing an unreachable target never gives up and stays stuck, and the hopeless fallback those consumers gate on it never runs.~~High
9~~P3~~scaleCooldown's 5-tick floor equals the melee base, so HARD and DEADLY never speed melee~~Put the floor below the base (e.g. Math.max(scaled, 1) or a separate minimum constant), or raise the melee base so the multiplier has room~~NpcDifficultyTest (enum values only)~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcCombatDelegate.java:243-246~~[found 2026-09-24, master 4192baa] `int scaled = (int) Math.round(baseCooldown * owner.difficulty.getFireRateMultiplier()); return Math.max(scaled, 5);` is called with base 5 from both melee paths (`owner.attackCooldown = scaleCooldown(5);` at lines 190 and 209). HARD (0.85 -> 4) and DEADLY (0.7 -> 4) both clamp back to 5, the same as NORMAL, so the fireRateMultiplier that NpcDifficulty's javadoc says multiplies "the melee perform-* paths" can only slow melee (EASY) and never speeds it up. Only the vanilla-ranged base of 15 actually varies across the faster tiers.~~High
10~~P3~~NpcMarkManager caches marks by entity UUID with no eviction for dead or unloaded entities~~Drop the cache (a PDC read is cheap), or evict on death/removal, e.g. key a WeakHashMap by Entity~~NpcMarkManagerTest~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/entity/NpcMarkManager.java:48-70~~[found 2026-09-24, master 4192baa] setMark does `entityMarks.put(entity.getUniqueId(), mark);` and getMark caches every persisted mark it reads (`entityMarks.put(entity.getUniqueId(), markStr);`), but the only removals are removeMark and onClear/clearCache on reload. An entity that is marked and then killed, removed by the server or unloaded with its chunk keeps its entry until the next reload. A server that marks every spawned cop or civilian therefore grows the HashMap for its whole uptime. Keystone offers no eviction hook, and AbstractNpc.destroy's onDespawn callback only covers entities torn down through that path. Each entry is small, but the map is unbounded.~~Medium
11~~P3~~NpcNavigationConfig.getMinRepathAfterLossTicks is required of consumers but never read~~Wire it into the path-loss re-path throttle it documents, or deprecate it~~-~~keystone-npc/src/main/java/org/luckyraven/keystone/npc/NpcNavigationConfig.java:56-60~~[found 2026-09-24, master 4192baa] The interface requires `int getMinRepathAfterLossTicks();`, documented as the minimum AI ticks before "a path-loss recovery re-path is allowed", but no class in keystone-npc calls it: AbstractNpc reads getAiTickRate and NpcNavigationDelegate's constructor reads the other knobs. Gangland still parses `Min_Repath_After_Loss_Ticks` from settings.yml (Settings.java:606) and forwards it through GanglandCopSettings, so admins tune a key that has no effect.~~High
