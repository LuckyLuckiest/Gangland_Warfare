1~~P1~~FileManager.runInitializer regenerates the live config from jar on any Exception, not just parse failures~~Only regenerate on parse/IO exceptions from the file itself; let unrelated initializer exceptions propagate or be logged without touching the file~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/FileManager.java:253-277~~runInitializer wraps the whole consumer callback in `catch (Exception first)` with no filtering on exception type, then calls `handler.createNewFile()` on any failure. createNewFile moves the admin's live file aside — `Files.move(file.toPath(), oldFile.toPath(), StandardCopyOption.REPLACE_EXISTING);` (FileHandler.java:125) — and writes the jar default in its place, logging "is an old build or corrupted, creating a new one". So a bug anywhere inside a consumer's initialize() (an NPE reading an unrelated manager, a NumberFormatException on a value it computed) silently swaps the admin's tuned config for defaults on the next boot. The edited file survives as `<name>-old.yml` so this is recoverable rather than true data loss, which is why it is P1 and not P0; the server still comes up running default settings with only a warn line.~~High
2~~P1~~FileHandler.registerYamlFile swallows InvalidConfigurationException with no log at all~~Log the parse failure through logException/Diagnostics before setting loaded=false~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/FileHandler.java:248-256~~The YAML load is guarded by `catch (InvalidConfigurationException exception) { loaded = false; }` — no log call, unlike every sibling catch in the class which routes through logException/log.warn/log.error. An admin who leaves a tab character or a duplicate key in a config gets no message whatsoever; the handler simply reports not-loaded and every value silently falls back to defaults. The identical silent pattern repeats in createNewFile's post-regeneration reload at line 139, `catch (IOException | InvalidConfigurationException ignored) { loaded = false; }`. This is the archetypal admin-invisible failure the repo's own Result/Diagnostics rule exists to prevent.~~High
3~~P2~~ConfigSerializer.scalarToJava does not strip digit-grouping underscores, so 10_000_000 degrades to a String~~Run the value through the same normalizeNumeric helper NodeReader uses before parseLong/parseDouble~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/config/ConfigSerializer.java:172-189~~NodeReader treats underscore digit grouping as a supported config convention — normalizeNumeric (NodeReader.java:55-67) strips `_` between digits so `10_000_000` parses, and the reader path uses it at line 231. ConfigSerializer's parallel path does not: the Tag.INT branch is `long parsed = Long.parseLong(value.trim());` and the Tag.FLOAT branch `return Double.parseDouble(value.trim());`, each with `catch (NumberFormatException e) { return value; }`. An underscore-grouped number therefore silently comes back as the String "10_000_000" instead of a number, so a consumer reading it as an int gets a type mismatch or its default, and a round trip through the serializer rewrites the value as a quoted string. Two supported spellings of the same config value behave differently depending on which reader path touches it.~~High
4~~P2~~ConfigParser.convert recurses without a cycle guard, so a self-referencing YAML anchor throws StackOverflowError~~Track visited snakeyaml nodes during convert and emit a ConfigIssue on a cycle instead of recursing~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/config/ConfigParser.java:93-156~~The class javadoc promises the parser never throws on bad input, and parse() does catch `MarkedYAMLException`/`YAMLException` around `yaml.compose(reader)`. But convert/convertMapping/convertSequence then walk the composed node graph with plain recursion and no visited-set, so a recursive anchor such as `a: &x {b: *x}` — which snakeyaml composes into a genuinely cyclic node graph — recurses until StackOverflowError. That is an Error, not an Exception, so the parser's own guards do not catch it and it escapes to the caller, breaking the documented never-throws contract. Requires a hand-written recursive anchor, hence P2 rather than higher.~~Medium
5~~P2~~FolderLoader.loadData only regenerates expected files when the whole folder is empty~~Diff expectedFolderFiles against the folder contents in the else branch and create the missing ones~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/FolderLoader.java:83-102~~expectedFolderFiles is consulted only inside `if (!folder.exists() || files == null || files.length == 0)`. The else branch is just `addFiles(files); if (folderFiles.isEmpty()) return;` and never checks whether each expected default is actually present. If an admin deletes one file out of a populated folder, or a new Keystone version adds an expected file to a folder that already has others, that file is never recreated and the feature depending on it is silently absent. Recovery only happens if the admin empties the entire folder.~~High
6~~P3~~FileLoader.tryAgain calls load() on every scheduled tick even after isLoaded flips true~~Return after time.stop() so a successful load is not reloaded by the remaining ticks~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/FileLoader.java:99-105~~The retry body is `if (isLoaded) time.stop(); load(disable, consumer, fileManager);` — stop() only cancels future ticks, so load() still runs once more on the tick that observed success, redundantly re-reading and re-consuming the file. No caller in the reactor invokes tryAgain, so the cost today is a latent trap in unused public API rather than observed behaviour, which is why it is P3.~~Medium
7~~P1~~NodeReader.asDouble accepts NaN and Infinity, which min() and max() range guards cannot reject~~Treat non-finite parse results as invalid in asDouble (report config.double and mark INVALID)~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/config/NodeReader.java:255-257~~asDouble parses with `return new DoubleAccess(Double.parseDouble(normalizeNumeric(scalar.value())), State.VALID, path, scalar.location(), report);`, and Double.parseDouble happily accepts the literals `NaN`, `Infinity` and `-Infinity`, marking them VALID. The range guards then cannot catch them, because every NaN comparison is false: `min` tests `if (state == State.VALID && value < min)` (line 492) so `NaN < 0` fails and the value passes, and `max`'s `value > max` fails identically — positive Infinity likewise slips through any lone `.min(0)`. The class javadoc's own worked example is `r.get("Sell_Price_Ratio").asDouble().min(0).required().orElse(1.0)`, so a config typo of `NaN` flows straight into price arithmetic where it silently poisons every derived value, since NaN propagates through arithmetic without ever throwing. Also Oriel OR-DI-03.~~High
8~~P2~~ConfigParser ignores YAML merge keys, so values inherited through <<: *anchor never reach NodeReader~~When a mapping key carries Tag.MERGE, flatten the referenced mapping(s) into the entries (explicit keys win), mirroring SnakeYAML's constructor~~ConfigParserTest (no anchor or merge-key case)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/config/ConfigParser.java:114-143~~[found 2026-09-24, master 4192baa] parse() builds the tree from `yaml.compose(reader)`, and SnakeYAML's composer only flags merge keys; the `<<` flattening happens in the constructor that Bukkit's YamlConfiguration uses. convertMapping copies node tuples verbatim (`String key = keyScalar.getValue(); ... entries.put(key, child);`), so `S: {<<: *b, C: 3}` becomes a mapping whose keys are literally `<<` and `C` (verified with snakeyaml 1.28: compose yields keys "<< C" while load yields {A=1, B=2, C=3}). Bukkit's getters on the same file see the merged keys, but a loader on NodeReader gets MISSING for every inherited key (silent default, or a config.required error) plus a spurious "unknown key '<<'" warning from the sweep. A config that loads correctly through FileConfiguration therefore loses values once its loader moves to the positional reader.~~High
9~~P2~~FileHandler(JavaPlugin, File) keeps only the immediate parent folder, so nested files regenerate against the wrong path~~Derive directory from the file's path relative to plugin.getDataFolder() instead of the last parent segment~~FolderLoaderRecursionTest (discovers nested files but never regenerates one); FileHandlerIntegrationTest.relativeParentName_handlesBothSeparators (pins separator handling only)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/FileHandler.java:54-68~~[found 2026-09-24, master 4192baa] The File constructor sets `directory` from relativeParentName, which returns `parentPath.substring(sepIndex + 1)`, the last segment only. FolderLoader recurses into sub-folders (FolderLoader.java:132-137, pinned by FolderLoaderRecursionTest) and accepts nested folders such as "configs/menus", so a file at <data>/menus/imported/inner.yml gets directory "imported/inner". Every regeneration then computes paths from that wrong root: createNewFile moves the file to `new File(plugin.getDataFolder(), directory + "-old" + fileType)`, i.e. <data>/imported/inner-old.yml, whose parent usually does not exist, so the move fails and regeneration is skipped with a warn; when that folder does exist the move succeeds, the jar lookup on the wrong path misses, and the fallback `file.createNewFile()` leaves an empty file in place of the admin's. Triggered by a Config_Version mismatch in registerYamlFile, or by FileManager.reloadFiles recovery on a parse failure.~~Medium
