1~~P1~~canRegisterRepository's Table-dependency check is inverted, breaking repository ordering~~Flip the condition: return false when tablesByName lacks tableName, continue when it's already present~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/repository/RepositoryRegistry.java:376-386~~canRegisterRepository is meant to hold back a repository whose constructor takes another repo's Table<?> until that table is registered. Instead it does `if (!tablesByName.containsKey(tableName)) { continue; }` then `return false` right after - so an UNregistered dependency is waved through ("continue" = fine) and an ALREADY-registered dependency is the one that returns false ("not ready"). registerRepositoryClass then finds `tablesByName.get(tableName)==null` for the wrongly-promoted repo, can't build a usable constructor, and silently drops the whole repository with only a log.warn; a satisfied dependency permanently reports as unready and falls into the misleading "Circular dependency in repositories detected" fallback log. No test exercises the Table-param dependency path (existing tests only cover the unrelated FK table-sort in sortTablesByDependencies).~~High
2~~P2~~RepositoryRegistry.getTableName guesses table names from the class name when reflection fails~~Require Table subclasses to expose their name via a documented static accessor instead of string-mangling the class name~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/repository/RepositoryRegistry.java:399-411~~getTableName(Class) tries `tableClass.getConstructor().newInstance()` to read the real name off a fresh instance, but Table<T> only declares a String-arg constructor, so any subclass without its own no-arg constructor throws and falls back to `tableClass.getSimpleName().replace("Table", "").toLowerCase()` - a naming-convention guess never validated against the table's actual registered name. This feeds both canRegisterRepository's dependency lookup and registerRepositoryClass's constructor-arg resolution (`tablesByName.get(tableName)`), so a Table subclass whose class name doesn't map onto its real name via that convention silently mismatches instead of erroring, compounding finding #1's already-broken ordering.~~Medium
3~~P1~~DatabaseHelper caches the Database instance at construction, going stale after DatabaseManager.backup()~~Read databaseHandler.getDatabase() fresh in runQueries instead of caching it in a final field set once in the constructor~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/DatabaseHelper.java:19-31~~DatabaseHelper's constructor does `this.database = databaseHandler.getDatabase();` once and keeps that reference forever; every AbstractRepository builds its own DatabaseHelper this way at construction time. DatabaseManager.backup() (reached from startBackup()/closeConnections() when isSqliteBackup() is on) disconnects the handler's current Database via databaseInformation() then calls `handler.enforceType(databaseType)` on that SAME live DatabaseHandler, which replaces its database field with a brand-new instance. Every already-constructed repository's DatabaseHelper still points at the old, now-closed Database, so a save reached after that point calls connect() on a closed HikariDataSource, throws inside runQueries, and is swallowed by the catch(SQLException) block there - a silent no-op write instead of a surfaced error.~~Medium
4~~P1~~AuditLogService/DatabaseFaultSink double-quote table identifiers, breaking inserts on default MySQL~~Quote through the connected DatabaseBackend's own dialect quoting (or backticks) instead of a hardcoded double-quote quote()~~AuditLogServiceSqliteTest, MysqlDialectTest (neither covers this: AuditLogServiceSqliteTest is SQLite-only, MysqlDialectTest is string-level and never touches AuditLogService/DatabaseFaultSink)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/audit/AuditLogService.java:105-113~~logEvent() builds `"INSERT INTO " + quote(tableName) + " (timestamp, ...) VALUES (...)"` where quote() (lines 154-156) wraps the name in double quotes. MySQL's default sql_mode has no ANSI_QUOTES, so a double-quoted identifier is parsed as a string literal, not a table reference - `INSERT INTO "audit_shop" (...)` is a syntax error on stock MySQL. The failure is caught and only logged ("Failed to write audit row..."), so every audit event silently fails to persist for a MySQL-backed consumer. DatabaseFaultSink.java:81-96 and its quote() at 114-116 build the identical double-quoted INSERT with the same failure mode for fault persistence; DatabaseFaultSink's own class javadoc even predates a "switch to backticks" caveat for AuditLogService's quote() that was never applied.~~High
5~~P2~~Legacy MySQL.java hardcodes the outdated com.mysql.jdbc.Driver class instead of the cj driver~~Use com.mysql.cj.jdbc.Driver as MysqlBackend already does~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/type/MySQL.java:42-50~~MySQL.initialize() sets `config.setDriverClassName("com.mysql.jdbc.Driver")` (switchSchema repeats it at line 83) - the legacy Connector/J 5.1 driver class name. MysqlBackend.driverClassName() deliberately returns `com.mysql.cj.jdbc.Driver` with a comment that "modern bundles ship com.mysql.cj.jdbc.Driver". DatabaseHandler.enforceType(MYSQL) still constructs `new MySQL()` for every consumer's legacy-path connection, so this is live driver-resolution code, not dead code, and a bundled JDBC driver without the deprecated shim class fails pool init with a driver-not-found style PoolInitializationException.~~Medium
6~~P2~~Legacy SQLite/MySQL Hikari pools set minimumIdle(5), eagerly opening 5 native connections~~Scale minimumIdle to the pool size like AbstractJdbcBackend's Math.min(maxPool, 2)~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/type/SQLite.java:51-54~~SQLite.initialize() and switchSchema() both call `config.setMaximumPoolSize(10); config.setMinimumIdle(5);`, and MySQL.java:56-59/89-92 duplicate the same values. HikariCP eagerly opens minimumIdle connections at pool startup, so every DatabaseHandler.enforceType(SQLITE) opens 5 native SQLite file handles up front. AbstractJdbcBackend.connect() (line 62) instead computes `config.setMinimumIdle(Math.min(maxPool, 2))`, showing the maintainers already know 5 is too aggressive for the newer path. On Windows these extra idle handles are not released synchronously on close, which matches the file-locking behaviour every consumer's test suite documents having to work around with @TempDir(cleanup=NEVER) plus a manual releaseDbFiles() helper.~~Medium
7~~P2~~SQLite.switchSchema leaves the Database disconnected instead of reconnecting like MySQL.switchSchema~~Call connect() and repopulate tableNames after creating the new dataSource, mirroring MySQL.switchSchema~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/type/SQLite.java:60-85~~SQLite.switchSchema() calls `disconnect()` then `this.dataSource = new HikariDataSource(config);` and returns true - it never calls connect() or refreshes tableNames. MySQL.switchSchema() (type/MySQL.java:64-109) performs the equivalent dataSource swap and then calls `connect(); tableNames.addAll(getTableNames(connection));`. Any caller of SQLite's switchSchema is left with connection==null and a stale/empty tableNames list, so the next table(...)/getColumns() call throws "There is no connection" instead of the schema switch actually taking effect - a dialect divergence in the shared Database contract that MySQL satisfies and SQLite does not.~~Medium
8~~P2~~AbstractJdbcBackend throws NPE instead of SQLException when used before/after a failed connect()~~Null-check dataSource in execute/query/applySchema/upsertAll and throw a clear SQLException~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/backend/AbstractJdbcBackend.java:85-91~~execute(), query(), applySchema() and upsertAll() all call `dataSource.getConnection()` directly with no null-check, unlike connect()/disconnect()/isConnected() which do guard on dataSource. If connect() was never called, or connect() threw and left dataSource null, every one of these methods throws a raw NullPointerException instead of the SQLException the DatabaseBackend interface declares. DatabaseFaultSink.onInitialize() and AuditLogService.registerLog() both only `catch (SQLException ex)` around their applySchema(...) call, so this NPE would propagate as an unhandled exception out of a bean lifecycle callback instead of being routed through Diagnostics like every other failure on these paths.~~Medium
9~~P1~~DatabaseManager.createDatabaseBackup keys row matching on columns[0], corrupting backups for composite/non-PK-first tables~~Match and update rows on the table's actual primary-key columns, not the first physical column~~DatabaseManagerTest (startBackup_sqliteToSqlite_copiesData only backs up a single-column-PK table whose PK happens to be listed first, so it never exercises this)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/DatabaseManager.java:158-176~~createDatabaseBackup() checks whether a row already exists with `config.select(columns[0] + " = ?", new Object[]{objects[0]}, ...)` and, if so, updates by that same `columns[0] = ?` condition - where columns is simply `config.getColumns()`, the table's physical column order, not its declared primary key. For any table whose first physical column isn't a unique/PK column (a composite primary key, or column order that doesn't put the PK first), this either matches zero existing rows (duplicate INSERTs failing on the real unique constraint, silently swallowed by the surrounding catch(SQLException)) or matches and overwrites the wrong row entirely, producing a backup database that doesn't faithfully mirror the source - defeating the point of DatabaseSettingsProvider.isSqliteBackup()'s failover backup.~~Medium
