1~~P1~~ConditionalOnBean is evaluated before any bean phase runs, so it can never gate on a framework-produced bean~~Evaluate class-/method-level ConditionalOnBean lazily, immediately before that bean's phase actually invokes it (or re-check at invocation time), not in the single upfront config-scanning loop~~BeanFactoryTest (methodLevelConditionalOnBean_requiresAPreRegisteredInstance pins the current, undocumented limitation)~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/BeanFactory.java:221-268~~instantiate() builds every @Configuration's BeanDefinitions in one upfront loop (`for (Class<?> configClass : configClasses) { if (!classConditionsMet(configClass)) ...`) and only invokes @Bean methods afterward in a separate per-phase loop at lines 270-305. Because classConditionsMet/methodConditionsMet call `container.hasInstance(required)` inside the upfront loop, before ANY phase's beans have been invoked, a `@ConditionalOnBean(SomeBean.class)` can only ever see beans the consumer registered manually before calling instantiate() — never a bean produced by an earlier phase's own @Bean method, even though Phase's own Javadoc says "a CONFIG bean can constructor-inject a FILE bean because the FILE phase has already finished". The pinning test proves the workaround (`container.registerInstance(...)` before `instantiate()`) is mandatory; ConditionalOnBean's own Javadoc ("already present in the dependency container at the time the condition is evaluated") never discloses this, so gating a CONFIG bean on a KERNEL-phase bean — the framework's most natural conditional use case — silently and permanently never activates.~~High
2~~P1~~Default @Bean names collide in DependencyContainer's global namedInstances map, silently overwriting the earlier bean~~Namespace default names per configuration class (e.g. prefix with the declaring class), or reject/log a collision instead of silently overwriting namedInstances~~-~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/autowire/DependencyContainer.java:46-50~~`Bean#name()` defaults to `method.getName()` (BeanFactory.java:409, `String name = beanAnnotation.name().isEmpty() ? method.getName() : beanAnnotation.name();`) and every single bean — not only ones anyone intends to look up by name — is registered under that name via `container.registerInstance(def.name(), returnType, bean)` (BeanFactory.java:549). DependencyContainer.registerInstance(String,...) does `namedInstances.put(name, instance)` with no collision check, so two @Bean methods in two unrelated @Configuration classes (core vs. a module, or two modules) that happen to share a method name (e.g. two `manager()`/`service()`/`provider()` factory methods) silently overwrite each other in the one flat namespace shared by the whole bean graph. Any `@Qualifier("thatMethodName")` elsewhere then resolves to whichever bean was registered last, with no error, warning, or Diagnostics fault — a wrong-but-same-typed bean is silently wired in. No test exercises this collision path (grep of keystone-bean/src/test/java for namedInstances/registerInstance(\" turns up only DependencyContainerTest's non-colliding named-lookup test).~~High
3~~P1~~ListenerService's guarded-registration exception fallback can double-register handlers already registered earlier in the same listener~~Track which EventExecutors were already registered via pluginManager.registerEvent before the throw and skip re-registering those methods in the Bukkit fallback (or fall back per-method rather than per-listener)~~ListenerServiceGuardTest (guardedRegistrationFailure_fallsBack covers only a single-@EventHandler listener whose very first registerEvent call throws — it does not cover a multi-handler class where an EARLIER method's registerEvent call already succeeded)~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/listener/ListenerService.java:89-133~~registerGuarded loops every @EventHandler method of a listener class and calls `pluginManager.registerEvent(eventType, listener, handler.priority(), executor, plugin, handler.ignoreCancelled());` per method (line 125-126). If that call throws for a LATER method after one or more EARLIER methods in the same class already registered successfully, the outer `catch (Throwable t)` (line 128) logs a warning and calls `pluginManager.registerEvents(listener, plugin)` — Bukkit's own whole-class annotation scan — which re-registers every @EventHandler method on that listener, including the ones already registered through the guarded EventExecutor path. The listener's earlier handlers then fire twice per event (once through the guarded executor, once through Bukkit's own dispatch), which is a real risk for any handler with a side effect (reward, spawn, state mutation) that must not double-fire.~~Medium
4~~P2~~DependencyContainer.getInstance(Class) silently returns the first-registered instance on ambiguity, inconsistent with BeanFactory's own fail-fast ambiguity check~~Make getInstance(Class) throw (or return null) when more than one instance is registered, matching the "Ambiguous bean" behaviour BeanFactory already enforces for @Bean parameters, or explicitly document the different contract~~DependencyContainerTest (distinctInstances_ofTheSameType_bothRemainVisible exercises getAllInstances with 2 registered instances but never asserts what getInstance(Class) returns in that state)~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/autowire/DependencyContainer.java:55-62~~`getInstance(Class<T> type)` is `List<Object> list = instances.get(type); ... return (T) list.get(0);` — it silently picks whichever instance happened to register first. BeanFactory.resolveParameter (BeanFactory.java:514-529), by contrast, calls `container.getAllInstances(req.type())`, de-duplicates, and throws "Ambiguous bean ... Add @Qualifier to disambiguate" the moment more than one candidate exists. But BeanFactory itself calls the silent, ambiguity-blind `container.getInstance(ListenerService.class)` / `container.getInstance(CommandService.class)` directly in runPhaseConventions (BeanFactory.java:380, 394) — if two ListenerService (or CommandService) instances ever end up registered in the same container, the LISTENER/COMMAND convention scan silently binds to whichever registered first instead of failing the way every other ambiguous bean resolution in this same class does.~~Medium
5~~P2~~DependencyContainer's outer map is a ConcurrentHashMap but each type's instance list is a plain, non-thread-safe ArrayList~~Use a concurrent/copy-on-write list (e.g. CopyOnWriteArrayList) for each type bucket, or synchronize addUnique/getAllInstances explicitly~~-~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/autowire/DependencyContainer.java:24~~`private final Map<Class<?>, List<Object>> instances = new ConcurrentHashMap<>();` guards concurrent map structure, but `addUnique` does `List<Object> list = instances.computeIfAbsent(key, k -> new ArrayList<>()); ... list.add(instance);` (lines 160-168) against a plain ArrayList with no synchronization. Any two threads registering an instance of the same type concurrently (e.g. one plugin's async module-loading path racing a listener/command scan reading getAllInstances on another thread) can corrupt the backing array or lose an add, since ArrayList is not safe for concurrent mutation/iteration. The choice of ConcurrentHashMap for the outer map without a matching concurrent inner collection is a classic half-applied thread-safety fix.~~Medium
6~~P2~~BeanGraph's @Qualifier edge resolution ignores the parameter's expected type, so a name collision can misdirect the dependency graph~~Filter byName lookups by req.type() before drawing an edge, the same way BeanFactory.resolveParameter validates type on the real qualifier lookup~~BeanGraphTest (qualifierEdge_resolvesByDefinitionName only covers the matching-type case; no fixture exercises a same-name, different-type collision)~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/BeanGraph.java:81-93~~`resolveProducer` does `if (req.qualifier() != null && !req.qualifier().isEmpty()) { return byName.get(req.qualifier()); }` with no check that the returned definition's returnType actually matches req.type(). Combined with the same unscoped default-name convention as finding 2 (`byName.put(def.name(), def)` at BeanGraph.java:38-41, later entries silently overwriting earlier ones for a shared method name), a @Qualifier value that happens to collide with an unrelated bean's default name draws a topological edge to the wrong producer. The real invocation-time lookup in BeanFactory.resolveParameter is type-checked and will fail loudly if the qualifier truly can't resolve, but the graph itself can still place beans in a misleading order or report a spurious/absent cycle path before that real check ever runs.~~Medium
7~~P2~~@AutowireTarget is a public annotation with zero consumers anywhere in the codebase~~Delete the annotation, or implement the scan-and-inject behaviour its Javadoc describes~~-~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/autowire/AutowireTarget.java:8-20~~`AutowireTarget` is documented as "Specifies which classes should be scanned for autowiring when instantiating this component," with a `Class<?>[] value()`. A repo-wide grep for `AutowireTarget` (excluding an unrelated worktree copy) finds only the annotation's own declaration — no code in keystone-bean or any consumer reads it, and no test exercises it. It is a dead feature that looks shipped: a developer who annotates a class with it, expecting the described scan-and-inject behaviour, gets silent no-op.~~High
8~~P3~~PostConstruct's Javadoc promises topological ordering across @Configuration classes that the implementation doesn't provide~~Either build configInstances in the same topological order BeanGraph would produce, or correct the Javadoc to say @PostConstruct on a @Configuration class runs in scan/registration order, not dependency order~~-~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/PostConstruct.java:11-12~~PostConstruct.java says "The BeanFactory runs them in the same topological order as bean instantiation, so post-construct hooks may freely call manager setters ... that depend on other beans being fully wired." In BeanFactory.instantiate(), `configInstances` is populated inside the raw `for (Class<?> configClass : configClasses)` loop (BeanFactory.java:221-268) in whatever order scan()/registerConfiguration() added them — never topologically sorted — before `runPostConstruct(configInstances)` is called at line 311. Only the @Bean-produced values in allRegisteredBeans are genuinely topological; a @PostConstruct method on a @Configuration class itself that assumes a dependency-respecting order relative to another configuration class's @PostConstruct is relying on a guarantee the code does not actually provide.~~Medium
