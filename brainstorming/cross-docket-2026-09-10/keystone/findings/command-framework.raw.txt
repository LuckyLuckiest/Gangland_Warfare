1~~P0~~CommandManager.onHelp calls Command.help() for a subcommand without ever checking that subcommand's permission~~In the dispatch loop, check `entry.getValue().getPermission()` against `sender.hasPermission(...)` (same gate Command.runExecute already applies) before routing to onHelp, sending CommandMessages.noPermission() otherwise~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandManager.java:196-204~~The dispatch loop matches `args[0]` to a registered Command, then chooses between execution and help purely on whether any token equals "help": `if (Arrays.stream(args).anyMatch("help"::equalsIgnoreCase)) onHelp(entry, sender, args); else entry.getValue().runExecute(label, sender, args);`. `runExecute` enforces `sender.hasPermission(permission)` before running, but `onHelp` (lines 273-288) goes straight to `entry.getValue().help(sender, page)` with no permission check at all. Typing `/<label> <restrictedSub> help` therefore renders that subcommand's help page (which can list its own sub-arguments/usage) to a sender who has no permission for it whatsoever, unlike every other listing surface (`permissibleCommands`, `CommandTabCompleter`) which all gate on permission first. No test (CommandManagerTest/CommandPermissionPrefixTest) exercises help against a permission-denied subcommand.~~High
2~~P0~~Argument.execute wraps any user token merely containing the substring "confirm" as a literal ConfirmArgument, corrupting tree matching~~Replace the substring check with an exact match (`arg.equalsIgnoreCase("confirm")`) or, better, only special-case a position whose real tree node is actually a ConfirmArgument~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:145-149~~`execute()` builds the comparison array with `if (arg.toLowerCase().contains("confirm")) return new ConfirmArgument(plugin, tree); return new Argument(plugin, arg, tree);` for every position in `args`, including the subcommand label itself. Any free-text value that merely contains "confirm" as a substring — a chat message, a player/item/warp name like "Unconfirmed" or "Reconfirm", or a subcommand literally named e.g. "unconfirmedpayments" — gets replaced by an object whose `arguments` is `["confirm"]`. Against a real `ConfirmArgument` tree leaf (arguments=["confirm"], see ConfirmArgument.java:28) this makes the traversal treat that substring-bearing text as equivalent to typing the literal confirmation keyword, letting a destructive double-check gate (CommandDispatchScenarioTest's `/shop stock reset confirm`) fire on unrelated input once the lock is armed; against any other literal it silently breaks that literal's own matching (permanent NOT_FOUND) whenever its token contains "confirm". No test types anything other than the exact word "confirm" so this is unpinned.~~High
3~~P1~~Brigadier's master-command visibility gate checks PluginCommand#getPermission(), not CommandManager's actual enforced `<prefix>.command.main`~~Thread the manager's real main-permission string (or the manager itself) into BrigadierPaperListener.registerOn / BrigadierTabRegistrar.registerSpigotBackend and check that instead of (or in addition to) command.getPermission()~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/brigadier/BrigadierPaperListener.java:125-131~~`handle()` gates the whole per-player tree with `String masterPerm = command.getPermission(); if (masterPerm != null && !masterPerm.isEmpty() && !player.hasPermission(masterPerm)) return;` — `command.getPermission()` is Bukkit's PluginCommand permission from plugin.yml, a completely different string from the `<prefix>.command.main` CommandManager.onCommand actually enforces (CommandManager.java:174-179). If a consumer's plugin.yml never sets `permission:` for the master command (reasonable, since CommandManager already enforces its own), masterPerm is null/empty and this check is a no-op: every player, including one lacking `<prefix>.command.main`, gets the full per-subcommand-permission-filtered Brigadier tree. The Spigot/Commodore path (BrigadierTabRegistrar.java:207-238) has the identical gap — `buildTree`'s root literal carries no `requires()` at all and relies on the same PluginCommand permission via Commodore's default. Execution stays safe (CommandManager.onCommand still checks the real permission), but client-side command existence/tab-complete leaks past the intended master gate.~~High
4~~P1~~BrigadierPaperListener.handle silently swallows every ReflectiveOperationException with no log or Diagnostics report~~Report through Diagnostics.active() (or at minimum plugin.getLogger().warning) inside the catch block, mirroring the funnel CommandManager.onCommand uses for command.dispatch~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/brigadier/BrigadierPaperListener.java:110-135~~The whole per-event handler body is wrapped in `catch (ReflectiveOperationException ignored) { // No realistic recovery — the unfiltered tree (if any) stays in place. }`. This class exists specifically to replace Commodore's unfiltered, permission-leaking shared tree with a per-player filtered one; if any of the three reflective accessors (`getPlayer`/`getCommandNode`/`hasFiredAsync`) or the `CommandNode` field names ever stop matching a future Paper build, every single `AsyncPlayerSendCommandsEvent` will throw here, silently, forever — permanently reverting to the unfiltered tree this class was built to prevent, with zero signal to server admins that the protection has stopped working. This directly contradicts the repo's "errors flow through Diagnostics, not log-and-swallow" convention.~~High
5~~P1~~Argument.traverseList aborts the whole sibling search on the first NO_PERMISSION child instead of trying the remaining children~~On NO_PERMISSION from one child, continue the for-loop over the remaining siblings and only report NO_PERMISSION if none of them ultimately succeeds~~ArgumentTreeTest (covers ResultState.NO_PERMISSION construction but not the sibling-fallthrough scenario)~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:278-286~~```for (Tree.Node<T> child : node.getChildren()) { ArgumentResult<T> result = traverseList(child, list, index + 1, dummy, sender, args); if (result.getState() == ArgumentResult.ResultState.SUCCESS) return result; else if (result.getState() == ArgumentResult.ResultState.NO_PERMISSION) return ArgumentResult.noPermission(node.getData()); }``` `addSubArgument` (Argument.java:122-130) inserts literal children at the front and OptionalArgument catch-alls at the back, so a literal sibling is tried before the wildcard one. A restricted literal (e.g. an admin-only `warp reset`) whose token happens to equal what a sender typed is tried first; if the sender lacks that literal's permission the whole traversal returns NO_PERMISSION immediately and the OptionalArgument sibling (e.g. a free-text warp name the sender is otherwise allowed to use, and which would also have matched "reset" as a name) is never attempted — a legitimate, lower-permission path is wrongly denied instead of falling through.~~Medium
6~~P1~~ArgumentLock never releases a sender's entry, leaking a strong reference for every half-completed confirm/double-run gate~~Remove the sender from lockedSenders on PlayerQuitEvent (or key by UUID with an expiry) instead of only on successful unlock()~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/ArgumentLock.java:10-24~~`lockedSenders` is a plain `HashSet<CommandSender>` populated by `lock()` and only ever emptied by `unlock()`, which fires solely on the second, successful invocation of a `ConfirmArgument`/`DoubleArgument` leaf (ConfirmArgument.java:46-53, DoubleArgument.java:37-45). A player who triggers the first half of a confirm/double gate and then disconnects, gets denied permission on the second try, or simply never re-runs the command leaves their `CommandSender` object permanently referenced in this set for the lifetime of that Argument instance (effectively the plugin's whole runtime, since command trees are built once at bootstrap). Every such gate on a busy server accumulates stale Player references indefinitely — an uptime leak with no cleanup path anywhere in this scope.~~High
7~~P2~~Argument.addSubArgument decides tree-insertion order by substring-matching "?" in the argument's text instead of checking its type~~Use `argument instanceof OptionalArgument` to decide ordering, not a substring check on toString()~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:122-130~~`if (argument.toString().contains(OPTIONAL_ARGUMENT)) node.add(argument.getNode()); else node.add(0, argument.getNode());` — `toString()` returns `arguments[0]`, the literal token, and `OPTIONAL_ARGUMENT` is the single character `"?"`. A plain literal sub-argument whose own token happens to contain a `?` (e.g. a confirmation prompt literal or an odd label) is misclassified as a wildcard and appended to the back of the children list instead of the front. Since `traverseList` returns the first matching child and OptionalArgument children match almost anything via the `dummy` fallback, a true OptionalArgument sibling positioned earlier (by luck of insertion order) can shadow the mis-ordered literal, changing which node's action actually runs for a given input.~~Medium
8~~P2~~TargetSelector.origin() overrides the selection's world without recomputing the origin coordinates for that world~~When `this.world` names a world different from the one the coordinates were computed in, reset originX/Y/Z to that world's spawn (or require explicit x/y/z) instead of keeping the sender's raw coordinates~~TargetSelectorTest (no cross-world `world=` + distance/sort case found)~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/TargetSelector.java:564-589~~`origin()` first computes originX/Y/Z from the sender's own location (or the default world's spawn) at lines 538-575, then at 577-583 does `if (this.world != null) { World named = Bukkit.getWorld(this.world); if (named == null) return null; world = named; }` — swapping only the `World` reference, never touching originX/Y/Z. A selector like `@p[world=nether,distance=..10]` run by a sender standing in the overworld ends up measuring "nearest"/"distance" against the overworld coordinates while filtering candidates to nether players, producing a distance-based ordering/cutoff that has no relationship to any real position in the target world.~~Medium
9~~P2~~CommandManager.onCommand treats any exact "help" token anywhere in the args array as a request for subcommand help~~Only treat a help request when the literal "help" is the immediately-next positional token expected by the tree (or restrict the check to args[args.length-1]), not via Arrays.stream(args).anyMatch over the whole array~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandManager.java:199~~`if (Arrays.stream(args).anyMatch("help"::equalsIgnoreCase)) onHelp(entry, sender, args);` scans every element of the typed command, not just a trailing help marker. Any command whose free-text/OptionalArgument value is exactly "help" at any position — e.g. `/mail send Steve help` where "help" is meant as the message body, or a warp/gang name literally called "help" — is silently diverted to the subcommand's help page instead of being executed with that literal value.~~Medium
10~~P2~~BrigadierTabRegistrar's Spigot permission/visibility predicates fail open when the Bukkit sender can't be reflectively extracted from the NMS source~~Default to hiding/denying the node (return false) when extractSender returns null instead of returning true, and log once per source class so the fail-open path is visible~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/brigadier/BrigadierTabRegistrar.java:377-385~~`permissionRequires` returns `source -> { CommandSender sender = extractSender(source); if (sender == null) return true; return sender.hasPermission(permission); }` and `visibilityRequires` (lines 244-250) does the same. `extractSender` relies on reflectively finding `getBukkitSender()`/`getSender()` on the NMS command-source class (cached per class, warning logged once — resolveSenderMethod, lines 402-419). On any server fork or future NMS revision where neither method name resolves, every permission- and visibility-gated node in the shared Brigadier tree becomes visible/"requires() == true" for every player, silently defeating the whole per-node permission model on the client side (dispatch-time permission checks in CommandManager/Argument still apply, so this is visibility-only, not execution).~~Medium
11~~P2~~CommandManager's deprecated static defaultInstance and its accessors mix per-plugin command state across every consumer sharing the classloader~~Finish removing getPermissibleCommands()/getCommands() ahead of 2.0.0, or fail fast if a second CommandManager is constructed while any deprecated accessor is still reachable~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandManager.java:37-108~~`private static volatile CommandManager defaultInstance;` is claimed by whichever `CommandManager` is constructed first per JVM (constructor at lines 67-71), and `getPermissibleCommands`/`getCommands` (lines 94-108) delegate to that single instance. Since Keystone is loaded once and shared by Gangland, Bartizan and Oriel, any code path in any of those plugins that still calls these deprecated statics receives whichever plugin's `CommandManager` happened to construct first — not necessarily its own — silently returning a foreign or empty command set. The class's own javadoc already flags this ("only these legacy statics funnel through one instance") and plans removal in 2.0.0, but the statics remain live and callable today.~~Medium
12~~P3~~Argument overrides equals() by content without overriding hashCode(), breaking the equals/hashCode contract~~Add a matching hashCode() derived from the (lower-cased) arguments array, or drop the custom equals() if reference semantics are actually intended~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:175-182~~```public boolean equals(Object obj) { if (this == obj) return true; if (!(obj instanceof Argument argument)) return false; return Arrays.stream(argument.arguments).anyMatch(arg -> Arrays.stream(this.arguments).anyMatch(arg::equalsIgnoreCase)); }``` No corresponding `hashCode()` override exists, so two `Argument`s that are `.equals()` (same token, different objects) can hash to different buckets. Nothing in this scope currently puts `Argument` into a `HashMap`/`HashSet`, so there's no live symptom today, but it's a latent trap for the next feature that does (e.g. a permission cache keyed by Argument).~~Medium
13~~P2~~Argument.notFound repeats the just-matched valid token as the "invalid" one when the typed args end exactly on a valid node~~When i is the last index of args (no following token was typed), send a distinct "expected another argument" message instead of reusing lastInput = args[length] (the token that just matched)~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:230-239~~```if (i + 1 < args.length) { length = i + 1; lastInput = args[length]; invalidArg.append(lastInput); } else { lastInput = args[length]; }``` When the last successfully-matched token (`lastValid`) is also the final element of `args` (i.e. `i == args.length - 1`), the else branch sets `length = i` and `lastInput = args[length]`, which is the same token that was just confirmed valid one line above. The resulting "Wrong arguments: <thatToken>" message and the "did you mean" suggestion built from it (line 252) both reference the correct, already-typed token as if it were the mistake, instead of indicating that another argument was expected.~~Medium
14~~P2~~CommandMessages and ArgumentMessages message-source overrides are static, so one consumer's install() replaces the framework's player-facing text for every plugin sharing the classloader~~Move the installed suppliers onto a per-CommandManager (or per-plugin) instance instead of static class state, ahead of the 2.0.0 instance-scoping work already planned~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandMessages.java:62-68~~Both classes hold their message suppliers as `private static Supplier<String> ...` fields (CommandMessages.java:62-68; ArgumentMessages.java:53-61) set via `install(Consumer<Overrides>)`. Their own javadoc already documents the consequence: "one consumer plugin owns the command framework's strings per server until the framework becomes instance-scoped (2.0.0)". On a server running Gangland, Bartizan and Oriel together (the exact deployment this repo targets), whichever plugin calls `CommandMessages.install(...)`/`ArgumentMessages.install(...)` last wins, and every other plugin's command errors, confirm prompts and "did you mean" text silently switch to that installer's localized strings.~~Medium
