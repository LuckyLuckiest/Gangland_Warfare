1~~P0~~EconomyHandler.setAmount ignores withdrawPlayer/depositPlayer EconomyResponse, can create or lose money~~Capture the EconomyResponse from both Vault calls and check transactionSuccess(); abort before depositing if the withdraw fails, and report a Diagnostics fault (never silently proceed) if either leg fails~~EconomyHandlerTest (userMode_setAmountWithdrawsEverythingThenDepositsTheNewBalance pins the unchecked withdraw-then-deposit call order without asserting response handling)~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/EconomyHandler.java:58-66~~setAmount runs `vaultEconomy.withdrawPlayer(offlinePlayer, vaultEconomy.getBalance(offlinePlayer))` then unconditionally `vaultEconomy.depositPlayer(offlinePlayer, this.amount.doubleValue())`, discarding both EconomyResponse return values. If the economy plugin behind Vault rejects the withdraw, the full new target amount is still deposited on top of the un-debited old balance, creating money. If the deposit is instead rejected (e.g. a configured maximum-balance cap), the local `amount` field is set to the new value while the real Vault balance never changed, silently desyncing the two and losing the difference from the player's real balance. No exception, log, or Diagnostics fault is raised in either case, contrary to the repo's Result/Diagnostics error-handling rule.~~High
2~~P1~~EconomyHandler.depositAmount accepts negative deltas and bypasses withdrawAmount's insufficient-funds guard~~Reject or clamp negative deltas in depositAmount (or route negative amounts through withdrawAmount) so every balance-reducing call passes through the same insufficient-funds check~~-~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/EconomyHandler.java:68-77~~depositAmount is `setAmount(getAmount().add(Currency.of(delta)))` with no sign validation, while the sibling withdrawAmount explicitly does `if (normalised.compareTo(current) > 0) throw new EconomyException(...)`. A caller that passes a negative delta to depositAmount (a mis-signed refund/penalty, or a fee charged through the wrong method) reduces the balance, even below zero, without ever hitting the balance guard, silently corrupting the account invariant withdrawAmount is supposed to protect.~~Medium
3~~P2~~EconomyHandler never checks vaultEconomy.isEnabled() before reading/writing balances~~Mirror VaultEconomyProvider.isAvailable() by also checking vaultEconomy.isEnabled() inside useUserInfo()/getAmount()/setAmount() before delegating to Vault~~-~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/EconomyHandler.java:51-66~~getAmount()/setAmount() gate only on `vaultEconomy != null`, never on `vaultEconomy.isEnabled()`. VaultEconomyProvider.isAvailable() (vault/economy/VaultEconomyProvider.java:39-41) treats isEnabled() as the real availability signal for the exact same Economy type, so if the economy plugin behind Vault disables at runtime (crash/reload) while the static vaultEconomy reference is still set, EconomyHandler keeps calling getBalance/withdrawPlayer/depositPlayer on a disabled Economy whose behaviour is undefined by Vault's own contract.~~Medium
4~~P2~~VaultEconomyProvider/VaultGroupProvider/VaultPermissionProvider don't fail closed on a throwing Vault backend~~Wrap the delegated calls in try/catch and return false plus a Diagnostics fault, matching the pattern already used in VaultOfflinePermissionService.fault(...)~~-~~keystone-hooks/src/main/java/org/luckyraven/keystone/vault/economy/VaultEconomyProvider.java:53-61~~withdraw/deposit call `economy.withdrawPlayer(...)`/`economy.depositPlayer(...)` directly with no try/catch, and VaultGroupProvider.inGroup/primaryGroup and VaultPermissionProvider.add/remove do the same with no guard. The only class in this package that defends against a throwing Vault backend is VaultOfflinePermissionService, whose has/grant/revoke/etc. all catch Exception and report through Diagnostics.active() before degrading to false/no-op. A buggy or transiently-failing third-party economy/permission plugin behind Vault propagates an uncaught exception out of these three classes into whatever listener/command triggered the transaction instead of failing closed like every other provider in this system.~~Medium
5~~P2~~EconomyHandler.vaultEconomy is a non-volatile static written and read across threads with no synchronization~~Mark vaultEconomy volatile (or route every set through a synchronized/atomic holder) so a write during bootstrap is guaranteed visible to reader threads~~EconomyHandlerTest (saveStatic/restoreStatic treat it as a 'server-global Vault fact' needing reset between tests, but does not exercise cross-thread visibility)~~keystone-hooks/src/main/java/org/luckyraven/keystone/economy/EconomyHandler.java:25-27~~`private static Economy vaultEconomy;` (Lombok-generated static getter/setter) is read on every getAmount()/setAmount() call across the shared classloader and can be set at any time via setVaultEconomy(...). Because the field carries no volatile and no other synchronization, the Java Memory Model gives no guarantee that a write on one thread (e.g. plugin bootstrap on the main thread) becomes visible to a reader thread (e.g. an async job crediting interest through Bank.accrueInterest) without an intervening happens-before edge.~~Low
