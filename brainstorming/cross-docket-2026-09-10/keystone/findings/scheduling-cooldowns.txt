1~~P0~~Timer.start after stop throws IllegalStateException because BukkitRunnable can only be scheduled once~~Stop extending BukkitRunnable and schedule a separate Runnable per start, so a Timer instance can be restarted~~-~~keystone-common/src/main/java/org/luckyraven/keystone/timer/Timer.java:49-66~~Timer extends BukkitRunnable and start() schedules the instance itself: `if (async) this.bukkitTask = runTaskTimerAsynchronously(plugin, delay, period); else this.bukkitTask = runTaskTimer(plugin, delay, period);`. stop() then does `this.bukkitTask.cancel(); this.stopped.set(true); this.bukkitTask = null;`, which clears Keystone's own guard so a later start() proceeds — but Bukkit's BukkitRunnable.cancel() is only `Bukkit.getScheduler().cancelTask(getTaskId())` and never clears its internal task field, so the next schedule hits `checkNotYetScheduled()` and throws `IllegalStateException("Already scheduled as " + task.getTaskId())` (verified against the Spigot 1.16.5 API sources). Any stop-then-start on the same Timer, RepeatingTimer, CountdownTimer, CountupTimer or SequenceTimer therefore crashes, which is exactly what a managed reload does when it cancels timers and starts them again. Every existing timer test drives run() directly against a mocked isRunning() and never touches the real scheduler, so nothing catches it.~~High
2~~P1~~InMemoryCooldownService and PersistentCooldownService never evict entries for players who leave~~Evict a player's map on quit (or sweep expired entries periodically) instead of only on a repeat lookup of the same id~~-~~keystone-common/src/main/java/org/luckyraven/keystone/cooldown/InMemoryCooldownService.java:48-62~~The backing maps are `ConcurrentHashMap<UUID, Map<String, Long>>` and the only removal is lazy, inside the lookup for that exact key: `if (expiry <= System.currentTimeMillis()) { byId.remove(id); return 0L; }`. A player who triggers a cooldown once and then logs out is never queried again, so their UUID and its inner map stay resident for the life of the server. On a busy server with a rotating player base this grows without bound, and the persistent subclass keeps the same shape in its cache, so the leak is in both implementations.~~High
3~~P1~~PersistentCooldownService performs its schema, purge and upsert JDBC calls synchronously on the calling thread~~Move the persistence writes off the caller's thread (async task or write queue), keeping the in-memory update synchronous~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/cooldown/PersistentCooldownService.java:110-125~~start() calls ensureReady() — which on first use runs backend.applySchema(...), a DELETE purge and a full-table SELECT — and then persistStart(), an upsert, all inline on whatever thread called CooldownService.start(). Cooldowns are set from gameplay paths (a click, a command, an ability), which run on the main server thread, so every cooldown write blocks the tick for a database round trip and the very first one blocks for a schema apply plus a full table read. On MySQL over a network that is a visible stall on a normal path; the in-memory sibling has no such cost, so a consumer swapping implementations silently inherits main-thread I/O.~~High
4~~P1~~PersistentCooldownService swallows every SQLException with a log.warn and continues as if persisted~~Report through Diagnostics and surface the degraded state rather than continuing silently~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/cooldown/PersistentCooldownService.java:186-188~~ensureReady() ends with `catch (SQLException exception) { log.warn("Failed to initialise persistent cooldown store: {}", exception.toString(), exception); }` and persistStart() (lines 217-219) and persistClear() (lines 236-238) repeat the pattern, each continuing normally afterwards. The whole point of this implementation over the in-memory one is that cooldowns survive a restart, so a failing write means the persistence guarantee is quietly gone while the service reports success to its caller. The admin gets one warn line per failure and no fault in the diagnostics report, which is the log-and-swallow shape the repo's error rule forbids on a data path.~~High
5~~P2~~ClickDebounce keys every call site under one shared CLICK_DELAY_ID, so one site's delay overrides another's~~Include the call site (or an explicit id parameter) in the cooldown key instead of a single constant~~-~~keystone-common/src/main/java/org/luckyraven/keystone/cooldown/ClickDebounce.java:39-49~~shouldSwallow takes a per-call `delayMillis` but always stores and reads the cooldown under the single constant CLICK_DELAY_ID. Two unrelated consumers of the same CooldownService — a GUI click handler on 200ms and an NPC interaction handler on 800ms — therefore share one debounce slot per player, so whichever fires first sets the window and the other is swallowed for a duration it never asked for. The per-call parameter reads like it scopes the debounce when it does not, making the wrong behaviour easy to miss.~~High
6~~P2~~SequenceTimer mutates its ArrayList and LinkedList queue with no synchronization against an async run()~~Use thread-safe collections or synchronize the queue mutations against run()~~-~~keystone-common/src/main/java/org/luckyraven/keystone/timer/SequenceTimer.java:43-54~~addIntervalTaskPair() mutates intervalTaskPairs (ArrayList) and intervalTaskQueue (LinkedList), and reset() clears and repopulates the same LinkedList, while run() polls and mutates intervalTaskQueue on whichever thread the scheduler ticks it on. Started with start(true) the timer body runs on an async scheduler thread, so a consumer adding a pair or resetting from the main thread races the tick with no happens-before edge between them. The result is a lost or duplicated task step, or a LinkedList corrupted mid-traversal.~~Medium
7~~P2~~CooldownFormat.human renders Duration.ZERO as "1s" instead of an elapsed cooldown~~Return an empty or zero rendering for Duration.ZERO before applying the sub-second floor~~CooldownFormatTest (subSecondRoundsUpToOneSecond pins the floor that produces this)~~keystone-common/src/main/java/org/luckyraven/keystone/cooldown/CooldownFormat.java:46~~The formatter floors every duration with `long seconds = Math.max(duration.toSeconds(), 1);`, which is the intended rounding for a genuine sub-second remainder but also catches Duration.ZERO — the exact value CooldownService.remaining() returns by contract when a cooldown is not active. A player whose cooldown has elapsed is therefore shown "1s" remaining, and a consumer rendering remaining time unconditionally displays a cooldown that does not exist. CooldownFormatTest.subSecondRoundsUpToOneSecond pins the rounding rule, so fixing this means special-casing zero rather than changing the floor.~~High
8~~P3~~A null cooldown id throws NPE from the ConcurrentHashMap rather than failing gracefully~~Null-check the id at the service boundary and return not-active instead of propagating an NPE~~-~~keystone-common/src/main/java/org/luckyraven/keystone/cooldown/InMemoryCooldownService.java:48-62~~expiryOf() does `byId.get(id)` and start() does `expiries.computeIfAbsent(player, ...).put(id, expiry)` against ConcurrentHashMap, which rejects null keys with a NullPointerException where a plain HashMap would simply return null. A consumer resolving a cooldown id from config that turns out to be missing therefore gets an NPE out of a cooldown check rather than a clean miss. It takes a caller passing null, so this is a hardening gap at the boundary rather than an observed failure.~~High
9~~P2~~UpdateChecker.downloadLatestVersion keeps a truncated jar that then blocks every retry~~Download to a temp file with connect/read timeouts and atomically move it into place only after a complete transfer; delete the temp file on any exception~~-~~keystone-common/src/main/java/org/luckyraven/keystone/update/UpdateChecker.java:121-159~~[found 2026-09-24, master 4192baa] The download opens `ReadableByteChannel readableByteChannel = Channels.newChannel(url.openStream());` with no connect or read timeout (unlike getLatestVersion, which sets TIMEOUT_MS), then streams straight into the final file `new File(releaseFolder, plugin.getName() + "-" + latestVersion + ".jar")` via `fileOutputStream.getChannel().transferFrom(readableByteChannel, 0, Long.MAX_VALUE)`. If the connection drops or the async thread is interrupted mid-transfer, the catch only logs and the partial jar stays on disk. Every later call (the UpdateNotifier auto-update path runs it once per interval) then hits `if (outputFile.exists()) { log.info("Version {} is already downloaded at {}", ...); return false; }`, so the corrupt jar is reported as already downloaded forever and an admin who copies it from release/ installs a broken plugin. A stalled server with no read timeout can also pin the async worker indefinitely.~~High
10~~P1~~PersistentCooldownService.ensureReady retries schema apply, purge and full SELECT on every isActive/remaining call while it keeps failing~~Latch the failure (retry on a backoff or only from onInitialize) so the read path never re-enters JDBC~~PersistentCooldownServiceSqliteTest, PersistentCooldownServiceBackendTest (happy path only)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/cooldown/PersistentCooldownService.java:93-107~~[found 2026-09-24, master 4192baa] isActive and remaining both open with `ensureReady();`, and ensureReady returns early only `if (ready || !isConnected())`; `ready = true` is set only after applySchema, the DELETE purge and the full-table SELECT all succeed (lines 147-185), and the catch just logs. So with a connected backend whose initialisation keeps failing (a DDL permission error, an incompatible pre-existing table, a lock timeout), every cooldown read, the path the class javadoc says "never touch[es] the database", re-runs the whole schema apply + purge + table scan synchronously and logs a full stack trace. Cooldown checks sit on click/command/ability paths on the main thread, so a persistent init failure becomes per-interaction main-thread JDBC plus log flooding. Distinct from row 3 (first successful init and write-through on start()): this is the unbounded retry on the read path.~~High
11~~P1~~PersistentCooldownService's legacy-Database path shares the Database table cursor and connection without synchronizing on it~~Wrap the legacy-path calls in synchronized (database), as DatabaseHelper.runQueries does, or route them through a DatabaseHelper~~PersistentCooldownServiceSqliteTest (single-threaded)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/cooldown/PersistentCooldownService.java:208-216~~[found 2026-09-24, master 4192baa] The legacy Database is a stateful cursor: SQLite/MySQL.table(name) stores `this.table = tableName` and the following insert/delete/buildUpsertQuery read getTable(), all over one long-lived Connection. AbstractRepository's legacy saves hold `synchronized (database)` through DatabaseHelper.runQueries while calling `database.table(name)` and, in Table.batchUpsertTableQuery, `conn.setAutoCommit(false)` on that same connection; PersistentCooldownService calls `QueryBuilder.on(database, table).delete()...execute()` and `.insert()...execute()` (and ensureReady's createTable/delete/selectAll at lines 169-176) with no lock, from the main thread. When a cooldown starts during an async autosave the threads interleave table() and the statement, so either side's SQL is built against the other's table ("no such column", swallowed with a warn, and the save or the cooldown row is lost). A cooldown write issued mid-batch also joins the repository's open transaction and is rolled back with it if the batch fails. The trigger is the documented construction, passing handler.getDatabase().~~Medium
