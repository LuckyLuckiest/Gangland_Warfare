# KS-SH keystone-shop (Phase H10, promoted from Gangland's shop-api) — verified findings
1~~P2~~DefaultShopDisplayResolverTest lost the live-name-before-pristine pin (informally "T-R4") when GanglandShopDisplayResolverTest was deleted at the Gangland-side shop-api promotion~~One more test in DefaultShopDisplayResolverTest registering a serializer/converter pair whose pristine rebuild returns a different display name than the live one, asserting live wins - ported from the deleted GanglandShopDisplayResolverTest~~FIXED - new test in DefaultShopDisplayResolverTest (commit b706f30, branch phase-e6-inventory): red under inverted precedence (asserts pristine wins), green after revert to the real live-first precedence; production DefaultShopDisplayResolver code untouched~~keystone-shop/src/test/java/org/luckyraven/keystone/shop/message/DefaultShopDisplayResolverTest.java (new test method, live/pristine conflict case)~~When Gangland's gangland-ui/shop-api module was deleted and its 26 headless classes promoted into keystone-shop (Keystone's own earlier G0), DefaultShopDisplayResolver (the promoted GanglandShopDisplayResolver) carried over cleanly, but its existing DefaultShopDisplayResolverTest only ever supplied a live name with no competing non-null pristine value (ItemDefinitions.pristine returns null for an unregistered stack), so it could not distinguish live-first precedence from pristine-first. The deleted GanglandShopDisplayResolverTest had registered a serializer/converter pair whose pristine rebuild returned a different display name and asserted live wins - that specific two-source-conflict pin was lost at promotion and nothing in keystone-shop's own suite caught it (a coverage gap, not a live production regression - the resolver's actual precedence logic was never touched). Found in the Gangland WS4 G1a review (Important 1, informally referenced there and in the WS2/WS4 dispatch as "T-R4"); fixed by a test-only commit on wt/keystone-1.11.0 (b706f30), dispatched upstream in parallel with the Gangland-side fix round per orchestrator ruling W45.~~High
2~~P1~~ShopPurchaseService.purchase accepts a negative finalTotal; a negative YAML Price credits the buyer via withdraw~~Return a failure outcome when finalTotal is null or signum() < 0 (mirror ShopSellService's signum guard) and have ShopYamlReader.parsePrice reject negative Price/Base_Price with a warning instead of accepting them~~ShopPurchaseServiceTest, ShopYamlReaderTest (neither covers a negative amount)~~keystone-shop/src/main/java/org/luckyraven/keystone/shop/transaction/ShopPurchaseService.java:32-45~~[found 2026-09-24, master 4192baa] purchase clamps `if (copies < 1) copies = 1;` but never validates the money side: `if (payment.getBalance().compareTo(finalTotal) < 0)` passes for any negative total and `payment.withdraw(finalTotal);` is called with it, then the item is delivered anyway. ShopYamlReader.parsePrice (ShopYamlReader.java:44-53) accepts any sign, so an admin typo `Price: -50` in a shop YAML loads as a live buy entry. The documented adapter path (Gangland TraderBuyListener.adapt -> EconomyHandler.withdrawAmount, keystone-hooks EconomyHandler.java:72-77) only rejects `normalised.compareTo(current) > 0` and then `setAmount(current.subtract(normalised))`, so a negative withdraw adds money: every click hands the player the item plus 50. ShopSellService already refuses `offeredTotal.signum() <= 0`; the purchase side has no equivalent guard.~~High
3~~P1~~CategorySellValuator/CategoryBarterValuator round per-item price to 2dp before applying the ratio (double rounding)~~Keep perItem unrounded (divide with a wide scale, e.g. MathContext.DECIMAL64) and round once after multiplying by ratio*mood, in both valuators~~CategorySellValuatorTest, CategoryBarterValuatorTest (the HALF_UP pin at ratio 1.0 cannot tell single from double rounding)~~keystone-shop/src/main/java/org/luckyraven/keystone/shop/valuation/CategorySellValuator.java:36-42~~[found 2026-09-24, master 4192baa] value() computes `perItem = bundlePrice.divide(BigDecimal.valueOf(templateAmount), PRICE_SCALE, PRICE_MODE);` (already rounded to 0.01) and then `perItem.multiply(BigDecimal.valueOf(sellPriceRatio * moodMultiplier)).setScale(PRICE_SCALE, PRICE_MODE)` rounds a second time. For a cheap bundle this inflates payouts: Base_Price 0.50 for a 64-item template at sellPriceRatio 0.5 gives 0.0078125 -> 0.01, then 0.01 * 0.5 = 0.005 -> 0.01 per item, so selling the 64 stack pays 0.64 against an intended 0.25 and more than the bundle's own 0.50 base price - a buy-then-sell loop mints money when the same stack is also a buy entry at or below 0.64. Single rounding gives 0.0039 -> 0.00. CategoryBarterValuator.java:38-42 has the identical two-step rounding for barter offers.~~High
4~~P3~~PurchaseOutcome.INVENTORY_FULL is never returned by ShopPurchaseService~~Delete INVENTORY_FULL (or document it as reserved) - overflow is always dropped at the player's feet and reported as SUCCESS~~ShopPurchaseServiceTest (pins drop-on-full as SUCCESS)~~keystone-shop/src/main/java/org/luckyraven/keystone/shop/transaction/PurchaseOutcome.java:7-12~~[found 2026-09-24, master 4192baa] The enum advertises `INVENTORY_FULL` to integrations, but ShopPurchaseService.purchase (ShopPurchaseService.java:53-57) always drops leftovers with `dropItemNaturally` and returns `PurchaseResult.success(...)`; no code path constructs INVENTORY_FULL. Consumers carry dead switch arms for it (Gangland TraderBuyListener: "Service drops leftovers naturally; kept for exhaustiveness."), a dead feature that looks shipped.~~High
