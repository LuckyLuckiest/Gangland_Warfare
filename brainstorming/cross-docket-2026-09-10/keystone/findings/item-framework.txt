1~~P2~~ItemConverterRegistry.resolve gates the MATERIAL converter on raw Material.valueOf instead of XMaterial~~Resolve through XMaterial.matchXMaterial(type) so cross-version material aliases still reach the MATERIAL converter~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemConverterRegistry.java:92-98~~resolve() decides whether an unknown type string is a material by attempting `Material.valueOf(type.toUpperCase());` and treating IllegalArgumentException as "not a material", returning null. Bukkit's Material constants drift across the 1.16.5-to-current span Keystone supports (GRASS_PATH became DIRT_PATH in 1.17, among others), so a config written against one server version fails this check on another and the item definition silently resolves to no converter at all. This is the only raw valueOf left in the module — ItemBuilder already resolves through `XMaterial.PLAYER_HEAD.get()` at lines 200-218 — so it is both a version-drift bug and a violation of the repo's XSeries convention. The failure is silent: the caller just gets null.~~High
2~~P2~~ItemBuilder.decreaseDurability and setDurability never clamp damage against getItemMaxDurability()~~Clamp the computed damage into 0..getItemMaxDurability() inside setDurability~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemBuilder.java:146-192~~decreaseDurability computes `return setDurability((short) (currentDurability + amount));` and setDurability applies it with `damageable.setDamage(durability);` — neither consults getItemMaxDurability(), which sits two methods away and knows the real limit. Repeated decreases, or one oversized amount from a consumer's config, push the stored damage past the material's maximum (and a negative amount pushes it below zero), producing a stack whose damage value is outside the range the client's durability bar can render. The item does not break at the boundary the way vanilla damage does, so a consumer implementing wear-and-tear gets an item stuck in an invalid damage state rather than a broken one.~~High
3~~P2~~ItemVocabularies.install leaves partial registrations behind when a vocabulary's contribute throws~~Buffer a vocabulary's registrations and commit to the shared registries only after contribute returns cleanly~~ItemVocabulariesTest~~keystone-item/src/main/java/org/luckyraven/keystone/item/spi/ItemVocabularies.java:66-73~~RegistrarImpl writes each converter/serializer/refresher straight into the shared live registries as the vocabulary calls it, and the failure path is `catch (Throwable t) { report(namespace, t); continue; }` — the calls that already landed are never rolled back. A vocabulary that registers several entries and then throws part-way leaves the item registries holding an arbitrary prefix of its contributions while the fault report tells the admin the vocabulary failed. The consumer then runs with a half-installed item vocabulary whose behaviour depends on where the exception happened, which is worse than either installing it or not.~~High
4~~P3~~ItemBuilder.setAmount and setItemMeta skip the null and bounds guards every sibling method applies~~Add the same `itemStack == null` guard the sibling methods use, and reject amounts outside 1..max stack size~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemBuilder.java:141-144~~The class deliberately tolerates a null stack — the copy constructor does `source.itemStack == null ? null : source.itemStack.clone()` and withMeta, decreaseDurability, getItemMaxDurability, getItemDamagedDurability and setDurability all open with an `itemStack == null` guard. setAmount does not: `public ItemBuilder setAmount(int amount) { itemStack.setAmount(amount); return this; }` throws NPE on exactly the state its siblings handle, and setItemMeta at line 194 has the same gap. It also passes the amount through unvalidated, so a config-supplied `0` yields an empty stack rather than an error. Filed P3 because no caller in the reactor is known to hit either state today.~~Medium
5~~P3~~ReflectiveNbtApiAccessor.method caches only successful lookups, re-reflecting and warning on every miss~~Cache the negative result (a sentinel) so an unresolvable method warns once instead of on every call~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/nbt/ReflectiveNbtApiAccessor.java:135-151~~method() consults methodCache first but writes to it only on the found branch; the not-found path ends `log.warn("NBT provider does not expose {}({} args)", name, argCount); return null;` without recording the miss. If the bound NBTAPI build lacks one of the hardcoded accessor names, every subsequent item operation redoes the full reflective scan and emits another warn line, so a single incompatibility becomes repeated reflection cost plus console spam proportional to item throughput. Cosmetic and performance rather than correctness, hence P3.~~High
6~~P3~~ItemParser drops a second {...} attribute block instead of reporting it as malformed~~Report a user-error Fault when more than one attribute block is present rather than stripping it silently~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemParser.java:100-108~~The parser reads attributes from the first block only — `if (matcher.find())` then `KEY_VALUE_PATTERN.matcher(matcher.group(1))` — but cleans up with `remaining = matcher.replaceAll("").trim();`, which strips every `{...}` occurrence in the string. A definition such as `tool{a=1}{b=2}` therefore parses as type `tool` with only `a=1`, and the admin gets no indication that the second block was discarded. The surrounding code does report other malformed shapes through Result.fail with a user-error Fault, so silence here is inconsistent with the file's own error handling.~~High
7~~P2~~ItemParser/ItemConverterRegistry case-fold with the default locale, breaking tr_TR hosts~~Use toUpperCase(Locale.ROOT)/toLowerCase(Locale.ROOT) at every registry and parser fold~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemParser.java:116-120~~[found 2026-09-24, master 4192baa] ItemParser does `String type = parts[0].toUpperCase();`, and ItemConverterRegistry keys and looks up with `type.toLowerCase()` (L33, L69, L75) and checks materials with `Material.valueOf(type.toUpperCase())` (L94), all using the JVM default locale. Under tr_TR, `diamond_sword`.toUpperCase() is `DİAMOND_SWORD` (dotted capital I), so Material.valueOf rejects it and ItemParser returns item.unknown_type for every material whose name contains an i (diamond, iron, stick, ...), and consumer kinds like `item` stop matching their registered keys. ItemSerializerRegistry (L69) and MaterialItemSerializer (L30) fold the same way on the write side. Bukkit's own Material.matchMaterial uses Locale.ENGLISH for exactly this reason, and the rest of keystone-common (DurationParser, PdcPlayerMetaService) already passes Locale.ROOT.~~High
8~~P2~~ItemParser.tryParse lets a throwing converter escape instead of returning a Fault~~Wrap converter.convert in Guard/Result.of (and the same call in ItemDslAdapter) and map any throwable to CODE_CONVERSION_FAILED with the input attached~~ItemParserTest~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemParser.java:120-134~~[found 2026-09-24, master 4192baa] tryParse is documented as "reporting the cause of any failure as a USER_ERROR Fault", but it calls `ItemStack converted = converter.convert(viewer, type, modifier, attributes);` unguarded and only handles a null return. Converters are consumer-supplied and take user-authored modifier and attribute strings (for example `{custom_model_data=abc}`), so a converter that does Integer.parseInt on one propagates NumberFormatException out of a method whose return type is Result, and a config load iterating many items aborts on the first bad entry. ItemDslAdapter.java:120 makes the same unguarded `converter.convert(type, modifier, attrs)` call, so the config-DSL path with file:line reporting loses the location as well.~~High
9~~P2~~ItemBuilder.setMaxStackSize writes an ignored custom_data tag, so it never takes effect~~On 1.20.5+ call ItemMeta#setMaxStackSize(Integer) reflectively (resolve it once, like WorldHeights does for getMinHeight) instead of writing an NBT tag~~ItemBuilderMaxStackSizeTest (pins only that a tag write happens)~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemBuilder.java:135-139~~[found 2026-09-24, master 4192baa] The method returns early below 1.20.5, and on 1.20.5+ it does `return addTag("max_stack_size", size);`, which goes through NbtBridge to NBT-API's NBT.modify(ItemStack, ...). On 1.20.5+ NBT.modify edits the item's minecraft:custom_data component, not the minecraft:max_stack_size data component, so the stack keeps its vanilla limit and the call has no effect on any server. The javadoc ("Sets the stack's maximum stack size") and the green test, which asserts only `assertEquals("max_stack_size", accessor.writes.get(0).tag())`, make the feature look shipped.~~High
