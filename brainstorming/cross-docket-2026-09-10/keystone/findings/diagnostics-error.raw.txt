1~~P1~~ReflectivePacketAdapter.updateTitleThroughPackets leaks NmsCache wiring failure as uncaught IllegalStateException~~Catch RuntimeException alongside ReflectiveOperationException (as setProperty/relativeCameraRotation already do) and report a dependency Fault instead of letting it propagate~~-~~keystone-common/src/main/java/org/luckyraven/keystone/nms/internal/ReflectivePacketAdapter.java:135-152~~updateTitleThroughPackets is the pre-1.20 fallback (VIEW_SET_TITLE null) and calls `NmsCache cache = NmsCache.get();` inside a `try { ... } catch (ReflectiveOperationException ex)` block. NmsCache.build() wraps any ReflectiveOperationException from wiring the reflective handles into `throw new IllegalStateException("Failed to wire reflective NMS cache for " + Bukkit.getBukkitVersion(), ex);` — an unchecked exception the narrower catch here does not match, so it propagates out of updateInventoryTitle into whatever listener/task called it. The two sibling methods in the same class, setProperty (`catch (ReflectiveOperationException | RuntimeException ex)`) and relativeCameraRotation (same), both already guard against exactly this, making the asymmetry clearly unintentional. Trigger: any 1.16-1.19 server (within the supported 1.16.5 floor) where the reflective field/ctor lookup in NmsCache.build() fails once (unexpected build/fork field names), crashing the first GUI title-update call instead of degrading.~~High
2~~P1~~PlayerInputInterceptor.channelRead swallows every exception from apply() with no Diagnostics report~~Route the catch through Guard.of(...)/Diagnostics.capture so subclass bugs in apply() are classified and recorded instead of vanishing~~-~~keystone-common/src/main/java/org/luckyraven/keystone/nms/input/PlayerInputInterceptor.java:197-206~~`channelRead` does `try { apply(session, parsePacket(msg)); } catch (Exception ignored) { } ` on every ServerboundPlayerInputPacket for every player, on the Netty IO thread. Any bug in a concrete subclass's apply() (vehicle/jetpack input handling in Gangland/Bartizan) — or a parsePacket reflection mismatch — is silently discarded with zero logging and zero Fault, directly contradicting this system's own "errors flow through Result/Diagnostics, never log-and-swallow" contract. Because this fires on nearly every packet, a persistent bug here (e.g. a NPE in a subclass's session write) produces no console noise and no `/menu debug` entry, only a permanently-broken feature.~~High
3~~P1~~PlayerInputInterceptor.ensureReflectionReady silently disables input parsing forever with no Diagnostics report~~On failure, report a dependency Fault (mirroring NmsCache/CraftBukkitRevision/ReflectionGuard) before leaving packetClass null~~-~~keystone-common/src/main/java/org/luckyraven/keystone/nms/input/PlayerInputInterceptor.java:92-105~~`ensureReflectionReady()` does `try { resolveAgainst(Class.forName(...ServerboundPlayerInputPacket)); } catch (Exception ignored) { packetClass = null; } finally { reflectionReady = true; }`. If the NMS packet class or its accessor methods ever fail to resolve (renamed field/method in a future Minecraft build, since this floor targets 1.16-1.21.x and current HEAD is on Keystone 1.9.1), the static packetClass stays null for the rest of the JVM's life and channelRead becomes a permanent no-op — with no WARN log and no Fault reported, unlike every comparable reflective probe in this same package (NmsCache.build() throws loudly, CraftBukkitRevision.detect() reports `nms.revision.unknown`, ReflectionGuard.orSkip reports `reflection.type.missing`). An admin debugging "vehicles/jetpacks stopped responding to WASD" after an update has nothing to go on.~~High
4~~P1~~PlayerInputInterceptor ships no shared unregister-on-quit/disable path, leaving Netty handler cleanup entirely to each of 3 consumer plugins~~Add a shared helper (e.g. install(player)/uninstall(player) with an internal registry, plus a PlayerQuitEvent listener and a static removeAll() for plugin disable) instead of only documenting manual pipeline().remove(HANDLER_NAME)~~-~~keystone-common/src/main/java/org/luckyraven/keystone/nms/input/PlayerInputInterceptor.java:17-21~~The class javadoc states "All NMS reflection, version detection, and pipeline guards are owned here. Install a subclass onto a player's connection with `getChannel(player).pipeline().addBefore(\"packet_handler\", HANDLER_NAME, handler)` and remove it with `pipeline().remove(HANDLER_NAME)`" — i.e. the framework centralizes every reflective concern except the one this audit specifically flags as highest-risk: removal on quit/disable. There is no tracking set of installed handlers, no bundled PlayerQuitEvent listener, and no plugin-disable sweep anywhere in this class, so each of Gangland/Bartizan/Oriel's vehicle/jetpack/etc. interceptor must independently remember to strip its handler on quit and on plugin disable; a forgotten removal keeps the player object and handler pinned in the pipeline and can install a duplicate on rejoin.~~Medium
5~~P2~~DefaultClassifiers.byOriginatingPackage misclassifies genuine internal bugs inside Keystone's own nms package as DEPENDENCY~~Narrow the package heuristic to actual reflection/lookup exception types (ReflectiveOperationException subtypes, NoSuchFieldException, etc.) rather than any throwable merely thrown from within org.luckyraven.keystone.nms~~-~~keystone-common/src/main/java/org/luckyraven/keystone/diagnostics/DefaultClassifiers.java:56-69~~`byOriginatingPackage` checks `className.startsWith(prefix)` and, failing that, `stack[0].getClassName().startsWith(prefix)` for `DEPENDENCY_PACKAGES = {"me.clip.placeholderapi", "org.luckyraven.keystone.nms"}`. This runs after the NumberFormatException/CommandException/IAE-ISE/SQLException-ReflectiveOperationException rules, so it catches things like a plain NullPointerException or ArrayIndexOutOfBoundsException whose top stack frame happens to be inside NmsCache/ReflectivePacketAdapter/CameraRotationPackets — i.e. an actual coding bug in Keystone's reflective adapter, not a missing external dependency. Such a fault is logged by LoggingSink at WARN with only a one-line cause summary (`causeSummary`), never the ERROR + full stacktrace INTERNAL_BUG gets, so a real Keystone bug in this exact nms package is systematically the hardest one to triage from the console.~~Medium
6~~P3~~Keystone javadoc names consumer products (Oriel/Gangland/Bartizan) directly, violating "Keystone must not know its consumers"~~Rewrite the javadoc in product-neutral terms (e.g. "the installing plugin", "a consumer's per-version recoil implementations") across all six call-outs~~-~~keystone-common/src/main/java/org/luckyraven/keystone/diagnostics/Diagnostics.java:11~~Diagnostics.java:11 reads "Every classified failure in Oriel flows through here exactly once"; DefaultClassifiers.java:13 says "The rules encode Oriel's house conventions"; FaultKind.java:12,23,28 say faults are "written to the oriel_faults table" and "Oriel caused this... not a bug in Oriel"; Result.java:14 says "scattered across Oriel today"; CameraRotationPackets.java:15 names "Gangland's per-version recoil implementations"; PacketAdapter.java:54 names "Bartizan's recoil manager". Each is javadoc-only (no compiled coupling) but is exactly the pattern the repo's own layering rule calls a bug, and it is a house-wide habit (6 occurrences across 6 files in this one system), not an isolated typo.~~High
7~~P1~~Diagnostics.install/active is one JVM-wide static hub, so on a server running two Keystone-based plugins simultaneously the second plugin's install() silently steals the shared fault sink from the first~~At minimum document (and ideally CAS-guard with a WARN) a second install() call so a shared-classloader double-install is visible instead of silent~~-~~keystone-common/src/main/java/org/luckyraven/keystone/diagnostics/Diagnostics.java:118-127~~`install(Diagnostics diagnostics) { active = diagnostics; }` unconditionally overwrites the single static `active` field with no check for an already-installed hub and no warning. Per this repo's own shared-classloader rule every consumer plugin sees one loaded copy of Diagnostics, and Gangland's own CLAUDE.md documents Bartizan running alongside Gangland as the normal deployment shape (both presumably calling `Diagnostics.install(...)` from their own KERNEL-phase bootstrap). Whichever plugin enables last wins process-wide: the other plugin's `Guard.of(...).run(...)`, `Result.of(...)` and `ReflectionGuard.orSkip(...)` calls all report through the winning plugin's sinks (its RecentFaultsSink, its `/menu debug` command, its persistence table) instead of their own, so an admin investigating one plugin's fault history is silently missing (or polluted with) the other plugin's faults.~~Medium
8~~P2~~LanguageLoader's disk-file failure path degrades to jar defaults through a console-only log.warn, never through Diagnostics/Fault~~Report a Fault (e.g. FaultKind.DEPENDENCY, code "message.file.load") through Diagnostics.active() before falling back to the jar copy~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/message/LanguageLoader.java:200-217~~`tryLoadFromDisk` catches `IOException` from the FileHandler pipeline with only `log.warn("FileHandler error for {}.yml: {}", fileName, exception.getMessage());` and returns null, which sends `loadMessage()` (line 162-176) silently back to the bundled jar resource. This is exactly the "log-and-swallow" idiom Result/Diagnostics exists to replace, and unlike a full parse failure (which disables the plugin loudly in `initialize()`'s catch block) this partial failure never reaches RecentFaultsSink or any admin-facing `/menu debug` equivalent — an admin whose custom message file silently reverted to defaults after a bad edit has no persisted record of why.~~Medium
