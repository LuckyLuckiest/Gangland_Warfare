1~~P0~~FileManager.runInitializer regenerates a valid config on any unrelated Exception, not just parse failures~~Catch a narrower failure signal (e.g. check handler.getParseReport().hasErrors(), or a dedicated load-failure type) before calling handler.createNewFile(); let unrelated RuntimeExceptions from initializer.initialize() propagate instead of backing up and wiping the file~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/FileManager.java:256-269~~`runInitializer` wraps `initializer.initialize()` in `catch (Exception first)` with no filtering on exception type, then unconditionally calls `handler.createNewFile()`, which renames the live file to `-old` and regenerates it from the bundled jar default. A single unrelated bug in the initializer's own registration/business logic (e.g. an NPE in a downstream manager, nothing to do with the YAML content) throws the same `Exception` a corrupt file would, so a perfectly valid, heavily-customized admin config gets reset to jar defaults whenever that unrelated bug fires. The method's own javadoc documents this as intended ("the underlying file is regenerated ... and the initializer is retried once"), conflating "bad config content" with "bug elsewhere in the loader". No test exercises `FileManager.initializeAll()`/`registerInitializer()` at all.~~High
2~~P1~~FileHandler swallows InvalidConfigurationException with zero logging in two places~~Log the caught InvalidConfigurationException (log.warn with UnhandledError, matching every other catch in this class) in both registerYamlFile() and createNewFile()'s post-regeneration load instead of only flipping loaded=false~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/FileHandler.java:248-256~~`registerYamlFile()`'s catch block is `catch (InvalidConfigurationException exception) { loaded = false; }` with no log call at all, unlike every sibling catch block in the class (`logException(...)`, `log.warn`, `log.error`). The same silent pattern repeats in `createNewFile()`'s post-regeneration load: `catch (IOException | InvalidConfigurationException ignored) { loaded = false; }` at lines 139-141. An admin whose initial YAML fails Bukkit's parser, or whose regenerated jar default is itself malformed, gets no console line at all — only a false `isLoaded()` that nothing automatically surfaces or checks eagerly.~~High
3~~P1~~FolderLoader only recreates addExpectedFile() defaults when the whole folder is empty or missing~~In the non-empty branch, after addFiles(files), diff folderFiles against expectedFolderFiles and create+register any expected file still missing, instead of gating recreation purely on folder emptiness~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/FolderLoader.java:78-102~~`loadData()` only consults `expectedFolderFiles` inside `if (!folder.exists() || files == null || files.length == 0)`; the `else` branch (`addFiles(files); if (folderFiles.isEmpty()) return;`) never checks whether every expected file is actually present among the folder's contents. If an admin deletes or renames just one of several expected default files (e.g. one npc yml among many sharing a folder), the folder is still non-empty so the recreate path never runs, and that expected file is silently never regenerated, with no warning that a feature's config is now missing. `addExpectedFile` is public API exercised by no test in this repo.~~High
4~~P1~~ConfigSerializer.scalarToJava doesn't strip digit-grouping underscores before parsing INT/FLOAT tags~~Route Tag.INT/Tag.FLOAT scalar values through the same underscore-stripping logic NodeReader.normalizeNumeric() already applies before Long.parseLong/Double.parseDouble~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/config/ConfigSerializer.java:172-189~~`NodeReader.normalizeNumeric()` explicitly strips `_` from numerics like `10_000_000` "so it parses the same way Bukkit's YamlConfiguration...used to accept it" - a documented, supported config convention. But `scalarToJava()`'s Tag.INT/Tag.FLOAT branches do `Long.parseLong(value.trim())` / `Double.parseDouble(value.trim())` directly on the raw scalar text with no stripping; for `10_000` the NumberFormatException is caught and the method returns the raw String instead. Any value written with digit-grouping underscores silently changes type from int/double to String the first time it round-trips through `toMutableTree`/`writeJavaTree` (the documented admin-GUI edit path), breaking the class's own "type fidelity" guarantee. No test covers an underscored numeric round-trip.~~High
5~~P2~~ConfigParser.convert has no cycle guard for self-referencing YAML anchors, crashing with StackOverflowError~~Track visited Node identities (e.g. an IdentityHashMap<Node,Boolean> threaded through convert/convertMapping/convertSequence) and emit a located ERROR instead of recursing when a node is revisited~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/config/ConfigParser.java:93-156~~The class javadoc promises "the parser never throws on bad input", and `parse()` catches `MarkedYAMLException`/`YAMLException` from `yaml.compose(reader)`. But `convert()`/`convertMapping()`/`convertSequence()` recurse into child nodes with no cycle detection, e.g. `ConfigNode child = convert(file, valueNode, childPath, report);`. A self-referencing anchor/alias (`a: &x {b: *x}`), which SnakeYAML composes into a genuine object-graph cycle rather than throwing, sends this recursion into an unbounded loop, producing an uncaught StackOverflowError that propagates straight through `FileHandler.parsePositional()` (no catch around `new ConfigParser().parse(...)`) - breaking the "never throws" contract the class advertises.~~Medium
6~~P2~~FileLoader.tryAgain always calls load() again even after isLoaded flips true and stop() is requested~~Return from the lambda immediately after calling time.stop() when isLoaded is already true, instead of falling through to an unconditional load(disable, consumer, fileManager) call on every scheduled tick~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/FileLoader.java:94-110~~The retry lambda is `time -> { if (isLoaded) time.stop(); load(disable, consumer, fileManager); }` - `load()` runs unconditionally on every tick regardless of the just-checked isLoaded/stop() call, so a successful load still gets redundantly reloaded by every remaining scheduled tick. Since `load()` disables the plugin on any Throwable when disable=true (`if (disable) Bukkit.getPluginManager().disablePlugin(plugin);`), a transient failure in one of these unnecessary post-success reloads can disable the plugin even though the real data already loaded fine moments earlier. `tryAgain` is public API but called nowhere in this repo, so it is an untested latent trap for any consumer plugin that adopts it.~~Medium
7~~P1~~FileManager.loadFromResources never closes the InputStream it opens~~Wrap the InputStream/InputStreamReader in try-with-resources (or explicitly close inputStream in a finally) so every call, including LanguageLoader's per-locale-file loads, releases the file/classloader handle~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/FileManager.java:163-190~~`loadFromResources(String, ClassLoader)` opens `inputStream = new FileInputStream(file)` (or a classloader resource stream) and passes it into `yamlConfiguration.load(new InputStreamReader(inputStream, StandardCharsets.UTF_8))`, but never calls close() on either the stream or the reader, and there is no try-with-resources anywhere in the method. Bukkit's FileConfiguration.load(Reader) does not close the reader it is handed either, so the underlying file descriptor / classloader resource handle leaks on every call. `LanguageLoader.loadFromResources(relPath)` calls this per language file, so every language load - including every plugin reload - leaks one handle; FileManagerResourceLoaderTest exercises loader-fallback/disk-priority/missing-throws behavior but never asserts the stream is closed.~~High
