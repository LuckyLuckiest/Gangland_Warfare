1~~P1~~ModuleLoader.purgeStale() lets one locked stale jar abort discovery of every module~~Wrap each deleteIfExists in purgeStale()'s second loop in its own try/catch that logs and continues, matching the first loop's per-file handling, so discover() never treats a stale-marker cleanup failure as "folder not usable"~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/ModuleLoader.java:334-345~~purgeStale()'s second loop calls `Files.deleteIfExists(jar)` and `Files.deleteIfExists(marker)` with no inner try/catch, unlike the first loop three lines above it which catches IOException per file and only logs. discover() wraps the whole purgeStale() call in one try/catch that reports FAULT_FOLDER "Modules folder is not usable" and returns an empty descriptor list on any IOException (ModuleLoader.java:100-109). On Windows, exactly the scenario this stale-marker mechanism exists for -- ModuleUpdateService.retire() leaving a marker because "the running loader still holds it open" -- means the jar can still be locked on the next start too; deleteIfExists then throws, and that single leftover marker silently drops every module in the folder, not just the stale one, until an admin manually deletes the file. ModuleLoaderTest's discover_purgesStaleAndMarkers test only covers the success path.~~High
2~~P1~~Unsanitized Maven groupId/artifactId/version let a malicious or compromised repository write outside the modules folder~~Validate groupId, artifactId and every version string (release/latest/list entries) against a strict `[A-Za-z0-9_.-]+` pattern with no path separators or ".." segments before they are used to build any Path, in both ArtifactCoordinate and where MavenMetadata's values are consumed~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/artifact/ArtifactCoordinate.java:48-58~~jarFileName() builds `artifactId + "-" + version + ".jar"` with zero character validation, and ArtifactResolver.download() does `Path target = targetDirectory.resolve(coordinate.jarFileName())` (ArtifactResolver.java:100-101) directly from that string. The version half of the coordinate for an update download comes from MavenMetadata.newestVersion(), which is raw untrusted XML text content from the configured repository (MavenMetadata.java:94-99, no character filtering) -- MavenRepository's own javadoc documents that a self-hosted mirror is a supported, trusted-by-the-operator but externally-reachable data source. A `<release>` or `<version>` value containing "../" segments therefore lets that repository steer the downloaded file's Path outside targetDirectory via normal Path.resolve() traversal, and the mandatory SHA-256 checksum offers no protection because a malicious repository controls both the jar bytes and its own published checksum.~~Medium
3~~P2~~ModuleUpdateService.downloadNow retires the working jar before the download is verified to actually be a module~~Before calling retire(), open the downloaded jar and confirm it parses as a valid ModuleDescriptor (or at least contains a module.yml) via ModuleDescriptorReader.read(), failing the download and leaving the installed jar in place otherwise~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/update/ModuleUpdateService.java:110-123~~downloadNow() only checks `downloaded.isFailure()` (a SHA-256 mismatch or I/O failure) before calling `retire(update.installedJar())`, which moves the currently-working jar into `.stale/`. Nothing verifies the newly downloaded bytes are even a valid module jar (right module.yml, matching Id, implements KeystoneModule) -- only that they match the checksum the same repository published. A wrong artifact coordinate, a bad publish under the same GAV, or an artifactId typo in module.yml silently swaps a working module for garbage; the failure only surfaces as a descriptor/main-class fault on the next restart, by which point the old working jar has already been relocated (manually recoverable from `.stale/`, but with no automatic rollback path).~~Medium
4~~P2~~ModuleUpdateService.pending is a read-modify-write race with no synchronization~~Guard the pending field's read-modify-write in downloadNow (and its wholesale replacement in checkNow) with a lock or an AtomicReference compare-and-set loop instead of a plain volatile field~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/update/ModuleUpdateService.java:117-119~~downloadNow() does `List<ModuleUpdate> remaining = new ArrayList<>(pending); remaining.removeIf(...); pending = List.copyOf(remaining);` against a plain `volatile` field with no lock. Each download normally runs on its own async task via ArtifactResolver.supplyAsync(); if an admin triggers two downloads close together (e.g. retrying a slow update command), both tasks can snapshot the same `pending` list, and whichever write lands second silently un-removes the other's already-downloaded entry, leaving a stale ModuleUpdate in pending() for a module that was already replaced on disk.~~Medium
5~~P3~~instantiate() mislabels a module's failing static initializer as "Main missing"~~Catch ExceptionInInitializerError separately from ClassNotFoundException/plain LinkageError and report it under its own fault code (e.g. module.main.initializer.failed) instead of FAULT_MAIN_MISSING~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/ModuleLoader.java:279-288~~`Class.forName(descriptor.mainClass(), true, classLoader)` is called with initialize=true, so a throwing static initializer in the Main class throws ExceptionInInitializerError, a LinkageError subtype. It is caught by the same branch as an absent class and reported as FAULT_MAIN_MISSING ("names Main X, which is not in its jar"), which is misleading to an admin reading the fault: the class was found, its own init code crashed.~~Medium
6~~P3~~ModuleResolution only reports the first missing/disabled Plugins entry per module~~Iterate all of descriptor.plugins() and add one FAULT_PLUGIN_MISSING per missing entry instead of removing the module and breaking out on the first miss~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/ModuleResolution.java:92-104~~The Plugins-check loop does `if (pluginEnabled.test(plugin)) continue; accepted.remove(descriptor.id()); faults.add(...); break;` -- the `break` exits after the first missing plugin. A module declaring `Plugins: [Bartizan, Vault]` with both absent only ever surfaces "needs plugin Bartizan" in the faults/diagnostics output; an admin who installs Bartizan and restarts then discovers Vault is also required only on the next failed start instead of being told about both up front.~~Low
