1~~P1~~RepositoryRegistry.canRegisterRepository inverts its Table check, defeating ordering and logging a false circular dependency~~Swap the branches so a missing table returns false and a present table continues the loop~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/repository/RepositoryRegistry.java:376-386~~The dependency check is backwards: `if (!tablesByName.containsKey(tableName)) { continue; }` skips the parameter when the table is absent, and then the code logs "depends on table {} which is not yet registered" and returns false for the case where the table IS present. sortRepositoriesByDependencies therefore refuses every repository whose Table dependency is already satisfied, makes no progress, and falls into its escape hatch at line 332 which logs "Circular dependency in repositories detected, registering remaining repositories in order" and appends the rest in declaration order. The net effect on every consumer boot is that table-dependency ordering is silently not applied, plus a misleading circular-dependency warning; FK-bearing schemas that need parent-before-child creation are then at the mercy of scan order.~~High
2~~P2~~DatabaseHelper caches getDatabase() at construction, so a helper built before connect() makes every runQueries a silent no-op~~Resolve the Database from the handler on each call instead of caching it in a final field~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/DatabaseHelper.java:19-24~~The constructor snapshots the handler's current Database once: `this.database = databaseHandler.getDatabase();`. Every query path then uses that captured reference and opens with `if (database == null) return;` (line 47), so a helper constructed before the handler has resolved its database swallows every subsequent query silently — no exception, no log, no fault. The same staleness applies after the handler swaps its Database (the MySQL to SQLite fallback, or a schema switch), leaving the helper pointed at a closed instance. Because the failure mode is a quiet return on a data path rather than an error, an admin sees writes simply not happen.~~High
3~~P1~~DatabaseManager.createDatabaseBackup matches existing rows on columns[0] only, so backups collapse rows on composite keys~~Match on the table's real primary-key column set from the schema rather than assuming columns[0] is unique~~DatabaseManagerTest (startBackup_sqliteToSqlite_copiesData uses a single-column PK listed first, so it never exercises this)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/DatabaseManager.java:158-176~~The backup writer decides insert-vs-update purely from the first column: `Object[] row = config.select(columns[0] + " = ?", new Object[]{objects[0]}, new int[]{dataTypes[0]}, new String[]{"*"});` and then updates with the same single-column predicate. For any table whose primary key is composite, or whose PK simply is not the first column in the column array, distinct source rows share a columns[0] value and each one updates the previously written row instead of inserting. The backup silently ends up with fewer rows than the source and with fields from the last-written row, i.e. corrupted backup data that only surfaces when someone restores from it.~~High
4~~P2~~AuditLogService and DatabaseFaultSink quote table identifiers with double quotes, which default-mode MySQL rejects~~Delegate identifier quoting to the backend dialect (backticks on MySQL) instead of hardcoding double quotes in the raw INSERT~~AuditLogServiceSqliteTest is SQLite-only; MysqlDialectTest is string-level and never touches these classes~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/audit/AuditLogService.java:154-156~~Both classes build their raw INSERT with `private static String quote(String identifier) { return '"' + identifier.replace("\"", "\"\"") + '"'; }` and the audit write is `"INSERT INTO " + quote(tableName) + " (timestamp, actor_uuid, action_type, payload) VALUES (?, ?, ?, ?)"`. MySQL only treats double-quoted text as an identifier under ANSI_QUOTES, which is not the default, so on a stock MySQL server every audit row and every persisted fault row fails with a syntax error caught by `catch (SQLException ex)` and reduced to a warn line. The class javadoc at line 150-153 openly concedes this ("if MySQL is run without ANSI_QUOTES, switch to backticks here"), so the audit trail and the fault sink are effectively dead on the default MySQL configuration while appearing configured. Same quote() duplicated at DatabaseFaultSink.java:114-116.~~High
5~~X~~SQLite.switchSchema rebuilds the pool but never calls connect(), unlike MySQL.switchSchema~~Call connect() after replacing the HikariDataSource, matching MySQL.switchSchema~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/type/SQLite.java:60-85~~SQLite.switchSchema ends with `disconnect(); this.dataSource = new HikariDataSource(config); return true;` — disconnect() nulls the `connection` field (line 135), and nothing re-establishes it. MySQL.switchSchema does the same swap but then calls `connect();` and repopulates tableNames (MySQL.java:105-106). After a SQLite schema switch the legacy Database is left with a live pool but a null connection, so the next schemaExists call throws "No connection established" and getConnection() returns null. This is in the @Deprecated(since=1.6.0) legacy stack scheduled for removal in 2.0.0, which is why it is tiered P2 rather than higher.~~High
6~~P2~~AbstractJdbcBackend.execute and query throw NullPointerException instead of SQLException when dataSource is null~~Null-check dataSource and throw SQLException("not connected") so the declared checked type is what callers actually get~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/backend/AbstractJdbcBackend.java:84-91~~isConnected defends itself with `return dataSource != null && !dataSource.isClosed();` but the query methods do not: `try (Connection conn = dataSource.getConnection(); ...)` dereferences the field directly. Used before connect() or after a connect() that failed, these methods raise an unchecked NPE while declaring `throws SQLException`, so every caller that guards with `catch (SQLException)` — AuditLogService.write and DatabaseFaultSink among them — misses it and the NPE escapes to whatever scheduled task or command invoked it. The consequence is an unhandled stack trace on a path the code visibly intended to handle gracefully.~~High
7~~P2~~RepositoryRegistry.getTableName guesses a table name from the class name when reflection fails~~Report a fault and skip the repository instead of substituting a guessed table name~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/repository/RepositoryRegistry.java:399-411~~When the no-arg instantiation of a Table class throws, the fallback is `return tableClass.getSimpleName().replace("Table", "").toLowerCase();` and the final fallback is the lowercased simple name. A Table whose real name does not match that derivation (a prefixed or pluralised table name, or a Table class needing constructor arguments) silently resolves to a name that exists in no schema, so the dependency decision above is made against a fabricated identifier. It is a latent trap today because the caller's own logic is already inverted (finding 1); fixing finding 1 without this one would make the guess load-bearing.~~Medium
8~~P2~~Legacy MySQL pools hardcode the removed com.mysql.jdbc.Driver class name~~Drop setDriverClassName and let DriverManager resolve, or use com.mysql.cj.jdbc.Driver~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/type/MySQL.java:50~~Both the initial pool and switchSchema set `config.setDriverClassName("com.mysql.jdbc.Driver");` (lines 50 and 83). That class is deprecated in mysql-connector-j 8 and removed in 9, and HikariCP resolves the name eagerly via Class.forName, so on a server shipping a current connector the pool fails to initialise with a ClassNotFoundException at boot rather than falling back. Modern JDBC needs no explicit driver class at all. Legacy deprecated stack, hence P2.~~Medium
9~~P2~~Legacy SQLite and MySQL pools set minimumIdle(5), eagerly opening five native handles~~Set minimumIdle to a small value (or match maximumPoolSize semantics) so idle consumers do not hold five connections~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/type/SQLite.java:52-53~~Both legacy pools configure `config.setMaximumPoolSize(10); config.setMinimumIdle(5);` in the initial pool and again in switchSchema. For SQLite this means five native file handles against the same .db file are opened at pool startup and held, which is what makes the database file undeletable on Windows after close — the consuming repo documents exactly this as a test-teardown hazard requiring CleanupMode.NEVER. For a plugin whose typical query load is bursty this is wasted handles rather than a correctness fault, so P2.~~Medium
10~~P0~~AbstractJdbcBackend.applySchema drops every live column the declared schema omits, destroying its data~~Never emit DROP COLUMN automatically; log the extra column and require an explicit opt-in or manual migration~~SchemaMigrationSqliteTest, SchemaConstraintsSqliteTest (cover ADD COLUMN, not the destructive DROP path)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/backend/AbstractJdbcBackend.java:132-137~~applySchema reconciles the live table against the declared schema and unconditionally destroys anything it does not recognise: `for (String drop : diff.toDrop()) { String dropSql = "ALTER TABLE " + quoteIdentifier(schema.tableName()) + " DROP COLUMN " + quoteIdentifier(drop); stmt.execute(dropSql); }`. SchemaDiff.diff builds toDrop as every live column name absent from the declared set (SchemaDiff.java:42-47), so a downgrade to an older plugin version whose schema lacks a newer column silently drops that column and all its rows' data on the next boot, as does any column an admin or a second plugin added to a shared table. There is no opt-out, no backup and no confirmation — the only trace is a `log.info("Schema migration: DROP COLUMN {}.{}", ...)` line after the data is already gone. Irreversible data loss on a routine version rollback is why this is the highest-tier finding in the system; also Oriel OR-DB-01, which observed it on MySQL.~~High
11~~P2~~AuditLogService and DatabaseFaultSink reduce a failed write to log.warn and never report it to Diagnostics~~Route the SQLException through Diagnostics/Fault so a failing audit or fault write appears in the diagnostics report~~AuditLogServiceSqliteTest (happy path only)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/audit/AuditLogService.java:108-112~~The audit write ends `catch (SQLException ex) { log.warn("Failed to write audit row for {}/{}: {}", logName, actionType, ex.toString(), ex); }` and continues, with no Diagnostics.report call anywhere in the class; DatabaseFaultSink handles its own write failures the same way. An audit log exists precisely so that privileged actions are recoverable after the fact, so a silently dropped row is the one failure mode that must never be invisible — yet it never reaches the fault report an admin actually reads. This is the same swallow the repo's Result/Diagnostics rule forbids on a data path, and it compounds finding 4, which makes every one of these writes fail on a default MySQL server; also Oriel OR-DB-02.~~High
12~~P1~~DatabaseHandler.setType leaves type MYSQL with a null database when MySQL fails and SQLite fallback is disabled~~Fail fast when useSQLite declines to fall back — disable the plugin or throw rather than returning with a null database~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/DatabaseHandler.java:115-140~~setType assigns `this.type = type;` up front, then runs `try { enforceType(MYSQL); } catch (RuntimeException exception) { useSQLite(getSchema()); }`. enforceType's MySQL branch nulls the field before rethrowing (`this.database = null; throw asPluginException(exception);`, and PluginException extends RuntimeException), and useSQLite opens with `if (!settings.isSqliteFailedMysql()) return;` — so an admin who deliberately disabled the fallback gets a silent early return leaving type == MYSQL and database == null. Boot then completes as if the database were configured and every later getDatabase() consumer dereferences null, so an ordinary MySQL outage turns into NullPointerExceptions on every persistence call instead of a clean startup failure. Filed P1 because the missing piece is a loud fail-fast rather than the crash itself; also Gangland CL-01, and still present at 1.9.1 despite the enforceType hardening.~~High
13~~P1~~AbstractRepository.saveAll snapshots the supplier's live collection on the caller's thread, so an async autosave copies a main-thread map~~Take the snapshot on the main thread before scheduling, or require the supplier to return an already-immutable copy~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/repository/AbstractRepository.java:121-131~~saveAll copies with `List<T> snapshot = new ArrayList<>(collection);` before handing the snapshot to the async task, which is safe only when saveAll itself is called from the main thread. The autosave path does not guarantee that: RepositoryRegistry.saveAll walks its entries calling `repo.saveAllFromMemory(countDown)`, which resolves `saveAll(dataSupplier.get(), onComplete)` (AbstractRepository.java:149) — and a data supplier is conventionally a live view such as `() -> userMap.values()`. Driven from a consumer's asynchronous autosave timer, the ArrayList copy therefore iterates a map the main thread is still mutating, yielding a ConcurrentModificationException or a torn snapshot; the registry's `catch (Exception e) { log.error("Failed to save data for repository: {}", ...) }` swallows it into one line and that repository's data is simply never written. Nothing in the API documents a main-thread requirement; also Gangland CL-02.~~High
14~~P1~~AbstractJdbcBackend.applySchema adds NOT NULL columns without a DEFAULT, so SQLite rejects every ADD on a populated table~~When a diff-added column is NOT NULL with no default, emit a type-appropriate DEFAULT (legacy Table.inferDefault parity) or add it nullable~~TableSchemaAdapterSqliteTest (sidesteps it: the added column is setCanBeNull(true) with the comment "SQLite rejects ADD COLUMN ... NOT NULL without a default on a non-empty table")~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/backend/AbstractJdbcBackend.java:118-122~~[found 2026-09-24, master 4192baa] The ADD path emits `" ADD COLUMN " + renderColumn(add)`, and renderColumn appends `" NOT NULL"` for any non-nullable column but a DEFAULT only `if (column.defaultValue() != null)` (lines 297-301). TableSchemas.fromAttribute maps nullability from Attribute.isCanBeNull(), whose field defaults to false (Attribute.java:23), so every ORM attribute a consumer adds without explicitly calling setCanBeNull(true) or setDefaultValue arrives as NOT NULL with no default. Verified against sqlite-jdbc 3.49: `ALTER TABLE "t" ADD COLUMN "level" INTEGER NOT NULL` on a one-row table fails with "Cannot add a NOT NULL column with default value NULL", so applySchema throws and RepositoryRegistry.createTables rethrows it (lines 286-288) on the first release that adds a column, which is the case the diff engine exists for. The legacy Table.buildColumnDefinition covers this with `def.append(" DEFAULT ").append(inferDefault(attribute.getType()))` (Table.java:446-449), so moving a repository onto the backend SPI regresses it; MySQL is hit too for TIMESTAMP NOT NULL columns under the default NO_ZERO_DATE strict mode.~~High
15~~P1~~DatabaseManager backup copies rows positionally between two independently ordered column lists~~Read the source with the target's column list (or map values by column name) instead of pairing source-physical values with target-physical columns, and catch RuntimeException per row~~DatabaseManagerTest (source and target are built from the same TestTable, so their column orders always match)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/DatabaseManager.java:141-167~~[found 2026-09-24, master 4192baa] databaseInformation reads each source table with `String[] columns = config.getColumns()...` then `info.addAll(config.selectAll(columns));` (lines 91-95), i.e. in the source's physical column order, while backupAllDatabases re-derives `String[] columns = config.getColumns()` from the target (line 145) and writes `config.insert(columns, objects, dataTypes)` positionally. Legacy validateSchema appends new columns at the physical end with ALTER TABLE ADD COLUMN, whereas a backup target created fresh by handler.initialize() uses attribute order, so any table that gained a non-trailing attribute in an upgrade has two different orders and values land in the wrong columns; same-typed columns (two INTEGER counters) swap silently. When the counts differ Database.insert throws IllegalArgumentException (Database.java:258-259), and a type mismatch throws ClassCastException at `(LocalDateTime) value` (Database.java:740); neither is caught by the per-row `catch (SQLException exception)` (line 172), so runQueries' Throwable catch abandons every remaining table while startBackup still logs "Backup done". Distinct from row 3 (columns[0] matching).~~Medium
16~~P1~~MysqlBackend hardcodes com.mysql.cj.jdbc.Driver, which Spigot 1.16.5 and 1.17.x do not bundle~~Return null from driverClassName() so Hikari resolves the server-bundled driver from the jdbc:mysql URL through DriverManager~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/backend/MysqlBackend.java:20-25~~[found 2026-09-24, master 4192baa] `protected String driverClassName() { ... return "com.mysql.cj.jdbc.Driver"; }` is passed to `config.setDriverClassName(driverClass)` in AbstractJdbcBackend.connect (lines 52-53), and HikariConfig.setDriverClassName throws a RuntimeException ("Failed to load driver class ...") when the class is absent. Keystone's API floor is Spigot 1.16.5, and the local spigot-1.16.5 and spigot-1.17.1 server jars contain com/mysql/jdbc/Driver.class (Connector/J 5.1) but no com/mysql/cj/jdbc/Driver.class; Connector/J 8 is only bundled from 1.18. So on the floor versions the new MysqlBackend can never connect, and every MySQL consumer migrated to the backend SPI fails at connect, while the class javadoc says it "Uses the server-bundled" cj driver. AbstractJdbcBackend already guards `if (driverClass != null)`, so no class name is needed; this is the mirror image of row 8 (legacy MySQL pinned to the old name, which newer servers drop).~~High
17~~P2~~AbstractJdbcBackend.connect lets HikariCP's unchecked pool-init failure escape its throws SQLException contract~~Wrap setDriverClassName and new HikariDataSource(config) and rethrow a RuntimeException as SQLException, as DatabaseHandler.enforceType does for the legacy stack~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/backend/AbstractJdbcBackend.java:52-68~~[found 2026-09-24, master 4192baa] connect ends with `this.dataSource = new HikariDataSource(config);`, and with Hikari's default initializationFailTimeout an unreachable MySQL makes that constructor throw HikariPool.PoolInitializationException, a RuntimeException; a missing driver class likewise throws RuntimeException from setDriverClassName (line 53). DatabaseBackend.connect documents only `throws SQLException`, so a consumer that follows the contract with `catch (SQLException)` to fall back to SQLite or disable persistence never sees the failure: it escapes into bean creation and an ordinary database outage takes the whole boot down. The legacy DatabaseHandler.enforceType catches `SQLException | RuntimeException` for exactly this reason (its javadoc: driver-level problems "surface as unchecked HikariCP exceptions"), so the backend SPI regressed it. Sibling of row 6 (NPE instead of SQLException in execute/query).~~High
18~~P1~~DatabaseFaultSink.accept runs a blocking JDBC insert on whatever thread reported the fault~~Hand the insert to a bounded async queue or executor (drop on full) instead of calling backend.execute inline from accept~~DatabaseFaultSinkSqliteTest (in-memory SQLite only, never an unreachable pool)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/diagnostics/DatabaseFaultSink.java:76-96~~[found 2026-09-24, master 4192baa] Diagnostics.deliver calls `sink.accept(fault)` synchronously on the reporting thread (Diagnostics.java:89-92), and accept does `backend.execute(sql, ...)` inline, which borrows a pooled connection with a 30 s connectionTimeout (AbstractJdbcBackend.java:64). SQLException is classified DEPENDENCY (DefaultClassifiers.java:49-51), exactly the kind this sink persists, so during a MySQL outage every reported database fault triggers another blocking connection attempt: a main-thread caller such as JdbcQueryBuilder.list (which reports "backend.query" before rethrowing) waits up to 30 s for its own query and then up to another 30 s inside the sink. In normal operation it still adds a synchronous database round trip to the reporting thread for every INTERNAL_BUG/DEPENDENCY fault. The outage is the moment faults are frequent, so a database blip becomes watchdog-length tick stalls.~~Medium
19~~P1~~AbstractRepository.runBackendAsync skips onComplete when the work throws anything but SQLException~~Catch RuntimeException too (report it) and run onComplete in a finally block, matching DatabaseHelper.runQueries' Throwable catch~~RepositoryBackendIntegrationTest, RepositoryRegistryBackendTest (happy-path onComplete only)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/repository/AbstractRepository.java:209-224~~[found 2026-09-24, master 4192baa] The backend branch runs `try { work.run(); } catch (SQLException exception) { reportBackendFailure("repository.save", exception); } if (onComplete != null) onComplete.run();`, so any unchecked exception, such as an NPE from a consumer's getData/processSave on one bad entity or the UnsupportedOperationException DatabaseBackend.upsert's default throws for a custom backend, bypasses both the Diagnostics report and onComplete. RepositoryRegistry.saveAll(onComplete) counts repositories down through exactly this callback (`repo.saveAllFromMemory(countDown)`, RepositoryRegistry.java:251-252), so one such failure in an async autosave means the registry-level completion never fires and whatever the consumer chained on it (a shutdown or kick sequence, a "save finished" notice) waits forever, with the exception visible only as a scheduler stack trace. The legacy branch has no such gap because DatabaseHelper.runQueries catches Throwable before runQueriesOnComplete runs onComplete.~~High
20~~P2~~RepositoryRegistry.invokeCondition resolves condition classes with Class.forName, ignoring the scan ClassLoader~~Pass the scan classLoader into invokeCondition and use Class.forName(className, true, classLoader)~~RepositoryRegistryScanTest (no @Repository condition case)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/repository/RepositoryRegistry.java:555-573~~[found 2026-09-24, master 4192baa] scanAndRegisterRepositories(basePackage, classLoader) exists (since 1.8.0) so a module jar's repositories are discovered through its own loader, but the condition check it runs, `boolean invoke = invokeCondition(condition);` (line 97), does `Class<?> clazz = Class.forName(className);`, which resolves against RepositoryRegistry's own defining loader (Keystone's plugin loader). A ModuleClassLoader is not a plugin loader, so a module repository declaring `@Repository(condition = "<module class>.enabled()")` hits ClassNotFoundException, invokeCondition logs "Failed to invoke condition" and returns false, and the repository is skipped on every boot even when its condition holds. Unconditional module repositories are unaffected, which is why only the conditional ones silently go missing.~~Medium
21~~P2~~RepositoryRegistry iterates a ConcurrentHashMap for saveAll and migrateSchema, discarding the dependency order it records~~Keep repositories in insertion order (LinkedHashMap, or iterate by registrationOrder) so saveAll, the migrate pass and getAllRepositories run parent-before-child~~RepositoryRegistryIntegrationTest (single repository only)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/repository/RepositoryRegistry.java:233-262~~[found 2026-09-24, master 4192baa] repositories is `new ConcurrentHashMap<>()` keyed by entity Class (line 51), and saveAll, the migrateSchema loop (line 293) and getAllRepositories, whose javadoc says "Get all registered repositories in registration order", all iterate `repositories.values()` in hash order; `registrationOrder` is written at lines 132 and 497 and never read. Tables declare FOREIGN KEYs (TableSchemas.fromTable and legacy createTableQuery), which InnoDB enforces. On shutdown the saves run inline in that arbitrary order, so if a child repository flushes first, a child row whose parent was created since the last autosave violates the FK and, because the batch upsert is one transaction, the whole child batch rolls back and is lost at stop; during play the async saves race the same way and only self-heal on a later cycle. SQLite does not enforce FKs, so this surfaces on MySQL only. Separate from row 1, which breaks the sort whose order this map then discards.~~Medium
22~~P2~~SchemaMigrations.rebuildSqliteTable copies with INSERT OR IGNORE, silently dropping rows that violate the new DDL~~Use a plain INSERT so a constraint violation rolls the rebuild back, or compare copied and source row counts and fail on a shortfall~~SchemaMigrationsTest (both fixture rows satisfy the new constraints; no violating-row case)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/SchemaMigrations.java:136-143~~[found 2026-09-24, master 4192baa] The rebuild runs `stmt.execute("INSERT OR IGNORE INTO " + tempName + " (" + columns + ") " + "SELECT " + columns + " FROM " + tableName);` and then unconditionally `stmt.execute("DROP TABLE " + tableName);` and commits. SQLite's OR IGNORE resolution skips any row that violates a UNIQUE, PRIMARY KEY, NOT NULL or CHECK constraint of the replacement table, so a legacy row with a NULL in a column the new DDL makes NOT NULL, or two rows colliding on the new key, are discarded with no error, count or log line, and the original table holding them is dropped in the same transaction. The javadoc promises the method "copies the specified columns over" and that a mid-flight failure "rolls back cleanly", but constraint violations are turned from failures into silent, irreversible row loss.~~Medium
23~~P2~~TableBackend.insert/update/delete/selectAll emit bare identifiers while applySchema and upsert quote them~~Quote table and column names through the backend dialect (expose quoteIdentifier or route reads through BackendQueryBuilder) in every TableBackend statement~~TableBackendSqliteTest (fixture column names are not keywords)~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/component/TableBackend.java:60-137~~[found 2026-09-24, master 4192baa] applySchema, upsert/upsertAll and where() all go through the dialect's quoteIdentifier, so a table with a column named after an SQL keyword (`rank`, reserved in MySQL 8, or `order`/`key`/`group` on either dialect) is created and upserted successfully. The remaining statements concatenate raw names: `"INSERT INTO " + table.getName() + " (" + String.join(", ", cols) + ")..."`, `sql.append(setCols.get(i)).append(" = ?")` and `"SELECT " + String.join(", ", cols) + " FROM " + table.getName()`. On such a table selectAll, the call the class javadoc and AbstractRepository.tableBackend() point doLoadAll at, fails with a syntax error; AbstractRepository.loadAll reports it and returns an empty collection, so the plugin boots as if the table were empty while the upsert path keeps writing to it.~~Medium
24~~P3~~MysqlBackend never enables rewriteBatchedStatements, so upsertAll still costs one round trip per row~~Add config.addDataSourceProperty("rewriteBatchedStatements", "true") in MysqlBackend.applyExtraConfig~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/backend/MysqlBackend.java:27-33~~[found 2026-09-24, master 4192baa] DatabaseBackend.upsertAll's javadoc states "flushing hundreds of dirty entities must not pay one round trip per row", and AbstractJdbcBackend implements it as a JDBC batch (`stmt.addBatch()` then `stmt.executeBatch()`, lines 169-176). MySQL Connector/J only collapses a batch into multi-row statements when rewriteBatchedStatements=true; without it executeBatch sends each INSERT ... ON DUPLICATE KEY UPDATE separately. MysqlBackend.applyExtraConfig sets only `useSSL`, so on MySQL an autosave of N rows is still N network round trips (inside one transaction), the cost the SPI method was added to remove; SQLite is unaffected. Performance only, hence P3.~~Medium
25~~P1~~Legacy Table.recreateTableSQLite copies only declared attributes, dropping kept undeclared columns and their data~~Carry every live column not in the declaration into the rebuilt table (definition from PRAGMA table_info) or refuse the rebuild and report a fault~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/component/Table.java:417-450~~[found 2026-09-24 by the review follow-up fixer, verified by the orchestrator] validateSchema calls recreateTableSQLite(database) on a nullability mismatch; it builds the new table from `createTableQuery(database)` and copies `String cols = String.join(", ", attributes.keySet());` then `DROP TABLE tmpName`. Since KS-DB-10 (1.11.2) validateSchema deliberately keeps undeclared live columns, but this rebuild path still discards them: any consumer on the legacy stack that changes one attribute's nullability loses every kept column's data on the next boot, contradicting the keep-by-default contract.~~High
