1~~P0~~DefaultPlaceholderProvider calls live Bukkit world/entity API with no main-thread guard~~Guard the Player-branch (getWorld/getLocation/getBlock().getBiome(), Bukkit.getOnlinePlayers()) behind a main-thread check or route through Bukkit's scheduler before resolving, or document/enforce that this provider is main-thread-only and never place it behind an async render path~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/provider/DefaultPlaceholderProvider.java:62-74~~The composite chain this provider sits at the tail of (per its own javadoc: "Register this provider last in the composite chain") is invoked from whatever thread the consumer's text-rendering path runs on, which the repo's own conventions note is sometimes async (scoreboard/timer render paths). This provider unconditionally calls `online.getWorld().getName()`, `online.getLocation().getBlock().getBiome()` and `Bukkit.getOnlinePlayers().size()` with zero `Bukkit.isPrimaryThread()` check anywhere in the class. Bukkit's World/Entity/Location API is main-thread-only; calling it off-thread can throw or silently corrupt server state, and there is no try/catch or Result/Diagnostics wrapper around any of it.~~High
2~~P1~~CompositePlaceholderProvider.resolve has no per-provider exception isolation~~Wrap each provider.resolve call in try/catch, log the fault through Diagnostics, and fall through to the next provider (or the untouched value) instead of letting the exception propagate~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/CompositePlaceholderProvider.java:59-69~~`for (PlaceholderProvider provider : providers) { value = provider.resolve(player, value); }` calls straight through with no try/catch. PlaceholderProvider's own contract says implementations "should ... never throw", but the composite is the one class whose entire job is to safely chain providers it does not control (consumer beans, PAPI adapters, third-party MiniPlaceholders bridges) — one misbehaving provider anywhere in the chain crashes the whole resolve() call, taking down whatever scoreboard render/chat line/GUI title triggered it, instead of degrading per the repo's mandated Result/Diagnostics error flow.~~High
3~~P2~~PlaceholderHandler never overrides PlaceholderRequest.onRequest, so direct use silently resolves nothing~~Either make PlaceholderHandler abstract (or require a PlaceholderRequest delegate in its constructor) so it cannot be instantiated without token-resolution logic, or add a javadoc warning plus a fail-fast check~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/PlaceholderHandler.java:14-27~~`PlaceholderHandler extends PlaceholderRequest` but declares no `onRequest` override, so it inherits `PlaceholderRequest.onRequest`'s `return null;` (PlaceholderRequest.java:9-12). `CharReplacer.apply` treats a null `request.onRequest(...)` result as "unresolved" and reconstructs the original `%token%` text unchanged. A consumer that writes `new PlaceholderHandler(prefix, closure)` directly (the constructor takes only prefix+closure, nothing else) gets a handler whose `replacePlaceholder`/`convert` compile and run fine but never replace a single token — a silent no-op with no crash or log, and the class is not `abstract` nor documented as subclass-only.~~Medium
4~~P2~~Built-in placeholder providers NPE on a null OfflinePlayer that the sibling effect SPI explicitly supports~~Null-check `player` up front in resolve() and pass tokens through unchanged (matching the "never throw for unknown input" contract), the same way FlashPlaceholderWrapper.PlaceholderResolver already documents @Nullable player~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/provider/CooldownPlaceholderProvider.java:81~~`UUID uuid = player.getUniqueId();` runs unconditionally once the quick-string-contains check passes, with no null guard on `player` itself; `MetaPlaceholderProvider.resolve` and `DefaultPlaceholderProvider.resolve` (`player.getName()` at DefaultPlaceholderProvider.java:56) have the identical gap. This conflicts with `FlashPlaceholderWrapper.PlaceholderResolver#resolve(@Nullable OfflinePlayer player, ...)` in the same package family, which explicitly supports a null player for no-player-context resolution (console/global broadcasts) — the two placeholder SPIs disagree on whether null player is legal input.~~Medium
5~~P2~~FlashEffect/FlashPlaceholderWrapper/ConditionalFlashWrapper are a fully built, tested, but entirely unwired placeholder effect layer~~Either wire FlashPlaceholderWrapper/ConditionalFlashWrapper into PlaceholderHandler/CompositePlaceholderProvider's resolution path (e.g. as a PlaceholderEffect stage) or delete the dead subsystem~~FlashEffectTest,FlashPlaceholderWrapperTest~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/effect/FlashPlaceholderWrapper.java:1-89~~A repo-wide grep for `processFlash`, `processConditionalFlash`, `isFlashPlaceholder` and `isConditionalFlash` outside this package and its own tests returns zero hits: nothing in PlaceholderHandler, CharReplacer, PlaceholderProvider or CompositePlaceholderProvider ever calls into this effect layer, so no consumer's `%flash:...%`/`%flashif:...%` token can ever actually resolve through the normal placeholder pipeline despite the fully-documented javadoc usage examples and passing unit tests making the feature look shipped.~~High
6~~P2~~FlashEffect/FlashPlaceholderWrapper hold mutable static state shared across the whole classloader with no per-plugin namespace~~Scope flashStates/currentTick per-provider-instance (or per calling plugin) instead of a bare static field, and back flashStates with a ConcurrentHashMap if it is ever wired up for concurrent render threads~~FlashPlaceholderWrapperTest~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/effect/FlashEffect.java:13~~`private static final Map<String, FlashState> flashStates = new HashMap<>();` is keyed only by the raw placeholder text, with no per-plugin prefix; `FlashPlaceholderWrapper.currentTick` (FlashPlaceholderWrapper.java:20-21, `@Setter private static long currentTick = 0;`) is a single JVM-wide tick counter with a Lombok-generated public static setter. Per the repo's shared-classloader rule, if this layer is ever wired into two consumer plugins on the same server (Gangland + Bartizan + Oriel), one plugin calling `updateTick()`/`setCurrentTick()` at its own cadence desynchronizes every other plugin's flash animation, two identically-named placeholder keys across plugins share one FlashState object, and the plain (non-concurrent) HashMap is unsafe if hit from more than one render thread at once.~~Medium
7~~P2~~PlaceholderHandler recompiles a regex Pattern and a format string on every call instead of caching them~~Cache a compiled Pattern per Closure as a static final field (or precompute per-instance in the constructor) and precompute the "%prefix_" format string once in the constructor~~-~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/PlaceholderHandler.java:37-47~~`containsPlaceholder` does `Pattern.compile(PLACEHOLDER_PATTERN).matcher(text).find()` — compiling a fresh Pattern on every single call even though the regex string is a `static final` constant — and `replacePlaceholder` does `String.format("%%%s_", prefix)` fresh every call even though `prefix` is immutable per instance. `convert()` calls `containsPlaceholder` on every line of text handed to it, so on a scoreboard/GUI render path invoked every tick per player per line this recompiles the same regex repeatedly, adding avoidable CPU/GC churn to a hot path instead of caching once.~~Medium
8~~P3~~Keystone placeholder javadoc/tests name concrete consumer products and Gangland domain concepts~~Rewrite the examples to consumer-neutral placeholder names (e.g. "vip_status", "custom_meta") and drop the specific consumer name from the javadoc~~MetaPlaceholderProviderTest~~keystone-common/src/main/java/org/luckyraven/keystone/placeholder/effect/ConditionalFlashWrapper.java:11-21~~The class javadoc's usage examples are `%flashif:user_wanted>0:5:5:user_wanted%` and `%flashif:gang_bounty>0:user_bounty%` — "wanted level" and "gang bounty" are Gangland Warfare's own domain entities (per Gangland's CLAUDE.md: `Bounty`, `Wanted`), and sibling `FlashEffect.java:9` calls the whole feature a "GTA-style flash effect", directly naming Gangland's theme. `MetaPlaceholderProvider.java:20-21` similarly documents "a consumer can keep its historical namespace (e.g. Oriel's `%oriel_meta_*%`)", naming the Oriel plugin by name, and `MetaPlaceholderProviderTest`'s `customPrefix` test repeats the same "oriel_meta" example — all violating the repo's own Layering rule that Keystone must not know its consumers.~~High
