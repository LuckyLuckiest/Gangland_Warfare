# KS-IV menu-inventory (Phase E6, Keystone 1.11.0) — verified findings (branch phase-e6-inventory, commit 53de9e6)
1~~P0~~Held items are lost on Escape and on disconnect - MenuListener's tracked close branch untracks and calls onClose but never returns an ItemHoldingComponent's contents~~default void returnHeldItemsOnUserClose(Player) on MenuTarget, called after untrack in the tracked close branch; ChestMenu overrides it with the guarded return~~MenuListenerTest:357, ChestMenuTest:210 (fix round 1)~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/listener/MenuListener.java:146-160 (tracked close branch), :164-183 (onQuit)~~The bug Oriel independently fixed as its own menu-chest #1 (ChestMenuCloseListener.java:69-73 there). CraftBukkit closes the inventory before PlayerQuitEvent fires (SPIGOT-5799), so onQuit finds nothing tracked and the PLAYER_DISCONNECT return path is dead code; MenuListener was final and MenuTarget had no user-close hook, so nothing could restore the item. FIXED fix round 1 (2026-09-17): MenuTarget.java:103 default hook, MenuListener.java:165-169/182-190, ChestMenu.java:247-250.~~High
2~~P1~~ItemComponent/adoptComponentsFrom leak and reset the builder-marked interactive flag, and fill decoration overwrites it, producing a stale-flag leak plus an infinite free-item fill exploit on rerender~~Decoration must never override a builder-interactive slot: clear the interactive flag wherever handlers are cleared, have adoptComponentsFrom adopt the fresh interactiveSlots set, and have fill/renderAll skip interactive slots~~ChestMenuTest (post-render assertions, fix rounds 1-2), ChestMenuBuilderTest (fix round 1)~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/component/ItemComponent.java:37,126; keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/chest/ChestMenu.java:85,376-377,387,406,491,496-501,517,526,542-545,573; ChestMenuBuilder.java:141-145,254~~Three-part bug found in the E6.1 review: (a) ItemComponent's render call to view.interactive(false) made builder.interactive(slot).fill(X) non-interactive after the first render; (b) dropped/empty components cleared handlers but not the interactive flag; (c) adoptComponentsFrom did not adopt fresh.interactiveSlots. FIXED fix round 1 (a/b/c), but the fix round 1 re-review found two NEW regressions from the fix itself: N1 rerenderSlot/the untouched-render branch cleared a builder-marked interactive flag instead of preserving it, and N2 fill decoration started landing in interactive slots (infinite free fill items via take+rerender). FIXED fix round 2 (2026-09-20): interactive slots floored/restored (ChestMenu.java:85,406,517; ChestMenuBuilder.java:254) and fill skips them (:526,573).~~High
3~~P1~~COLLECT_TO_CURSOR on an interactive top-inventory slot collects the item, bypassing the interactive-slot click policy that blocks every other action~~Cancel the click before the interactive check succeeds, unless handleDepositClick explicitly accepted it~~MenuListenerTest (policy matrix, fix round 1)~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/listener/MenuListener.java:82 (moved to :88 by the fix)~~The click-policy matrix blocked every other action on a non-deposit interactive slot but let COLLECT_TO_CURSOR through, letting a player vacuum items out of slots the menu never intended to be collectible from. The same shape exists today in Gangland's own custom inventory framework (InventoryClickHandler.java:34-43, gangland-ui/inventory-api) - Gangland's own docket row (T-41) notes this is fixed by its WS2 cutover onto keystone-inventory. FIXED fix round 1 (2026-09-17).~~High
4~~P0~~A menu veto followed by the player disconnecting before the scheduled reopen fires loses the held item permanently; a plugin reload/disable landing inside that same veto-to-reopen window loses it too~~Record a pending reopen per InventoryService when a tracked close is vetoed; consume it atomically on PlayerQuitEvent and return the held items with PLAYER_DISCONNECT (quit fires before player data saves); drain any still-pending reopen from closeAll(RELOAD/PLUGIN_DISABLE) and onPreClear/onShutdown too, not only from onQuit~~InventoryServiceLifecycleTest:69-98 (drain/reload case, fix round 3), MenuListenerTest:539 (exactly-once veto+quit case, fix round 2)~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/InventoryService.java:42,84-96 (pendingReopen record/consume), :99-110 (onPreClear/onShutdown drain), :124-134 (closeAll drain); keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/listener/MenuListener.java:159-177 (veto branch), :200-203 (onQuit)~~Found by the E6.1 fix round 1 re-review as a pre-existing gap (also present in Oriel's own listener pair - see that project's docket twin row) while confirming the F1 fix. FIXED across fix rounds 2 and 3 (2026-09-17 to 2026-09-20): round 2 added the per-service pendingReopen record/consume on veto+quit; the round 2 re-review found closeAll(RELOAD/PLUGIN_DISABLE) still did not drain it (a reload inside the veto-to-reopen window still lost the item, since the vetoed player was already untracked before the record); round 3 closed that gap with an atomic Set.copyOf snapshot drain in closeAll plus onPreClear/onShutdown hooks, and a same-instance tracked+pending conflict is structurally impossible since untrack always precedes record.~~High
5~~P1~~ChestMenu.close(Player) calls tracker().untrack(player) unconditionally; if menu B was opened while menu A was still live, A's later programmatic close untracks the player from B, so B's Escape then misses MenuListener's tracked branch and B's held items are silently discarded~~Untrack only when service.tracker().currentMenuOf(player) == this, not unconditionally~~-~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/chest/ChestMenu.java:233 (close(Player))~~A close(Player) call from menu A (e.g. a stale reference kept past a switch, or two independent InventoryService consumers racing) blindly calls the shared tracker's untrack(player) regardless of which menu the tracker currently associates with that player. If the player has since moved on to menu B, A's close silently steals B's tracking entry, so when the player then presses Escape on B, MenuListener's tracked-close branch (the one that returns held items, see row 1/4 above) never fires for B - its ItemHoldingComponent contents are discarded with no error. Not reachable from Gangland's npc-shops today (its flows do not hold two live menus for one player simultaneously), but a real hazard for any future multi-menu consumer. Found by the WS2 G4 lead, seconded by the review.~~High
6~~P1~~A disconnect during a suspend() detour is swallowed by suppressClose(), so end() never runs and the flow's FlowCloseListener is never unregistered - one leaked listener holding a dead Player per abandoned anvil-style prompt~~Add a PlayerQuitEvent handler on the flow (or clear the suspended flag on quit) so a disconnect mid-detour still runs the normal end()/unregister path~~-~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/flow/MenuFlow.java:330 (suppressClose during a suspend() detour)~~When a flow is suspended for an external detour (anvil prompt, sign edit, etc.) and the player disconnects instead of completing the detour, suppressClose() treats the resulting close event as expected and swallows it, so end() is never called and the FlowCloseListener Bukkit registered for that flow is never unregistered - it leaks for the plugin's uptime, holding a reference to the now-offline Player. Reachable today through Gangland's BankerAmountView, BankerCreateAccountView, BankerRenameAccountView and QuantitySelectorView (all suspend() during an anvil prompt). Found by the WS2 G4 lead, seconded by the review.~~Medium
7~~P3~~MenuFlow.rerender() and switchTo() have no `if (ended) return;` guard (openAt() has one), so a task scheduled before end() can still write into an already-closed inventory~~Add the same `if (ended) return;` guard rerender()/switchTo() that openAt() already has~~-~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/flow/MenuFlow.java:216 (rerender), :163 (switchTo)~~openAt() already guards against running after end() with `if (ended) return;`, but rerender() and switchTo() do not, so a task scheduled while the flow was still live (e.g. a delayed recompute) can fire after end() and write into an inventory that has already been closed and torn down. Harmless today - Gangland's BarterView.scheduleRecompute is the only known caller in this shape, and its own cleanup already tends to beat the scheduled task in practice - but the guard is structurally missing. Found by the WS2 G4 lead, seconded by the review.~~Low
8~~P2~~Keystone compiles keystone-inventory against Bukkit 1.16.5, where InventoryView is a class; a consumer on 1.21.x (Gangland pins 1.21.11) sees it as an interface, so keystone-inventory bytecode calling ChestMenu.close(Player, CloseReason) throws IncompatibleClassChangeError against any mocked Player in a consumer's unit test~~A Keystone test-support helper that hides the version gap, or compile keystone-inventory's own module against a 1.21 api while keeping plugin.yml's api-version at 1.16~~-~~keystone-inventory module (compiled against Bukkit 1.16.5); consumer-side symptom in Gangland's item-survival tests~~Live servers are unaffected: Keystone's plugin.yml declares api-version 1.16 and CraftBukkit's Commodore rewrites the InventoryView class/interface mismatch transparently at the packet/reflection layer server-side. The gap only bites a consumer's own unit tests, which run against the raw 1.21.x Bukkit API/mocks with no Commodore rewrite in play - Gangland's item-survival tests currently wrap the call in a documented catch (closeIgnoringKnownBukkitVersionGap) to route around it rather than fail. Test-fragility tier (P2), not a live-server bug. Found by the WS2 G4 lead, seconded by the review.~~Medium
9~~P0~~Opening another menu of the same InventoryService over a menu with held items (ctx.openMenu, openPrevious, registry.open, B.open) skips returnHeldItemsOnUserClose and discards A's held items~~In MenuListener.onClose also call target.returnHeldItemsOnUserClose(player) on the non-tracked branch (safe: close(Player, reason) already cleared the held state, and the guarded return no-ops on an empty held list)~~ChestMenuTest:237 covers only the reverse (stale close of A with B holding); nothing covers A holding then B opened~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/listener/MenuListener.java:150-186~~[found 2026-09-24, master 4192baa] ChestMenu.open tracks the incoming menu before opening it (`service.tracker().track(player, this);` then `player.openInventory(inventory);`, ChestMenu.java:215-217), so the InventoryCloseEvent CraftBukkit fires for the outgoing menu A sees `service.tracker().currentMenuOf(player) == target` false and skips the whole tracked branch, including `target.returnHeldItemsOnUserClose(player);` (MenuListener.java:184); only `target.onClose(event, player)` runs. Trigger: any menu with an ItemHoldingComponent (Oriel deposit slot, a trade-input tray) plus a navigation button using ClickContext.openMenu/openPrevious (ChestClickContext.java:86,91 route to InMemoryMenuRegistry.doOpen -> menu.open) or a direct B.open(player). A's Bukkit inventory is discarded with the item in it: permanent item loss. MenuTarget.returnHeldItemsOnUserClose's own doc (MenuTarget.java:93-95) promises the return for opening another inventory, which only holds when the other inventory is not a menu of the same service.~~High
10~~P1~~ClickContext.closeMenu() goes through the plain ChestMenu.close(Player), which untracks before closeInventory, so a Close button discards every ItemHoldingComponent's items~~Have ChestClickContext.closeMenu() call menu.close(player, CloseReason.USER) (the returning overload), or fold into the KS-IV-09 fix so the non-tracked close branch also returns held items~~-~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/chest/ChestClickContext.java:95-97~~[found 2026-09-24, master 4192baa] `public void closeMenu() { menu.close(player); }` reaches ChestMenu.close(Player) (ChestMenu.java:240-247), which runs `service.tracker().untrack(player);` and then `player.closeInventory();`; the resulting close event fails MenuListener's `currentMenuOf(player) == target` check, so returnHeldItemsOnUserClose never runs and the plain overload returns nothing by itself. ClickContext.closeMenu is the only close exposed to click handlers and its doc is just 'Close the current menu for the player', so a deposit or input menu with a standard Close button loses whatever the player put in. docs/keystone-inventory.md:72-75 documents that the plain overload does not return items, but not that the canonical click-context close uses it.~~High
11~~P0~~ChestMenu.adoptComponentsFrom drains an ItemHoldingComponent only when its slot vanishes; a slot that a different component replaces loses the held items on an in-place MenuFlow switch~~Drain when fresh.components.get(oldSlot) != outgoing (identity), not only when the slot is absent from fresh~~ChestMenuTest.adoptComponentsFromReturnsHeldItemsForDroppedSlots covers only the dropped-slot case~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/chest/ChestMenu.java:387-405~~[found 2026-09-24, master 4192baa] The drain is gated on `if (!fresh.components.containsKey(oldSlot)) { ... if (outgoing instanceof ItemHoldingComponent held) { returnHeldItemsForSlot(...` and then `components.clear(); components.putAll(fresh.components);` drops the outgoing component unconditionally. Trigger: MenuFlow.switchTo to a different panel with the same size and title (the in-place branch, MenuFlow.java:295-302) where panel A has a deposit or holding component at slot 13 and panel B puts any other component (a confirm button) at slot 13. The holding component is removed from the map without a return, renderAll overwrites the physical slot with B's item, and no later close path (returnHeldItems iterates `components`) can find it again: item loss. The same happens on MenuFlow.rerender() if Panel.render builds a new holding-component instance per render. docs/keystone-inventory.md:67 only promises the drain for a dropped slot.~~High
12~~P1~~renderAll never clears a re-rendered slot's old handlers, so click kinds bound by the previous component (outgoing panel, previous page) stay live and fire~~clearHandlers(slot) before renderOne for every component slot in renderAll/rerenderSlot (renderInto rebinds what it needs), not only on the untouched branch~~-~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/chest/ChestMenu.java:515-528~~[found 2026-09-24, master 4192baa] renderAll clears handlers only for components that rendered nothing (`if (!touched) { inventory.setItem(entry.getKey(), null); clearHandlers(entry.getKey());`); a touched slot keeps every previously bound ClickKind because SlotHandlers.set replaces only the kinds the new component sets (SlotHandlers.java:24-26) and ItemComponent.renderInto binds only its non-null handlers (ItemComponent.java:127-135). adoptComponentsFrom clears handlers only for slots missing from fresh (ChestMenu.java:396). Trigger: an in-place MenuFlow switch or rerender where slot N had onAnyClick(buy) and now holds a component with only onLeftClick(confirm); a right, middle, drop or number-key click resolves to the stale ANY handler and runs the old panel's action. PagedRegion makes it routine: an entry with a handler on page 1 (`component.onAnyClick(entry.handler())`, PagedRegion.java:66-67) followed by a handler-less Entry(item) at the same cell on page 2 keeps firing page 1's handler after an in-place page change.~~High
13~~P1~~ChestMenu.open() on a menu the player is already viewing runs MenuListener's tracked close branch on itself: untracks the player, fires on_close, and returns held items still rendered in the slot~~In ChestMenu.open, untrack (or skip the re-track) before openInventory when currentMenuOf(player) == this, or have MenuListener.onClose ignore a close whose menu is being reopened (a reopening flag set around openInventory)~~-~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/chest/ChestMenu.java:205-219~~[found 2026-09-24, master 4192baa] open() does `service.tracker().track(player, this); renderAll(player); player.openInventory(inventory);`; when the player's top inventory is already this menu (same or, after ensureTitleFor rebuilt it, the previous inventory), CraftBukkit fires InventoryCloseEvent for it first, and MenuListener sees `service.tracker().currentMenuOf(player) == target` true (MenuListener.java:155), so it untracks the player, calls returnHeldItemsOnUserClose, and dispatches the on_close handler. The player ends up viewing the menu while untracked: a later Escape takes the non-tracked branch (held items deposited afterwards are lost), vetoClose no longer applies, and closeAll(RELOAD/PLUGIN_DISABLE) does not close it. The held items were returned while the component's rendering still shows them in the slot (renderAll ran before openInventory), so an interactive holding slot can be emptied a second time: duplication. Trigger: re-opening a live menu, which is the only way to apply a changed %placeholder% title (ensureTitleFor runs only from open, ChestMenu.java:485-499), or a MenuRegistry factory that returns a cached menu instance.~~Medium
14~~P1~~MenuFlow's FlowCloseListener ends the flow on a close that MenuListener vetoed; the next-tick reopen then shows a menu whose flow is dead~~Have FlowCloseListener skip end() when the closing menu vetoes the close (holder.target().vetoesClose(player)), or have MenuListener expose the pending reopen so the flow can check service.consumePendingReopen state~~-~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/flow/MenuFlow.java:333-342~~[found 2026-09-24, master 4192baa] FlowCloseListener.onClose calls `end()` whenever `holder.target() == currentMenu && event.getPlayer().equals(viewer)` and suppressClose() is false; it never consults vetoesClose. When a panel sets ChestMenuBuilder.vetoClose, MenuListener (registered first at bootstrap) untracks, records a pending reopen and schedules `menu.open(player)` (MenuListener.java:156-178), while the flow listener runs end(): onEnd fires, held items are returned, and the close listener is unregistered. Next tick the dead flow's menu reopens and is tracked; its buttons call switchTo/back, which no-op on `if (ended) return;` (MenuFlow.java:166,193), and each Escape re-triggers the veto. The player sits in a window whose navigation does nothing and cannot close it while the predicate holds.~~Medium
15~~P2~~MenuFlow.end() from FlowCloseListener calls ChestMenu.close, which calls player.closeInventory() inside the InventoryCloseEvent; the nested close dispatches the menu's on_close handler twice~~In ChestMenu.close(Player) skip closeInventory when invoked from the close of this same inventory (or have FlowCloseListener end the flow without closing: return held items and untrack only)~~-~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/chest/ChestMenu.java:244-246~~[found 2026-09-24, master 4192baa] On Escape, MenuListener.onClose dispatches `target.onClose(event, player)` once, then FlowCloseListener calls end() -> `currentMenu.close(viewer, CloseReason.NAVIGATION)` (MenuFlow.java:270) -> close(Player), where `if (player.getOpenInventory().getTopInventory() == inventory) { player.closeInventory(); }` is still true because the view is only swapped after the event. CraftBukkit's closeInventory re-fires InventoryCloseEvent with no reentrancy guard, so MenuListener runs again (now on the non-tracked branch) and dispatches on_close a second time. Any non-idempotent on_close handler on a flow panel (message, refund, releaseHold-style cleanup) runs twice per Escape.~~Medium
16~~P2~~An in-place MenuFlow switch keeps the outgoing panel's vetoClose, onClose/onOpen, clickDelay, permission, name, placeholders and fill; adoptComponentsFrom copies only components~~Adopt the per-panel settings too (closeVeto, onCloseHandler, onOpenHandler, clickDelayMillis, requiredPermission, name, placeholders; make fillComponent non-final and adopt it), or restrict the in-place branch to panels with identical settings~~MenuFlowTest.switchToADifferentPanelWithMatchingSizeAndTitleSwitchesInPlace asserts only menu identity and panel id~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/chest/ChestMenu.java:387-405~~[found 2026-09-24, master 4192baa] adoptComponentsFrom copies `components`, `interactiveSlots` and `builderInteractiveSlots` from the throwaway menu and nothing else; `fillComponent` is final (ChestMenu.java:68). switchInternal's in-place branch (MenuFlow.java:295-302) uses it for any different panel with the same rows and title, so panel B's `.vetoClose(...)`, `.onClose(...)`, `.clickDelay(...)` and `.fill(...)` are silently ignored and panel A's stay in force (A's veto can trap the player on B; B's on_close cleanup never runs). The full-reopen branch builds a real menu with B's settings, so behaviour differs depending only on whether two panels happen to share a title.~~High
17~~P2~~KS-IV-02 fix is incomplete: border and line decoration still render into builder-interactive slots, so a take-and-rerender still mints free decoration items~~Skip border and line placement for slots in interactiveSlots in ChestMenuBuilder.build (as renderAll already does for fill)~~ChestMenuTest.fillNeverLandsInAnInteractiveSlotEvenAfterRerender (fill only)~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/chest/ChestMenuBuilder.java:237-241~~[found 2026-09-24, master 4192baa] KS-IV-02 set the invariant that decoration never overrides a builder-interactive slot but applied it only to fill (ChestMenu.java:535,582). build() still does `if (border != null) applyBorder(finalSlots, size); for (LineComponent line : lines) applyLine(finalSlots, line, size);` without consulting `interactiveSlots`, so the border or line component sits in `components` for that slot and renderAll re-renders it on every pass while the slot stays interactive (floor). Trigger: `.rows(1).border(M).interactive(4)` (with rows 1 or 2 every slot is border, applyBorder at ChestMenuBuilder.java:279-289) or a line through an input slot; the player takes the pane, the next rerender puts it back.~~Medium
18~~P2~~A vetoed-close reopen that open() refuses (permission gate, subclass view gate) consumes the pending entry and never returns the held items~~In the scheduled reopen, return stillPending's held items when the menu is not tracked after menu.open(player) (open refused)~~MenuListenerTest:539 covers veto then quit only~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/listener/MenuListener.java:166-177~~[found 2026-09-24, master 4192baa] The veto branch untracks without returning held items, and the next-tick task does `MenuTarget stillPending = service.consumePendingReopen(playerId); ... if (player.isOnline()) { menu.open(player); }`. ChestMenu.open returns early when `!player.hasPermission(requiredPermission)` (ChestMenu.java:207-210), and a subclass open gate (Oriel's view requirements) can refuse the same way. The pending entry is already consumed, the player is untracked and not viewing the menu, and no later path (onQuit, closeAll) can find the held items: item loss. Trigger: permission revoked, or a view requirement turning false, between the vetoed close and the next tick.~~Medium
19~~P2~~InMemoryMenuRegistry.open records a NavigationHistory entry even when the menu's open() was refused by its permission gate, so openPrevious lands on the wrong menu~~Record only when the player is actually tracked on the new menu after open (service tracker check), or have Menu.open report refusal~~-~~keystone-inventory/src/main/java/org/luckyraven/keystone/inventory/registry/InMemoryMenuRegistry.java:52-59~~[found 2026-09-24, master 4192baa] `boolean opened = doOpen(player, name); if (opened) { history.recordOpen(player.getUniqueId(), name); }`, and doOpen returns true whenever `menu.open(player)` does not throw (InMemoryMenuRegistry.java:88-94); ChestMenu.open returns normally after `noPermissionHandler.accept(player); return;` (ChestMenu.java:207-210). Trigger: a button doing ctx.openMenu("admin") for a player lacking the permission while in menu main: the trail becomes [.., main, admin] although the player never left main, and a Back button then pops admin and reopens main instead of the menu before it.~~Medium
