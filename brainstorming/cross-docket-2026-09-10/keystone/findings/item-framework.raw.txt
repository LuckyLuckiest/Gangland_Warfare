1~~P1~~ItemConverterRegistry.resolve() uses raw Material.valueOf instead of XMaterial~~Resolve the material check via XMaterial.matchXMaterial(type) instead of Material.valueOf so renamed/legacy names still resolve~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemConverterRegistry.java:L87-L98~~resolve() falls back to the MATERIAL converter only when `Material.valueOf(type.toUpperCase())` succeeds, catching IllegalArgumentException as "not a material". Across Keystone's supported 1.16.5-1.21+ span some material constants were renamed (e.g. GRASS_PATH -> DIRT_PATH in 1.17), so a definition saved under an older server version throws on the newer one and resolve() returns null instead of the material converter. ItemParser then reports item.unknown_type for what is really a valid, just renamed, material - the exact version-drift case the repo's XSeries rule exists to prevent. ItemConverterRegistryTest and ItemParserTest only exercise current-version names (DIAMOND_SWORD) so the gap is untested.~~High
2~~P2~~ItemBuilder.setAmount performs no bounds check before ItemStack#setAmount~~Clamp amount to [1, itemStack.getMaxStackSize()] (or reject <=0) before delegating to ItemStack#setAmount~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemBuilder.java:L141-L144~~`public ItemBuilder setAmount(int amount) { itemStack.setAmount(amount); return this; }` passes the value straight through with no validation. A caller that hands it 0, a negative count, or a value above the material's real max stack size gets a stack Bukkit will happily hold in that state; zero/negative-amount stacks are a known ghost-item/duplication vector once such a stack reaches an inventory or shop flow. ItemBuilderMetaTest.stackPassThroughs only asserts the happy-path value 5 is forwarded, so this is untested.~~Medium
3~~P2~~ItemBuilder.decreaseDurability/setDurability never clamp against getItemMaxDurability()~~Clamp the damage value written in setDurability to [0, getItemMaxDurability()]~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemBuilder.java:L146-L192~~`decreaseDurability` computes `currentDurability + amount` and hands it to `setDurability`, which calls `damageable.setDamage(durability)` with no upper bound check even though `getItemMaxDurability()` exists two methods away and knows the real limit. Repeated decreases (or one large `amount`) can push the stored damage past the material's max durability, which Bukkit does not itself clamp, leaving an over-damaged/inconsistent stack instead of a clean break-or-cap outcome. No test exercises a decrease past the material's max durability.~~Medium
4~~P2~~ItemBuilder mixes null-tolerant and null-unsafe methods over the same itemStack field~~Null-guard setAmount/getType/setItemMeta/customHead the same way withMeta does, or forbid a null itemStack in the constructor~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemBuilder.java:L141-L144~~The single-arg `ItemBuilder(ItemStack itemStack)` constructor accepts null with no check, and the copy constructor explicitly supports it (`source.itemStack == null ? null : source.itemStack.clone()`), and `withMeta`/`decreaseDurability`/`getItemMaxDurability`/`getItemDamagedDurability`/`setDurability` all guard `itemStack == null`. But `setAmount` (`itemStack.setAmount(amount)`), `getType` (`itemStack.getType()`), `setItemMeta`, and all three `customHead` overloads (`itemStack.getType() != XMaterial.PLAYER_HEAD.get()`) dereference the field with no guard, so a builder built or copied from a null stack throws NullPointerException on those calls instead of degrading like the rest of the class.~~Medium
5~~P2~~ReflectiveNbtApiAccessor.method() has no negative-result cache for an unresolvable method name~~Cache a null/sentinel Method for a name that resolution failed to find so it is only scanned and warned once, not on every call~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/nbt/ReflectiveNbtApiAccessor.java:L135-L151~~`method()` checks `methodCache` for a hit but only populates the cache in the found branch; when `candidate` is never found it falls through to `log.warn("NBT provider does not expose {}({} args)", name, argCount); return null;` without caching that outcome. If the bound NBTAPI variant ever lacks one of the hardcoded setter/getter names this class expects, every single tag read/write of that shape re-scans the full `getMethods()` array and re-logs a warning, instead of degrading once like `NoOpNbtAccessor.warnOnce()` does for the no-provider case.~~Medium
6~~P2~~ItemVocabularies.install() writes a vocabulary's registrations directly into the live registries with no rollback on partial failure~~Buffer a vocabulary's converter/serializer/refresher calls and only flush them into the shared registries after contribute() returns normally~~ItemVocabulariesTest~~keystone-item/src/main/java/org/luckyraven/keystone/item/spi/ItemVocabularies.java:L66-L77~~`RegistrarImpl` forwards every `converter(...)`/`serializer(...)`/`refresher(...)` call straight onto the shared `converters`/`serializers`/`refreshers` registries as it happens; if `vocabulary.contribute(registrar)` throws after some calls succeeded, those registrations are never undone even though `report(namespace, t)` marks the whole vocabulary as failed. `ItemVocabulariesTest.throwingVocabularyIsolated` only covers a vocabulary that throws before registering anything, so a converter registered with no matching serializer from a half-failed vocabulary is an untested state.~~Medium
7~~P3~~ItemParser silently drops a second {...} attribute block instead of reporting it~~Fail with a fault (or fold every block) instead of matcher.replaceAll("") silently stripping every brace block after only the first is parsed~~-~~keystone-item/src/main/java/org/luckyraven/keystone/item/ItemParser.java:L96-L109~~`Matcher matcher = ATTRIBUTE_PATTERN.matcher(remaining); if (matcher.find())` only extracts key/value pairs from the first `{...}` block via `matcher.group(1)`, but `remaining = matcher.replaceAll("").trim()` strips every `{...}` occurrence in the string. A malformed definition with two attribute blocks, e.g. `tool{a=1}{b=2}`, parses `a=1` correctly but silently discards `{b=2}` with no fault or log, instead of surfacing the malformed input.~~Low
