1~~P0~~Timer.start() cannot re-schedule the same BukkitRunnable after stop(), crashes on restart~~Have Timer create/schedule a fresh Runnable per start() (or restrict restart via a copy()-style factory like SequenceTimer already provides) instead of re-invoking runTaskTimer(Async) on `this`~~-~~keystone-common/src/main/java/org/luckyraven/keystone/timer/Timer.java:49-57~~`start(boolean async)` calls `runTaskTimerAsynchronously(plugin, delay, period)` or `runTaskTimer(plugin, delay, period)` on `this` (Timer extends BukkitRunnable). Bukkit's BukkitRunnable can only ever be scheduled once per instance - cancel() never resets its internal task id, so a second runTaskTimer(Async) call on the same object throws IllegalStateException("Already scheduled as ..."). Any stop() then start() sequence on the same Timer/RepeatingTimer/CountdownTimer/CountupTimer/SequenceTimer instance therefore crashes, yet RepeatingTimer.stop() resets tickCount/justStarted and SequenceTimer.reset()/copy() exist specifically to support reuse after stopping. RepeatingTimerTest/SequenceTimerTest only drive run() directly with a mocked isRunning(), so the real scheduler path is never exercised and this never surfaces in CI.~~High
2~~P1~~InMemoryCooldownService/PersistentCooldownService cooldown maps never evict entries for players who leave~~Evict a player's sub-map on PlayerQuitEvent (or a periodic sweep) instead of relying only on the lazy per-(player,id) expiry check in expiryOf()~~-~~keystone-common/src/main/java/org/luckyraven/keystone/cooldown/InMemoryCooldownService.java:15,48-62~~`expiries`/`cache` are ConcurrentHashMap<UUID, Map<String, Long>> that only shrink when expiryOf(player, id) is called again for the exact same (player, id) after it has expired: `if (expiry <= System.currentTimeMillis()) { byId.remove(id); return 0L; }`. A player who triggers a cooldown once and never logs in again (or never re-triggers that exact id) leaves their UUID entry in the map for the life of the server process - both InMemoryCooldownService and PersistentCooldownService (keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/cooldown/PersistentCooldownService.java:58,246-260) share this pattern, and on a public server with high player churn this is unbounded memory growth over uptime.~~High
3~~P1~~CooldownFormat.human(Duration.ZERO) renders "1s" instead of no time remaining~~Special-case duration.isZero()/isNegative() to return "" (or a caller-supplied zero label) before the Math.max(...,1) sub-second floor~~CooldownFormatTest~~keystone-common/src/main/java/org/luckyraven/keystone/cooldown/CooldownFormat.java:46~~`long seconds = Math.max(duration.toSeconds(), 1);` floors every duration - including Duration.ZERO, which CooldownService.remaining() returns by contract when a cooldown is not active - up to 1 second. CooldownFormatTest.subSecondRoundsUpToOneSecond pins exactly this: `assertEquals("1s", CooldownFormat.human(Duration.ZERO));`. Any caller that formats cooldownService.remaining(...) directly for display (a placeholder, a fail message) without a separate isActive() check will show "1s" for a cooldown that has already fully expired.~~High
4~~P1~~PersistentCooldownService runs its JDBC schema/purge/upsert calls synchronously on the caller's thread~~Offload ensureReady()/persistStart()/persistClear() onto an async task (mirroring UpdateNotifier's async-then-hop-to-main pattern in the same scope) instead of blocking whatever thread calls start()/clear()/isActive()~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/cooldown/PersistentCooldownService.java:110-125~~start() calls ensureReady() (which on first use runs backend.applySchema(...), a DELETE purge, and a full-table SELECT synchronously) and then persistStart() (a synchronous upsert or DELETE+INSERT) inline, on whatever thread invoked CooldownService.start(). CooldownService.start()/clear() are gameplay-triggered (e.g. from an event handler on the main thread), so every cooldown grant/clear blocks on a live database round trip; over a network MySQL connection this stalls the calling thread for the query latency. Nothing in this class wraps the DB calls in runTaskAsynchronously, unlike UpdateNotifier in the same scope which explicitly does this to avoid freezing the server thread.~~Medium
5~~P1~~PersistentCooldownService swallows SQLException with only a log line, never through Diagnostics~~Route the caught SQLException through Keystone's Diagnostics/Result pipeline (per the persistence layer's own error-handling convention) in addition to logging, so a broken cooldown table is visible to fault tooling, not just the console~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/cooldown/PersistentCooldownService.java:186-188~~ensureReady() (`catch (SQLException exception) { log.warn("Failed to initialise persistent cooldown store: {}", exception.toString(), exception); }`), persistStart() (:217-219) and persistClear() (:236-238) all catch SQLException and only log.warn, then continue as if nothing happened - ready stays false or the cache silently diverges from disk. Per this repo's persistence convention, errors are meant to flow through Result/Pipeline + Diagnostics, not be logged and swallowed; here a persistently failing cooldown table degrades every write to in-memory-only with no operator-facing signal beyond a console warning that can scroll away.~~High
6~~P2~~SequenceTimer.addIntervalTaskPair()/reset() mutate shared queue state with no synchronization against an async run()~~Synchronize addIntervalTaskPair()/reset() against run() (or restrict them to the timer's own thread / pre-start only) so mutating the sequence while start(true) is ticking asynchronously can't corrupt intervalTaskQueue~~-~~keystone-common/src/main/java/org/luckyraven/keystone/timer/SequenceTimer.java:43-54~~addIntervalTaskPair() mutates intervalTaskPairs (an ArrayList) and intervalTaskQueue (a LinkedList), and reset() (:116-124) clears and repopulates the same LinkedList. Neither collection is thread-safe. run() (:56-93) polls and mutates intervalTaskQueue on whatever thread the scheduler ticks it on, which is a dedicated async thread when the timer was started with start(true). Calling addIntervalTaskPair() or reset() from the main thread - a plausible pattern for a dynamically-built sequence - while an async SequenceTimer is ticking races two threads over the same non-thread-safe LinkedList/ArrayList.~~Medium
7~~P2~~A null cooldown id crashes CooldownService.isActive/start with NPE instead of failing gracefully~~Objects.requireNonNull(id, ...) with a clear message (or treat null as a no-op) at the top of isActive/remaining/start/clear in both implementations~~-~~keystone-common/src/main/java/org/luckyraven/keystone/cooldown/InMemoryCooldownService.java:48-62~~expiryOf() calls `byId.get(id)` and start() calls `expiries.computeIfAbsent(player, ...).put(id, expiry)` against ConcurrentHashMap, which throws NullPointerException on any get(null)/put(null, ...) rather than returning null the way HashMap would. A caller that resolves a cooldown id from config/placeholder input and gets null (a missing YAML key, a mistyped lookup) crashes with an unhelpful NPE instead of a clear IllegalArgumentException or a safe no-op; PersistentCooldownService (same pattern, persistence/cooldown/PersistentCooldownService.java:94-125) has the identical exposure.~~Medium
8~~P2~~ClickDebounce's single shared cooldown id lets one call site's delayMillis silently override another's~~Namespace the debounce id per call site (e.g. CLICK_DELAY_ID plus a caller-supplied key) instead of one global constant, so two features configuring different delays don't collide~~-~~keystone-common/src/main/java/org/luckyraven/keystone/cooldown/ClickDebounce.java:39-49~~shouldSwallow(CooldownService cooldowns, Player player, int delayMillis) takes a per-call delayMillis but always keys the cooldown under the single constant CLICK_DELAY_ID. If two unrelated call sites (e.g. a GUI click handler configured for 200ms and an NPC-interaction handler configured for 800ms) both debounce through the same CooldownService, whichever site's click lands first wins the window length for both: `cooldowns.start(uuid, CLICK_DELAY_ID, Duration.ofMillis(delayMillis))` only runs when not already active, so the second site's configured delayMillis is silently discarded for the remainder of the first site's window.~~Low
