1~~P1~~Diagnostics.install unconditionally overwrites the process-wide active hub, so the last consumer to boot steals every plugin's fault sink~~Make install() reject or warn when a hub is already active, or key the active hub per owning plugin~~DiagnosticsTest (resets the static between tests rather than asserting install-collision behaviour)~~keystone-common/src/main/java/org/luckyraven/keystone/diagnostics/Diagnostics.java:118-121~~The installer is `public static void install(Diagnostics diagnostics) { active = diagnostics; }` — a bare assignment to one JVM-wide static with no already-installed check and no warning. Keystone is loaded once and shared, so on a server running Gangland plus Bartizan or Oriel the second plugin to bootstrap silently replaces the first plugin's hub, and every later `Diagnostics.active()` call from the first plugin routes its faults into the second plugin's sink and fault table. The repo's own shared-classloader rule names exactly this pattern — a static singleton holding per-plugin state — as forbidden, and the consequence is misattributed and misrouted diagnostics on precisely the multi-consumer servers this library exists to serve.~~High
2~~P1~~PlayerInputInterceptor.channelRead swallows every exception from apply() with an empty catch~~Report through Diagnostics (rate-limited) instead of an empty catch, so a broken subscriber is visible~~-~~keystone-common/src/main/java/org/luckyraven/keystone/nms/input/PlayerInputInterceptor.java:198-205~~Every inbound player-input packet runs `try { apply(session, parsePacket(msg)); } catch (Exception ignored) { }` on the Netty IO thread. Any failure in a concrete subclass's apply() — a consumer's vehicle or jetpack input handling — or in parsePacket's reflection is discarded with no log, no fault and no counter, so the feature simply stops responding while the server looks healthy. This is the exact log-and-swallow shape the repo's Result/Diagnostics rule forbids, made worse by sitting on a per-packet hot path where an admin has no other signal.~~High
3~~P1~~PlayerInputInterceptor.ensureReflectionReady silently disables input parsing forever when the NMS packet class fails to resolve~~Report a fault when resolution fails so the degraded state is visible instead of silently permanent~~-~~keystone-common/src/main/java/org/luckyraven/keystone/nms/input/PlayerInputInterceptor.java:96-103~~Resolution is one-shot and its failure path is silent: `try { resolveAgainst(Class.forName("net.minecraft.network.protocol.game.ServerboundPlayerInputPacket")); } catch (Exception ignored) { packetClass = null; } finally { reflectionReady = true; }`. Setting reflectionReady in the finally means the failure is cached permanently for the JVM, and channelRead's guard `if (reflectionReady && packetClass != null && ...)` then skips every packet for the rest of the server's uptime. On a Minecraft build that renames the packet class or its accessors, every consumer's input-driven feature goes dead at once with not a single line in the console explaining why.~~High
4~~P2~~ReflectivePacketAdapter.updateTitleThroughPackets lets NmsCache wiring failure escape as an uncaught IllegalStateException~~Catch IllegalStateException alongside ReflectiveOperationException and degrade to the non-packet path~~-~~keystone-common/src/main/java/org/luckyraven/keystone/nms/internal/ReflectivePacketAdapter.java:135-152~~The pre-1.20 title fallback calls `NmsCache cache = NmsCache.get();` inside a block guarded only by `catch (ReflectiveOperationException ex)`. NmsCache.build() wraps any reflective wiring failure in an IllegalStateException, which is unchecked and therefore sails straight through that catch to the caller. On a server in the supported 1.16.5-to-1.19 range where the reflective handles fail to wire, a routine title update throws instead of degrading, surfacing as a stack trace on whatever consumer path requested it. The adapter's whole purpose is to degrade rather than throw, so the narrow catch defeats its own contract.~~High
5~~P2~~PlayerInputInterceptor offers no shared unregister path, leaving Netty handler removal to each of three consumers~~Ship a static uninstall(player)/uninstallAll() helper beside the documented install snippet~~-~~keystone-common/src/main/java/org/luckyraven/keystone/nms/input/PlayerInputInterceptor.java:17-21~~The class javadoc hands the caller both halves of the lifecycle as raw pipeline calls — install with `pipeline().addBefore("packet_handler", HANDLER_NAME, handler)` and remove it with `pipeline().remove(HANDLER_NAME)` — while owning all the reflection and version guards itself. Removal on quit and on plugin disable is therefore reimplemented independently by every consumer, and a consumer that misses either case leaks the handler and the Player reference it closes over for the rest of uptime, with a duplicate handler installed on rejoin. No leak exists inside Keystone itself, so this is an API-hardening gap rather than a live fault, but it is a sharp edge on a class three plugins subclass.~~Medium
6~~P2~~DefaultClassifiers.byOriginatingPackage classifies genuine bugs inside Keystone's own nms package as DEPENDENCY~~Drop org.luckyraven.keystone.nms from DEPENDENCY_PACKAGES, or match only genuinely third-party frames~~-~~keystone-common/src/main/java/org/luckyraven/keystone/diagnostics/DefaultClassifiers.java:56-69~~DEPENDENCY_PACKAGES contains both `"me.clip.placeholderapi"` and `"org.luckyraven.keystone.nms"`, and the rule matches when either the fault's class name or `stack[0].getClassName()` starts with one of them. The first entry is a genuine third party; the second is Keystone's own code, so an internal defect in the NMS layer is reported to admins as someone else's dependency problem rather than as a Keystone bug. That misdirects triage for exactly the reflection-heavy area most likely to break on a server upgrade.~~High
7~~P2~~LanguageLoader.tryLoadFromDisk degrades to jar defaults through a console-only log.warn, never through Diagnostics~~Route the IOException through Diagnostics/Fault so the silent fallback to bundled defaults is a reportable event~~-~~keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/message/LanguageLoader.java:200-217~~The disk-load failure path catches IOException from the FileHandler pipeline with only `log.warn("FileHandler error for {}.yml: {}", fileName, exception.getMessage());` and returns null, which sends loadMessage() quietly back to the bundled jar resource. An admin whose translated language file is unreadable or malformed therefore sees their customised strings silently replaced by shipped defaults, with a single warn line and nothing in the fault report. It is the log-and-swallow pattern the repo's own error rule forbids, on the path that decides what every player reads.~~High
8~~P3~~Keystone javadoc names consumer products (Oriel, Gangland, Bartizan) across six files~~Rewrite the affected javadoc in neutral terms (the consumer, the host plugin)~~-~~keystone-common/src/main/java/org/luckyraven/keystone/diagnostics/Diagnostics.java:11~~Diagnostics.java:11 states "Every classified failure in Oriel flows through here exactly once", DefaultClassifiers.java:13 says "The rules encode Oriel's house conventions", and FaultKind.java:12,23,28 describe faults as "written to the oriel_faults table" and "Oriel caused this... not a bug in Oriel". The hard layering rule is that Keystone must not know its consumers; these are comments rather than code, so nothing misbehaves, but they misinform the other two consumers reading the same API and describe a fault table name that does not match what a Gangland or Bartizan server actually writes to.~~High
9~~P2~~PlayerInputInterceptor binds only Mojang names, so it is dead below Paper 1.21.2~~Resolve the channel through NmsCache's multi-name connection lookup and add the Spigot packet names (PacketPlayInSteerVehicle) and the getXxa/getZza/isJumping/isShiftKeyDown accessors~~PlayerInputInterceptorTest (fixture classes only)~~keystone-common/src/main/java/org/luckyraven/keystone/nms/input/PlayerInputInterceptor.java:80-137~~[found 2026-09-24, master 4192baa] getChannel walks only Mojang field names: `Object packetListener = getField(serverPlayer, "connection");` then `getField(packetListener, "connection")`; on Spigot 1.16.5 the fields are playerConnection/networkManager (and obfuscated b/c on Spigot 1.17+), so it returns null. ensureReflectionReady loads only `Class.forName("net.minecraft.network.protocol.game.ServerboundPlayerInputPacket")`, a Mojang name that does not exist on any Spigot runtime, and its pre-1.21.2 branch asks for `packet.getMethod("xxa")` / `zza` / `jumping`, while the real pre-1.21.2 Mojang packet exposes getXxa()/getZza()/isJumping()/isShiftKeyDown(), so that branch cannot bind on any real server either. The class javadoc advertises pre-1.21.2 support and Keystone's floor is Spigot 1.16.5, yet the only runtime where input parsing works is Mojang-mapped Paper 1.21.2+. NmsCache already solves the connection lookup with `firstField(entityPlayerClass, "playerConnection", "connection", "c", "b")`. Distinct from #3 (which is about the silent failure, not the unreachable bindings); the green test only binds synthetic fixture classes named after the assumed accessors.~~High
10~~P1~~CameraRotationPackets omits DELTA flags, so on 1.21.2+ each recoil zeroes player velocity~~On the PositionMoveRotation shape add the Relative DELTA_X, DELTA_Y, DELTA_Z (and ROTATE_DELTA) constants to the flag set so the client keeps its momentum~~CameraRotationPacketsTest (pins the five-flag selection only)~~keystone-common/src/main/java/org/luckyraven/keystone/nms/internal/CameraRotationPackets.java:95-107~~[found 2026-09-24, master 4192baa] relativeFlags always picks `String[] wanted = {"X", "Y", "Z", "Y_ROT", "X_ROT"};` and the modern shape builds `moveCtor.newInstance(zero, zero, deltaYaw, deltaPitch)` with a Vec3.ZERO delta movement. From 1.21.2 the client computes the new velocity as the packet's delta movement plus the current velocity only for axes flagged DELTA_X/DELTA_Y/DELTA_Z, so with those flags absent it sets the player's velocity to zero on every packet (pre-1.21.2 the X/Y/Z relative flags alone preserved momentum, which is why the legacy shapes were fine). ReflectivePacketAdapter.relativeCameraRotation is the recoil path Bartizan installs, so on 1.21.2+ every shot stops a sprinting, strafing or falling shooter mid-motion. CameraRotationPacketsTest deliberately pins that DELTA_X and ROTATE_DELTA are excluded, which proves the flag selection, not the client-side effect.~~Medium
11~~P2~~PacketBridge.reset() from one disabling consumer turns off every consumer's packets~~Reference-count installs per owning plugin (install(plugin, adapter) / release(plugin)) or make reset() a no-op while other owners remain~~PacketBridgeTest~~keystone-common/src/main/java/org/luckyraven/keystone/nms/PacketBridge.java:21-37~~[found 2026-09-24, master 4192baa] The adapter lives in `private static volatile PacketAdapter adapter = NoOpAdapter.INSTANCE;` and `reset()` unconditionally sets `PacketBridge.adapter = NoOpAdapter.INSTANCE;`. Oriel (AnimationConfig) and Bartizan (KernelConfig) each call `PacketBridge.install(adapter)` at bootstrap, and Bartizan's KernelConfig documents that `Bartizan.onDisable()` calls `PacketBridge.reset()`. When Bartizan is disabled at runtime (a failed enable, a plugin-manager reload) while Oriel keeps running, Oriel's inventory-title animation, furnace fake windows and property updates silently route to the no-op adapter for the rest of uptime with nothing logged. This is the same shared-classloader last-writer shape as Diagnostics.install (#1), but on a separate static that no existing row covers.~~High
