1~~P2~~Duplicate default @Bean names silently overwrite each other in namedInstances and BeanGraph.byName~~Detect a duplicate BeanDefinition name at registration and fail fast (or namespace the default name by declaring @Configuration class)~~-~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/autowire/DependencyContainer.java:44-50~~A @Bean method's default name is just the method name: `String name = beanAnnotation.name().isEmpty() ? method.getName() : beanAnnotation.name();` (BeanFactory.java:409). Two @Configuration classes each declaring a same-named method (e.g. two `settings()` beans in a consumer with many configuration classes) therefore produce the same name, and both registration sites overwrite blindly with no check or warning: `namedInstances.put(name, instance);` here, and `byName.put(def.name(), def);` at BeanGraph.java:39. A later @Qualifier("settings") injection then silently receives whichever bean was registered last, and BeanGraph.resolveProducer draws its dependency edge from `return byName.get(req.qualifier());` (BeanGraph.java:87) without ever comparing req.type(), so the graph edge can point at a bean of an entirely different type. Nothing logs the collision, so a consumer gets a wrong-but-plausible object injected with no diagnostic at boot.~~High
2~~P2~~ListenerService.registerGuarded falls back to registerEvents after per-method registration already partly succeeded~~Track the methods already registered and unregister them (or register into a staging list and commit only on success) before falling back~~ListenerServiceGuardTest (covers only a listener whose very first registerEvent throws, not a partially-registered multi-handler class)~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/listener/ListenerService.java:118-133~~registerGuarded loops the listener's methods calling `pluginManager.registerEvent(eventType, listener, handler.priority(), executor, plugin, handler.ignoreCancelled());` one at a time, wrapped in a single try. The catch is `catch (Throwable t)` and its recovery is `pluginManager.registerEvents(listener, plugin);` which registers every @EventHandler in the class again. If the loop throws part-way — Bukkit's registerEvent raises IllegalPluginAccessException for an event class with no static getHandlerList, so handler #3 failing after #1 and #2 succeeded is a concrete trigger — the earlier handlers end up registered twice and every matching event runs them twice. In a consumer that is duplicated gameplay effect on one event (double payout, double item grant), which is why this is filed above P2.~~High
3~~P2~~classConditionsMet/methodConditionsMet run during configuration discovery, before any phase creates a bean~~Re-evaluate @ConditionalOnBean lazily at the point the owning phase instantiates the bean, not during the up-front discovery sweep~~BeanFactoryTest (methodLevelConditionalOnBean_requiresAPreRegisteredInstance pins the current limitation)~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/BeanFactory.java:221-268~~The discovery loop evaluates `if (!classConditionsMet(configClass))` and `if (!methodConditionsMet(method))` while walking configuration classes, and only after that entire loop finishes does `for (Phase phase : Phase.values())` begin actually creating beans. A @ConditionalOnBean that names a bean produced by an earlier phase (say a CONFIG bean gating a LISTENER bean) is therefore always evaluated against a container that does not hold it yet, so the condition never passes and the bean is silently skipped with only a debug line. Only instances registered before instantiate() runs can satisfy the condition, which is not what the annotation's name implies and is not documented.~~High
4~~P2~~DependencyContainer stores each type's instances in a plain ArrayList inside a ConcurrentHashMap~~Use CopyOnWriteArrayList (or synchronize addUnique and the read paths) for the per-type instance lists~~-~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/autowire/DependencyContainer.java:23-27~~The outer map is concurrent — `private final Map<Class<?>, List<Object>> instances = new ConcurrentHashMap<>();` — but the List values it holds are plain ArrayLists, giving the appearance of thread safety without it. A managed reload re-registers beans on the main thread while an async task started by a consumer resolves a dependency out of the same list, so a structural modification can race a read and yield a torn read or ConcurrentModificationException. No test exercises concurrent access, and the trigger requires a reload overlapping an async resolve, so this is filed as a latent trap rather than a proven live fault.~~Medium
5~~P3~~@AutowireTarget is a public shipped annotation with no consumers anywhere in the reactor~~Delete the annotation, or wire it into DependencyContainer.createInstance if constructor selection was the intent~~-~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/autowire/AutowireTarget.java:8-20~~AutowireTarget is declared and exported as public API but nothing in keystone-bean or any sibling module reads it reflectively — a repo-wide grep finds only the declaration and its import. It reads as a constructor-selection marker a consumer would reasonably expect to work, so it is a dead feature that looks shipped rather than merely unused private code.~~High
6~~P3~~PostConstruct javadoc promises cross-@Configuration topological ordering the implementation does not provide~~Correct the javadoc to state that @PostConstruct order follows scan order across configuration classes~~-~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/PostConstruct.java:11-12~~The annotation's javadoc tells the reader that @PostConstruct callbacks run in topological order across configuration classes. BeanGraph.topologicalSort is applied per phase to that phase's BeanDefinitions, and the configuration classes themselves are walked in raw scan order, so the promised guarantee does not hold between two configuration classes in the same phase. This is documentation drift, not a behaviour bug, but a consumer relying on the stated ordering would write code that works by accident.~~High
7~~P1~~shutdownLifecycleBeans/reloadLifecycleBeans abort the whole pass when one bean throws~~Wrap each onShutdown/onPreClear/onClear/onInitialize/onPostInitialize call in a per-bean try/catch that reports through Diagnostics and continues~~BeanFactoryTest (shutdown/reload ordering only; no throwing-bean case)~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/BeanFactory.java:360-369~~[found 2026-09-24, master 4192baa] Shutdown is a bare loop, `for (BeanLifecycle bean : reversed) { bean.onShutdown(); }`, and reloadLifecycleBeans (lines 333-353) runs three bare loops plus runPostInitialize the same way. BeanLifecycle documents onShutdown as the place to `flush pending state` and the order is reverse topological, so the beans that run last are the dependencies (repositories, data managers). A dependent whose onShutdown throws, for example touching a Bukkit API that is already unavailable during disable, skips every remaining bean's shutdown and loses their unflushed data. On reload, a throw in one onPreClear/onClear leaves some beans cleared and none re-initialized, so the plugin keeps running with empty state until restart. Neither path reports through Diagnostics, so the admin sees only a raw stack trace.~~High
8~~P2~~BeanGraph draws no edge for two same-phase producers of one type, hiding ambiguity~~When more than one same-phase definition can satisfy an unqualified requirement, add an edge from every candidate (or fail fast as ambiguous at graph-build time)~~BeanFactoryTest.unqualifiedParameterWithTwoCandidates_failsWithAmbiguousBean (producers in KERNEL, consumer in a later phase only)~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/BeanGraph.java:90-109~~[found 2026-09-24, master 4192baa] resolveProducer returns an edge only for a single exact match (`if (exact != null && exact.size() == 1) return exact.get(0);`) and returns null as soon as a second assignable candidate appears (`if (assignable != null) return null;`), on the assumption stated in its comment that BeanFactory will flag the ambiguity at invocation. BeanFactory.resolveParameter (lines 514-531) only sees what is already registered, though, and with no edge the consumer's position in Kahn's order is simply its discovery position (config scan order plus getDeclaredMethods order, neither specified). If the consumer happens to run before both producers the bootstrap fails with a misleading `no bean of that type is registered`. If it runs between them it silently receives the first one and the ambiguity is never reported. The existing test passes only because its duplicates live in an earlier phase.~~High
9~~P2~~DependencyContainer ignores an unmatched @Qualifier and picks the first candidate~~Fail with a clear error when a @Qualifier name is not registered and when a type has more than one distinct instance, matching BeanFactory.resolveParameter~~DependencyContainerTest.createInstance_qualifierPicksTheNamedInstance (happy path only)~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/autowire/DependencyContainer.java:231-244~~[found 2026-09-24, master 4192baa] resolveConstructorParameters tries `instance = getInstance(qualifierName, paramType);` and, when that is null, falls straight back to `instance = getInstance(paramType);`, which returns `list.get(0)` whatever the list size (lines 56-62). Every @ListenerHandler, @CommandHandler and @Configuration instance is built through this path, so a constructor parameter annotated `@Qualifier("primary")` with a misspelled or unregistered name, or an unqualified parameter whose type has two beans, gets whichever instance registered first instead of failing. The @Bean-method path (BeanFactory.resolveParameter) rejects both cases (`no bean named '...'` / `Ambiguous bean`), so the same wiring mistake fails fast in one place and injects the wrong object in the other.~~High
10~~P2~~ListenerService.registerGuarded skips non-public @EventHandler methods, no setAccessible~~Collect getMethods() plus getDeclaredMethods() like JavaPluginLoader.createRegisteredListeners and call method.setAccessible(true) before registering~~ListenerServiceGuardTest (fixtures use only public handlers on public classes)~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/listener/ListenerService.java:91-126~~[found 2026-09-24, master 4192baa] The guarded path reflects with `ReflectionGuard.<Method[]>orSkip(listenerClass, null, listenerClass::getMethods)`, so private, protected and package-private @EventHandler methods are never registered. Bukkit's own registerEvents, which this path replaces, unions getMethods() with getDeclaredMethods() and calls setAccessible(true). invokeHandler then does a plain `method.invoke(listener, event)` with no setAccessible, so a public handler declared on a non-public listener class throws IllegalAccessException from Keystone's package on every event; the Guard catches and reports it each time and the handler never runs. Neither case reaches the registerEvents fallback because registration itself succeeds. A consumer writing the ordinary Bukkit idiom `@EventHandler void onJoin(PlayerJoinEvent e)` gets a listener that silently does nothing. Current consumer listeners all use public handlers on public classes, so this is a latent trap rather than a live outage.~~High
