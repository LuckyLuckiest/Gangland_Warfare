1~~P2~~ModuleLoader.purgeStale lets one undeletable stale jar abort discovery of every module in the folder~~Wrap the marker loop's deleteIfExists calls in a per-file try/catch that logs, mirroring the stale-directory loop above it~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/ModuleLoader.java:334-345~~The first loop deletes each file in `.stale/` defensively, catching IOException per file and only warning. The second loop does not: `if (Files.deleteIfExists(jar)) { log.info(...); } Files.deleteIfExists(marker);` runs bare, and discover() wraps the whole purgeStale() call in a single try/catch that reports FAULT_FOLDER "Modules folder is not usable" and returns. On Windows a stale jar still held open by a previous run (the classloader handle problem this project already documents) throws IOException here, so one leftover file makes the host plugin load zero modules instead of skipping that one file. The asymmetry with the loop three lines above shows the per-file tolerance was the intent.~~High
2~~P1~~ArtifactCoordinate.jarFileName builds a filename from unsanitized Maven coordinates, letting a repository escape the modules folder~~Reject coordinates containing path separators or "..", and verify the resolved target stays inside targetDirectory~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/artifact/ArtifactCoordinate.java:49-52~~jarFileName() is `requireVersion(); return artifactId + "-" + version + ".jar";` with no character validation, and ArtifactResolver.download() feeds it straight to the filesystem: `Path target = targetDirectory.resolve(coordinate.jarFileName());` (ArtifactResolver.java:100). For an update download the version half comes from the remote maven-metadata.xml via MavenMetadata, so the value is controlled by whatever repository the admin configured — a compromised or hostile mirror can return a version containing `../` (or an absolute path, which resolve() honours outright) and place the downloaded jar anywhere the server process can write. Filed P1 rather than P0 because it requires a hostile repository rather than firing on a normal update.~~High
3~~P2~~ModuleUpdateService.downloadNow retires the working jar before confirming the download is a usable module~~Validate the downloaded jar's module.yml and Id before calling retire() on the installed jar~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/update/ModuleUpdateService.java:111-114~~The only gate before replacement is `if (downloaded.isFailure()) return downloaded;` followed immediately by `retire(update.installedJar());`, which moves the currently-working jar into `.stale/`. isFailure covers I/O errors and a SHA-256 mismatch against the repository's own checksum, so it proves the bytes match what the repository published — not that they are a valid module with the right module.yml, matching Id and a KeystoneModule main class. A repository publishing a bad-but-correctly-checksummed jar therefore costs the admin the working module, and the failure only becomes visible on the next restart.~~High
4~~P2~~ModuleUpdateService.pending is updated by an unsynchronized read-modify-write across async download tasks~~Guard the pending mutation with a lock, or use an AtomicReference update loop~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/update/ModuleUpdateService.java:117-119~~The update is a three-step read-modify-write on a plain volatile field: `List<ModuleUpdate> remaining = new ArrayList<>(pending); remaining.removeIf(candidate -> candidate.moduleId().equals(update.moduleId())); pending = List.copyOf(remaining);`. Downloads normally run on their own async task, so two overlapping downloads (an admin retrying a slow one, or two modules updating together) can both read the same snapshot and the second write loses the first's removal. The consequence is a stale entry left in the pending list, so an already-downloaded update is offered again; volatile makes the field visible but does nothing for the compound update.~~High
5~~P3~~ModuleLoader.instantiate reports a module's failing static initializer as "Main missing"~~Separate the LinkageError case from ClassNotFoundException and report the initializer failure with its own message~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/ModuleLoader.java:277-284~~Class loading uses `Class.forName(descriptor.mainClass(), true, classLoader)` with initialize=true, and the handler is `catch (ClassNotFoundException | LinkageError exception)` reporting FAULT_MAIN_MISSING with the text "names Main X, which is not in its jar". A main class whose static initializer throws raises ExceptionInInitializerError, a LinkageError, so an admin whose module has a perfectly present Main class but a broken static block is told the class is absent. The underlying cause is attached via .cause(exception), but the headline message sends the admin looking for a packaging problem that does not exist.~~High
6~~P3~~ModuleResolution reports only the first missing Plugins entry per module~~Collect every unsatisfied plugin for the module before breaking, or drop the break and deduplicate the removal~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/ModuleResolution.java:92-104~~The plugins check is `if (pluginEnabled.test(plugin)) continue; accepted.remove(descriptor.id()); faults.add(...); break;` — the break ends the scan at the first unsatisfied entry. A module declaring `Plugins: [Bartizan, Vault]` with both absent surfaces only "needs plugin Bartizan", so an admin installs Bartizan, restarts, and is then told about Vault. Correct module gating either way; this costs an extra restart cycle per additional missing plugin.~~High
7~~P2~~A module bean body that hits a deleted host member crashes the whole host bootstrap instead of skipping that module~~Give the host a per-module guard once beans start: catch LinkageError (NoSuchMethodError/NoSuchFieldError/NoClassDefFoundError) thrown from a module @Bean body or module listener/command construction, report it as a module fault (e.g. module.bean.failed with the module id and jar) and drop that module and its dependants, instead of letting IllegalStateException escape BeanFactory.instantiate(); needs rollback-capable registration in keystone-bean, so it is a Keystone phase of its own. Until then the gangland-api contract + minor-compatible Host_Api are the prevention.~~-~~keystone-bean/src/main/java/org/luckyraven/keystone/bean/BeanFactory.java:488-492 (invokeBean rethrows InvocationTargetException as IllegalStateException) + keystone-module/src/main/java/org/luckyraven/keystone/module/ModuleLoader.java:176-187 (the last per-module catch)~~ModuleLoader isolates instantiate/configure/onEnabled per module (catch Throwable at ModuleLoader.java:176-187, :204-210) and ReflectionGuard (1.9.1) skips a scanned class whose SIGNATURE names an absent type, but nothing guards a module bean BODY: a module compiled against a newer host that calls a method the running host lacks throws NoSuchMethodError inside method.invoke, BeanFactory.invokeBean wraps it as IllegalStateException, and the host plugin (Gangland.onEnable at Gangland.java:91-96 has no try/catch around context.bootstrap()) is disabled by Bukkit - every module and the core go down for one stale module jar. Found 2026-09-10 by the module-API wave research (agent A section 2.2); not reproduced on a server.~~Medium
8~~P1~~Unquoted Host_Api: 1.10 parses as YAML float 1.1, collapsing the Host_Api minor floor~~Reject a non-String Host_Api scalar with a descriptor fault telling the author to quote it (or read the raw node), and validate Host_Api against \d+(\.\d+)? so garbage no longer parses as 0.0~~ModuleDescriptorReaderTest (only 0.8), ModuleResolutionTest~~keystone-module/src/main/java/org/luckyraven/keystone/module/ModuleDescriptorReader.java:82-83~~[found 2026-09-24, master 4192baa] parse reads `String hostApi = yaml.getString("Host_Api");`, and Bukkit's YamlConfiguration returns the SnakeYAML-typed scalar stringified, so the documented unquoted form (`Host_Api: 0.8` in docs/keystone-module.md and every test) is a Double. Verified against spigot-api 1.16.5: `Host_Api: 1.10` reads back as "1.1" and `1.20` as "1.2". ModuleDescriptor.isCompatibleWith (ModuleDescriptor.java:73-75, `required.minor() <= host.minor()`) then sees minor 1, so a module built for host 0.10 is accepted on any 0.1-0.9 host instead of refused (Gangland's host line is 0.9 today, so its next minor is exactly this case), and the module then fails at runtime with NoSuchMethodError (see row 7). The same coercion truncates a two-part `Version: 0.10` (line 76) to 0.1, breaking duplicate resolution and update comparisons, and a non-numeric Host_Api such as `latest` parses as 0.0 and passes the floor on every 0.x host because nothing validates the format.~~High
9~~P2~~downloadNow retires the working jar for an update whose Host_Api the host cannot load~~Before retire(), refuse with module.update.invalid when the downloaded descriptor fails isCompatibleWith(hostApi) (pass the host api into ModuleUpdateService), and skip such releases in checkNow~~ModuleUpdateServiceTest (identity check only)~~keystone-module/src/main/java/org/luckyraven/keystone/module/update/ModuleUpdateService.java:127-138~~[found 2026-09-24, master 4192baa] The KS-MO-03 gate checks only `descriptor == null || !descriptor.id().equals(update.moduleId())` and then runs `retire(update.installedJar());`. checkNow chose the release purely by `PluginVersion.parse(newest.get()).compareTo(descriptor.parsedVersion()) > 0` (line 98), so the newest release is offered even when it was built for a newer host minor. Downloading it moves the working jar into .stale/ (deleted by the next discover), and on restart ModuleResolution drops the new jar with module.host.incompatible, leaving the admin with no module at all until the host is upgraded. The downloaded descriptor is already parsed at line 127, so the Host_Api (and Plugins) check is one condition. This is not a re-file of row 3 (KS-MO-03): that fix covers a wrong Id, not an incompatible jar with the right Id.~~High
10~~P2~~downloadNow deletes the module when the download target equals the installed jar path~~Skip retire() (and the wrong-Id deleteQuietly) when the downloaded path is the installed jar (normalized equals or Files.isSameFile), and refuse a download whose descriptor Version is not newer than the installed one~~-~~keystone-module/src/main/java/org/luckyraven/keystone/module/update/ModuleUpdateService.java:120-138~~[found 2026-09-24, master 4192baa] The resolver names the download `artifactId + "-" + version + ".jar"` and moves it over any existing file (ArtifactResolver.java:100 `Path target = targetDirectory.resolve(coordinate.jarFileName());`, :152 `move(part, target)` with REPLACE_EXISTING), then downloadNow unconditionally calls `retire(update.installedJar())`. When the hand-written module.yml Version lags the Maven version, the jar the service installed last time is named e.g. foo-1.2.0.jar while its descriptor says 1.1.0, so every check re-offers 1.2.0 and the next download targets that same path. On Linux the move replaces the open jar, the identity check passes, and retire() moves the fresh download into .stale/, which the next discover() deletes, so the module disappears. The wrong-Id branch has the same hole: `deleteQuietly(jar)` deletes the path that was the installed jar. On Windows the replace fails on the locked jar and surfaces as module.artifact.io instead.~~Medium
11~~P3~~checkNow re-lists an already-downloaded update as pending until the restart~~Remember the module ids downloaded this session and exclude them from checkNow's result while restartRequired is true~~ModuleUpdateServiceTest~~keystone-module/src/main/java/org/luckyraven/keystone/module/update/ModuleUpdateService.java:98-106~~[found 2026-09-24, master 4192baa] checkNow rebuilds pending wholesale from the LoadedModule descriptors (`pending = List.copyOf(updates);`), which keep the pre-download Version until the restart. After downloadNow removed the entry, any later check (a periodic check task or an admin re-running the check command) puts it back, so pending(), documented as "Updates found by the last check that have not been downloaded yet", offers the same download again, and a host that auto-applies pending re-downloads it on every cycle. The damage is bounded because the second download overwrites the same file, but it is operator-visible noise and wasted bandwidth.~~Medium
