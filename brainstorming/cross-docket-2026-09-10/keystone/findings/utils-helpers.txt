1~~P1~~ResourcePackTracker.active is one process-wide static, so each consumer installing a tracker clobbers the others~~Reject or warn on a second install, or scope the tracker per owning plugin rather than per JVM~~-~~keystone-common/src/main/java/org/luckyraven/keystone/sound/ResourcePackTracker.java:24-69~~The tracker holds genuinely per-plugin state — `private final Set<UUID> loaded = ConcurrentHashMap.newKeySet();`, the players who accepted *that* plugin's resource pack — behind a JVM-wide latch: `private static volatile ResourcePackTracker active;` with `public static void install(@Nullable ResourcePackTracker tracker) { active = tracker; }`. Keystone is loaded once and shared, so on a server running Gangland alongside Bartizan or Oriel the second plugin to install silently replaces the first, and every later `active()` lookup answers the wrong plugin's pack state. This is the same forbidden shape as the Diagnostics hub filed under DG, in a different class with a different fix site; the consequence here is pack-gated sounds or assets being enabled or suppressed for the wrong players.~~High
2~~P1~~PdcPlayerMetaService silently drops set() and remove() for offline players~~Return a boolean or Result so callers learn the write was refused, or persist offline writes through the OfflinePlayer PDC~~-~~keystone-common/src/main/java/org/luckyraven/keystone/meta/PdcPlayerMetaService.java:39-46~~set() resolves the player and abandons the write when they are not online: `Player online = Bukkit.getPlayer(player); if (online == null) { return; }` — no exception, no log, no return value, and remove() repeats it. The interface it implements is also satisfied by InMemoryPlayerMetaService, which stores the value regardless of online state, so the two implementations of one contract disagree about whether an offline write happens at all. A consumer writing player metadata during quit handling or from an async task that lands a tick late therefore loses the value silently, and the same call would have succeeded against the in-memory implementation used in its tests.~~High
3~~P2~~Tree.size(), getAllNodes() and clone() throw NullPointerException once clear() has nulled the root~~Guard the root-walking methods for a null root and return 0 / an empty list~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/Tree.java:40-56~~clear() sets `this.root = null;`, but the traversal entry points do not check for it: `size()` is `return getNumberOfDescendants(root) + 1;` and getNumberOfDescendants immediately dereferences with `int size = root.getChildren().size();`. Any call to size(), getAllNodes() or clone() after a clear — or on a Tree that was never populated — throws NPE instead of reporting an empty tree. Tree is the structure backing the whole command argument system, so this sits underneath command registration rather than in an isolated helper.~~High
4~~P2~~JsonFormatter.formatToJson mis-scopes its brace handling through operator precedence, corrupting quoted braces~~Parenthesise the conditions as `(c == '{' || c == '[') && !hasQuotes`~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/JsonFormatter.java:25-31~~The branches read `} else if (c == '{' || c == '[' && !hasQuotes) {` and `} else if (c == '}' || c == ']' && !hasQuotes) {`, and `&&` binds tighter than `||`, so each parses as `c == '{' || (c == '[' && !hasQuotes)`. The quote guard therefore applies only to brackets, never to braces: a `{` or `}` appearing inside a quoted string value still increments or decrements the indent level and injects a newline mid-string. Any JSON payload with a brace inside a quoted value comes out reindented and structurally mangled, and the sibling `:` and `,` branches show the guard was meant to apply to all of them.~~High
5~~P2~~ScientificCalculator registers its custom operator and functions only when a non-empty variables map is supplied~~Register operators() and functions() on the builder unconditionally, and apply variables separately~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/ScientificCalculator.java:20-31~~The constructor folds three unrelated concerns into one condition: `if (!(variables == null || variables.isEmpty())) builder.variables(variables.keySet()).operator(operators()).functions(functions());`. The custom factorial operator and the logb/neg/ln functions therefore exist only for expressions that also happen to declare variables, so a variable-free formula like `5!` or `ln(2)` is built without them and exp4j fails to resolve the token. The single-argument constructor below routes to exactly this path, so the simplest usage is the one that loses the features.~~High
6~~P2~~LinkedList.clone() returns a shallow copy that shares the original's Node chain~~Deep-copy the node chain in clone() so the copy is independent~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/LinkedList.java:168-175~~clone() delegates straight to Object's shallow copy: `return (LinkedList<E>) super.clone();`. That copies the head and tail references rather than the nodes behind them, so the clone and the original share one chain — adding to or removing from either is visible in the other, and the size fields then disagree with the shared structure. A caller cloning a list precisely to mutate it without disturbing the original gets the opposite of what the method name promises, with no error to signal it.~~High
7~~P2~~DurationParser.parse throws ArithmeticException on an overflowing duration instead of returning null like every other rejection~~Catch the overflow and return null, matching the method's existing contract for unparseable input~~DurationParserTest~~keystone-common/src/main/java/org/luckyraven/keystone/util/DurationParser.java:71-82~~Every other rejection path in parse returns null, but the unit conversion is unguarded: `case 'd' -> Duration.ofDays(amount);` and its siblings multiply into seconds with exact arithmetic, so a large admin-supplied value such as `999999999999d` raises an ArithmeticException that escapes the parser. A caller written against the documented null-on-failure contract has no catch for it, so a config typo becomes an exception on the config-load path rather than a rejected value.~~High
8~~P2~~NumberUtil.resizeLinear divides by zero when the target size is 1, silently yielding 0~~Special-case targetSize == 1 (take the first or mean value) before the interpolation loop~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/NumberUtil.java:239-251~~The interpolation maps indices with `double position = (double) i * (originalSize - 1) / (targetSize - 1);`, so a targetSize of 1 divides by zero. In double arithmetic that yields NaN for the i=0 case rather than throwing, and NaN then flows through `Math.floor` and the fraction into `Math.round`, which returns 0 — so shrinking a populated list to a single element silently produces a 0 instead of a representative value. The caller gets a plausible-looking result with no error, which is the failure mode hardest to notice downstream.~~Medium
9~~P2~~PermissionWorker.permissionRefactor tests startsWith with no dot boundary, so a same-prefixed permission escapes canonicalisation~~Compare against `indicator + "."` (or check the first segment) rather than a bare startsWith~~-~~keystone-common/src/main/java/org/luckyraven/keystone/permission/PermissionWorker.java:26-31~~Canonicalisation is decided by a bare prefix test: `return (!permission.startsWith(this.indicator) ? this.indicator + "." : "") + permission;`. With an indicator of `gang`, a permission such as `gangster.teleport` already "starts with" it, so the node is returned unprefixed and is registered and checked in a foreign namespace instead of `gang.gangster.teleport`. The mismatch is silent and self-consistent within one plugin only by luck — the same string can canonicalise differently depending on an unrelated plugin's naming. Note: the raw scanner cited lines 186-192, which do not exist in this 66-line file; the defect is real and verified at line 30.~~High
10~~P2~~ActionBarManager's static per-player maps never evict entries for players who disconnect~~Remove a player's entries on quit, or sweep entries whose timestamps are long expired~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/ActionBarManager.java:31-37~~The arbitration state lives in two class-level maps, `private static final Map<UUID, Long> foregroundUntil = new ConcurrentHashMap<>();` and `private static final Map<UUID, BackgroundSlot> backgroundSlot = new ConcurrentHashMap<>();`, and every send path only ever puts into them. Nothing removes a UUID when the player leaves, so both grow by one entry per distinct player for the life of the server. The entries are small, so this is a slow leak rather than an urgent one; the statics themselves are arguably deliberate, since cross-plugin arbitration of a single action-bar slot only works if the state is shared.~~High
11~~P2~~StaticResets omits EconomyHandler.vaultEconomy and CommandManager.commands, so consumer tests hand-roll their own resets~~Add reset helpers for the remaining process-wide statics the repo's testing conventions enumerate~~-~~keystone-testkit/src/main/java/org/luckyraven/keystone/testkit/StaticResets.java:15-17~~The class documents its own coverage as "the NmsVersion detection latch ..., the NbtBridge and PacketBridge seams", and the code matches — resetNmsVersion, resetNbtBridge, resetPacketBridge and nothing else. The repo's testing conventions name a longer list of process-wide statics that suites must reset, including `EconomyHandler.vaultEconomy` and `CommandManager.commands`, and the hooks tests already prove the need by implementing their own saveStatic/restoreStatic for the economy field. A consumer that trusts StaticResets to cover the documented set gets cross-test contamination from the two it silently does not handle.~~High
12~~P2~~ChatUtil.capitalize throws StringIndexOutOfBoundsException on an empty string~~Return the input unchanged when it is empty~~ChatUtilTest~~keystone-common/src/main/java/org/luckyraven/keystone/util/ChatUtil.java:144-146~~The helper is a bare two-substring expression with no length check: `return text.substring(0, 1).toUpperCase() + text.substring(1);`. An empty string — an unset config value, a blank display name, an empty enum label — makes `substring(0, 1)` throw. The parameter is annotated `@NotNull` so null is handled by contract, but emptiness is not, and the sibling `center` two lines below does validate its input via Preconditions.~~High
13~~P3~~SpellChecker's optimised Damerau-Levenshtein transposition step computes the wrong distance~~Correct the transposition index arithmetic against the reference algorithm, and pin it with a test~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/SpellChecker.java:107-124~~The reduced-memory Damerau-Levenshtein implementation mishandles the transposition case, so distances involving swapped adjacent characters come out higher than they should. The only consumer is the did-you-mean suggestion path behind an unknown command, so the visible effect is that a transposed typo may not surface the command the user meant, or ranks below a worse candidate. Suggestion quality only — no incorrect command ever executes — which is why this is P3 despite being a genuine algorithmic defect.~~Medium
14~~P3~~ReflectionUtil.newInstance throws NullPointerException when any constructor argument is null~~Track the declared parameter types instead of deriving them from the argument instances~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/ReflectionUtil.java:27-42~~The helper derives its parameter types from the supplied values, so a null argument has no class to read and the lookup NPEs before reflection is even attempted. That makes it impossible to construct an object through this helper with a legitimately null argument, which is an ordinary thing to want. A caller-supplied null is the only trigger and no current call site passes one, so it is a usability trap rather than a live fault.~~Medium
15~~P3~~DbFiles.release relies on a one-second retry loop for Windows SQLite cleanup, the strategy this ecosystem documents as insufficient~~Close the pool's handles deterministically before deleting, keeping the retry only as a backstop~~-~~keystone-testkit/src/main/java/org/luckyraven/keystone/testkit/DbFiles.java:19-42~~release walks the directory deleting `.db`, `-wal` and `-shm` files and, on failure, sleeps and retries — "retrying up to 20 times with 50 ms pauses" per its own javadoc, a one-second total budget. The consuming project's documented lesson is the opposite: Windows file locks from HikariCP's eagerly-opened handles can persist for several seconds and retry loops alone are not a sufficient cleanup strategy. So the shared fixture bakes in the approach the ecosystem already learned does not hold, and consumer suites inherit flaky temp-directory cleanup. Test infrastructure only, never shipped, hence P3.~~High
16~~P3~~Color enum maps two pairs of constants to the same chat code, leaving two legacy codes unreachable~~Give BROWN/ORANGE and PURPLE/MAGENTA distinct codes so all sixteen legacy colours are addressable~~-~~keystone-common/src/main/java/org/luckyraven/keystone/color/Color.java:11-22~~Two pairs collide: `BROWN("&6", ...)` and `ORANGE("&6", ...)` share `&6`, and `PURPLE("&5", ...)` and `MAGENTA("&5", ...)` share `&5`. The sixteen constants therefore cover only fourteen distinct codes, and `&9` and `&c` are unreachable through this enum entirely. A consumer selecting BROWN and ORANGE gets identical output, and any reverse lookup from code to constant is ambiguous for the two duplicated codes.~~High
17~~P3~~ServerEnvironment javadoc names the consumer plugin Oriel inside shared infrastructure~~Reword the javadoc in neutral terms~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/ServerEnvironment.java:102-103~~The javadoc refers to Oriel by name in a keystone-common utility, breaking the hard rule that Keystone must not know its consumers. Comments only, so no behaviour is affected; it is the same class of drift filed for the diagnostics and placeholder packages under DG and PH, listed separately here because it is a different file with a different owner.~~High
18~~P2~~NumberUtil.formatDouble's zero-strip regex corrupts non-English and no-decimal output~~Format with an explicit Locale (Locale.ROOT or a caller-supplied one) and strip zeros only after a decimal separator, e.g. via BigDecimal.stripTrailingZeros~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/NumberUtil.java:45-63~~[found 2026-09-24, master 4192baa] formatDouble is `String.format(format, value).replaceAll("\\.?0*$", "")`, and valueFormat runs every value through it. String.format uses the JVM default locale, so on a German or French locale host `%,.2f` of 10 renders as `10,00` and the regex strips only the zeros, leaving `10,` (verified: 10 -> `10,`, 100 -> `100,`, -2500 -> `-2.500,`). With a caller format that has no decimal point, the regex eats trailing zeros from the integer part: `valueFormat("%.0f", 500)` returns `5`. Gangland feeds gang balances, bounties and experience into NumberUtil.valueFormat for its placeholders, so the displayed amounts are wrong on those hosts.~~High
19~~P3~~NumberUtil.valueFormat(double) skips negatives and renders 999999 as 1,000K~~Scale on Math.abs(value) and re-apply the sign as the BigDecimal overload does, and re-check the suffix after rounding~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/NumberUtil.java:53-63~~[found 2026-09-24, master 4192baa] The loop is `while (modValue >= 1_000 && index < SUFFIXES.length - 1)`, so any negative balance skips it and prints in full (-5000000 renders -5,000,000 while 5000000 renders 5M), unlike the BigDecimal overload, which handles `negative` explicitly. Rounding happens after the suffix is chosen, so 999999 becomes 999.999, formats as `1,000.00` and renders `1,000K` instead of `1M`. Cosmetic, but these strings reach player-facing placeholders.~~High
20~~P2~~PermissionManager.removePermission skips permissionRefactor, so it removes nothing~~Canonicalise through permissionHandler.permissionRefactor(permission) before both the handler call and permissions.remove~~-~~keystone-common/src/main/java/org/luckyraven/keystone/permission/PermissionManager.java:50-60~~[found 2026-09-24, master 4192baa] addPermission stores the canonical form `permissions.add(permissionHandler.permissionRefactor(permission));` and PermissionWorker.addPermission registers `new Permission(permissionRefactor(permission))`, so `addPermission("update.check")` becomes `<prefix>.update.check`. removePermission passes the raw string through: PermissionWorker.removePermission throws `IllegalAccessException("Not allowed to access other plugins registered permission.")` because `update.check` does not start with the indicator (logged as a warning), and `permissions.remove(permission)` removes a string that was never in the set. The Bukkit Permission and the tracked entry both survive, and nothing tests removePermission.~~High
21~~P3~~ParticleUtil ships product effects (jetpack, car exhaust, flashbang) inside Keystone~~Move the product-named effects to the consumer (Gangland or Bartizan) and keep only the generic primitives (spawnLine, cone, ring, burst) in keystone-common~~-~~keystone-common/src/main/java/org/luckyraven/keystone/util/ParticleUtil.java:245-577~~[found 2026-09-24, master 4192baa] CLAUDE.md's layering rule says a class that knows about weapons or any product concept does not belong in Keystone, yet ParticleUtil exposes `spawnJetpackFlame(Player player)` (L245), `spawnJetpackGlide` (L296), `spawnBurnoutSmoke(Location vehicleLocation, float yaw)` (L336), `spawnCarExhaust(..., boolean leftNozzle, boolean rightNozzle)` (L369), `spawnFlashbangBurst` (L400) and the deprecated `createBloodSplash(Entity entity, double damage)` (L557). These are Gangland/Bartizan vehicle, gadget and weapon visuals, not generic infrastructure. The conventions sweep matched only consumer symbols, not product vocabulary, so it did not catch these.~~High
22~~P1~~LinkedList.removeAllOccurrences loops forever on the first match because current is never advanced after the unlink~~Capture next before unlinking and advance current = next in the match branch~~-~~keystone-common/src/main/java/org/luckyraven/keystone/datastructure/LinkedList.java:122-135~~[found 2026-09-24 by the UC fixer, verified by the orchestrator] The loop only advances in the else branch: `if (current.data.equals(data)) { ... size--; } else current = current.next;`. When current matches, it is unlinked from its neighbours but current itself still points at the same node whose data still equals the argument, so the loop re-enters the match branch forever, decrementing size into negatives and re-unlinking. Any call with a value that is present hangs the calling thread (the main thread in a plugin), i.e. a server freeze.~~High
23~~P3~~StaticResets.resetAll throws NoClassDefFoundError for log4j on the testkit's own test classpath~~Add log4j-api/log4j-core to keystone-testkit at test scope only~~StaticResetsTest~~keystone-testkit/pom.xml~~[found 2026-09-24 by the UC fixer] Some reset in StaticResets.resetAll touches the Keystone Logger, which needs log4j; keystone-testkit's own test classpath had none, so `resetAll()` threw `NoClassDefFoundError: org/apache/logging/log4j/LogManager` there and the testkit could not test its main entry point. Consumer classpaths carrying log4j were unaffected.~~High
