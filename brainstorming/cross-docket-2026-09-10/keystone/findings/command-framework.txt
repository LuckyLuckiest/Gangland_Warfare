1~~P1~~CommandManager.onHelp renders a subcommand's help without the permission check runExecute enforces~~Check sender.hasPermission(command.getPermission()) in onHelp before calling help(), as onHelpAll already does via permissibleCommands~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandManager.java:199-200~~The dispatch loop picks between execution and help with `if (Arrays.stream(args).anyMatch("help"::equalsIgnoreCase)) onHelp(entry, sender, args); else entry.getValue().runExecute(label, sender, args);`. runExecute opens with `if (!sender.hasPermission(permission)) { sender.sendMessage(CommandMessages.noPermission()); return; }` (Command.java:78-81) but onHelp goes straight to `entry.getValue().help(sender, page);` with no check at all. Any player can therefore type `/<label> <restricted-subcommand> help` and read the full syntax and description of a command they cannot run. The sibling path onHelpAll deliberately iterates `permissibleCommands(sender)`, so permission-filtered help is clearly the intent and this branch is the hole; filed P1 rather than P0 because it discloses command structure but executes nothing.~~High
2~~P2~~Argument.execute coerces any token merely containing the substring confirm into the literal confirm token~~Match the token exactly (equalsIgnoreCase("confirm")) instead of contains, when wrapping input args~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:146-149~~Every user token is wrapped with `if (arg.toLowerCase().contains("confirm")) return new ConfirmArgument(plugin, tree); return new Argument(plugin, arg, tree);`, and ConfirmArgument's constructor calls `super(plugin, "confirm", tree, action)` so its arguments array is exactly {"confirm"}. Any free-text value containing the substring — a mail body word like "confirmed", a gang name "Confirmation" — is therefore rewritten to the literal token "confirm" before tree matching, so it stops matching the OptionalArgument node it belongs to and instead matches a confirm branch. The destructive-action gate itself still holds, because the lock test lives on the registered tree node's ConfirmArgument.executeArgument (`if (!lock.isLocked(sender))`), not on this input wrapper — so this is input corruption, not a confirm bypass.~~High
3~~P1~~BrigadierPaperListener.handle swallows ReflectiveOperationException, leaving the unfiltered permission-leaking tree in place~~Report the reflective failure through Diagnostics (once) instead of an empty catch, so the degraded state is visible~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/brigadier/BrigadierPaperListener.java:132-134~~The entire per-event body is wrapped in `catch (ReflectiveOperationException ignored) { // No realistic recovery — the unfiltered tree (if any) stays in place. }` with no log and no Diagnostics report. This class exists specifically to remove PaperCommodore's unfiltered shared command node and replace it with a per-player permission-filtered tree, so when the reflection fails the fallback state is exactly the leak it was written to prevent: every player's client sees completions for commands they cannot run. Because the catch is silent, an admin gets no signal that command-completion filtering has stopped working on their server build.~~High
4~~P1~~ArgumentLock never releases a sender who disconnects mid-confirmation, retaining the Player strongly~~Remove the sender on PlayerQuitEvent, or key the set on UUID with a timeout instead of holding CommandSender~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/ArgumentLock.java:12-28~~The lock set is `private final Set<CommandSender> lockedSenders = new HashSet<>();` populated by lock() and emptied only by unlock(), which fires solely from ConfirmArgument.executeArgument on a successful second invocation. A player who triggers the first half of a confirm gate and then logs out is never removed, so the set retains a strong reference to a Player object — and through it the entity and its world references — for the remaining uptime of the server. Every abandoned confirmation adds another, so this grows monotonically on a busy server. There is no correctness impact on rejoin (the returning player is a new CommandSender and correctly unlocked), making this purely an uptime leak.~~High
5~~P2~~Argument.traverseList returns NO_PERMISSION from the first child instead of trying the remaining siblings~~Record the NO_PERMISSION result and keep scanning siblings, returning it only if no sibling succeeds~~ArgumentTreeTest (constructs NO_PERMISSION but not the sibling-fallthrough case)~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:278-284~~The sibling loop is `if (result.getState() == ResultState.SUCCESS) return result; else if (result.getState() == ResultState.NO_PERMISSION) return ArgumentResult.noPermission(node.getData());` — the first child that denies permission ends the whole search. Where two sibling nodes can match the same token (a permission-gated literal alongside an ungated OptionalArgument wildcard, which is a normal shape in this framework), a player who legitimately matches the second branch is told they lack permission instead of running it. Requires that specific overlapping-sibling tree shape, hence P2.~~High
6~~P2~~BrigadierPaperListener gates the tree on PluginCommand#getPermission() rather than the permission CommandManager enforces~~Gate on the same permission string CommandManager checks, not the plugin.yml PluginCommand permission~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/brigadier/BrigadierPaperListener.java:125-126~~The master gate is `String masterPerm = command.getPermission(); if (masterPerm != null && !masterPerm.isEmpty() && !player.hasPermission(masterPerm)) return;`, reading Bukkit's plugin.yml-declared PluginCommand permission — a different string from the one CommandManager actually enforces at dispatch. When plugin.yml declares no permission the gate is skipped entirely; when it declares a different one the wrong check runs. Per-argument predicates still filter individual nodes, so the consequence is a mismatch between what the client tree shows at the root and what dispatch enforces rather than an outright bypass.~~High
7~~P2~~BrigadierTabRegistrar permission and visibility predicates return true when extractSender cannot resolve the Bukkit sender~~Fail closed (return false) when the sender cannot be extracted from the NMS source~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/brigadier/BrigadierTabRegistrar.java:377-385~~permissionRequires builds `source -> { CommandSender sender = extractSender(source); if (sender == null) return true; return sender.hasPermission(permission); }`, and visibilityRequires does the same. extractSender resolves the Bukkit sender reflectively from the NMS command source, so on any server build where that reflection does not match, every permission predicate answers true and the client is offered completions for commands the player cannot run. The failure is silent and version-dependent, and it fails open rather than closed on a permission decision.~~High
8~~P2~~CommandManager's static defaultInstance shares per-plugin command state across every consumer on the shared classloader~~Remove the static accessors (already deprecated) so each consumer resolves its own CommandManager instance~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandManager.java:37~~`private static volatile CommandManager defaultInstance;` is claimed by whichever CommandManager is constructed first in the JVM, and the static getCommands/getPermissibleCommands accessors delegate to it. Keystone is loaded once and shared by Gangland, Bartizan and Oriel, so on a server running two consumers the second plugin's static lookups return the first plugin's command registry — exactly the per-plugin static state the repo's shared-classloader rule forbids. The field's own javadoc concedes the accessors "go away in 2.0.0", so it is a known deprecated hazard rather than an unnoticed one.~~High
9~~P2~~CommandMessages and ArgumentMessages hold their overrides in static fields, so one consumer's install() rewrites every plugin's command text~~Move the override state onto the CommandManager instance so each consumer owns its own strings~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandMessages.java:62-68~~Every message supplier is static — `private static Supplier<String> noPermission = DEFAULT_NO_PERMISSION;` and six siblings, with the same pattern in ArgumentMessages — and install() replaces them process-wide. With Keystone shared across consumers, whichever plugin calls install() last owns the framework's player-facing strings for all of them, so a Gangland player can be shown Oriel's localized no-permission text. The javadoc documents this as "one consumer plugin owns the command framework's strings", but it still contradicts the hard shared-classloader rule and produces visibly wrong output on a multi-consumer server.~~High
10~~P2~~CommandManager.onCommand treats an exact help token anywhere in args as a help request~~Only treat help as a help request in the position immediately after the subcommand~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandManager.java:199~~The check is `Arrays.stream(args).anyMatch("help"::equalsIgnoreCase)`, scanning every typed token rather than the position after the subcommand. Any command carrying free text where a value can legitimately be the word "help" — a mail message, a search term, a player-supplied name — is silently diverted into onHelp and the intended action never runs. The user sees a help page instead of their command executing, with no indication why.~~High
11~~X~~TargetSelector.origin overrides the selection world without recomputing the origin coordinates in that world~~Recompute originX/Y/Z relative to the named world (its spawn) when a world= filter replaces the sender's world~~TargetSelectorTest (no cross-world world= plus distance/sort case)~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/TargetSelector.java:564-589~~origin() first derives originX/Y/Z from the sender's own location (or the default world's spawn), and only afterwards applies the explicit world filter with `if (this.world != null) { World named = Bukkit.getWorld(this.world); if (named == null) return null; world = named; }`, swapping the World reference while leaving the coordinates from the previous world in place. A selector combining a world filter with a distance or nearest-sort then measures distances from a point that does not correspond to anything in the target world, so the wrong entities are selected or ordered. Only affects selectors that cross worlds, hence P2.~~Medium
12~~P3~~Argument.addSubArgument picks tree-insertion order by substring-matching "?" in the token text instead of checking the type~~Test `argument instanceof OptionalArgument` rather than searching the token for the "?" character~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:128-129~~Insertion order is chosen by `if (argument.toString().contains(OPTIONAL_ARGUMENT)) node.add(argument.getNode()); else node.add(0, argument.getNode());`, where toString() returns the literal token and OPTIONAL_ARGUMENT is the single character "?". A plain literal sub-argument whose own token happens to contain a question mark is appended last like a wildcard instead of prepended, changing which sibling wins during matching. The trigger requires a literal token containing "?", which is unusual, so this is a latent correctness trap rather than an observed fault.~~High
13~~X~~Argument.notFound reports the last valid token as the invalid one when the args end on a valid node~~Distinguish "ended on a valid node" from "next token invalid" and word the message accordingly~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:230-239~~When the matched token is also the final element of args, the else branch keeps `length = i` and sets `lastInput = args[length]`, which is the token that just matched successfully. The error message therefore names a valid token as the invalid input, telling a user that the very argument they typed correctly is wrong. Purely a message-accuracy defect on an already-failing path.~~High
14~~P3~~Argument overrides equals() by content but never overrides hashCode()~~Add a matching hashCode(), or drop the equals override if identity comparison is what the tree needs~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:175-182~~equals() compares the two arguments arrays case-insensitively with anyMatch, so two distinct Argument objects sharing one alias are equal, yet no hashCode() override accompanies it and Object identity hashing remains. Equal instances therefore hash differently, which silently breaks any HashSet or HashMap keyed on Argument. Nothing in the reactor currently hashes an Argument — the tree stores children in lists — so this is a contract violation waiting for the first hashed use rather than a live fault.~~High
15~~P1~~Free-text argument nodes on the Paper Brigadier path carry no suggestion provider, so tab completion never asks the server~~Attach a suggestion provider (ASK_SERVER: (ctx, builder) -> builder.buildFuture()) to every free-text string node BrigadierTabRegistrar builds (OptionalArgument and the ListArgument fallback) on both backends, so the client round-trips to CommandTabCompleter and OptionalArgument suppliers become reachable~~BrigadierTabRegistrarTest (asserts every ArgumentCommandNode of a give <name> <amount> tree has non-null getCustomSuggestions())~~keystone-command/src/main/java/org/luckyraven/keystone/command/brigadier/BrigadierTabRegistrar.java (four string() node sites)~~FIXED Keystone 1.9.2, branch phase-h9-host-api (uncommitted 2026-09-13, docs/phase-h9-host-api.md section 6; found and fixed by the Bartizan session while diagnosing /bartizan weapon give <tab> on Paper 1.21.11). BrigadierTabRegistrar mapped every OptionalArgument and the ListArgument fallback to a plain string() node with no suggestion provider; the client tree serialiser marks a node ask-the-server only when a provider is present, so on the Paper path (BrigadierPaperListener ships the tree as built) the client never asked CommandTabCompleter and every live OptionalArgument supplier (player names, module ids, waypoint names) was unreachable. Spigot was unaffected only because Commodore installs its own dummy provider.~~High
16~~P2~~CommandTabCompleter.descend skips permission checks on intermediate argument nodes~~Check each matched node's permission in descend/findArgument (as Argument.traverseList does) and return no suggestions below a node the sender lacks~~CommandTabCompleterTest (no gated-intermediate-node case)~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandTabCompleter.java:158-173~~[found 2026-09-24, master 4192baa] descend matches each child purely on its token (`if (!argumentMatches(child.getData(), args[currentIndex], children)) continue; if (currentIndex == targetDepth) return child.getData();`) and never consults the matched node's getPermission(); the only permission filter is on the final children list (`if (permission.isEmpty() || sender.hasPermission(permission)) suggestions.addAll(...)`, lines 103-108). Sub-arguments default to an empty permission (Argument.java:50), so for a tree `/cmd gang admin <player>` where only `admin` is gated, a player without that permission who types `/cmd gang admin <tab>` receives admin's children, including OptionalArgument customStrings (player/gang name lists). Dispatch rejects the same path at the gated node (Argument.traverseList returns NO_PERMISSION), so completion and execution disagree. The Brigadier trees filter per node, but this server-side completer is what answers on the fallback path (Commodore unsupported or registration failed) and for any tab-complete request a client sends for the gated prefix.~~High
17~~P2~~Argument.notFound suggests permission-gated sub-argument names in its did-you-mean line~~Filter the did-you-mean dictionary to children whose permission is empty or held by the sender, as CommandManager.onCommand already does via permissibleCommands~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:243-254~~[found 2026-09-24, master 4192baa] On a NOT_FOUND result the suggestion dictionary is every child of the last valid node: `Set<String> dictionary = children.stream().map(node -> node.getData().arguments).flatMap(Stream::of).filter(s -> !s.equals(OPTIONAL_ARGUMENT)).collect(Collectors.toSet());` with no permission check, and the closest match is sent back via `ArgumentMessages.didYouMean(suggestion)`. A player who mistypes near a gated literal under a parent they can reach (e.g. `/cmd sub resett` where the child `reset` carries a permission they lack) is told the exact name of a sub-argument they are not permitted to see. The master-level suggestion in CommandManager.onCommand builds its dictionary from `permissibleCommands(sender)`, so filtering is clearly the intent and this branch is the hole; disclosure only, nothing executes.~~High
18~~P2~~BrigadierPaperListener.handle drops master-command alias nodes and never re-adds them~~After building the per-player tree, also add a renamed copy (redirect or rebuilt literal) under each of command.getAliases(), as Commodore does~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/brigadier/BrigadierPaperListener.java:122-131~~[found 2026-09-24, master 4192baa] The handler strips the Bukkit stub for the name and every alias (`removeChild(root, command.getName()); for (String alias : command.getAliases()) removeChild(root, alias);`) and then adds exactly one node, `root.addChild(tree)`, whose literal is `LiteralArgumentBuilder.literal(command.getName())` (BrigadierTabRegistrar.java:153). On the Paper backend (servers that still fire AsyncPlayerSendCommandsEvent) every master-command alias from plugin.yml therefore disappears from the client's command tree: typing `/<alias> ` renders as an unknown command with no suggestions at all, not even the server-side CommandTabCompleter stub that was there before the listener removed it. The Spigot backend does not have this gap because Commodore registers the node under every alias. Execution is unaffected (Bukkit dispatch still resolves the alias), so this is a missing-UX defect.~~Medium
19~~P2~~BrigadierTabRegistrar emits only arguments[0], so subcommand aliases vanish client-side~~Emit one literal (or a redirect to the canonical node) per entry in Command.getAlias() and per extra token in Argument.getArguments() on both backends~~BrigadierTabRegistrarTest (no alias case)~~keystone-command/src/main/java/org/luckyraven/keystone/command/brigadier/BrigadierTabRegistrar.java:163-172~~[found 2026-09-24, master 4192baa] Subcommand nodes are built as `LiteralArgumentBuilder.literal(cmd.getLabel())` (lines 163 and 227) and plain sub-arguments as `LiteralArgumentBuilder.literal(arg.getArguments()[0])` (lines 359 and 380); Command.getAlias() and the remaining Argument tokens are never emitted. CommandManager.onCommand dispatches aliases (`entry.getValue().getAlias().contains(args[0].toLowerCase())`) and CommandTabCompleter.resolve completes them, but once the Brigadier tree is shipped the client parses locally, so `/cmd <sub-alias> ...` shows as an incorrect argument in red and nothing below it completes, on both the Paper and Spigot backends. Any consumer that gives a subcommand a short alias (the Command constructor's `String... alias`) hits this on every use; the command still runs when sent.~~High
20~~P2~~DoubleArgument/ConfirmArgument stay armed forever: ArgumentLock arms never expire~~Store an arm timestamp per sender in ArgumentLock and treat arms older than a short window (e.g. 30s) as unlocked; clear stale arms on the next lock check~~DoubleArgumentTest, ConfirmArgumentTest (neither covers a delayed second invocation)~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/types/DoubleArgument.java:36-45~~[found 2026-09-24, master 4192baa] DoubleArgument's safety gate is `if (!lock.isLocked(sender)) { sender.sendMessage(ArgumentMessages.confirmPrompt()); lock.lock(sender); return; } lock.unlock(sender); super.executeArgument(sender, args);`, and ArgumentLock (ArgumentLock.java:12-28) is a bare `Set<CommandSender>` with no time or context attached. A player who runs a type-it-twice destructive command once, reads the prompt and backs out, then runs it again an hour later expecting the prompt gets immediate execution instead; ConfirmArgument has the same shape, so `/<cmd> confirm` completes a confirmation armed at any earlier point in the session (Gangland's gang transfer flow arms `confirmTransfer.lock(sender)` and relies on this gate). Distinct from KS-CM-04, which files the same set's memory retention on disconnect; this row is the correctness side, a confirmation gate that never lapses.~~High
21~~P2~~CommandManager.clearCommands advises a re-scan that CommandService silently skips~~Clear CommandService.scannedPackages in clearCommands (or add a resetScans() the javadoc points to) so the documented reload flow repopulates the registry~~CommandManagerTest.unregisterAndClear_removeCommands (clears but never re-scans)~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandManager.java:146-151~~[found 2026-09-24, master 4192baa] clearCommands is documented as `Removes every registered subcommand — call before a re-scan on reload.` and does `registry.clear()`, but the re-scan it points to, CommandService.scanAndRegisterCommands, opens with `if (!scannedPackages.add(basePackage)) { log.debug("Package {} already scanned for commands — skipping", basePackage); return; }` (keystone-bean CommandService.java:54-57) and nothing ever removes entries from scannedPackages. A consumer following the javadoc (clearCommands, then scanAndRegisterCommands with the same package, as Gangland/Oriel/Bartizan bootstraps call it) ends the reload with an empty registry: every subcommand answers as unknown until restart, and the only trace is a DEBUG line. No consumer calls clearCommands yet, so this is a documented reload path that cannot work rather than a live outage.~~High
22~~P3~~BrigadierTabRegistrar.paperListenerCommands is static per-plugin state keyed by name~~Key the dedup on the plugin (or keep it on the CommandManager instance) and drop the entry when the plugin disables~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/brigadier/BrigadierTabRegistrar.java:97~~[found 2026-09-24, master 4192baa] `private static final Set<String> paperListenerCommands = ConcurrentHashMap.newKeySet();` gates registration with `if (!paperListenerCommands.add(command.getName())) { ...; return; }` (lines 186-190) and is never cleared. Keystone's classes outlive a single consumer, so when a consumer is disabled and re-enabled without Keystone reloading (PlugMan-style reload), Bukkit unregisters the old BrigadierPaperListener on disable but the re-enable skips registering a new one because the name is still in the set, leaving no per-player tree for that command. Two consumers whose master commands share a name likewise only get one listener. This is the per-plugin static state the shared-classloader rule forbids.~~Medium
23~~P3~~CommandManager.addCommand ignores label/alias collisions; dispatch and completion diverge~~Reject (or Diagnostics-report) a subcommand whose label or alias is already claimed in addCommand~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/CommandManager.java:240-242~~[found 2026-09-24, master 4192baa] addCommand is `registry.put(sub.getLabel(), sub);`, so a second subcommand with the same label silently replaces the first, and aliases are never compared against other labels. Dispatch walks the registry in insertion order and takes the first entry where `entry.getKey().equalsIgnoreCase(args[0]) || entry.getValue().getAlias().contains(args[0].toLowerCase())` (lines 195-197), while CommandTabCompleter.resolve checks every label before any alias (CommandTabCompleter.java:116-127). If subcommand A, scanned first, declares alias `b` and subcommand B has label `b`, then `/cmd b ...` executes A while tab completion walks B's argument tree, and nothing warns the author. Needs an authoring mistake to trigger, hence P3.~~High
24~~P3~~Argument.notFound blames the free-text token after an OptionalArgument, not the typo~~Walk the tree for the last valid node with the same OptionalArgument wildcard matching traverseList uses, so free-text positions count as valid~~-~~keystone-command/src/main/java/org/luckyraven/keystone/command/argument/Argument.java:218-239~~[found 2026-09-24, master 4192baa] notFound finds the last valid node with `tree.traverseLastValid(modifiedArg)`, but Tree.traverseLastValid only matches by `node.getData().equals(list[index])` (keystone-common Tree.java:122-131) with no wildcard, and an OptionalArgument's tokens are just `?`, so the walk always stops at the literal before the first free-text position. For `/cmd set Steve of` on a tree set -> <name> -> {on, off}, the loop finds `set` and reports `lastInput = args[length]`, the valid name `Steve`, as the wrong argument, and the typo `of` is never mentioned; the did-you-mean line is built from set's children, which after the `?` filter is empty. This hits any typo after a free-text argument. Distinct from KS-CM-13, which covers args that end on a valid node.~~High
