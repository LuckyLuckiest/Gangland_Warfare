1~~P1~~Weapon.clone shares the tags map, so cloning a built weapon wipes the source's tags~~Copy the map in clone() (reassign tags to a new HashMap) before initClone clears it.~~WeaponCloneTest clone_bug_tagsMapIsSharedNotCopied (pins BUG; flip when fixed)~~gangland-features/gangland-weapon Weapon.java clone()/initClone()~~Weapon.tags is a final field, so Object.clone() copies only the reference; initClone's tags.clear() empties the one map both the template and the clone share. Cloning a weapon that already had initializeTags/buildItem called silently wipes the template's tag cache. Found by the test initiative; not listed in weapons.md.~~weapons
2~~P2~~MailManager.findPendingByGangAndRecipient has no null guard for the recipient~~Null-guard the recipient (return empty).~~MailManagerTest (pins)~~gangland-features/gangland-mail MailManager.java:126~~Passing a null recipient throws NullPointerException instead of returning an empty result. Called out in the gangs-ranks-mail.md Test Surface and pinned by the mail tests.~~gangs-ranks-mail
3~~P2~~ParkedVehicle.addFuel has no upper clamp at maxFuel~~Clamp the result to maxFuel.~~ParkedVehicleTest (pins)~~gangland-features/gangland-gadget ParkedVehicle.addFuel~~addFuel clamps at zero for negative amounts but never at maxFuel, so a parked car can be over-fuelled past its tank size through refuelParkedCar. Observation-adjacent finding from the gadget tests.~~gadgets-cars-fuel-jetpack
4~~P2~~TeleportCommand.teleportCost dereferences a null User~~Guard user == null before touching user.getUser(); done as part of the LS-01 fix.~~FIXED 0.8.3 wave 3, commit 8a731fd5 (guard only, no dedicated test)~~gangland-impl command/sub/waypoint/TeleportCommand.java teleportCost~~Both callers pass userManager.getUser(player) straight through without a null check, and the first statement in the invalid-waypoint branch is user.getUser().sendMessage(...), so a player with no cached User NPEs on /glw teleport. Same shape as GR-01. Found while applying the LS-01 waypoint access gate.~~lootchests-signs-waypoints
5~~P2~~Gang invite-cancel and ally pending-cancel are still ungated after GR-03~~Apply GangPermissions.allows with the INVITE / ALLY node the same way the send paths now do.~~-~~gangland-features/gangland-mail GangInviteCancelCommand, GangAllyPendingCancelCommand~~The GR-03 wave gated every subcommand that creates or breaks gang state (invite, ally request/accept/reject, abandon) but left the two cancel leaves open, so any member can still cancel their gang outgoing invites and alliance requests. Lower impact than the send paths - the state cancelled belongs to the caller own gang - but it is the same hole.~~gangs-ranks-mail
6~~P1~~FileConfig.weaponLoader registers only 5 of the 22 shipped weapon YAMLs, so a fresh install never writes out or loads 17 weapons~~Add addExpectedFile calls (or loop the module's weapon-YAML resource list) for all 22 weapon YAMLs in the module's WeaponFileConfig.weaponLoader; the weapon module flip (commit 0272008b) copied the 5-name list verbatim to stay behaviour-preserving, so this still needs its own fix.~~-~~gangland-features/gangland-weapon/src/main/java/org/luckyraven/gangland/weapon/WeaponFileConfig.java:89-97 (weaponLoader) - moved here from gangland-impl config/FileConfig.java by the weapon module flip (0272008b)~~weaponLoader() registers only rifle, grenade, knife, flamethrower and syringe_gun via addExpectedFile even though gangland-impl/src/main/resources/weapon/ ships 22 weapon YAMLs. FolderLoader.loadData only creates files from expectedFolderFiles when the data folder is missing or empty, so on a fresh install the other 17 (awp, crowbar, flashbang, golden_ak47, machete, minigun, molotov, mp5, pistol, ray_gun, revolver, rocket_launcher, sawn_off, shotgun, smoke_grenade, steyr_aug, tomahawk) are never written out and never loaded. Found 2026-09-07 during module-split planning (brainstorming/module-split-2026-09-07/weapon.md B-1); the weapon module flip (T18) was explicitly scoped to copy the list verbatim so it stayed behaviour-preserving, and commit 0272008b (0.8.4, weapon flip 4 of 4) confirms that: the same 5-name List.of(...) now lives in WeaponFileConfig.weaponLoader in the gangland-weapon module. Verified still open 2026-09-08 during the module-split docket sweep - still needs its own fix, now in the module.~~weapons
7~~P3~~SignManager.setupSigns() runs twice per boot: explicit GameplayConfig.signManager() call plus Keystone's convention initialize() pass~~Delete the explicit manager.initialize() call in GameplayConfig.signManager() and let Keystone's convention initialize() pass call it once; planned as part of the gadget module SignManager seam flip.~~FIXED 0.8.4, commit 4304172c (gadget flip): the explicit manager.initialize() call in GameplayConfig.signManager() is gone from gangland-impl/src/main/java/org/luckyraven/gangland/config/GameplayConfig.java; Keystone's convention initialize() pass now calls SignManager (via SignService) once; SignManagerContributionTest~~gangland-impl config/GameplayConfig.java:274 (signManager) + keystone-bean BeanFactory.java (reflective initialize() convention pass)~~GameplayConfig.signManager() calls manager.initialize() explicitly right after construction; SignManager (via SignService) is neither a FileInitializer nor a BeanLifecycle, so Keystone's BeanFactory convention pass, which reflectively invokes any public zero-arg initialize() method on every bean, also calls it, running setupSigns() a second time. Harmless today because both sign registries are Map-backed (setupSigns just re-puts the same entries), but it is a real double pass building every SignTypeDefinition, SignHandler and SignAspect twice. Found 2026-09-07 during module-split planning (brainstorming/module-split-2026-09-07/weapon.md B-2, gadget.md OQ-3); fixed by the gadget module flip - deleting the explicit call is required for that module seam anyway.~~lootchests-signs-waypoints
8~~P3~~ViewSignValidator.isValidContent runs dead weapon/ammo lookups before returning true unconditionally~~Remove the unreachable weapon/ammo lookup branches (or replace with real validation) in ViewSignValidator.isValidContent; removed as part of the weapon module flip.~~FIXED 0.8.4, commit 0272008b (weapon flip): isValidContent is now `return !content.isEmpty();` (behaviour-identical, dead weapon/ammo lookup branches removed); no dedicated test~~gangland-impl sign/validation/ViewSignValidator.java:24-51 (isValidContent)~~isValidContent computes a weapon-template lookup and an ammunition-key lookup whose only branches are return true, then falls through to an unconditional return true regardless of either lookup's result, so the weapon/ammo checks are dead code on every view-sign edit. Found 2026-09-07 during module-split planning (brainstorming/module-split-2026-09-07/weapon.md B-3); removed by the weapon module flip.~~lootchests-signs-waypoints
9~~P3~~gangland-weapon pom.xml declared an unused anvilgui dependency; viaversion-api looked unused too but is actually required~~Drop the unused net.wesjd:anvilgui dependency from gangland-features/gangland-weapon/pom.xml; keep com.viaversion:viaversion-api at provided scope - it is required, not unused. Done as part of the weapon module flip.~~PARTIALLY FIXED 0.8.4, commit 0272008b (weapon flip): anvilgui removed (confirmed absent from gangland-features/gangland-weapon/pom.xml). viaversion-api turned out to be REQUIRED at provided scope - RecoilCompatibility (version-impl) references ViaAPI, and provided scope is not transitive, so RecoilManagerTest's Mockito inline mock maker throws TypeNotPresentException retransforming RecoilCompatibility's type hierarchy without it on the classpath. Remaining scope: none - closed, not a bug (see pom.xml:84-91 for the in-code explanation of why it stays).~~gangland-features/gangland-weapon/pom.xml (anvilgui removed; viaversion-api ~L92-96, kept at provided scope)~~gangland-weapon pom.xml declared both net.wesjd:anvilgui and com.viaversion:viaversion-api. No source file under gangland-features/gangland-weapon/src imports AnvilGUI or any com.viaversion type directly, and AnvilGUI is additionally relocated by the shade plugin, so anvilgui was a genuine latent runtime/classpath hazard once weapon became a runtime module jar - removed by the weapon module flip (commit 0272008b). viaversion-api is NOT unused: 'no direct import' only checked source-level imports, not what mocking a provided-scope type pulls in at test time. RecoilCompatibility (version-impl) declares ViaAPI at compile scope for its own compilation, but provided scope is not transitive, so a module that only depends on version-impl at provided never sees it transitively; RecoilManagerTest mocks RecoilCompatibility, and Mockito's inline mock maker retransforms its full type hierarchy (including the ViaAPI field type), which throws TypeNotPresentException without viaversion-api on the classpath. Found and fixed while running T24 (2026-09-08); the dependency's own pom.xml comment (lines 84-91) records this. Originally found 2026-09-07 during module-split planning (brainstorming/module-split-2026-09-07/weapon.md B-4), which incorrectly assumed both deps were unused.~~weapons
10~~P2~~ItemRefresherRegistry is first-match-wins with no priority, so a late registrant can never outrank an earlier one~~Add a priority/ordering knob to ItemRefresherRegistry.register (or sort refreshers) so a module-registered refresher can outrank a core one; fixed as part of the weapon module flip.~~FIXED 0.8.4, commits 4304172c (gadget flip: ItemSerializerRegistry.register(predicate, serializer, priority) + CATCH_ALL_PRIORITY so the core's MATERIAL catch-all stays last) and 0272008b (weapon flip: ItemRefresherRegistry.register(refresher, priority) + CATCH_ALL_PRIORITY; weapon and wearable refreshers register at priority 10, ammunition at the default priority 0, entries kept sorted highest-first with a stable sort); ItemSerializerRegistryPriorityTest + ItemRefresherRegistryPriorityTest (gangland-infra/gangland-item/src/test/java/org/luckyraven/gangland/item/)~~gangland-infra/gangland-item ItemRefresherRegistry.java:19-56 (register/refresh/decorate)~~refresh() and decorate() both walk the refreshers list in insertion order and return the result of the first refresher whose canRefresh() matches; register() only appends, with no way to insert ahead of or replace an earlier entry. Latent today because every ItemRefresher registration happens in one core @Bean method in a fixed order, but it goes live the moment a feature that registers its own ItemRefresher (e.g. unique weapons) becomes a runtime module loaded after the core registrations - a later, more specific refresher can never outrank an earlier, more general one, so an item could refresh via the wrong refresher. Found 2026-09-07 during module-split planning (brainstorming/module-split-2026-09-07/weapon.md B-5); fixed by the weapon module flip.~~items-unique
11~~P1~~GameplayConfig.lootChestLoader resolves loot_chests.yml item strings synchronously inside its CONFIG-phase @Bean body, with no ordering edge to a module's item converter~~FIXED 0.9.0 (1340d97c, branch 0.9.0): GameplayConfig.lootChestLoader no longer calls fileManager.initializeAll() eagerly - deferred to a @PostConstruct mirroring initializeInventoryLoader(); the executor also found the eager item-resolution path itself was already lazy pre-stream (SlotItemFactory/InventoryLoader resolve loot-chest item strings at roll time), so the entry's original observation is partly stale~~covered by the gangland-impl suite at gate G (194 tests green); no dedicated pin~~gangland-impl config/GameplayConfig.java:272-278 (lootChestLoader) - unchanged, still synchronous~~lootChestLoader() calls fileManager.initializeAll() directly inside its own CONFIG-phase @Bean body (gangland-impl/src/main/java/org/luckyraven/gangland/config/GameplayConfig.java:272-278), taking only LootChestManager and FileManager as parameters, so loot_chests.yml item refs are parsed at that instant with no ordering edge to any module's item-converter bean. InventoryLoader already avoids this exact trap by deferring its initialize() to a @PostConstruct (GameplayConfig.java ~L287+) that runs once every CONFIG bean is registered and the ItemConverterRegistry is populated; lootChestLoader was never given the same treatment. Found 2026-09-07 during module-split planning (brainstorming/module-split-2026-09-07/gadget.md OQ-2; same triage class also covers the weapon and turf flips) and originally filed as latent, since no shipped loot_chests.yml used a car: ref. Re-verified 2026-09-08 after the gadget and weapon flips landed (commits 4304172c, 0272008b): `grep -c 'Item: "weapon:' gangland-impl/src/main/resources/lootchests/loot_chests.yml` = 44, `Item: "wearable:` = 12, `Item: "ammo:` = 29, `Item: "car:` = 0. weapon:, wearable: and ammo: refs are exactly the item kinds now registered by WeaponModuleConfig.weaponItemRegistrations (itemConverterRegistry.register(WEAPON/AMMUNITION/"ammo"/WEARABLE, ...)) in the weapon runtime module, a bean with no parameter dependency on GameplayConfig.lootChestLoader or vice versa - so whichever way the CONFIG-phase topological sort happens to order these two beans on a given boot, there is no guarantee weaponItemRegistrations runs before lootChestLoader. This is no longer a latent/future risk gated on a car: ref that doesn't exist yet - it is a live ordering hazard against 56 existing item refs across the majority of the game's ~20 configured loot chests, so it is raised from P2 to P1: if the sort ever lands the wrong way (e.g. after any future bean-graph change touching either side), the affected chests silently lose their weapon/wearable/ammo drops instead of failing loudly. No code change made in this task - still open.~~lootchests-signs-waypoints
12~~P0~~cops-n-crooks module present crashes boot with NoClassDefFoundError net/wesjd/anvilgui/AnvilGUI$StateSnapshot because the core relocates AnvilGUI~~FIXED 0.8.4 (uncommitted): removed the net.wesjd.anvilgui <relocation> block from gangland-build/pom.xml (kept the fastboard/bstats relocations and the anvilgui shade include/filter unchanged) and added an explanatory comment above the relocations recording why AnvilGUI must stay unrelocated. Verified: `unzip -l target/gangland_warfare-0.8.4.jar | grep -c net/wesjd/anvilgui` = 85 (AnvilGUI$StateSnapshot.class present at its original path), `grep -c gangland/dependency/anvilgui` = 0; fastboard (9) and bstats (12) relocations still present. Checked gangland-features/*/src/main/java for any other core-only relocated library referenced by a module (grep fr.mrmicky, org.bstats) - none found; only AnvilGUI is consumed by a module (cops-n-crooks: BankerEditNameCommand, TraderEditNameCommand, BankerAmountView, BankerCreateAccountView, BankerRenameAccountView, QuantitySelectorView), matching the observation. No dedicated unit test (jar-level; covered by the console smoke matrix rows S4/S5). mvn clean install (with tests, full reactor, Keystone 1.8.1) green.~~- (jar-level; covered by the console smoke matrix rows S4/S5 in brainstorming/bartizan-split-2026-09-08/README.md)~~gangland-build/pom.xml:34-38 (relocation net.wesjd.anvilgui -> org.luckyraven.gangland.dependency.anvilgui); consumers in gangland-features/cops-n-crooks command/banker/BankerEditNameCommand, command/trader/edit/TraderEditNameCommand, npc/banker/view/{BankerAmountView,BankerCreateAccountView,BankerRenameAccountView}, npc/trader/view/QuantitySelectorView~~Observed 2026-09-08 01:25:42 on the test server (Paper 1.21.11, Keystone 1.8.0, all four 0.8.4 modules): "Error occurred while enabling Gangland_Warfare v0.8.4 ... java.lang.NoClassDefFoundError: net/wesjd/anvilgui/AnvilGUI$StateSnapshot at Class.getDeclaredMethods0 ... BeanFactory.runPostConstruct(BeanFactory.java:543) ... GanglandContext.bootstrap(GanglandContext.java:191)". The core jar shades AnvilGUI relocated to org.luckyraven.gangland.dependency.anvilgui, but the cops-n-crooks module jar is a plain jar compiled against net.wesjd.anvilgui and is never relocated, so the first cops bean whose declared methods mention an AnvilGUI type fails to link and the whole plugin disables. Any module that uses AnvilGUI (today only cops-n-crooks; the future gangland-npc-shops module inherits the same views) is unusable until fixed. The same run without cops (weapon + gadget) boots.~~cops-detainment-jail
13~~P1~~Keystone ListenerService.registerGuarded dispatches sibling events sharing a HandlerList into the wrong handler (argument type mismatch on every ArmorStand/creature spawn)~~Upstream (Keystone keystone-bean ListenerService.registerGuarded): mirror Bukkit JavaPluginLoader and return early from the guarded executor when !eventType.isAssignableFrom(event.getClass()); red-first unit test registers a handler for a subclass event whose HandlerList is inherited from its parent (two fake events sharing one HandlerList) and asserts the sibling event is ignored instead of throwing. Ship as Keystone 1.8.1 and bump keystone.version in the root pom.~~- (Keystone keystone-bean ListenerServiceTest sibling-event case, red first)~~E:/Programming/java/Keystone keystone-bean/src/main/java/org/luckyraven/keystone/bean/listener/ListenerService.java:86-110 (registerGuarded executor lambda) and :113-119 (invokeHandler); observed through gangland-features/gangland-weapon/src/main/java/org/luckyraven/gangland/weapon/listener/reload/WeaponItemSpawnListener.java:35 (onItemSpawn(ItemSpawnEvent))~~Observed 2026-09-08 01:24:46-47 and 01:27:09-10 on the test server whenever the weapon module is loaded: "[Keystone Common.LoggingSink] argument type mismatch (listener=WeaponItemSpawnListener event=ItemSpawnEvent) [listener.dispatch] java.lang.IllegalArgumentException ... ListenerService.invokeHandler(ListenerService.java:116)" once per hologram ArmorStand spawn (stack: Hologram.createArmorStand <- ChestCooldownManager.showAvailableHologram <- LootChestManager.registerLootChests), six times in ten seconds. Bukkit ItemSpawnEvent does not declare its own HandlerList - it inherits EntitySpawnEvent.getHandlerList() - so pluginManager.registerEvent(ItemSpawnEvent.class, ...) registers the executor on the shared EntitySpawnEvent list and Bukkit invokes it for CreatureSpawnEvent/SpawnerSpawnEvent/EntitySpawnEvent too. Bukkit's own JavaPluginLoader.createRegisteredListeners guards this with eventClass.isAssignableFrom(event.getClass()); the guarded executor in ListenerService omits that check, so Method.invoke throws IllegalArgumentException, the Guard classifies it as a fault and reports it (fault sink + log) for every non-item entity spawn. Affects every Keystone-dispatched handler for an event that shares a parent HandlerList (EntityDamageByEntityEvent/EntityDamageEvent, PlayerInteractAtEntityEvent/PlayerInteractEntityEvent, BlockMultiPlaceEvent/BlockPlaceEvent, ...) across Gangland, Oriel and every module.~~core-lifecycle
14~~P2~~Core WearableEquipListener (gangland-item) requires WearableEquipService, which only the weapon runtime module provides~~FIXED 0.8.4 (uncommitted): git mv WearableEquipListener from gangland-item (item/listener/wearable) to the weapon module (org.luckyraven.gangland.weapon.listener.wearable), a sub-package of WeaponModule.LISTENER_PACKAGE which the module already scans recursively (confirmed by the existing weapon/listener/reload WeaponItemSpawnListener registered from the same LISTENER_PACKAGE). No separate core-side scan to drop: GanglandContext's listener scan is a single blanket "org.luckyraven.gangland" pass (GanglandContext.java:77/222), not a package-specific one - it simply stops finding the class once it is no longer compiled into gangland-impl's classpath (gangland-impl never depends on gangland-weapon). item/listener still has fuel/, money/ and unique/ sub-packages remaining, none of which reference a module-only bean. WearableEquipService interface left in gangland-item as the seam. Test: red-first ItemListenerModuleBoundaryTest (gangland-infra/gangland-item/src/test/java/org/luckyraven/gangland/item/listener/) walks every class actually compiled under item.listener and fails if any constructor references WearableEquipService - confirmed red pre-fix (WearableEquipListener found with that constructor param), green post-fix; WeaponModuleTest.wearableEquipListener_livesUnderModuleListenerPackage asserts the moved class lives under WeaponModule.LISTENER_PACKAGE.~~- (GanglandContext zero-module boot test / WeaponModuleTest listener package assertion)~~gangland-infra/gangland-item/src/main/java/org/luckyraven/gangland/item/listener/wearable/WearableEquipListener.java:26-31 (@AutowireTarget WearableEquipService, constructor); provider gangland-features/gangland-weapon/src/main/java/org/luckyraven/gangland/weapon/WeaponModuleConfig.java (wearableEquipService bean) + weapon/wearable/WearableService.java:35~~Observed 2026-09-08 01:22:15 on the test server with an empty modules/ folder: "[Keystone Bean.ListenerService] Failed to instantiate listener org.luckyraven.gangland.item.listener.wearable.WearableEquipListener: Cannot resolve required parameter of type org.luckyraven.gangland.item.contract.WearableEquipService". The core still scans a listener whose only dependency is a bean the weapon module registers, which breaks the two-tier rule (the core must boot cleanly with zero modules and never depend on a module bean) and logs a warning on every weapon-less server. No functional loss today because without the weapon module no wearable exists to equip.~~items-unique
15~~P2~~Keystone UpdateNotifier.start() runs the first update check synchronously on the server thread (10 s boot stall when the network is unreachable)~~Upstream (Keystone keystone-common UpdateNotifier.start): run the initial task() on the async scheduler (or let the async RepeatingTimer perform the first tick immediately) and hop back to the main thread only for the operator notification / download; keep the checked flag semantics; hit the network once per tick instead of twice. Test: start() returns without invoking UpdateChecker.getLatestVersion on the calling thread (fake checker records the thread). Ship with the 1.8.1 hotfix.~~- (Keystone keystone-common UpdateNotifierTest thread assertion, red first)~~E:/Programming/java/Keystone keystone-common/src/main/java/org/luckyraven/keystone/update/UpdateNotifier.java:62-66 (start() calls task() inline) and UpdateChecker.java:77 (HttpURLConnection getInputStream); Gangland call site gangland-impl/src/main/java/org/luckyraven/gangland/Gangland.java:236 (updateCheckerInitializer) from onEnable :108~~Observed 2026-09-08 01:26:54-01:27:04 on the test server: "[Keystone Common.UpdateNotifier] Checking for updates" then two "[Keystone Common.UpdateChecker] Unable to check for the latest version. java.net.SocketTimeoutException: Connect timed out ... at UpdateNotifier.start(UpdateNotifier.java:63) at Gangland.updateCheckerInitializer(Gangland.java:236) at Gangland.onEnable(Gangland.java:108)" five seconds apart, both on the Server thread. start() calls task() before starting the async RepeatingTimer, and task() calls getUpdateMessage() -> updateAvailable() -> checker.updateAvailable() -> getLatestVersion() twice (once in getUpdateMessage, once in the updateAvailable() branch), so an unreachable network blocks plugin enable for 2 x the connect timeout. Related but distinct from CM-01 / CM-27 (NPEs when the updater is disabled).~~core-lifecycle
16~~P3~~DatabaseManager.startBackup logs "Failed to create a backup" on every shutdown of the test server~~FIXED 0.9.0 (f4149f46, branch 0.9.0): settings.yml Database.SQLite.Backup defaults to false and Settings.java bool(sqlite, "Backup", false); Keystone 1.9.0 (0.8.1 hotfix + no-host guard) covers the MySQL-side cause. Existing servers keep their on-disk Backup: true until edited - documentation/migration-0.9.0.md must say so~~gangland-impl SettingsDefaultsTest (red first: expected false but was true)~~E:/Programming/java/Keystone keystone-persistence/src/main/java/org/luckyraven/keystone/persistence/database/DatabaseManager.java:49-70 (startBackup, catch Throwable swallows the cause); triggered from closeConnections :38-47 on Gangland disable~~Observed 2026-09-08 01:24:14, 01:25:17 and 01:25:42 on the test server on every "Disabling Gangland_Warfare": "[Keystone Persistence.DatabaseManager] Failed to create a backup for 'SQLite' in 'gangland' database." The warning carries no cause (catch Throwable drops it), so an admin cannot tell whether a backup was ever expected. Low impact: nothing is lost, but the shutdown path always ends with a warning.~~core-lifecycle
17~~P3~~Gangland logs "Found Vault, linking..." / "Linked Vault" twice per boot~~FIXED 0.8.4 (uncommitted): the two Vault Dependency instances in Gangland.dependencyHandler() (economy hook, permission hook) both used name="Vault" for both the plugin lookup AND the "Found {}, linking..."/"Linked {}" log text, so the identical pair printed twice per boot. Added an optional label parameter to the Dependency inner class (defaults to name, so every other dependency is unaffected) - the plugin lookup still targets "Vault" for both, but the log lines now read "Linked Vault economy" / "Linked Vault permissions". No behaviour change beyond the log text. No dedicated test (cosmetic log-only change; no existing Dependency/dependencyHandler test coverage to extend, consistent with the other P3 fixes on lines 7-10).~~-~~gangland-impl/src/main/java/org/luckyraven/gangland/Gangland.java (dependency linking in onEnable, the Vault branch)~~Observed 2026-09-08 on every boot of the test server: the pair "Found Vault, linking..." then "Linked Vault" is printed twice in a row (01:22:15, 01:24:41, 01:26:54). Cosmetic, but it reads as a double initialisation.~~core-lifecycle
18~~P2~~message_en.yml declares Errors.Bounty twice, so YAML last-wins drops Errors.Bounty.Below_Minimum and the WB-02 bounty-minimum message never resolves~~FIXED 0.8.4 (uncommitted): merged the two Errors.Bounty blocks in gangland-impl/src/main/resources/message/message_en.yml (Below_Minimum now sits beside No_Bounty/User_Set_Bounty); MessageFileDuplicateKeysTest (red on the duplicate, green after) scans the shipped file for duplicate mapping paths and asserts the key loads. Follow-up idea for Keystone: LanguageLoader/FileHandler could warn on duplicate mapping keys at load time.~~MessageFileDuplicateKeysTest (gangland-impl, red first: 'Errors.Bounty at lines [471, 494]')~~gangland-impl/src/main/resources/message/message_en.yml:471 (first Errors.Bounty block, Below_Minimum) and :494 (second Errors.Bounty block, No_Bounty/User_Set_Bounty)~~Observed 2026-09-08 on every boot and /glw reload of the test server (including a data folder with NO on-disk message file, so the jar copy itself was at fault): "[Keystone Persistence.LanguageLoader] message_en.yml is missing 1 declared key(s): - Errors.Bounty.Below_Minimum". The WB-02 fix (61d45ddc) added Below_Minimum under a NEW Errors.Bounty block at line 471 while an Errors.Bounty block already existed at line 494; SnakeYAML keeps the last mapping for a duplicate key, so the first block vanished silently and the loader's declared-key check correctly reported it. The earlier version-gated-regeneration theory (FileHandler only rewrites on a Config_Version change) is real behaviour but was not the cause here.~~commands-messages-platform
19~~P3~~RecoilManager.clone() calls super.clone() but the class does not declare implements Cloneable~~NOT FIXED (out of scope for bartizan.md B5, which explicitly forbids touching it): CloneNotSupportedException is always thrown at runtime since Object.clone()'s protected-native-clone contract requires the declaring type to implement Cloneable; the catch block wraps it in a PluginException, so any caller of RecoilManager.clone() (Weapon's own clone() at :422-423 in the ported bartizan-api file) always fails.~~none~~bartizan-api/src/main/java/org/luckyraven/bartizan/api/weapon/recoil/RecoilManager.java (ported verbatim from Gangland 0.8.4 gangland-features/gangland-weapon/src/main/java/org/luckyraven/gangland/weapon/projectile/recoil/RecoilManager.java:72-79, same bug pre-existing there)~~Noticed 2026-09-08 while porting RecoilManager to Bartizan (bartizan.md B5). Not exercised by any existing test (RecoilManagerTest does not call clone()); Weapon.clone() calls source.reload/recoilManager style cloning elsewhere but nothing in the current test suite constructs two Weapon clones to trigger it.~~weapons
20~~P2~~PeriodicalUpdatesTest also references the deleted DataCleanupTask class, same as the already-known PluginDataCleanupServiceTest~~Drop the DataCleanupTask import and the `when(container.getAllInstances(DataCleanupTask.class)).thenReturn(List.of())` stub in setUp() the same way group N is already rewriting PluginDataCleanupServiceTest; the rest of the CL-02 main-thread-scheduling assertions are unaffected.~~PeriodicalUpdatesTest (currently red, compile error)~~gangland-impl/src/test/java/org/luckyraven/gangland/bootstrap/PeriodicalUpdatesTest.java:13,64~~Found 2026-09-08 during the gangland-0.9.0.md group C execution (T-C3, weapon-deletion fallout): deleting DataCleanupTask.java (no implementor remains once the weapon module leaves) breaks compilation of PeriodicalUpdatesTest.java, not just the already-checklisted PluginDataCleanupServiceTest.java - both stub/import the same now-deleted interface. Since Maven compiles a module's entire test tree in one javac invocation before Surefire's -Dtest filter applies, this second file blocks test-compile for the whole gangland-impl module (and therefore blocks `mvn clean install -DskipTests`'s test-compile phase too), not just its own test run. Left un-edited per the executor brief's explicit "do not delete or edit the test" instruction for the sibling file; deferred to the same group N test-sweep task.~~core-lifecycle
21~~P3~~Ten Messages.java members beyond the seven named by the gangland-0.9.0.md weapon-deletion stream are already-dead and now point at deleted YAML paths~~Delete RECEIVED_AMMO, RECEIVED_WEAPON, INVALID_AMOUNT, KILLED_PLAYER, GUN_NOT_IN_INVENTORY, GUN_BOUGHT, GUN_SOLD from Messages.java (zero consumers repo-wide, confirmed by grep) in a follow-up cleanup pass; harmless today (nothing calls .toString() on them) but they now reference Commands.Weapons.*/Weapons.* YAML paths that T-C5 deleted.~~none~~gangland-impl/src/main/java/org/luckyraven/gangland/file/configuration/Messages.java (RECEIVED_AMMO/RECEIVED_WEAPON were at old :32,34; INVALID_AMOUNT/KILLED_PLAYER/GUN_NOT_IN_INVENTORY/GUN_BOUGHT/GUN_SOLD were at old :315-319)~~Found 2026-09-08 during the gangland-0.9.0.md group C execution (T-C5): the stream's own inventory names exactly 7 dead Messages members to delete (GAVE_AMMO, GAVE_WEAPON, AMMO_NOT_IN_INVENTORY, AMMO_BOUGHT, AMMO_SOLD, NOT_ENOUGH_AMMO, DEAD_USING_WEAPON) as having zero live consumers, but a repo-wide grep for these ten additional members (also declared right beside the deleted ones) turns up no consumer either - they were already dead before this stream and are simply not counted in its 7-member scope. After T-C5 deletes the Weapons: and Commands.Weapons: message_en.yml blocks, these ten Messages constants point at YAML paths that no longer exist; since nothing calls them this is inert, not a live bug, but worth a follow-up sweep.~~commands-messages-platform
22~~P3~~Downed-path kills announce the core's generic Death.Global message without the weapon name once weapons move to Bartizan~~NOT FIXED (design gap, later wave): CustomPlayerDeathListener cancels the lethal damage on the downed path, so PlayerDeathEvent never fires and Bartizan's EventPriority.HIGH weapon death message cannot apply; PlayerDeathListener.broadcastDeathMessage renders Messages.DEATH_GLOBAL (%killer%/%victim% only). Fix direction: Bartizan listens to PlayerDownedEvent (gangland-core) and sets the message through a small core seam, or the core exposes a death-announcement hook on the downed path~~DeathGlobalMessageTest pins the core list; no test for the downed-path announcement~~gangland-impl/src/main/java/org/luckyraven/gangland/listener/player/PlayerDeathListener.java (buildDeathMessage, broadcastDeathMessage); gangland-impl/.../listener/player/CustomPlayerDeathListener.java:107-115~~Found 2026-09-08 by the gate-C review of gangland-0.9.0.md (finding B1): the T-C4 rewrite had removed the fallback entirely, so downed-path kills went silent; the orchestrator restored a core-owned Death.Global list, which announces the kill but cannot name the weapon~~core-lifecycle
23~~P3~~ProjectileDamageListener's @AutowireTarget names the wrong class~~Change @AutowireTarget({WeaponService.class}) to @AutowireTarget({WeaponVisualSpawner.class}) to match the actual constructor parameter, or drop the annotation entirely if the container resolves concrete-typed constructor params without a hint.~~none~~bartizan-plugin (Bartizan repo) src/main/java/org/luckyraven/bartizan/listener/projectile/ProjectileDamageListener.java (pre-existing at gangland-weapon/.../listener/projectile/ProjectileDamageListener.java before the split)~~Found 2026-09-08 during bartizan.md group H execution (B13): the class constructor takes only WeaponVisualSpawner, but its @AutowireTarget hint list names WeaponService instead - pre-existing in Gangland 0.8.4 (verified against the snapshot), ported verbatim per the PKG-only edit since the checklist does not name this file for a behavior change. Harmless today because WeaponVisualSpawner is a concrete @Bean-produced type resolved by direct parameter type, not through the AutowireTarget search path, but worth a follow-up cleanup.~~weapons
24~~P3~~BiologicalAction's charge action-bar string uses literal § color codes instead of &-coded ChatUtil.color~~Change "§6Charging... §e[" to "&6Charging... &e[" and route it through BartizanChatUtil.color(...) before ActionBarManager.send, matching the house convention every other user-facing string in the action files follows.~~none~~bartizan-plugin (Bartizan repo) src/main/java/org/luckyraven/bartizan/weapon/action/BiologicalAction.java (pre-existing at gangland-weapon/.../types/biological/BiologicalAction.java before the split)~~Found 2026-09-08 during bartizan.md group H execution (B13): pre-existing in Gangland 0.8.4 (verified against the snapshot), ported verbatim per the PKG-only edit since the checklist does not name this file for a behavior/style change. house rule feedback_chat_color_codes says colored chat must use ChatUtil.color() with & codes, never literal section signs.~~weapons
25~~P2~~JetpackSession.java still imports the deleted org.luckyraven.gangland.item.wearable.Wearable, missing from every group-F/L census~~Re-point JetpackSession's Wearable usage to Bartizan's org.luckyraven.bartizan.api.wearable.Wearable the same way group L repoints JetpackService/JetpackTask/CarDamageListener.~~FIXED 0.9.0 (7e512679, branch 0.9.0): import re-pointed to org.luckyraven.bartizan.api.wearable.Wearable (T-L2)~~gangland-features/gangland-gadget/src/main/java/org/luckyraven/gangland/gadget/jetpack/JetpackSession.java:6,16-17,41~~Found 2026-09-08 during the gangland-0.9.0.md group F execution (gate F): §1.2's item census and §1.3's 16-file/35-line weapon-import census both omit this file. It imports org.luckyraven.gangland.item.wearable.Wearable (deleted by T-D4, group D) - not one of the six generic keystone-item classes T-F1 re-points, and not a weapon.* import either, so no existing group F/L task claims it. Compile errors: [6,45] package org.luckyraven.gangland.item.wearable does not exist plus three cannot-find-symbol usage sites (:16-17, :41). Group L (gadget re-point onto bartizan-api) is the natural owner since Bartizan's wearable.Wearable replaces the deleted class the same way JetpackService/JetpackTask already need repointing.~~gadgets-cars-fuel-jetpack
26~~P3~~items/wearables.yml's shipped header comment still says permission nodes derive as "gangland.wearables.<key>", but Wearable.getPermission() actually returns "bartizan.wearables.<key>"~~Edit the two comment lines in bartizan-plugin/src/main/resources/items/wearables.yml's header block ("Permission nodes are derived automatically as \"gangland.wearables.<key>\" (e.g. police_vest -> gangland.wearables.police_vest)") to the bartizan.* prefix, matching Wearable.java:232's real behavior and documentation/migration.md's permission-rename section.~~FIXED (Bartizan master eb8bf8a: wearables.yml header comment corrected)~~bartizan-plugin (Bartizan repo) src/main/resources/items/wearables.yml:17-19 (header comment) vs bartizan-api/src/main/java/org/luckyraven/bartizan/api/wearable/Wearable.java:230-233 (getPermission())~~Found 2026-09-09 while writing documentation/migration.md's permission-rename section (bartizan.md B21, gate-GD review m3): the YAML ships with a Gangland-era comment nobody updated when the permission prefix was changed to bartizan.* during the split; harmless (the comment isn't parsed), but misleads a server owner reading the shipped file directly instead of the migration doc.~~weapons
27~~P3~~BukkitRegistryFixture's defaultValue() proxy fallback boxes every remaining primitive as Integer, which throws ClassCastException for a registry-routed short-returning method~~Add explicit byte.class/short.class cases (boxed as (byte) 0 / (short) 0) to defaultValue(Class<?>) alongside the existing boolean/void/char/long/float/double cases, mirroring the fix already applied to bartizan-plugin's own copy of this fixture.~~none (latent - no bartizan-api test currently drives a short-returning registry method through this fixture)~~bartizan-api (Bartizan repo) src/test/java/org/luckyraven/bartizan/api/testsupport/BukkitRegistryFixture.java defaultValue(Class<?>) (final `return 0;` fallback line)~~Found 2026-09-09 while writing bartizan-plugin's own duplicate of this fixture for WeaponItemApiTest (bartizan.md N-gap): Material.getMaxDurability() (called from Weapon.buildItem() via ItemBuilder.getItemMaxDurability()) routes through the registry proxy and returns `short`; the proxy's default-value fallback returned a boxed Integer 0 for every unmatched primitive type, and the JDK dynamic-proxy invocation contract does not coerce a mismatched boxed numeric type, so the call threw "class java.lang.Integer cannot be cast to class java.lang.Short" at the call site. Fixed in bartizan-plugin's new copy (bartizan-plugin/src/test/java/org/luckyraven/bartizan/testsupport/BukkitRegistryFixture.java); bartizan-api's original copy still has the gap but nothing there currently exercises a short-returning registry method through it.~~weapons
28~~P0~~WeaponManager persistence never ran — nothing called RepositoryRegistry.saveAll/DatabaseManager.closeConnections/BartizanDatabase.disconnectBackend, so every runtime weapon state was lost on restart and the SQLite pool outlived a reload~~Add a self-scheduling WeaponAutoSaveTask (Keystone Timer) wired via a CONFIG @Bean in WiringConfig that calls RepositoryRegistry.saveAll() every Auto_Save.Time minutes (guarded so <= 0 disables it), and isolate Bartizan.onDisable() into per-stage try/catch: shutdown.beans -> shutdown.save (saveAll) -> shutdown.connections (DatabaseManager.closeConnections) -> shutdown.backend (BartizanDatabase.disconnectBackend), mirroring Gangland's ShutdownSequence stage-isolation shape.~~FIXED (Bartizan master eb8bf8a)~~bartizan-plugin (Bartizan repo) src/main/java/org/luckyraven/bartizan/data/WeaponAutoSaveTask.java (new), config/WiringConfig.java (weaponAutoSaveTask bean), Bartizan.java (onDisable)~~Found 2026-09-09 during the R-B-FINAL review of the Bartizan split (bartizan.md executor X-B-R, task B1, blocker). WeaponManager.initialize() wired setDataSupplier(...) but nothing in the bean graph or the shutdown path ever called saveAll/closeConnections/disconnectBackend — every persisted weapon uuid (and its NBT ammo/durability state) was lost on every restart, and the HikariCP SQLite pool behind the DatabaseBackend outlived a /reload. Red-first: new WeaponAutoSaveTaskTest failed to compile against pre-fix code (class did not exist, verified by temporarily moving the new production file out and back); green after.~~weapons
29~~P1~~WeaponDeathListener could override a death message Gangland's own LOWEST-priority listener deliberately nulled (NPC victim, NPC killer, or a duplicate event)~~Add `if (event.getDeathMessage() == null) return;` at the top of WeaponDeathListener.onPlayerDeath, immediately after reading the victim, so a null message from an earlier suppressing listener is never overridden.~~FIXED (Bartizan master eb8bf8a)~~bartizan-plugin (Bartizan repo) src/main/java/org/luckyraven/bartizan/listener/death/WeaponDeathListener.java onPlayerDeath~~Found 2026-09-09 during the R-B-FINAL review (bartizan.md executor X-B-R, task M1). WeaponDeathListener runs at EventPriority.HIGH specifically so it runs after Gangland's own PlayerDeathListener (EventPriority.LOWEST), which nulls the death message for an NPC victim, an NPC killer, or a duplicate event; without the guard this listener could set a weapon-attributed message where Gangland deliberately wanted none. Standalone (no Gangland present) the vanilla message is non-null so the weapon path is unaffected. Red-first test WeaponDeathListenerTest.onPlayerDeath_nullDeathMessage_neverOverridden failed the verify(event, never()).setDeathMessage(any()) assertion against pre-fix code (verified via git stash on the listener file); green after.~~weapons
30~~P1~~WeaponTableImportTask wrote the one-shot import marker even after a caught SQLException, permanently skipping the legacy weapon-table import on a transient failure~~Only call writeMarker(marker) on the file-absent path and the success path; on a caught SQLException, report through Diagnostics + log.error and return without writing the marker, so the import retries on the next boot.~~FIXED (Bartizan master eb8bf8a)~~bartizan-plugin (Bartizan repo) src/main/java/org/luckyraven/bartizan/database/WeaponTableImportTask.java run()~~Found 2026-09-09 during the R-B-FINAL review (bartizan.md executor X-B-R, task M2; the always-write-the-marker behavior was itself a recorded judgment call at B15, since bartizan.md §1.9 did not settle the failure case). A transient error (e.g. SQLITE_BUSY while Gangland's own pool still holds gangland.db on the first boot) previously wrote the marker anyway, permanently skipping the legacy weapon-uuid import. Red-first test WeaponTableImportTaskTest.run_importThrows_doesNotWriteMarker failed (expected false but was true — marker existed) against pre-fix code, verified via git stash on the source file (forced with a garbage-bytes gangland.db so the SQLite JDBC driver genuinely throws); green after.~~weapons
31~~P2~~CivilianNpcFactory.createCivilian and CivilianSpawnManager.spawnCivilian call CitizensAPI directly with no NpcSupport.available() guard, so any Citizens-dependent spawn (civilian, cop, or a turf Quartermaster/garrison-defender routed through them) throws instead of no-op when Citizens is absent~~Guard the top of CivilianNpcFactory.createCivilian (or CivilianSpawnManager.spawnCivilian, the shared choke point both cops and turf route through) with `if (!NpcSupport.available()) return null;`, matching the guard T-I6 added ad-hoc at TurfPowerupManager.spawn/TurfDefenderDeployer.deploy for the turf-owned callers; cops-n-crooks' own Citizens-dependent call sites (CopNpcFactory etc.) likely need the same treatment~~-~~gangland-features/gangland-civilians CivilianNpcFactory.java createCivilian, CivilianSpawnManager.java spawnCivilian~~Found 2026-09-09 while implementing T-I6 (gangland-0.9.0.md, turf-NPC Citizens gate): civilians' own onEnabled fault report (T-H4) assumes "the spawn tasks themselves are guarded in CivilianService#onInitialize", but reading CivilianNpcFactory/CivilianSpawnManager shows no such guard exists anywhere in the shared spawn path — the actual protection T-I6 needed had to be added locally at the two turf-owned call sites (TurfPowerupManager.spawn, TurfDefenderDeployer.deploy) rather than relying on a deeper guard that turns out not to exist~~civilians-traders-shops
32~~P2~~TraderManager.getByEntity/BankerManager.getByEntity call CitizensAPI.getNPCRegistry() unguarded, reached from every EntityDamageEvent/interact event via TraderDamageListener/TraderInteractListener/BankerDamageListener/BankerInteractListener (plus TraderRemoveCommand/BankerRemoveCommand), so any damage or interact anywhere on the server throws once Citizens is absent, not only when a trader/banker exists~~Guard the top of TraderManager.getByEntity and BankerManager.getByEntity with `if (!NpcSupport.available()) return null;`, mirroring the guard added at TraderManager.spawn/BankerManager.spawn (gangland-0.9.0.md T-J4) and at CivilianNpcFactory.createCivilian (T-HR5, closing docket #31) — same root cause, different module~~FIXED 0.9.0 (7e512679, branch 0.9.0): guard added as the first line of both getByEntity methods~~gangland-features/gangland-npc-shops TraderManager.java getByEntity, BankerManager.java getByEntity~~Found 2026-09-09 while implementing T-J4 (gangland-0.9.0.md, npc-shops Citizens gate): T-J4's own "Do" only named NpcShopsModule.onEnabled's diagnostic report; the actual spawn-path guard (TraderManager.spawn/BankerManager.spawn) was added following the T-HR5/T-H4/T-I6 precedent, but getByEntity — reached from four listeners and two commands on every damage/interact event, independent of whether any trader/banker NPC exists — was left unguarded since it's outside T-J4's literal scope and fixing it would need its own red-first test~~civilians-traders-shops
