1~~P0~~MySQL connect failure with fallback off aborts enable with a raw NPE~~In DatabaseConfig.ganglandDatabase, after setType(), check getDatabase()==null and throw a PluginException naming the MySQL settings (or catch RuntimeException around createSchema).~~GanglandDatabaseTest (pins parts 1+2; flip when fixed)
2~~P0~~Async autosave iterates the live user caches (CME, lost writes)~~Snapshot both caches on the main thread (a sync task copies the values to lists), then push only the DB write async; never call offlineUserManager.clear() off-thread.~~-
3~~P1~~Weapon-table wipe runs on the async thread while the main thread uses the cache~~Run resetWeapons on the main thread and push only the DELETE async; clear the cache after the save completes, not before.~~PluginDataCleanupServiceTest (covers the branch, not the threading)
4~~P0~~DatabaseBackend Hikari pool is never disconnected on disable~~Keep the backend reference in GanglandDatabase and call backend.disconnect() from DatabaseManager.closeConnections() after forceUpdate().~~-
5~~P0~~Unguarded shutdownBeans skips the final save and DB close if one bean throws~~Wrap context.shutdownBeans() and disableModules() in try/catch (log + Diagnostics) and run forceUpdate() and closeConnections() in a finally block.~~-
6~~P1~~initialize()-style managers are never re-initialised by /glw reload~~Convert TurfManager, MailManager, ActiveBuffManager and GarrisonManager to BeanLifecycle (onPreClear/onInitialize) or have the reload pipeline call their initialize() explicitly.~~-
7~~P2~~Dependency check (NBTAPI, Citizens) runs after the full bootstrap~~Move the required-plugin check to the top of onEnable, before new GanglandContext, so a missing dependency disables before files, DB and timers exist.~~-
8~~P3~~Cleanup service constructed twice per reload pass~~Drop the duplicate initializeCleanupService() call from start(); keep the one in onInitialize.~~-
9~~P2~~Diagnostics hub static is never uninstalled on disable~~Uninstall (or install a no-op hub) in Gangland.onDisable after closeConnections so a stale DatabaseFaultSink is not left active.~~-
10~~P2~~PlaceholderService.convert allocates a new provider chain per call~~Build the CompositePlaceholderProvider once and rebuild only when a resolver registers; synchronise the papiProvider assignment. Same fix as CM-28.~~-
11~~P1~~PluginData id counter seeded to the last row id, so ids are reused and rows overwritten~~Seed PluginData.ID = max(id)+1 in PluginManager.initialize and make getNewId() safe.~~PluginDataTest + PluginManagerTest (pin; flip when fixed)
12~~P2~~The "current" plugin_data record is whichever row loaded last~~Pick the record by max id (or keep exactly one row read by a fixed id), not list order.~~-
13~~P1~~/glw reload runs the whole save tick plus an offline SELECT on the main thread~~Keep the DB writes synchronous but load offline players async with a completion callback; or document the stall behind a flag.~~-
14~~P3~~Two @Bean methods named repositoryRegistry return the same instance~~Delete the CONFIG-phase duplicate in DataConfig and inject the DATABASE-phase bean.~~-
15~~P2~~@ConditionalOnSetting is evaluated before Settings is loaded~~Evaluate conditions per phase (after FILE) in BeanFactory or make SettingsLookupImpl read lazily; add a test before any bean uses the annotation. Keystone upstream.~~-
16~~P1~~Schema and table creation failures are only logged; the plugin runs without tables~~Treat createSchema/createTables failure as fatal in GanglandDatabase.initialize (throw PluginException, disable). Keystone upstream: DatabaseHelper.runQueries.~~-
17~~P2~~A repository whose constructor throws surfaces later as "No repository registered"~~Rethrow (or collect and fail bootstrap) in RepositoryRegistry.scanAndRegisterRepositories instead of WARN-and-skip. Keystone upstream.~~-
18~~P1~~insertInitialData returns silently when the rank repositories are missing~~Log at ERROR and throw when the head/tail rank cannot be inserted on a fresh install.~~-
19~~X~~Settings helpers silently substitute defaults (verified correct code)~~No code change; optionally add key-level WARN lines through ConfigReport for missing sections.~~SettingsTest (documents the behaviour)
20~~P1~~A bad Language value on reload disables the plugin mid-reload~~LanguageLoader.initialize should fall back to the previous or English file and report, never call disablePlugin from a reload pass. Keystone upstream.~~-
21~~P3~~updateAllData builds a fresh TableBackend per call and bypasses processSave~~Reuse repository.tableBackend() (or call repository.saveAll) so processSave pre-processing applies.~~-
22~~P3~~/glw reload inventory calls an empty resetCache()~~Remove resetCache() and its call, or implement it.~~-
23~~P0~~Both UserManager beans register data suppliers on the same repositories~~Give the offline manager its own supplier registration (or merge both caches into one supplier) so RepositoryRegistry.saveAll persists both. Same root as US-12.~~UserManagerTest (pins the collision)
24~~P1~~Bean order is JVM-dependent for beans with equal dependency sets~~Sort bean definitions by name (stable) in BeanFactory/BeanGraph; keep parameter edges for real ordering. Keystone upstream.~~-
25~~P1~~offlineUserManager.clear() runs before repositoryRegistry.saveAll()~~Reorder: saveAll first, then clear; add a test pinning the order.~~-
