<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.luckyraven</groupId>
        <artifactId>gangland-compatibility</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>version-1_20_R3</artifactId>

    <properties>
        <spigot.version>1.20.4-R0.1-SNAPSHOT</spigot.version>
    </properties>

    <dependencies>
        <!-- NMS server internals (pre-1.20.5 revisions compile directly against the
             obfuscated server jar; no remap step needed) -->
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot</artifactId>
            <version>${spigot.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.luckyraven</groupId>
            <artifactId>gangland-impl</artifactId>
        </dependency>
    </dependencies>

</project>
